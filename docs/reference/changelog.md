# Changelog

> Full commit-body-level history. The hub [CLAUDE.md](../../CLAUDE.md) carries one-line summaries for the most recent entries only.


| Date | Change | File(s) |
|------|--------|---------|
| 2026-07-23 | **A1 agentic-voice — `answer_user` + `inspect_session_state`: the model gets a VOICE (backend + one web companion commit; zero iOS change; master flag `VOICE_AGENTIC_ANSWERS`, shipped DARK at `false`).** **Why:** Derek's 2026-07-22 ask — recording should feel like chatting with an LLM over STT/TTS: answer questions, accept values that don't fit a gate pattern. The live model was structurally MUTE for answers (16 extraction/mutation tools, no answer egress; assistant text never reaches TTS) so a question turn became a zero-tool end_turn → the misleading marker-① "didn't catch that" apology; and the server pre-LLM gate silently dropped non-matching turns AFTER the client chime — an un-nettable beep-then-silence class (the block returns before the marker nets exist). **What:** (1) `answer_user` (≤300 chars/≤2 sentences, deterministic normaliser, `checkForPromptLeak` output filter, at-most-once latch on successful staging; explicit is_error matrix — `empty_answer` invites ONE corrected retry, filtered/already-given/exhausted never loop) stages into `perTurnWrites.answer`; the bundler projects `result.spoken_response` (+ NON-ENUMERABLE `answer_source`/`answer_meta` so the three spread sites and `pendingExtractions` can never leak them) and the EXISTING `voice_command_response` machinery emits — sync + P4d reconnect replay, `utterance_id` stamp, FIFO replay, ZERO wire-shape change. (2) `inspect_session_state` (summary/board/circuit/field) reads authoritative state outside the `recent_3`/board-scoped snapshot through the multi-board helpers, with the sanitise+USER_TEXT-wrap helpers EXTRACTED to `stage6-snapshot-user-text.js` (identical hygiene, never duplicated) and the Derek-APPROVED completeness-policy appendix (`docs/reference/inspect-session-state-policy.md`: 18 base schedule columns, ring/RCD/distribution conditional groups, spare-way exemption, LIM/N-A satisfy requiredness, 4096-byte deterministic truncation, is_error matrix). (3) Gate: terminal `LOW_CONTENT` → `BORDERLINE_FORWARD` under the session-latched flag (fail-closed on session-absent; EMPTY still blocks — Phase 0.5 proved both clients block empty pre-chime; NO server question word-lists, permanently — the model decides). Evidence: 1 server-side gate_blocked in 30 days → <\$0.02/month. (4) Marker nets: staged answer = speech-intent (marker-② mutual exclusion); the two names join the A3 `ask_user` name-guard + `producedNothing` gains the spoken_response conjunct; BOTH F7 branches require no surviving answer; post-loop finalization (cancelled turns INCLUDED — `turnAnswerState` is dispatcher-fed and survives the throw) stages the FIXED fallback answer when the feature was attempted, nothing staged, and no write/ask owns the turn — the answer feature owns its OWN audibility in BOTH confirmation-toggle states (the apology nets are confirmationsEnabled-gated). (5) Conditional prompt: `renderAgenticSystemPrompt(flag)` over `<!--A1:OFF/ON-->` marker blocks — flag-off render BYTE-IDENTICAL to the pre-A1 prompt (verified against `git show main:`; ships dark, zero cache invalidation), flag-on adds ANSWERING QUESTIONS, rewrites the "There are NO query_* tools" steer, makes the inventory EXHAUSTIVE (TOOLS(18) — the 12 listed + 4 previously-omitted + 2 new); ONE `_resolveSystemPromptForMode()` at BOTH assignment sites survives `applyModeChange`. Leak filter: 8/12/18 count literals + `TOOLS (12)/(18)` headers + `TOOL_KEYWORD_RE` completed to all 18 names. (6) Telemetry leak-rule strict: `voice_latency.answer_user_emitted` (source/chars/truncated/hash — never raw model text; the legacy sync-VCR substring(0,80) preview branched off), `voice_latency.gate_borderline_forwarded`. (7) Web companion (MANDATORY, one commit): `{force:true}` on the `onVoiceCommandResponse` → `speakConfirmation` call (web muted VCR speech when the toggle — web default — was off; iOS speaks unconditionally, `DeepgramRecordingViewModel.swift:9888`); replay harness gains VCR `mock_frames` + per-scenario `confirmation_mode` (was hard-coded true); RED-proven `a1-vcr-answer-toggle-off.yaml` + backend-shape decode contract test. **Tests:** 5716 backend green (61 new across 3 A1 suites: dispatcher/normaliser/projector matrix, harness audibility matrix incl. cancelled turns + both toggle states, flag latch/render/gate 4-way matrix); 1430 web green; corpus 5/5; env-audit + replay-environment drift guards extended (`VOICE_AGENTIC_ANSWERS` pinned-from-task-def). **Delivery:** PR 1 dark (flag `false` in task-def, byte-parity everywhere), PR 2 = the task-def flip to `true` — THE single cache-invalidation event (Derek-approved same-night flip; live ear-verified probes A/A2/B/C/D at his convenience, rollback = one task-def commit). Phase 0.2 bounded loss envelope (ask-collision drops on both clients, no dedupe on the VCR path) documented on ledger row `recording/assistant-answers`. Follow-ups: A2 client-gate question lane (FAST-FOLLOW, own /rp), A3 mid-script fallthrough, A4 wake phrase, A5 numeric-gate divergence check. | `src/extraction/stage6-tool-schemas.js`, `stage6-dispatchers-answer.js` (new), `stage6-inspect-projector.js` (new), `stage6-snapshot-user-text.js` (new), `stage6-dispatchers.js`, `stage6-shadow-harness.js`, `stage6-event-bundler.js`, `stage6-per-turn-writes.js`, `stage6-prompt-leak-filter.js`, `pre-llm-gate.js`, `sonnet-stream.js`, `eicr-extraction-session.js`, `config/prompts/sonnet_agentic_system.md`, `ecs/task-def-backend.json`, `scripts/field-replay/replay-environment.mjs`, `web/src/lib/recording-context.tsx`, `web/tests/harness/*`, `docs/reference/inspect-session-state-policy.md` (new) |
| 2026-07-22 | **P1 ring-script-hardening — ring-continuity dialogue confirmation loop hardened (backend-only; session B4C45F25 feedback 90/91/92).** **Why:** a cascading field failure: (90) "Can you delete the readings for the ring continuity on circuit 13" trigger-matched the ring schema, jumped straight to the all-filled confirmation, and the delete intent never reached the model; (91) bare "No" answers to "All correct?" fell through to a model no-op (no read-back in its context) and drew rotating marker-① apologies — the confirmation state was effectively immortal (the 180s timeout resets every turn); (92) "Circuit 17 recontinuity lives are 0.77…" was silently consumed as an AMENDMENT to circuit 13 — a data-corruption class only field-masked because c17's values were coincidentally identical to c13's. **Fixes:** (1) entry guard generalised to per-schema OPT-IN `entryExclusionPattern` — RCD's combined imperative+denial pattern moved verbatim to `schemas/rcd.js` (telemetry + guard test byte-identical), ring gets destructive verbs ONLY (`delete|undo|remove|clear|cancel|fix`; question-form entries keep working — evidence-backed recovery path, Derek-confirmed), telemetry generalised to `${schema.name}_entry_guard_skipped`. (2) Canonical `awaiting_confirmation` decision order (positions 0–5h): destructive-broadcast exemption at the pre-filter; a confirmation-gated delete/clear-intent preflight (ordered-proximity verb-before-object pattern) that exits the script and falls through with a FIXED `[Server note: …]` antecedent + the raw reply — `clear_reading` ownership stays with the model (Derek-confirmed; both live probes A/B PASSED post-implementation: 3× clear_reading on entry AND confirm paths); purge-first cancel inversion in confirmation mode; generic `detectDifferentEntry` gated off; a masked+qualified 5a different-circuit preflight (circuit-span masking so a ref can never be captured as a reading value, non-ring-context rejection for `CPC size`/`earth fault loop`-class extractor traps, multi-ref NEGATION POLARITY so "No, not circuit 17 — circuit 13 …" never seeds the rejected circuit, seeding via `runEntry` with `overwriteVolunteered:true`); an anchored pending-slot correction machine (`confirmation_pending_slot`, schema `pendingValuePattern` — never the unrestricted `parseOhms`, which reads "circuit 13" as 13; slot-name selectors for "R1."/"No, R1"/"Okay, R1"); a per-EPISODE at-most-once negation re-ask (`confirmation_negation_reask_emitted` — a counter-based rule would re-emit a byte-identical re-ask into the client 30s text-keyed dedupe, the exact feedback-91 silence loop) with full-string-distinct alternates; a negated-positive guard ("That's not correct" can no longer false-finish); a 2-consecutive-miss audible cap exit; and a pinned reading-like classifier (`detectStructuredReading(…)?.complete` OR number+unit OR circuit-ref OR enumerated field anchors + digit) so a dictated reading is NEVER consumed by the miss counter — reading-like and plain idles CLEAR the stale formality and fall through untouched. (3) Enumerated Flux garble `re-?continuity` joins the ring trigger (exact alternative, no open suffix — §3E) in schema AND legacy twin (whose Pattern-1 also gains the long-standing `bring|wing` alternation — deliberate convergence). (4) Raw-reply contract: sonnet-stream threads `rawReplyText: msg.text` into all three script wrappers; every confirmation decision parses the RAW reply (the `in_response_to` annotation quotes "All correct?", letting `detectPositive` match "correct" inside the QUESTION); the delete exit REPLACES the annotation with the server note. (5) Audio-First purge contract: every confirmation-abandonment exit (delete, cap, 5a seed, confirmation-mode cancel, broadcast clear, topic switch out of confirmation, guarded fallthroughs, 5h clears, 180s hard-timeout sweep) sends `cancel_pending_tts` BEFORE replacement speech; the 5f positive finish is exempt by design; all new wordings full-string distinct from all four apology/notice families (pinned). Legacy twin fully mirrored; replay corpus gains 6 byte-parity scenarios; 75-test pinned suite + two-layer delete integration contract (real sonnet-stream ingress spy + real runShadowHarness with canned 3× clear_reading rounds); marker-①/② nets untouched and green. Phase-0 finding: NO replayable capture of B4C45F25 exists — corpus ran as a non-regression gate only (5/5); vault follow-ups: corpus-boundary extension to dialogue ingress + iOS queued-alert tool-call-id purge. ZERO new wire SHAPE and zero client-decoder change — the new paths emit additional frames of EXISTING shapes (cancel_pending_tts, ask_user_started) that both clients already handle; server-side for both clients. Full backend suite 5630 passed / 0 failed. | `src/extraction/dialogue-engine/engine.js`, `src/extraction/dialogue-engine/schemas/ring-continuity.js`, `src/extraction/dialogue-engine/schemas/rcd.js`, `src/extraction/sonnet-stream.js`, `src/extraction/ring-continuity-script.js`, tests |
| 2026-07-20 | **PLAN-C Phase 4 part 2 (P4d) — ask/question/voice-command emit-site response-epoch matrix (backend-only; completes Phase 4, stacks on P4c #106).** **Why:** P4c gave the ANSWER side of the client chime-silence watchdog contract — post-answer confirmations carry the answering utterance's response epoch. P4d gives the OTHER direction: when a chime arms the watchdog and the server replies with an *ask* rather than a written reading, that ask's TTS must carry the arming utterance's epoch (`utterance_id`) so the watchdog disarms on the spoken question instead of false-firing a 20s native apology over an ask the inspector already heard. A MISSED emit site = an audible ask with no epoch = a false-fire once clients arm, so the eight rows are enumerated exhaustively and the Codex gate checks the diff against the table. **Row 1 — dialogue engine (`dialogue-engine/helpers/wire-emit.js` + `engine.js`):** the three ask builders (`buildScriptAsk`/`buildScriptConfirm`/`buildScriptInfo`) gain a REQUIRED creation-time `responseEpoch` arg via a `RESPONSE_EPOCH_REQUIRED` sentinel default — a builder called without the arg (a missed nested-fn thread) THROWS, turning a silent-null epoch leak into a loud test failure; an explicit `null` (no arming utterance) is allowed and omits the wire field. A shared `stampResponseEpoch` adds `utterance_id` only for a non-empty string epoch (mirrors P4c's advance-only-on-non-empty; byte-identical when absent). A single captured snapshot is threaded parallel to `now` through EVERY nested engine fn (processDialogueTurn, runEntry, runActivePath, runPivot, askNextOrFinish, transitionToConfirmation, handleBulkApplyReply, finishScript, enterScriptByName, tryResumePausedScript, tryEnterScriptFromWrites) to all 18 builder call sites; NEVER derived inside `safeSend` (the mutable-at-emit trap the contract forbids). The `ASK_STARTED_OBSERVER` send-time callback now also reports the stamped id (backstop). Epoch SOURCES: sonnet-stream active-path turns (`processRing/IR/PDTurn`) pass `msg.utterance_id`; the shadow-harness resume/entry hooks (`tryResumePausedScript`/`tryEnterScriptFromWrites`) pass `responseEpochRef.current`; `dispatchStartDialogueScript` reads a live `responseEpochRef` stashed on the session at the top of `runLiveMode` (cleared in its finally, same pattern as `activeTurnTranscript`) so the first server-driven ask is stamped at emit time. **Rows 2-3 — legacy `ring-continuity-script.js` / `insulation-resistance-script.js`:** their own builders + inline asks stamp the epoch via a local `stampResponseEpoch` threaded from the turn-function ctx. DELIBERATE deviation from row 1's REQUIRED sentinel — these use a null DEFAULT because the files are no longer on the live path (sonnet-stream drives the dialogue-engine wrappers from `dialogue-engine/index.js`, not these; verified no live importer), so there is no live threading to enforce and a throw would add a production-abort risk with zero watchdog benefit; carried for wire-contract completeness + the Codex table check. **Row 4 — `stage6-dispatcher-ask.js`:** the initial (step-3c) + brokered-pvr `ask_user_started` QUESTION frames carry `responseEpochRef.current` at emit time (creation == emit; the ref is not yet advanced for the initial ask so it carries the arming id). Distinct from P4c, which stamps the ANSWER-side confirmations. Optional-chained so the legacy/test null-ref path never throws. **Rows 5-8 — legacy sonnet-stream / batch cluster (`sonnet-stream.js` + `eicr-extraction-session.js`):** row 8 is the SOURCE — `_processUtteranceBatch` combinedOptions now carries the LAST NON-EMPTY buffered `utteranceId` (a batch collapses several utterances into one API call; the reply answers the newest) and `_extractSingle` preserves it as `result.utterance_id` (the KEY is OMITTED when there is no epoch — never `result.utterance_id = null` — because the result spreads into the `extraction` frame via `...rest`, so a null would break the byte-identical-when-absent contract, Codex r1; covers both the buffer-full and timeout-flush paths at the single return). Row 5 — a new pure `stampQuestionsWithUtteranceId` clones each `question` with the epoch BEFORE it enters the QuestionGate (so the frame the gate later flushes carries it via `...rest`; all three sender bindings already spread rest — no per-binding change); onBatchResult + sync stamp `result.utterance_id`, the periodic `reviewForOrphanedValues` path (its own Sonnet call has no result epoch) stamps the turn's `consumedUtteranceId`. Row 6 — the sync + onBatchResult `voice_command_response` frames carry `utterance_id`. Row 7 — the reconnect replay (`flushPendingExtractions`) STRIPS `spoken_response`/`action` from the extraction replay (iOS plays spoken text only from `voice_command_response`, never from an extraction frame — so on reconnect it was effectively dropped) and emits a SEPARATE `voice_command_response` carrying the buffered `result.utterance_id`; SANCTIONED behaviour change (the buffered spoken_response is now canonicalised onto the `voice_command_response` channel — the same channel both clients decode for spoken command replies — instead of riding inside the extraction envelope; per the plan it was effectively dropped on reconnect); `pendingExtractions` preserves `result.utterance_id` via row 8. **Codex diff-review hardening (r1 + per-fix mini-review + r2):** the reconnect replay sends the audible `voice_command_response` LAST (after the extraction + `current_board_changed` + `observation_update` replays) and HALTS at the first closed-socket/throw, re-queuing the current entry plus the untouched suffix in FIFO order — so the spoken reply is never lost, never reordered, and never double-spoken (a re-sent extraction/board/obs frame is idempotent-benign); `dispatchObservationUpdates` gained a `failFast` flag so a swallowed obs-update send failure in the flush also re-queues instead of losing the correction while the VCR still sends. **Tests (~36 new):** `plan-c-p4d-emit-sites.test.js` (16 — builders REQUIRED-throw / stamp / null-omit, live engine ring/IR/enterScriptByName asks carry the id, ASK_STARTED_OBSERVER backstop, legacy ring/IR asks, dispatcher initial ask); `plan-c-p4d-batch-id.test.js` (4 — real EICRExtractionSession + mocked SDK: buffer A then B → result.utterance_id=B, empty-newest falls back to A, none→null, single-flush parity); `plan-c-p4d-legacy-frames.test.js` (6 — the WS-handler harness: onBatchResult clones the question with the epoch before enqueue, voice_command_response carries + omits it, reconnect replay strips spoken_response/action and emits a separate epoch-carrying voice_command_response) + the `_test_stampQuestionsWithUtteranceId` seam for the orphan-review stamp mechanism; the existing `wire-emit-ask-started-observer` assertion updated for the new `utteranceId` field. 5539 backend green (baseline 5503 + ~36, incl. the Codex-review additions: a real-session end-to-end A/B where the actual batched result carries B on extraction/question/live-VCR/reconnect-replay, the sync-transcript VCR + question, the periodic orphan-review consumedUtteranceId stamp, and the reconnect send-failure re-queue / FIFO / obs-failFast cases); corpus 5/5; lint 0 errors. ZERO client change (all fields additive-optional; the emit-site completeness gate is THIS plan, clients self-protect via the P4b `session_ack speech_epochs:1` capability + kill switches). Ship: both P4c (#106) + P4d land on `main` before any client arms; after merge, wait for the deploy JOB conclusion + backend task-def increment past `:331` before Phase 5 (web watchdog) / Phase 6 (iOS TestFlight). | `wire-emit.js`, `engine.js`, `ring-continuity-script.js`, `insulation-resistance-script.js`, `stage6-dispatcher-ask.js`, `stage6-shadow-harness.js`, `stage6-dispatchers-script.js`, `sonnet-stream.js`, `eicr-extraction-session.js`, `src/__tests__/plan-c-p4d-*.test.js` |
| 2026-07-19 | **F/U-2 + F/U-3 — specific voice notices for two successful-but-writeless dispatcher outcomes (backend-only; marker-② follow-ups).** **Bugs:** (F/U-2) `rename_circuit` with `from_ref === circuit_ref` and no meta is an idempotent noop — no circuitOp, no state-change TTS; (F/U-3) a calculator call whose EVERY selected circuit skipped `already_set` (meter wins) computes nothing. Both are successful noops with zero speech, so post-marker-② they drew the GENERIC "didn't give me anything to work with" apology — reads as failure, invites a confused re-dictation. **Fix — dispatcher→harness voice-notice channel:** dispatchers record `{text}` on the new `perTurnWrites.voiceNotices` ("Circuit N is unchanged — I didn't catch a new name or number for it."; "Zs for circuit 4 is already recorded — say a new reading to replace it." — refs listed ≤4 then "those circuits", per-tool friendly name); the notices are the SPECIFIC-FIRST branch of the marker-② net itself (Codex r1): they fire ONLY when the net would fire — zero other speech-intent — so a corrected rename's state-change TTS, a sibling call's F/U-1 calc read-back, or any D2/F7 prompt always supersedes a stale notice (never contradictory stacking); the §A4 drain emits field:null the same turn. Dispatchers deliberately cannot queue directly: an unstamped `pendingVoicePrompts` entry counts as current-generation and a cancellation would leak it onto the next turn; the accumulator dies with a cancelled turn. Wording ROTATES by a turn-derived index and the >4 batch scope carries the COUNT (Codex r1: the client field-nil dedupe is a 30 s text-keyed TTL with no operation token — byte-identical distinct notices would swallow the second chime's TTS; residual same-variant-same-scope repeat within 30 s = accepted D-2 class). `confirmationsEnabled`-gated. Mixed skip reasons / partial computed batches record NO notice (the F/U-1 read-back or the generic catch-all owns those). 11-case unit suite (incl. rotation + count-scope) + harness integration/composition (wire emission, apology suppression, stale-notice drop when other speech owns the turn, mode-off drop); catch-all test (f)'s pinned-decision comment updated (the machinery pin stands). Backend 5492 green; corpus 5/5. Zero wire change. | `stage6-per-turn-writes.js`, `stage6-dispatchers-circuit.js`, `stage6-shadow-harness.js`, tests |
| 2026-07-19 | **F/U-5 — backend consumes the PWA `board_info` containers; web board-level fields no longer stale in the snapshot (backend-only; found + deferred in the F/U-4 Codex review; Codex-hardened over 4 cycles).** **Bug:** the TOP-LEVEL `board_info` container — the PRIMARY board-fields carrier for single-board PWA jobs (`boards: null`) and the ONLY container the web regex-apply layer writes voice board fields to (boards[] rides along stale in the same `job_state_update`) — was NEVER read by the seeder or `_mergeIncomingJobStateIntoSnapshot`: board-level fields were invisible from turn 1 and mid-recording web board edits were silently dropped. A nested `boards[].board_info` (documented PWA shape; NO live producer today — the Board tab writes flat entries) additionally rode the shallow board copy into the snapshot as an unreadable blob. **Fix (shared `_applyTopLevelBoardInfo`, seeder + updater):** board_info FIELDS route to `circuits[0]` — the canonical main-board bucket where `record_board_reading` writes, the serialiser reads, and the board-aware Ze resolver's circuits[0] source looks (r1 BLOCKER: a record-targeted merge would hide fields from the serialiser AND let a stale seeded `ze` SHADOW a later dictated correction — the resolver checks the record first); identity (`designation`/`board_type`) → the main board record. The accompanying boards[] payload is CLASSIFIED (r1 BLOCKER + r2 IMPORTANT): ≥2 usable boards or a sole SUB-board mirror → skip (the web mirrors boards[0], possibly a reordered sub-board — never stamp it onto main); a sole MAIN board → fields still merge (identity stays with the authoritative record — the regex-apply shape would otherwise lose every board write on any job whose Board tab was ever used); no usable boards → full consumption. Precedence: facts client-authoritative, measurements fill-empty (dictated wins — pinned end-to-end: `record_board_reading` ze=0.20 → stale mirror replay → `calculate_zs` computes with 0.20). `FACT_FIELDS` gains the real Board-tab property keys (r1 IMPORTANT — verified against `board/page.tsx`; measurements stay fill-only). `flattenBoardRecord` (flat-wins hoist, proto-safe, strips the nested key even when malformed) at both boards[] sites; `board_info` joins `MERGE_SKIP_KEYS`. 23 tests (r3: an id-less/junk boards[] at seed no longer destroys the synth main record — hydration keeps only usable id-bearing entries; r4: when no usable entry is main-typed the main record is preserved at the head so a surviving sub-board is never crowned main and sub circuits keep composite keys). Backend 5479 green; corpus 5/5. Zero wire change. | `eicr-extraction-session.js`, tests |
| 2026-07-19 | **F/U-1 — explicit `calculate_zs`/`calculate_r1_plus_r2` results are SPOKEN ("calculated as" read-back), never silently written (backend-only; marker-② follow-up).** **Bug:** calculator writes carry `source_turn_id '::calc::<tool>'` and were excluded from spoken read-back by the 2026-06-18 Audio-First auto-derivation exemption — an explicit "calculate Zs for circuit 4" computed and wrote the value with beep-then-silence (field probe B; marker-② deliberately exempted the fully-computed clean turn, so no apology fired either; the prompt's "read it back to the inspector" instruction was dead — model prose never reaches TTS in live mode). Post the Phase-4 steer (PR #99) the calc tools are reserved for EXPLICIT compute intent, so every `::calc::` write is an explicitly-requested result the hands-free inspector must hear. **Fix:** the bundler now synthesises a deterministic read-back for every computed calc result with distinct phrasing — "Circuit 4, Zs calculated as 1.21" / grouped "Circuits 1 to 3, Zs calculated as 0.55" (`buildConfirmationText`/`buildGroupedConfirmationText` gain a `calculated` option; calc-ness is a grouping dimension so a calculated and a dictated same-field/same-value entry never collapse into one line). Mirror/polarity derivations (`derived: true`) remain the designed-silent exception. **marker-② net simplification:** the designed-silent exemption (predicate 5 — outcome parser + loop-ledger exhaustiveness guard) is REMOVED as unreachable-by-design; keeping it would mask a calc read-back regression as designed silence. A computed turn whose confirmation is lost anywhere downstream now draws the catch-all apology (fail-audible; new regression-pin test), and a legitimately debounced calc read-back is predicate 4's already-heard evidence (new test). Partial batches speak their computed circuits; per-reason skip wording (`already_set`/`no_r1_r2`…) is the batched F/U-3 follow-up; a wholly-empty calc turn still fires the marker-② apology. **Wire/clients:** zero wire-shape change — the read-back rides the existing `confirmations[]` channel (standard field/circuit/board_id entry, new text only); iOS + web speak it verbatim, no client change (web companion: none needed). **Codex riders (3 cycles):** (r1, IMPORTANT) the loaded-barrel speculator built calc confirmation text WITHOUT the calculated flag — with `VOICE_LATENCY_LOADED_BARREL=true` in prod every calculator write opened a speculative ElevenLabs synth that could only DRIFT at `validateAgainstConfirmations` (wasted spend, no latency win; unit-pinned since the recorded lane disables the barrel by design). Fixed by threading write provenance (`::calc::` / `derived`) from the `onSnapshotPatch` entries into `_speculate` — calc speculations are bundler-byte-identical and servable, derived (mirror) writes skip speculation entirely (also removes a pre-existing waste class), calc-ness joins the broadcast-bucket identity. (r2, IMPORTANT) the bucket **back-link** rebuilt the key by hand and diverged from the lookup key — every bucket lost its `correlationId`, so broadcast detection could suppress but never ABORT the first in-flight synth (no prior test exercised the abort path). Fixed with one shared key builder used at both sites + dictated→dictated and calc→calc fan-out abort regression pins; (r3, NIT) the bundler's group key was STILL a hand-rolled twin with different value normalisation — unified as `buildFanoutGroupKey` in `confirmation-text.js` (trimmed value, matching the spoken-text contract; pinned vectors), now the ONLY fan-out identity across bundler grouping + speculator broadcast buckets. **Corpus:** new required_green fixture `frc_9aa68a43…` (audible twin of the F/U-4 write-contract fixture per its pinned note) RED-proven `audibility.output.out_zs_spoken` against the pre-fix base, 5/5 with the fix. Full backend suite 5456 green. | `stage6-event-bundler.js`, `confirmation-text.js`, `stage6-shadow-harness.js`, `loaded-barrel-speculator.js`, tests, fixture |
| 2026-07-18 | **F/U-4 — supply Ze/PFC canonical-key contract fix + board-aware Ze resolution + PWA job-state ingestion repairs (backend-only; marker-② follow-up; Codex-hardened over SIX review cycles to a clean verdict).** **Core bug:** `_seedStateFromJobState` + the updateJobState supply merge stored supply Ze/PFC under the legacy wire aliases (`circuits[0].ze`/`pfc`) while `dispatchCalculateZs`/`dispatchCalculateR1PlusR2` and every DICTATED supply reading use the canonical field_schema keys — so on any job-seeded session every "calculate Zs" skipped `no_ze` while the model-facing snapshot SHOWED the Ze (split brain feeding the marker-② class), and seeded-vs-dictated dedup missed. **Core fix:** supply-scope-only canonicalisation — `selectSupplyContainer` (key-filtered PER-FIELD merge across all plain-record containers, precedence supplyCharacteristics → supply_characteristics → supply — an earlier container with only unrelated facts cannot shadow Ze/PFC in a later one) + `normaliseSupplyIngest` (canonical snake FIRST, iOS camelCase, then short alias; STRICT finite-numeric SCALAR validation so non-numeric strings incl. prompt-injection text and array values are DROPPED, never serialized into the trusted server_canonical snapshot block) + `SUPPLY_MERGE_KEY_ALIASES` with `Object.hasOwn` discipline + `MERGE_SKIP_KEYS` gains `__proto__`/`constructor`/`prototype`. Deliberately NOT a `LEGACY_TO_CANONICAL_CIRCUIT_KEYS` rename: bare `ze` is a DISTINCT board_fields key (dictated board-level Ze lives at circuits[0] on main / on `boards[n]` records on subs) — scope guard pinned by test. **Codex-review riders (6 cycles):** (1) BOARD-AWARE Ze in both calculators — chain `board.ze → board.earth_loop_impedance_ze (a dictated sub-board bare Ze lands under the canonical key) → board.ze_at_db → board.zs_at_db (PWA record spelling) → origin circuits[0].earth_loop_impedance_ze`, mirroring shared-utils `resolveZe`/iOS canon; precedence-first-parse-once so whitespace never coerces to 0 and a present-but-invalid board Ze ("N/A"/"LIM") is a TERMINAL `no_ze`, never a silent fallback to the origin measurement; without this the newly-visible seeded origin Ze would have silently mis-computed sub-board Zs (probe: 0.50 instead of 0.75). (2) Board-target validation on both calculators: `board_id:'*'` rejects `{code:'board_id_star_unsupported',field:'board_id'}` (was ok-with-empty), unknown ids reject `board_not_found` (was misleading `circuit_missing`); the resolved target id (`input.board_id ?? currentBoardId ?? getMainBoardId`) threads through selector + bucket lookup (custom-main-id snapshots stay consistent) while the WRITE path keeps raw `input.board_id` (wire-preserving). (3) **PWA `job_state_update` was a silent NO-OP** — web nests the job under `msg.jobState`, backend read top-level keys only, so every web mid-session job-state update never refreshed the snapshot; new `src/extraction/job-state-frame.js` `unwrapJobStateFrame` (plain-record-only on BOTH the outer frame and the nested job; malformed → null sentinel → handler skips with a warn — the envelope fallback would CLEAR the circuit schedule). (4) The PWA snake-case `supply_characteristics` shape is now actually ingested at both sites. **Tests:** ~50 across the two F/U-4 suites (canonical seeding, precedence, scope guards, board-aware resolver incl. the record_board_reading→calculate integration, '*'/unknown-board envelopes, injection/array/inherited/__proto__ hardening, frame sentinels) + phase1-dedup case 22 updated. **Fixture:** `frc_a5f8cfeb…` — explicit "Calculate Zs for circuit 4." with seeded supply Ze; RED-proven EXACTLY `reading.op_zs_calc` against pre-fix main @ 38781f55 (file overlay); flipped `expected_red → required_green`; corpus 4/4; `chime_observed:false` deliberate (write-only lock; the audible calc contract is open F/U-1). | `src/extraction/eicr-extraction-session.js`, `src/extraction/stage6-dispatchers-circuit.js`, `src/extraction/stage6-dispatch-validation.js`, `src/extraction/stage6-tool-schemas.js`, `src/extraction/sonnet-stream.js`, `src/extraction/job-state-frame.js` (new), `src/__tests__/eicr-extraction-session.fu4-supply-ze-canonical.test.js` (new), `src/__tests__/sonnet-stream-job-state-frame.test.js` (new), `src/__tests__/eicr-extraction-session.phase1-dedup.test.js`, `tests/fixtures/field-replay-corpus/frc_a5f8cfebcb5cd7749412d09d984ebf09/fixture.yaml` (new) |
| 2026-07-18 | **marker-② — a chimed turn with zero audible output ALWAYS speaks: catch-all audibility net + calculate_zs prompt steer (backend-only; numeric-gate-redesign wave).** **The bug (live prod test 2026-07-18, marker-① deployed on `eicr-backend:326`, reproducible 8/8):** dictating a field+circuit with NO value — "Zs for circuit 4." — makes the model call `calculate_zs` (CloudWatch `turn_core_summary`: `tool_names_per_round=[["calculate_zs"],[]]`, `tool_error=0`, `readings_count=0`). The tool SUCCEEDS structurally but computes nothing (`{ok:true, computed:[], skipped:[…]}` — no inputs), so the chimed turn ends silent. **Why every existing net missed it:** a successful tool call defeats `producedNothing` (A3/marker-①), `is_error:false` defeats the M1 all-rejected net, and no `ask_user` was attempted so the F7 pre-emission net declines — the distinct class "a tool ran, didn't error, but emitted nothing audible". **The net (Phase 1):** placed AFTER A3/D2/F7, BEFORE the §A4 drain in `runLiveMode`; fires ONE rotating apology (new `CATCHALL_AUDIBILITY_PROMPTS`, 5 phrasings, string-distinct from every other apology family — pinned by test so the client A1(b) text-keyed dedupe never cross-dedupes channels) onto the pendingVoicePrompts FIFO when `confirmationsEnabled` ∧ `chimeObserved` ∧ not-cancelled ∧ ZERO speech-intent survived ∧ not designed-silent. Speech-intent = audible confirmations + `emittedAskToolCallIds` + current-generation queued prompts + a NEW per-turn `debouncedConfirmationCountThisTurn` (produced-then-debounced = already heard — no false apology; the cumulative session counter would falsely exempt later turns). Readings/observation COUNTS deliberately excluded (writes are UI state, not speech). **Exemption classified by dispatcher OUTCOME, never tool name, and WHOLE-TURN (Codex diff-review cycle 1):** exempt only when EVERY tool call is a FULLY-computed calculator success — `ok:true ∧ computed.length>0 ∧ skipped===[]` (calculator writes carry `::calc::` and are read-back-exempt BY DESIGN — the Phase-0 source check CORRECTED the plan's assumption that a successful compute is audible) — AND the loop ledger is clean (not aborted/cap-hit, zero per-round errors, attempted==accumulated: thrown dispatchers / padded `internal_no_result` never reach `tool_calls`, so `every()` over the visible subset alone would exempt invisible failures; missing ledger arrays fail CLOSED). Partial batches (computed+skipped in ONE envelope), mixed turns (computed + empty/rejected/silent sibling) and malformed `skipped` shapes all FIRE; `computed:[]` fires for BOTH missing-input AND already-recorded (pinned: never silent this wave). Preparatory hoist: `isAudibleText`/`isCurrentGenPrompt` lifted to `runLiveMode` scope (were block-scoped in F7+A4). **Fixture (Phase 2):** `frc_b6ec5356…` — RED-proven (`expected RED confirmed: audibility.turn` with the net stashed, no infra failure) then flipped `expected_red → required_green` changing ONLY `gate_state` + removing `expected_failure_id` (transition-immutability). Authored to the seeded state's ACTUAL skip reason `no_ze` (the job_state seeder writes supply Ze as the short `ze` key, never `circuits[0].earth_loop_impedance_ze`); the `no_r1_r2` variant is pinned in the unit suite. Corpus 3/3. **Generic-failure finalization (Codex cycle 2):** a GENERIC live `runToolLoop` failure (network/API/stream error) previously early-returned an EMPTY extraction BEFORE every audibility net — beep-then-silence on any transport error; it now deliberately REUSES the F7 Item-3 cancellation finalization (cancelled latch, pre-crash writes read back, the F7 nothing-audible fallback speaks ONE apology, well-formed partial result to iOS, `stage6_live_error` retained; parity residuals — emitted-ask-counts-as-audibility, script hooks skipped — identical to the shipped cancellation semantics, owned by PLAN-C). **Tests (Phase 3):** 24 new (fires: no_r1_r2 / already_set / silent-write / mixed computed+empty / mixed computed+rejected / mixed computed+silent-op / partial batch / invisible-failure ledger / cap-hit / malformed-skipped / abnormal-stop max_tokens / truncated-empty-mismatched ledgers / generic-transport-failure; gates: chime, confirmationsEnabled; exemptions: audible read-back, debounced-already-heard, board-switch verified-audible TTS, designed-silent computed>0; marker-① mutual exclusion; whitespace + generation-ownership helper semantics; apology-family distinctness + rotation). **Prompt steer (Phase 4, same PR):** `sonnet_agentic_system.md` — `calculate_zs`/`calculate_r1_plus_r2` reserved for EXPLICIT compute intent ("calculate/work out/derive"); a bare value-less field+circuit mention is an INCOMPLETE READING → exactly ONE `ask_user` (`reason:missing_value`, `expected_answer_shape:number`), even when inputs are present (pinned: a computed value would mask the measured one; meter wins). Explicit-compute-with-inputs path PRESERVED. Verification = the three post-deploy live probes A/B/C (REQUIRED after the ECS rollout, not yet run at merge time — model behaviour is live-lane, not fixture-lockable). **Follow-ups logged (not fixed here):** successful calc-only turns are silent by design (candidate: deterministic spoken read-back of calc results); `rename_circuit` no-designation same-ref silent edge; specific "already recorded" wording. Clients: ZERO change — the apology rides the existing field-nil confirmation channel (ledger row `recording/catchall-audibility-apology`); the per-client chime-silence watchdog is the separate PLAN-C wave. | `src/extraction/stage6-shadow-harness.js`, `src/__tests__/stage6-catchall-audibility-net.test.js`, `tests/fixtures/field-replay-corpus/frc_b6ec5356f67d8655db214b4f16ae8d83/fixture.yaml`, `config/prompts/sonnet_agentic_system.md`, `docs/reference/architecture.md`, `web/docs/parity-ledger.md` |
| 2026-07-17 | **marker-① — a chimed no-op ALWAYS speaks (backend-only; EICR #97). Numeric-chatter gate HELD after Codex review.** Closes the chime-then-silence-via-model-no-op class (keystone `frc_c55c996fa1014e088455af77216220d1`). **The bug:** field session 36731498 turn 4 — "Circuit 2 is upstairs lights" was Flux-garbled to "Chuck it too is upstairs lights"; the live model treated the garble as a NO-OP (one `end_turn` round, zero tools). A processing chime had already fired, so the inspector heard a beep then SILENCE and lost the reading — Audio-First #1 / the F7 headline invariant ("there must ALWAYS be a follow-up TTS if there has been a beep"). **Why it slipped:** the A3 orphan net is already the zero-tool (`producedNothing`) net, but its content gate `(carriesValue \|\| carriesObservation)` requires a digit or an observation lead-in; the garble has neither. The F7 pre-emission net only covers the ask-ATTEMPTED-but-suppressed class (tool calls happened). **Backend fix (Shape A — a GUARANTEE, not a model best-effort):** loosen the A3 gate to `(carriesValue \|\| carriesObservation \|\| chimeFired)` where `chimeFired = options.chimeObserved === true`; `chimeObserved` threaded through `runShadowHarness` — production sets it `true` at the sole post-gate call site (the `if (!gateDecision.forward) return` above it means gate-pass ⟺ chime), the recorded replay lane sets it per-turn from the fixture's `chime_observed` (so a fixture with no recorded chime never triggers a false apology). New rotating `NOOP_AUDIBILITY_PROMPTS` (turnNum % len, like ORPHAN_PROMPTS — a repeat garble re-apologises rather than being swallowed by the A1(b) 30 s field-nil dedupe) with NO "reading" noun and distinct from every ORPHAN_/REJECTED_/OBSERVATION_ text; forensic cause `chimed_noop_no_content`. `carriesValue`/`carriesObservation` keep precedence (better wording). Chose the A3 net over the F7 pre-emission net the draft first proposed: the A3 net IS the `producedNothing` net and already owns the rotation/dedupe/context-carry plumbing + the answer-turn guard. **Gate:** keystone `frc_c55c996…` flipped `expected_red → required_green` in the same change (`red_proof_failure_id: audibility.turn` retained as dual-proof); the RED→GREEN transition was proven live (XPASS the instant the net shipped, then green on the flip). 5 new marker-① unit tests (positive, the load-bearing chime gate, `carriesValue` precedence, answer-turn skip, rotation). The apology rides the existing field-null confirmation channel the client already renders (F7 apologies use it) → **NO client change** for marker-①. **Numeric-chatter gate — HELD after Codex review (built, then reverted):** the intent was to stop bare-number chatter ("0.05", "30") chiming so it never draws a marker-① apology; a letter-free bare-number block was built on the CLIENT gates (web `transcript-gate.ts` + iOS `TranscriptGate`, byte-identical — the chime is client-side, so client-side is where a chime is suppressed; the backend `pre-llm-gate.js` deliberately kept forwarding so it never strands an un-updated client's chime). **Codex `gpt-5.6-sol` review found a genuine reading-loss (Audio-First #2 violation):** a bare-value repeat ("0.35") that COMPLETES a server-side `orphanContext` (turn 1 "Zs for upstairs sockets" no-ops → orphan stored; turn 2 "0.35") would be BLOCKED by the client gate → the reading is silently lost. The client cannot see the server's orphanContext, so **no client-side bare-number gate can avoid this** — the gate is unsafe as designed. Reverted from the wave (web + iOS #34 held); surfaced to Derek for a proper cross-platform design (a backend→client "orphan pending" signal so the client suppresses the chime ONLY when there is nothing to complete). Consequence until then: bare-number chatter draws a marker-① apology — safe (never silent, never dropped), just chatty on the rare bare number. **Tests:** 5371 backend green; corpus gate 2/2; 3 gate-machinery unit tests migrated to an ANSWER-turn RED (`in_response_to:true`) — the marker-① net now heals the old non-answer no-op vehicle, but the answer-turn path is deliberately uncovered (the ask-resolution path owns it) so a chimed answer-turn no-op is still a valid `audibility.turn` RED. Backend-only ship via CI. | `src/extraction/{stage6-shadow-harness.js,sonnet-stream.js}`, `scripts/field-replay/lib/session-builder.mjs`, `tests/fixtures/field-replay-corpus/frc_c55c996…/fixture.yaml`, `src/__tests__/{stage6-orphan-net.test.js,field-replay/replay-runner-core.test.js,field-replay/replay-cli.test.js}` |
| 2026-07-17 | **Retire chitchat-pause (backend + web; iOS companion rides TestFlight). Decision: Derek.** The electrical-term forward-gate (`TranscriptGate`/`pre-llm-gate.js`, STRONG_TRIGGERS zs/ze/pfc/r1/r2… + earthing/identity/observation) already filters non-electrical chat, so the separate 8-no-op / missing-context-ask pause (`chitchat_paused` envelope → stop forwarding to Sonnet + client banner/Resume) was a redundant second brain AND, under the beep-is-a-promise rule, a split-brain chime-then-silence risk (backend pausing while the client keeps chiming). Removed `chitchat-pause.js` + 2 tests, the sonnet-stream transcript-path pause gate / `chitchat_resume` case / both recordChitchatTurn hooks / the chitchatNotifier closure, the chitchatNotifier plumbing in stage6-dispatcher-ask + stage6-shadow-harness, the field-replay session-builder chitchat modeling + CLI runner + tests, the two CHITCHAT_* env-var classifications, and 3 now-unused pre-llm-gate pattern imports; web: chitchat-pause-banner + recording-context state/handlers + sonnet-session decode + harness stubs. The forward-gate is UNTOUCHED. Accepted trade-off (Derek): a sustained numeric/electrical-adjacent chat streak now forwards each turn (and, once the beep-is-a-promise ①-fix ships, apologises each turn) instead of pausing — lever is gate precision, never re-add chitchat-pause. 5350 backend + 1426 web tests green. | `src/extraction/{chitchat-pause.js(del),sonnet-stream.js,stage6-dispatcher-ask.js,stage6-shadow-harness.js}`, `scripts/field-replay/lib/session-builder.mjs`, `scripts/voice-latency-bench/transcript-replay-direct-runner.mjs`, `scripts/field-replay/replay-environment.mjs`, `scripts/audit-env-var-source.sh`, `web/src/{components/recording/chitchat-pause-banner.tsx(del),app/job/[id]/layout.tsx,lib/recording-context.tsx,lib/recording/sonnet-session.ts}` |
| 2026-07-17 | **Field-replay correctness gate — accident-class MVP + keystone fixture ① (backend test-infra + CI; ZERO backend-behaviour change, ZERO client runtime change).** The missing live-exercise gate the F7/D2 re-failure demanded: replays REAL captured field sessions through the REAL `runShadowHarness` and asserts the audibility/write/clarification invariants as a MERGE gate — a fix for a field bug must pass that bug's captured transcript before it can ship. **Re-scoped from the original governance-heavy foundation** (held as draft #92 after a Codex review found 14 BLOCKERs concentrated in the signed-governance/trusted-evidence/oracle core) and built VERTICALLY — one proven slice at a time. **Slice 1 ships:** the deterministic recorded lane as the WHOLE blocking gate (`npm run replay:field-corpus` inside Test Backend — each expected_red fixture must fail with EXACTLY its target id, each required_green must pass, XPASS fails, infrastructure_error is a distinct outcome that can never satisfy a RED; the runner also runs structural + PII validation on every fixture) + the FIRST real on-disk fixture, keystone ① (session 36731498 turn 4: a Flux garble the model no-op'd → chime-then-silence), which reproduces the bug end-to-end and confirms `audibility.turn` RED. The runner/assertions were hardened in review: complete write oracle (grouped circuits require ALL, exactly_once cardinality, fail-closed infrastructure on kinds without a faithful oracle), schema/dispatcher expectations verified against the REAL tool schema (Ajv) + dispatch log, prestate fail-closed (accepted-but-unapplied → rejected; seed via job_state + real preceding turns), cross-fixture fake-timer + voice-latency-finalizer reset, expired-fixture terminal verdict (no corpus crash). **Deferred to field-replay-hardening-followups** (malice-hardening the accident-class threat model defers; primitives retained): signed-commit governance, trusted-run evidence + ci-history-checks history closure, HMAC source commitments, the nightly live lane (ANTHROPIC_API_KEY + protected environment), the per-fixture signed-attestation REQUIREMENT (validate-fixture still runs structural + PII). Delivery is PR-only. 5406 backend tests green. Full detail: [field-replay-corpus.md](field-replay-corpus.md). | `scripts/field-replay/**`, `scripts/voice-latency-bench/transcript-replay-direct{,-runner}.mjs`, `src/__tests__/helpers/f7-audibility-core.js`, `src/__tests__/field-replay/**`, `config/field-replay-runtime.json`, `tests/fixtures/field-replay-corpus/**` (new), `.github/workflows/deploy.yml`, `.husky/pre-push`, `docs/reference/field-replay-corpus.md` |
| 2026-07-15 | **F7 hardening wave (harness-first; backend-only, ZERO client). Audibility-invariant sweep + pre-emission ask-audibility net (task #16) + extraction-watchdog re-arm & generation cancellation (task #14).** Origin: field-feedback-2026-07-14 Codex out-of-scope findings Derek ordered executed post-ship. **Item 1 (harness-first, task #17):** a two-lane Stage-6 audibility-invariant sweep — the class-killer for the 28 review findings the PWA replay harness can't cover. Integration lane drives the REAL composition (real pendingAsks registry / `createAskDispatcher` / gate / `runToolLoop` via a mock Anthropic client + WS stub + MANDATORY fake timers advanced by imported `QUESTION_GATE_DELAY_MS`+`ASK_USER_TIMEOUT_MS`, no literals); the NINE invariant-(a) suppression cases were committed `test.failing` (verified RED on the audibility assertion, not a jest timeout) and flipped GREEN by Item 2 — the RED→GREEN proof inside one PR. Fast matrix lane covers the read-back-exactly-once oracle (grouped / same-slot overwrite / two-designations-per-op / derived-exempt), one-telemetry-row-per-confirmation, no `_confidence` on the wire, no `__`-sentinel in spoken text, and the A3/A4/§D2 families. Enum-driven ask-outcome classification asserts `GUARANTEED_PRE_EMIT_OUTCOMES` (8) and `EMISSION_EVIDENCE_REQUIRED_OUTCOMES` (7) DISJOINT + union == `ASK_USER_ANSWER_OUTCOMES` (a 16th member fails the suite until classified). **Item 2 (task #16):** closes the chime-then-silence gap — a per-turn `emittedAskToolCallIds` Set fed by a best-effort `onAskUserStarted({toolCallId, source})` hook fired ONLY on a successful send (initial / `pvr` broker / dialogue-engine `safeSend` via the `ASK_STARTED_OBSERVER` WS symbol — the single choke point covering all engine paths); a pre-emission net (AFTER §D2, BEFORE the A4 drain, `confirmationsEnabled`-gated) queues ONE fixed field-null apology when an ask was attempted, emitted-set empty, and nothing audible survives; §D2 tightened to require the continuation ∈ `emittedAskToolCallIds`; step-3b FAST-FAIL of closed-WS/throwing-send/`fallbackToLegacy` as `dispatcher_error`+`pre_emit` (no 45s dead-air); `tool_call_id` added to `toolLoopOut.tool_calls`; A4-drain trim fix; two telemetry rows + `generationId` (minted in `sonnet-stream.js`) on them + `ios_send_attempt`. **Item 3 (task #14):** per-turn extraction-watchdog CONTROLLER — an `askChainObserved` latch (via the scalar `onAskRegistered` CONTROL hook) extends the 30s no-ask deadline to `EXTRACTION_WATCHDOG_ABSOLUTE_MS = 3*ASK_USER_TIMEOUT_MS + 2*EXTRACTION_WATCHDOG_MS` (195000ms); REAL cancellation via one `AbortController` per generation threaded through `runShadowHarness`→`runToolLoop` (signal on every `messages.stream`, `throwIfStage6Cancelled` guards, SDK `APIUserAbortError` canonicalised) + `rejectAll('timeout')`, NEVER force-clearing `isExtracting` (the generation-guarded `finally` does, so no concurrent extraction); one shared FATAL discriminator (`stage6-control-flow-errors.js`) rethrown before every generic recovery (tool-loop / `gateOrFire` now `new Promise(resolve,reject)` / dispatcher outer-catch / shadow live+shadow catches); on cancellation `runLiveMode` FINALIZES a partial (bundler + designation maps + generation-owned drain + fallback + `ios_send_attempt`) — every applied write still read back once, SKIPping only `toolLoopOut`-dependent A3/§D2/cost/core-summary blocks + dialogue hooks (realized via inline `cancelled` guards, behaviorally identical to the plan's finalizeLiveTurn contract); two watchdog telemetry rows. Comment sweep 20s→45s. `onAskRegistered` fail-closed as `AskRegistrationHookError`; stale-generation `false` verdict resolves `timeout`. 5207 backend tests / 19 skipped green. | `src/extraction/stage6-control-flow-errors.js` (new), `stage6-shadow-harness.js`, `stage6-dispatcher-ask.js`, `stage6-tool-loop.js`, `stage6-ask-gate-wrapper.js`, `stage6-pending-asks-registry.js`, `sonnet-stream.js`, `dialogue-engine/helpers/wire-emit.js`, `scripts/voice-latency-bench/transcript-replay-direct.mjs`, `src/__tests__/{stage6-audibility-invariants,stage6-audibility-matrix,stage6-ask-audibility-net,wire-emit-ask-started-observer,stage6-control-flow-errors,sonnet-stream-extraction-watchdog,stage6-live-cancellation}.test.js` + helpers/f7-audibility-matrix.js, `docs/reference/architecture.md` |
| 2026-07-15 | **D2 mutation-to-chain correlation (own wave; backend, ZERO client — closes the multi-observation beep-then-silence path).** Origin: field-feedback-2026-07-14 Codex cycle-5 finding #4 (out-of-scope there; Derek ordered post-ship). The §D2 dropped-net previously evaluated only the globally-latest answered clarify chain and let ANY later successful `record_observation` qualify it, so for `[answered chain A, answered chain B, one record_observation]` the single write suppressed the whole fallback even though one clarified observation was necessarily dropped, and the earlier chain never retired. **Fix, 5 parts:** (1) SCHEMA (`stage6-tool-schemas.js`) — new `record_observation.clarification_chain_id` `anyOf[string,null]`, IN `required` per the STS-05 nullable-required convention (`null` = direct/unclarified; `undefined` treated defensively as `null`). All THREE model-facing chain-id descriptions (ask_user tool desc :515, ask_user.clarification_chain_id :605, the new record_observation property) rewritten to require the echo on the eventual `record_observation` too, "and nowhere else" removed, and the ask_user success-shape documented as `{answered, untrusted_user_text, clarification_chain_id?}`; both exact-required-list assertions 7→8; stale strict:true / "array of 8 tools" comments refreshed to 16-tools / additionalProperties:false; stream-assembler observation fixture relabelled partial-assembler-only. (2) PROMPT (`sonnet_agentic_system.md`) — CHAIN ID bullet requires the same server-issued id echoed on BOTH the continuation ask AND the resolving `record_observation`; Example 13 shows the echoed id; Examples 11/12 pass `clarification_chain_id:null`. ONE edit (prompt-cache paid once with the schema change). Example 13 was later (Codex diff-review) expanded to THREE record_observation outcomes (C1/C2/C3), each echoing the id AND carrying a `suggested_regulation` so a coded observation is ACCEPTED at dispatch, not rejected (a coded observation with null/empty regulation is rejected → the exact fallback this wave prevents); Examples 11/12 likewise gained `suggested_regulation` and dropped the stray `source_turn_id`. The examples stay ILLUSTRATIVE (concise, `schedule_item:<pick>` convention preserved; other nullable fields omitted and defaulted null by the non-strict dispatcher) — deliberately NOT exhaustive eight-field literals, matching the prompt's existing worked-example style. This raised the STQ-01 token regression locks by ~60 tokens (base 14850→14950, combined 20100→20200) — documented in both cap comments as legitimate worked-example growth for the wave's core model-facing contract, not bloat. (3) HELPER (`stage6-ask-gate-wrapper.js`) — pure exported `normaliseObsClarifyChainId(value)` (non-empty string or null, NO minting), shared by the ask wrapper and the net; the net resolves matches against the current turn's answered-anchor map only, never broker membership. (4) D2 NET (`stage6-shadow-harness.js`) — GROUP answered clarify asks by chain id (latest anchor per chain), evaluate each chain once against events after ITS anchor, correlate each successful write (`is_error !== true` AND parsed body `ok === true`, internal-catch parser) to its chain. Mutation-id resolution LENIENT on both sub-cases (Derek): id-less (D-1a) or unknown non-null (D-1b) qualifies EVERY evaluated chain whose anchor precedes it (avoids false apology → re-dictation → duplicate); only a non-null id matching a DIFFERENT evaluated chain fails to qualify this one. COLLAPSED single count-aware fallback (singular vs plural — per-chain identical texts would be client-swallowed by the A1(b) 30s field-nil TTL); every evaluated non-null chain retires exactly once, after all decisions. Telemetry: `stage6.observation_clarify_dropped_net` extended (unqualified_chain_ids, qualified_chain_ids, anchor-index-ordered anchor_tool_call_ids for ALL evaluated chains, lenient_qualification bool, tool-call-ordered mutation_id_kinds array) + new `stage6.observation_clarify_lenient_qualification` INFO row (one per lenient mutation that newly qualifies ≥1 chain). The raw model-controlled chain id is NEVER logged (it bypasses the observation leak filter) — only mutation_id_kind + server-minted chain ids. (5) ZERO-CLIENT — dispatcher returns exactly `{ok:true, observation_id}`; `clarification_chain_id` never enters extractedObservations / perTurnWrites / renameObservationsForLegacyWire / any log row (pinned). **Known limitation D-2 (dated, ACCEPTED):** any same-wording D2 fallback repeated within 30s may be client-deduped (stable wording preserves the A1(b) purpose; no wording variants added). Test matrix items 1-16 added (multi-chain correlation, ordering, lenient telemetry, no-raw-id, real-shape anchor ids, real-dispatcher echo, zero-client). Full suite 5159 passed / 19 skipped (+28 new D2 tests over the 5131 baseline). | `src/extraction/stage6-tool-schemas.js`, `config/prompts/sonnet_agentic_system.md`, `src/extraction/stage6-ask-gate-wrapper.js`, `src/extraction/stage6-shadow-harness.js`, `src/__tests__/stage6-{tool-schemas,agentic-prompt,dispatcher-ask,dispatchers-observation,observation-clarify,shadow-harness-observation-rename,shadow-harness-mode-plumbing,stream-assembler}.test.js`, `docs/reference/architecture.md` |
| 2026-07-15 | **Field-feedback wave 2026-07-14 — WEB COMPANION (deliverable 3; web-only, ZERO backend).** Closes the mandatory web-companion section of the 6B6FE011 wave now that the backend mirror (PR #87) is in main. **A1(a) twin — token-aware dedupe key:** `confirmation-dedupe-key.ts` gains the `DEDUPE_TOKEN_FIELDS` allowlist (circuit_op, observation, observation_deletion, field_cleared, circuit_designation) and prefers the backend-stamped `${field}_${dedupe_token}` key in EVERY branch (top-of-function check, iOS-canon; empty-string = absent); the wire `Confirmation` type declares `dedupe_token` (confirmations pass through the envelope raw — the type declaration IS the decode); measured-value fields ignore tokens (bare `{field}_{circuit}` stays load-bearing for the correction cross-match); token absent = legacy shapes byte-unchanged. Vectors REGENERATED by executing the token-aware backend mirror `src/extraction/ios-dedupe-key.js` (read-only script) and pinned identical to the Swift + backend drift tests — desig single (`desig_2_turn-9`), ordinal (`_ord0`/`_ord1`), board-scoped (`desig_1_board-A_turn-9`), clear `_ord0`, obsdel/obs/circop, spurious-token negatives, degenerate-fallback hashes. **A1(b) twin — 30 s field-nil apology TTL + shared forget:** new `confirmation-dedupe-store.ts` (three stores, injected clock): permanent Set for field read-backs (unchanged D6 semantics, inserted at reserve), 30 s TTL map for FIELD-NIL confirmations (apologies — `conf.field == null`) stamped at AUDIBLE PLAYBACK START, and AGELESS reservations while queued (reservation ≠ TTL entry, plan round-24 — an apology deferred past 30 s can't expire unheard and duplicate). `tts-queue.ts` gains an `onPlaybackStarted(dedupeKey)` module hook (mirror lifecycle of `onDiscarded` — cleared by `reset()`); recording-context wires isLive/reserve at the D6 loop, routes `onDiscarded` through the shared `forget` (clears ALL THREE stores — previously permanent-set-only, which would have left a TTL stamp suppressing a never-heard apology), and resets all stores at BOTH session-boundary sites. F7/F10's identical apologies 11+ min apart would now both play. **C4 web aliases:** `transcript-field-matcher.ts` + `number-normaliser.ts` gain `\bzedi\b`→Ze (both tables — the normaliser output is the backend-facing text) and RCD_TIME_PATTERN/RCD_TIME_FLEX accept `(?:rcd\|icd)`; exact mirror of iOS 67ffb9d (third trip-time pattern untouched, matching iOS; `keyword-boosts.ts` untouched — iOS made no keyterm change, WS4 curation HELD). **A2 mock-lane pin:** replay harness gains a `field_corrected` mock-frame type (scenario/fake-session/runner) + `a2-clear-wire-canonical.test.ts` — canonical `r1_plus_r2` clears the `r1_r2_ohm` cell (not the bare-R2 cell, not the sibling circuit, no raw-key leak) and CLEAR_WIRE_EXEMPT `r2_ohm` arrives RAW and clears `r2_ohm`; RED-proofed by removing the map entry on a scratch edit. **B1 verification — NO GAP:** web has no client-local address-for-client ask path (zero `address_for_client` hits; all asks server-driven) — the F1 dual-ask class is structurally impossible on web; ledger row `recording/address-dual-ask` records the verification. **B2 — DEFERRED with owner Derek:** web has NO observation-processing cue at all (the base cue pre-dates this wave and was never ported), so the mirror = base cue + defer-drain delta — its own ear-verifiable wave; dated ledger row `recording/observation-processing-cue` (missing). Ledger: `readback-dedup-rekey` updated (token-aware), new rows `confirmation-dedupe-ttl` (match) / `garble-aliases-f8-f10` (match) / `observation-processing-cue` (missing, owner Derek) / `address-dual-ask` (match, no-gap); files-map extended. 1431 web tests green. | `web/src/lib/recording/confirmation-dedupe-key.ts`, `confirmation-dedupe-store.ts` (new), `tts-queue.ts`, `sonnet-session.ts`, `recording-context.tsx`, `number-normaliser.ts`, `transcript-field-matcher.ts`, `web/tests/harness/{scenario.ts,fake-services.ts,runner.tsx,a2-clear-wire-canonical.test.ts}`, `web/docs/parity-ledger.md`, `web/docs/parity-ledger-files.json` |
| 2026-07-14 | **Field-feedback wave — session 6B6FE011 (14 Winkelman Way, Reading; 10 voice reports F1-F10). Backend deliverable: five silent-path closures + two wrong-write fixes + garble aliases + two Derek-decided product changes. Headline invariant: "There should always be a follow-up TTS if there has been a beep."** **A1(a)** dedupe_token wire property: the five collision-prone text-op confirmation fields (`circuit_op`, `observation`, `observation_deletion`, `field_cleared`, `circuit_designation`) now carry a backend-emitted replay-stable operation token (deletion→obs ID; clear→{field,circuit,turn}; rename/designation→turn+op identity); `ios-dedupe-key.js` builders take an opToken and prefer `{field}_{token}` in EVERY branch (every "Observation deleted" is circuit:null + constant text → identical degenerate keys without it — the F2/F7/F10 client-swallowed read-backs); `confirmationDebounceKey` is token-aware (distinct ops survive, same-token replay suppressed); the `ios_send_attempt` telemetry loop + `_confidence` strip MOVED from the bundler to the harness post-debounce (three of the five fields never got rows; suppressed confirmations produced rows — the forensic contract was false both ways); measured-value fields keep the bare key (correction-TTS cross-match). **A2** clear_reading wire canonicalisation (F5: "clear R1+R2" spoke "cleared" while the cell stayed populated — raw `r1_r2_ohm` hit zero iOS clearer entries): outbound `field_corrected` wire copy canonicalised via FIELD_CORRECTIONS with NEW objects (perTurnWrites stays raw for same-turn clear+write TTS suppression); `CLEAR_WIRE_EXEMPT={r2_ohm}` (canonical `r2` would MIS-clear R1+R2 on build-418); schema excludes `circuit_ref`/`is_distribution_circuit`/`feeds_board_id` from clear_reading; a SEMANTIC round-trip audit over committed Swift-table fixtures (raw→wire→snakeToCamel→Circuit property vs record-APPLY property, domain == schema enum exactly, build-418 no-mis-clear lane) gates the deploy. F5 fully closes only after the iOS TestFlight (r1_plus_r2 + 4 audit-discovered gap fields). **A3** orphan net widened `carriesValue || OBSERVATION_PATTERN` with an observation-flavoured apology (F3/F9: digit-less observations + Haiku zero-tool turns = chime-then-total-silence); verbatim session transcripts pinned. **A4** pending-value write-or-reask guarantee (F8: "ICD trip time… 26 ms" → context_field:"none" ask → "RCD trip time." reply → silent `no_value_context`, value stored NOWHERE): new `stage6-pending-value.js` (extractPendingValue w/ unit-binding + scope-token exclusion + never-guess; NET-NEW resolveFieldNameAnswer; typed schema-aware detectStructuredReading spanning all field families); registry stores `pendingValue` (sibling of pendingWrite) and copies it into the resolve outcome (the delete-before-resolution lifecycle trap); dispatcher captures at registration (transcript first, question fallback); resolution dispatches record_reading through the NORMAL path (read-back included) or re-asks via a `pvr-*` BROKER (registers BEFORE ask_user_started — a bare emit orphans the answer; NOT `srv-*` which bypasses the registry), 4-shape state machine, retry cap 1/shape, terminal = audible apology through the confirmation channel; classifyOvertake gains the 'none'+pendingValue free-text continuation (typed-detector-guarded — a complete fresh reading like "Ze is 0.22"/"earthing arrangement is TT" is an OVERTAKE, never consumed) + a narrow pvr numeric branch; handleTranscript classifies pending asks PRE-QUEUE while isExtracting (answers resolve+consume; evidence-backed moved-on rejects; everything else queues unchanged); the direct ask_user_answered gate re-injects detector-complete fresh readings; no-CPC/Class-II 'none' asks fall through untouched (pinned). **B3** leak filter sanitises a sole `suggested_regulation` `non-regulation-shape` offender to its embedded regulation token before validation instead of rejecting the whole call (F6: ~9s retry round; coded observations can't null the field); genuine leak signatures still whole-call reject. **C1** matchDesignation conservative fuzzy pass (Derek Q1): normalise case/plurals then length-aware Levenshtein (d0 ≤3 chars, ≤1 at 4, ≤2 above — both sides constrain) + strict margin; ask-ANSWER path only; EV/EM/AC negatives pinned; select_board stays id-only. **C4** rcd.js ICD + triptan deterministic aliases. **D2 mechanics**: ask_user gains optional `clarification_chain_id` — the gate wrapper mints a per-OBSERVATION chain id (NOT ctx.turnId — two ambiguous observations can share a turn), keys the ask budget `observation_clarify#<chain>`, dispatcher echoes it for the single bounded continuation; harness post-answer net anchored at the LATEST answered observation_clarify ask requires a later observation mutation or an unanswered continuation, else ONE deterministic audible re-ask (independent of isAnswerTurn/producedNothing — the A3 net structurally can't catch this). **Prompt wave (C1-C4+D1+D2)**: designation-match-outranks-ambient + creation-only-on-explicit-intent + garbled-ref ask; CLEAR-never-re-homes (F5's unrequested re-record of the cleared 0.86); ICD/Zedi aliases; D1 professional observation rewording (RULE 1b fact-preserving BS 7671 tone, Examples 11-12 reworded — Derek F6 ask) replacing "verbatim"; D2 AMBIGUOUS C2/C3 SEVERITY (ONE targeted factual ask naming the deciding fact — never "C2 or C3?" — three-way crack outcomes C1/C2/C3, clear-cut guards, one bounded continuation; RULE 1/1a/3 + FI + RESTRAINT reconciled; no-CPC question pinned AUTHORITATIVE); token caps 13200→14850/18400→20100 (dated). **INV-2** ElevenLabs proxy `bytes_per_char` + WARN `elevenlabs_tts_audio_anomaly` >2500 (F1: 172,661 bytes for 33 chars — telemetry-first, retry guard deferred). iOS companion (A1 decode/TTL/reservation, A2 clearer, B1 dual-address, B2 cue defer, C4, INV-3) ships via CertMateUnified PR + TestFlight; web twin ships as deliverable 3. | `src/extraction/ios-dedupe-key.js`, `stage6-event-bundler.js`, `stage6-shadow-harness.js`, `stage6-tool-schemas.js`, `stage6-dispatchers-circuit.js`, `stage6-dispatcher-ask.js`, `stage6-pending-value.js` (new), `stage6-pending-asks-registry.js`, `stage6-overtake-classifier.js`, `stage6-answer-resolver.js`, `stage6-ask-gate-wrapper.js`, `stage6-prompt-leak-filter.js`, `stage6-dispatchers-observation.js`, `dialogue-engine/schemas/rcd.js`, `sonnet-stream.js`, `src/routes/keys.js`, `config/prompts/sonnet_agentic_system.md`, `tests/fixtures/ios-stage6-field-clearer.fixture.json` (new) |
| 2026-07-08 | **PWA replay harness Waves 2-7 + A4 feedback capture (pwa-replay-harness /ep run; web/scripts/tests/docs/CI, ZERO backend).** Wave 2 (B0/B1): provider-mount spike GO — the REAL RecordingProvider mounts headlessly in jsdom; module-level injection registry `test-services.ts` consulted at every external-effect boundary (Deepgram/Sonnet factories, mic, runtime-config, scheduler, diagnostic tap, job-state observer x5 apply sites, chime/haptic, TTS players; prod = null = byte-identical; full suite proved zero behaviour change). Wave 3 (B2-B4 + keystone): TraceCollector (3-source per-utterance behavioural trace), mock/live modes, YAML runner (`npm run pwa-replay`, hints-ON default), D1 seed invariants 1/3/5; KEYSTONE red/green proof — A1/A2/A3 reverted on scratch (`pwa-replay-prefix-2026-07-08` = 49b786ed) went RED with 16+12 violations across all three bug classes vs the sess_mrbnds2d_jczh + a3-gate-consequence fixtures, GREEN unmodified; the first RED attempt exposed the fake Deepgram bypassing the real Flux mapping (A1 stayed green) → FakeDeepgramService now wraps a REAL DeepgramService around a captive fake WS. Wave 4 (C1-C4): convert-session.mjs (server-origin-only frame reconstruction — regex-category events NEVER become frames, pinned by mock-frame-provenance.test.ts; initial_state_fidelity ladder), diff-traces.mjs (strict/loose lanes; iOS stale-circuit-hint quirk → WARN; derivative divergences non-strict; empty_fallback downgrade), `npm run pwa-replay:session` one-command; corpus ios-b1916ad6 (hand-authored initial state, PASS 0 strict fails — the Wave-4 gate) + ios-a02b018d. Wave 5 (D1-D4): invariants extended to all 7 (gate-pass justification re-derived on the DISPATCHED text — 'Two sugars'→'2 sugars' legitimately gains a digit trigger); generate-field-sweep.mjs → 116 committed scenarios (clean+garbled+chitchat per schema field) + --check CI sync guard; garbles.json (12 real-session entries w/ provenance + sweep_safe flags) + 30-line chitchat corpus; sweep surfaced+fixed: cross-scenario TTS-echo-fingerprint leakage in the runner, section readings must ride circuit 0 (applyCircuit0Readings === 0 — generator/converter/fixtures corrected), EIC-only comments divert; 117/117 green, ZERO voice-coverage gaps. Wave 6 (A4): feedback-capture.ts — literal iOS TranscriptProcessor HEAD port (trigger anchor, 3 exits incl. utterance-final and/an/in garble close, 30s/20-entry pre-trigger window, clear-on-capture §G3), dispatchFinal placement AFTER voice-command/BEFORE cumulative-append-regex-gate-chime-send, api-client debugReport → existing POST /api/debug-report, stop() auto-close pre-sessionId-rotation, 'Feedback logged' gated ack; iOS-verbatim leading-dot quirk kept (backend >=3-char guard is the noise filter); xfail removed — four-bug proof complete; NEW ledger row recording/voice-feedback-capture. Wave 7 (E1-E3): per-PR path-filtered mock lane in deploy.yml (sweep sync-check + harness + 116-sweep, deterministic/zero tokens); advisory nightly live lane (pwa-replay-nightly.yml: Postgres service + schema bootstrap + node-pg-migrate + per-run JWT_SECRET + harness-mint-jwt + Haiku/1h-cache env + no-AWS-credentials assertion + issue-on-failure; £10/month cap = fixtures-only corpus; PENDING ANTHROPIC_API_KEY repo secret — guard no-ops with a notice until provisioned, vault todo); docs/reference/pwa-replay-harness.md; skills updated. 1410 web tests green. | `web/src/lib/recording/test-services.ts`, `web/src/lib/recording/feedback-capture.ts`, `web/tests/harness/*`, `scripts/pwa-replay/*`, `tests/fixtures/pwa-replay*`, `.github/workflows/deploy.yml`, `.github/workflows/pwa-replay-nightly.yml`, `docs/reference/pwa-replay-harness.md` |
| 2026-07-08 | **PWA voice-pipeline live-bug fixes A1–A3 (pwa-replay-harness Wave 1; web-only, ZERO backend).** Root-caused from field session `sess_mrbnds2d_jczh` (2026-07-08, 2.2 min, four client bugs while the backend behaved perfectly). **A1 — Flux utterance-end parity** (`d27a88ef`): web `deepgram-service.ts` fired only `onFinalTranscript` for a transcript-bearing Flux `EndOfTurn`; iOS canon fires final + utterance-end. `isInspectorSpeaking` (set by the first interim, cleared ONLY by utterance-end/phantom-watchdog) stuck true after the first real utterance → `shouldDeferPlayback` stayed true → EVERY FIFO confirmation deferred forever — universal read-back silently dead on web since the Flux prod flip (2026-07-03, `ff620997`). Fix: EndOfTurn-with-transcript now fires final THEN utterance-end (iOS ordering); composition regression `flux-utterance-end-fifo-resume.test.ts` wires the REAL DeepgramService to the REAL tts-queue (units were green while the composition was broken). **A2 — non-circuit rescue set** (`39c322e4`): the orphan classifier buffered ANY `circuit < 1` reading, so a dictated client name was applied AND buffered → a false "Which circuit was that client_name reading for?" ask 2s later, whose direct-prompt preempt-flush DISCARDED the deferred read-back. Fix: new `non-circuit-fields.ts` — verbatim copy of iOS `supplyFields` (~77 entries); classifier extracted to pure `classifyReadingsForBuffer()`; rescued fields emit `non_circuit_field_rescued_from_buffer` (mirrors iOS). Drift guard asserts membership against the real `CIRCUIT_0_SECTION` route map with documented exception lists both ways. **A3 — regex freshness gate** (`4902ff2d`): the cumulative matcher re-scans old text by design, and the apply layer had NO value-equality check — after "Customer is Michael Payden." matched once, every chitchat utterance re-reported `install.client_name` as changed → gate PASS → sent-for-processing chime → empty Sonnet turn → silence (and the phantom hint reset the backend chitchat pause counter every turn). Fix: pure `computeFreshRegexWrites()` (all four write loops, tracker gating + trimmed-string value-equality vs an injected baseline) shared by BOTH env paths — hints-ON baselines on job state; hints-OFF (dev) on a per-session freshness shadow map (the value is never written to the job there). iOS canon: `applyRegexValue` `newValue != currentValue` → `thisTurnRegexWrites` → gate `hasRegexHit`. Gate semantics change deliberately: phantom passes are now blocked. **Fixtures** (`sess_mrbnds2d_jczh.yaml` + `a3-gate-consequence.yaml`, own commit): byte-exact transcripts hand-transcribed from the forensic CloudWatch log — this WEB session has no S3 debug_log (analytics-upload gap, new ledger/todo item), so the WS-C converter can never regenerate it. A4 (feedback marker capture) ships in Wave 6. 1383 web tests green. Ledger: flux-migration/tts-fifo/recording-context/transcript-gate rows annotated. Device ear-check A1 = vault todo. | `web/src/lib/recording/deepgram-service.ts`, `web/src/lib/recording/non-circuit-fields.ts`, `web/src/lib/recording/pending-readings-buffer.ts`, `web/src/lib/recording/apply-regex-match.ts`, `web/src/lib/recording-context.tsx`, `web/tests/*`, `tests/fixtures/pwa-replay-sessions/*` |
| 2026-07-08 | **CCU quality gate: classifier confidence softened to a corroborated signal + dead `too_many_nulls` filter fixed (backend, zero wire-shape change, zero client).** Field failure (Derek, live EIC job `job_1783503603620`, three uploads 09:39–09:43 UTC): a Contactum board pinned the Stage-1 `classifyBoardTechnology` confidence at exactly **0.82 on three different photos** (different angles/resolutions) because the board *model* is unidentifiable — while the downstream single-shot extraction was excellent every time (up to 13/13 circuits, full label coverage, all ratings read, quad fit 0.53). The gate's `classifier_low_confidence < 0.85` hard-fail converted each perfect extraction into a 422 "photo not clear, retake" — and since the score is a property of the BOARD, not the photo, **no retake could ever pass**. Fix in `evaluateQualityGate`: (1) classifier confidence between a new **0.65 hard floor** and the existing 0.85 threshold now passes when the extraction **corroborates itself** (≥ 3 OCPD circuits AND ≤ 50% null ratings — new `classifierMinConfidenceHard`/`corroborationMinOcpdSlots` thresholds; diagnostic gains `classifierSoftFail`/`corroboratedByExtraction`). Below 0.65 still hard-fails regardless — a truly confused classifier may have routed the photo down the wrong prompt (modern vs rewireable), so plausible output can't vouch for it. (2) **Latent bug fixed**: the OCPD filter for `too_many_nulls`/corroboration only matched slot-shaped rows (`device_kind`/`kind`/`classification`) — but live `geometric-merged` circuits from `buildCircuitFromSlot` carry NO kind key, so the circuits-based check was **dead on all live traffic**; OCPD circuits are now also recognised via `ocpd_bs_en` (every mcb/rcbo/rewireable/cartridge gets a class default; blanks stay null) / `is_rcbo` / `ocpd_rating_a`. Deliberately NOT fixed by improving board-model identification: model numbers have too much internal hardware variance to be a trustworthy signal (Derek, 2026-07-08). Field recovery: the rejected 13-circuit extraction was pulled from the S3 training log (`ccu-extractions/.../1783503721724-18k47n/result.json`) and written into the job's `test_results.csv` + `extracted_data.json` board patch, mirroring the client `apply-ccu-analysis` mapping. 19 gate tests (7 new), 304 CCU tests, full backend suite 4,974 green. Docs: architecture.md §CCU gains the missing quality-gate step and sheds four stale claims (uniform_low_conf, SPD-from-main-switch, classifier-only fallback, prompt-side label alignment); ccu-pipeline skill §6/§11 synced. | `src/extraction/ccu-quality-gate.js`, `src/__tests__/ccu-quality-gate.test.js`, `docs/reference/architecture.md`, `.claude/skills/certmate-ccu-pipeline/SKILL.md` |
| 2026-07-06 | **Web TTS FIFO queue + `cancel_pending_tts` handler — iOS AlertManager Phase 7.1 → web (web-only, ZERO backend; `web-tts-fifo-parity-2026-07-06` /ep session).** Root-caused from a live PWA session (`sess_mr8qrvcm_20jn`, CloudWatch `/ecs/eicr/eicr-backend`, 2026-07-06 04:50-04:52 UTC). **Two symptoms, one cause — the unshipped iOS TTS FIFO (WS3-FU `parity-ws3b-voice-latency-2026-07` item 8).** Symptom 1: a Sonnet turn that renamed TWO circuits emitted `readings:2, confirmations:2` correctly, but the web client fired both `speakConfirmation` 5ms apart and the second's `cancelElevenLabs()` aborted the first — only the LAST circuit was read back (Audio-First #1 violation). Symptom 2: "Zs is 0.6" with no circuit → backend correctly asked "Which circuit is the Zs 0.6 reading for?", but a phantom Deepgram SpeechStarted cleared `isInspectorSpeakingRef` via the 1.2s `speechConfirmTimer` WITHOUT draining `deferredTtsRef` (only `onUtteranceEnd` drained, and a phantom produces none) → the question was stranded, never spoken. **Fix = TWO paths, mirroring iOS Phase 7.1 scope (confirmations/fast-path ONLY; `ask_user`/alerts use a SEPARATE `deferredTTS` slot).** (1) NEW `web/src/lib/recording/tts-queue.ts` — a framework-free confirmation FIFO pump, unit-testable against a fake clock + injected player (no `tts.ts`↔`tts-queue.ts` circular import): a `startedPlayback` "was it ever heard?" flag set from the player's `onStart`; a LAST-MILE deferral gate that re-checks `shouldDeferPlayback()` POST-fetch/pre-`audio.play()` (iOS `playOrDeferQueueHead`, AlertManager.swift:1648 — so an inspector who starts speaking DURING the 1-3s ElevenLabs fetch isn't talked over; the prepared audio is parked and replayed on resume with NO re-fetch); drop-OLDEST overflow at `MAX_QUEUE_DEPTH=6` (iOS drop-oldest parity, keeps freshest read-backs); `preemptFlush()` / `purge(prefix)` / `reset()` teardown primitives; and a SYNCHRONOUS `onDiscarded(dedupeKey)` un-record for EVERY item discarded before it plays (overflow / preempt / purge / reset / deferred head / current head torn down mid-fetch — the Codex round-1 BLOCKER) so the backend can re-speak it on a later re-emit (never a permanent zero read-back). `elevenlabs-tts.ts` gains `prepareElevenLabs` (fetch/play split for the last-mile gate; `speakElevenLabs` LEFT UNCHANGED for the direct path + its 20 tests). (2) `tts.ts` two-path split: `speakConfirmation()` no longer calls `dispatch()` — it FIFO-enqueues via a thin injected player that replicates `dispatch()`'s responsibilities (echo-suppression fingerprint, ElevenLabs-primary + native-fallback, per-head `ttsWindow`); `speak()` (ask_user/alerts/tour) stays DIRECT and PREEMPTS the FIFO (`preemptFlush()` before `dispatch`, iOS `speakWithTTS`+`resetQueueAfterInterrupt`), NOT merged into it — routing an `ask_user` through the FIFO would make a "which circuit?" question wait behind up to 6 queued read-backs. Token-guarded module state `activeAudioOwner: {owner:'direct'|'queue', token}` set SYNCHRONOUSLY in `dispatch()` (the SOLE `'direct'` set-site, before the fetch, so the flag is `'direct'` throughout the pre-audio window a fast cancel arrives in) and from the FIFO player's `onStart` for `'queue'`; `cancelSpeech({resetQueue:false})` (barge-in) cuts ONLY a direct-owned prompt (BOTH ElevenLabs + native backends) and is a no-op against a queue-owned confirmation; `cancelSpeech({resetQueue:true})` (stop/unmount/tour) is a full teardown that runs `ttsQueue.reset()` FIRST (nulls `currentHeadId` so a stray synchronous `onEnd` from the subsequent cancel no-ops via the pump's id-guard — the post-stop stray-audio guard) THEN cancels. `dispatchNative` now fires `onStart` (from `utterance.onstart`) + `onError` (load-bearing for the iPhone/iPad-Safari native default). The misleading `tts.ts:32-40` "iOS cancels … we mirror for parity" doc comment (the prose ROOT of the bug — iOS FIFO-queues, does NOT cancel) rewritten. (3) `recording-context.tsx`: shared pure helper `handleInspectorStoppedSpeaking` (in new `tts-prompt-helpers.ts`) called from BOTH `onUtteranceEnd` AND the phantom `speechConfirmTimer` reset — drains the deferred DIRECT prompt via `speakDirectPrompt` (stays cancellable) AND calls `ttsQueue.resumeIfDeferred()` (releases a deferred confirmation head — the Symptom-2b clone, round-2 BLOCKER); defer gate registered as `() => isInspectorSpeakingRef.current || isDirectAudioActive()` (so a lower-priority confirmation can't cut off an in-flight direct prompt); `onDiscarded` registered to delete the un-recorded key from `confirmedFieldKeysRef`; the onExtraction confirmation loop threads the already-computed `buildConfirmationDedupeKey` into `speakConfirmation(sentence, { dedupeKey })`; barge-in switched to `cancelSpeech({ resetQueue: false })`. (4) `cancel_pending_tts` (iOS Phase 6.3) — the backend ALREADY emitted `{ type:'cancel_pending_tts', prefix }` on every `*_script_cancelled` (`engine.js:1020-1024`) and web had ZERO handling (a cancelled focused-mode script prompt could still play + its stale ask state mis-attributed the next utterance): `sonnet-session.ts` gains an `onCancelPendingTts` callback + a guarded `case 'cancel_pending_tts'` decode (ignores empty prefix) + `clearInFlightToolCallIdByPrefix`; `in-flight-question.ts` gains `removeByToolCallIdPrefix` (drops matching pending+active entries); pure `tts-prompt-helpers.ts handleCancelPendingTts` silences the DIRECT prompt in BOTH states (deferred slot AND in-flight — the in-flight cancel is NOT gated on the audio window, so a fast pre-audio-fetch cancel still lands — Codex round-1 BLOCKER) and clears the ask state everywhere it lingers (SonnetSession / InFlightQuestionTracker / `questions` UI / dismiss timers); `ttsQueue.purge(prefix)` is a forward hook (no-op today — confirmations carry no `cancelKey`). **Deliberate divergence** (ledger-noted): the voice-command `spoken_response` path stays on `speakConfirmation` (FIFO) rather than iOS's direct `speakResponse` — rare/low-stakes ack; future-pass candidate. ~90 new tests: `tts-queue.test.ts` (serial play, last-mile defer, drop-oldest, mid-fetch/native `startedPlayback` teardown, abort-advances-pump, discard-then-re-enqueue), `tts-prompt-helpers.test.ts` (deferred drain + resume + idempotency; cancel_pending_tts deferred/pending/playing/state-clear/cancellable-drain), `sonnet-session.test.ts` decode, `tts-fifo-confirmation.test.ts` (serial-through-`speakConfirmation` + tour regression), `in-flight-question.test.ts` prefix-removal; the existing `phase-8-tts.test.ts` gains a `__resetForTests()` between tests (FIFO state hygiene). Ledger-gate (Derek 2026-07-06): flip on merge/deploy, iPhone/iPad Safari ear-verify is a dated vault todo NOT a merge gate. Ledger: `recording/tts-fifo`→`match`, new `recording/cancel-pending-tts`→`match`, `recording/recording-context-tsx`+`recording/fast-path-tts` notes updated. Full web suite green (parallel + serial). | `web/src/lib/recording/tts-queue.ts` (new), `web/src/lib/recording/tts-prompt-helpers.ts` (new), `web/src/lib/recording/tts.ts`, `web/src/lib/recording/elevenlabs-tts.ts`, `web/src/lib/recording/sonnet-session.ts`, `web/src/lib/recording/in-flight-question.ts`, `web/src/lib/recording-context.tsx`, `web/tests/{tts-queue,tts-prompt-helpers,tts-fifo-confirmation}.test.ts` (new), `web/tests/{phase-8-tts,sonnet-session,in-flight-question}.test.ts`, `web/docs/parity-ledger.md`, `web/docs/parity-ledger-files.json`, hub `CLAUDE.md`/`AGENTS.md` |
| 2026-07-03 | **Test-harness hardening — make "green locally" mean "green in CI" (web-only test infra + CI hook + docs; ZERO backend; `test-harness-hardening-2026-07-03` /ep session).** Systemic fix for the CLASS of failure behind the WS7 CI break (the acute cause — a CONDITIONAL `localStorage` shim in `web/tests/setup.ts` — was already fixed in PR #81, commit `1bb83112`, unconditional shim; this plan builds on that and does NOT re-touch it). Four root causes closed. **(A) Environment divergence (the big lever).** The dev box runs Node v25 while CI pins Node 20 (`.github/workflows/deploy.yml` `node-version:'20'`, 4 sites) with no local pin, so "passed locally" was meaningless and the failure was unreproducible (jsdom/Storage/experimental-webstorage behaviour differs across majors). Added repo-root `.nvmrc`=20 (local pin for nvm users; CI still pins via `actions/setup-node`), `web/package.json` `engines:{node:'>=20 <21'}` (scoped to `web/` ONLY — NOT `engine-strict` in root `.npmrc`, which would hard-fail `npm install` for the whole monorepo incl. backend on Node 25), and `web/scripts/check-node.mjs` — a WARN-level preflight (reads `.nvmrc` for the expected major, warns + exits 0 on a mismatch; `CHECK_NODE_STRICT=1` makes it hard-fail) wired as `pretest` in `web/package.json` so it fires on EVERY `npm test --workspace=web`, not just pre-push (a plain local web run on Node 25 previously went silently green — the exact misleading signal removed). Root `package-lock.json` regenerated to record the new web `engines` metadata. **(B) No global test-cleanup hygiene.** `web/vitest.config.ts` set NONE of the cleanup flags (all default false), so spies, `vi.stubGlobal`/`vi.stubEnv` state, and mock call history leaked across tests/files → order-dependent "passes in isolation / fails in a full run". Set `restoreMocks`/`clearMocks`/`unstubGlobals`/`unstubEnvs`=true + `isolate:true` (explicit; already default). Verified vs vitest 4.1.4: those four flags do NOT restore fake timers, so a global `afterEach(() => vi.useRealTimers())` was added to `web/tests/setup.ts`. New `web/tests/harness-leak-lock.test.ts` regression guard pollutes shared state (fake timers + stubbed global + stubbed env + a spy) in one test and asserts the next test sees it all auto-reverted — fails loudly if any flag/afterEach is later dropped. Change-B remediation pre-check (grep for `beforeAll`-scoped persistent `vi.spyOn`) found NONE, so no existing test relied on cross-test spy persistence. **(C) Fragile direct-global overrides.** Direct `window.localStorage.{setItem,getItem,removeItem}=fn` reassignments (`terms-gate.test.ts` all three, `terms-page.test.tsx`) — the pattern a real jsdom Storage silently IGNORES, i.e. the WS7 failure mode — converted to `vi.spyOn(...).mockImplementation(fn)` (auto-restored by `restoreMocks`); `globalThis.fetch=fn` reassignments (`api-client.test.ts`, `runtime-config.test.ts`, `ws6-pending-ccu-queue.test.ts`) converted to `vi.stubGlobal('fetch', fn)` (auto-restored by `unstubGlobals`). Manual `try/finally`/`originalFetch` scaffolding removed where the auto-restore covers it; the terms-page async-accept macrotask race the old finally worked around is gone (spy reverts in afterEach AFTER the test resolves). The reverted 2026-07-03 experiments (afterEach force-restore, per-file pristine capture, FileReader macrotask-drain) were NOT re-introduced. Acceptance grep confirms zero direct `localStorage.{setItem,getItem,removeItem}=`/`globalThis.fetch=` reassignments remain (only explanatory comments). **(D) No local==CI full-suite gate.** `.husky/pre-push` ran ONLY root `npm test` (backend Jest) and force-prepended Homebrew Node v25 — so the web vitest suite that broke was never gated locally at all. Rewrote the hook: keep the load-bearing `export PATH=/opt/homebrew/bin` FIRST (minimal-PATH GUI-git fallback), source nvm best-effort AFTER (`[ -s "$HOME/.nvm/nvm.sh" ] && . … && nvm use --silent … || true`) so nvm's Node 20 wins when present, WARN via `node web/scripts/check-node.mjs` (deliberate early heads-up before the long backend run; also fires again via the web `pretest` lifecycle — expected/harmless double warning), then run BOTH `npm test` (backend Jest) AND `npm test --workspace=web`. A hard Node-20 gate at pre-push is NOT claimed (nvm is a shell function unavailable in husky's non-interactive `sh`; a hard gate would break GUI-git pushes) — WARN is the realistic mechanism. **Governance (WS1 tie-in):** a MANDATORY block added to BOTH hubs (`CLAUDE.md` + `AGENTS.md`) — parallel workstreams touching overlapping test files / shared harness must re-run `main`'s full suite after each merge before the next merges (the WS5×WS7 interaction that surfaced only post-merge and SKIPPED WS7's deploy). **NOTE (environment limitation):** the dev machine has no Node 20 available (no nvm/fnm/asdf/volta/homebrew node@20 — only v25), so the local runs used Node 25; CI's Node-20 run (watched via `gh run watch` before deploy) is the authoritative Node-20 verification. 1319 web tests green under Node 25, both default-parallel and `--no-file-parallelism`. | `.nvmrc` (new), `web/scripts/check-node.mjs` (new), `web/package.json`, `package-lock.json`, `web/vitest.config.ts`, `web/tests/setup.ts`, `web/tests/harness-leak-lock.test.ts` (new), `web/tests/{terms-gate,terms-page,api-client,runtime-config,ws6-pending-ccu-queue}.test.*`, `.husky/pre-push`, `docs/reference/deployment.md`, hub `CLAUDE.md`/`AGENTS.md` |
| 2026-07-03 | **Parity WS7 — mobile "indistinguishable" PWA chrome + T&Cs signature + circuit keyboard accessory (web-only, ZERO backend; `parity-ws7-pwa-chrome-2026-07-02` /ep session). Deployed; every WS7 ledger row `partial` until Derek's on-device iPhone A2HS smoke.** **(a) Circuit-cell keyboard accessory bar** (LIM / N/A / prev / next / Done, iOS `JobDetailView.swift` toolbar) across ALL THREE circuit surfaces — card view, sticky table, desktop schedule. Shared foundation: `web/src/components/job/circuit-focus-fields.ts` (single source of truth — `IOS_CIRCUIT_FOCUSABLE_FIELDS` = the exact 13-field iOS traversal order = `Constants.circuitFieldOrder` minus dropdowns minus booleans; `CIRCUIT_ACCESSORY_TOKEN_FIELDS` = those minus `circuit_ref`/`circuit_designation` per `JobDetailView.swift:1070-1075`; `WEB_EXTRA_CIRCUIT_KEYBOARD_FIELDS` = 12 iOS-dropdown-origin fields the web grids keep as free-text inputs — DATED DIVERGENCE: they register + get prev/next/Done but NEVER LIM/N/A; `CIRCUIT_FOCUS_ORDER` leads with the iOS 13 then the web-extras so core prev/next matches iOS and is never perturbed) + `web/src/components/job/circuit-keyboard-accessory.tsx` (`CircuitKeyboardAccessory` bar + `useCircuitAccessoryController` hook + pure `computeNavTarget`; positioned from `window.visualViewport` — renders only while a registered input is focused AND `keyboardInset>32`, falls back to `env(safe-area-inset-bottom)` on coarse-pointer no-visualViewport; LIM/N/A/prev/next `preventDefault` on pointerdown for blur-survival, Done blurs; prev/next wrap across circuits, disabled only at grid edges; LIM/N/A write via the surface's normal onPatch). Each surface owns a ref registry + `focusField` and feeds the shared controller through a small in-file context; the card's `focusField` uses a pending-focus latch (collapsed cards mount no inputs → cross-circuit Next expands the target card then focuses on register), and `applyCardToken` reads `patchCircuit` via a ref so a token write merges into the latest circuits array (data-loss guard). LIM wires to the WS3 sentinel path (`circuits/lim-sentinel-display`, PR #76). 34 tests (constants/order, `computeNavTarget` wrap+edge, controller visualViewport both cases + token eligibility + blur-survival + Done + coarse fallback; real sticky+desktop mounts; Tier-1 card auto-expand mirror). **(b) T&Cs acceptance-signature port** (iOS `TermsAcceptanceView` `termsAcceptanceSignature`, un-parked Derek 2026-07-01): `signature-canvas.tsx` gains `helperText`+`onContentChange` (fires true on bg-load/pointer-up, false from both clear paths via a shared `clearAll`); `legal-texts-gate.ts` adds `TERMS_STORAGE_KEYS.signature` + rewrites `recordTermsAcceptance({signatureDataUrl,now}):boolean` to write the signature FIRST then accepted/version/date in ONE try/catch, rolling back ALL four keys + returning false on any throw (closes the soft-bypass hole where accepted/version could persist without a signature); `terms/page.tsx` makes the signature the 7th attestation (completion 6→7), gates Accept on `hasContent()`, reads the PNG blob→FileReader→data URL, and redirects ONLY on a true persist (storage failure → inline retry error, no redirect). `hasAcceptedCurrentTerms()` stays accepted/version-only (existing users don't re-sign). Client-side only, NO backend write. 27 terms tests. **(c) Haptics** — `haptic('heavy')` at the gate-pass "sent for processing" beat (`recording-context.tsx`, mirrors `DeepgramRecordingViewModel.playChime()`; scoped to the live gate-pass, NOT inside `playSentForProcessingChime()` which the WS6 tour replays) + `haptic('light')` on every job tab-rail tap (`job-tab-nav.tsx`, `JobDetailView.swift:190`); `AppTabBar.swift` is legacy so no app-shell haptic. `playConfirmationChime` stays sound-only. **(d) Standalone chrome** — `globals.css`: `overscroll-behavior:none` on html (both axes) + body, `-webkit-tap-highlight-color:transparent`, scoped `user-select:none`+`-webkit-touch-callout:none` on chrome (button/[role=button]/[role=toolbar]/[role=tab]/nav[aria-label]/label) with input/textarea/[contenteditable]/`.cm-selectable` kept selectable, and `.pt/pb/pl/pr-safe` `env(safe-area-inset-*)` helpers; AppShell header now carries `pt-safe`/`pl-safe`/`pr-safe` + `min-h-14` (was fixed `h-14`) so the logo/back clear the notch under `viewport-fit:cover`+black-translucent. pull-to-refresh SUPPRESSED (no gesture added — policy confirmed on `dashboard/dashboardview-188`). **(e) Splash** — `BrandedSplash` (bolt.shield in a blue→green hero-gradient circle + blue/green CertMate wordmark, WS5 tokens) as root `app/loading.tsx`. `manifest.ts`+`layout.tsx` audited already-correct. DEFERRED: view-transition push/pop feel (needs Next's experimental `viewTransition` config flag + device validation — not flipped autonomously). **Governance:** de-duped 15 duplicate ledger IDs (kept the shipped `match`/CLOSED copies, deleted the stale to-do copies — 11 WS3 + 4 WS5); flipped 6 WS7 rows to `partial`+`last-verified 2026-07-03` (then `match` after device smoke EXCEPT `crosscutting/uiimpactfeedbackgenerator` = permanent `partial`, no iPhone Safari Vibration API); corrected the parent §7 WS3 row (was stale `NOT STARTED` though PR #76 merged); extended `parity-ledger-files.json`; INDEX-2026-07 WS7 → DEPLOYED/partial; superseded the old INDEX.md T&Cs signature-divergence wording. Gates: full web suite green (~1311, +~76 new), typecheck/lint at main baseline, build green. Device-smoke todo in vault `todos-certmate.md`. | `web/src/components/job/{circuit-focus-fields.ts,circuit-keyboard-accessory.tsx}` (new), `web/src/components/job/{circuits-sticky-table.tsx,circuits-schedule-desktop.tsx,job-tab-nav.tsx}`, `web/src/app/job/[id]/circuits/page.tsx`, `web/src/lib/recording-context.tsx`, `web/src/components/settings/signature-canvas.tsx`, `web/src/app/terms/{page.tsx,legal-texts-gate.ts}`, `web/src/app/globals.css`, `web/src/components/layout/app-shell.tsx`, `web/src/components/brand/branded-splash.tsx` (new), `web/src/app/loading.tsx` (new), 5 new/updated test files, `web/docs/parity-ledger{.md,-files.json}`, `web/audit/{INDEX-2026-07.md,INDEX.md}`, hub `CLAUDE.md`/`AGENTS.md`, parent PLAN-final §7 |
| 2026-07-03 | **Parity WS4 — web Deepgram Flux migration + runtime STT kill-switch, shipped BEHIND the switch (web-only + frontend-infra exception; ZERO backend; `parity-ws4-flux-wave-2026-07-02` /ep session). WS4 = PARTIAL/BLOCKED.** ONE coordinated wave with the iOS keyterm sprint. **Phase 0 gate (net-new tooling):** wrote a synthetic TTS→Flux probe — render the garble utterances via the app's ElevenLabs Archer voice at `pcm_16000`, stream through TWO live `flux-general-en` `/v2/listen` WS (with/without candidate keyterms), diff the `TurnInfo/EndOfTurn` transcripts. Result: **mechanism VALIDATED** on LIM (without-keyterms "Live to live is lion." → with-keyterms "Live to live is LIM."), **INCONCLUSIVE** on insulation + trip time (the Archer synthetic voice spoke both cleanly even without keyterms — the documented decision-4 risk; the first pass's "trip time" FAIL was disproven as a flash-TTS artifact on any utterance starting with the acronym "RCD", which rendered ~11s of garbage). Zero keyterm-doesn't-correct failures. Per the plan, an inconclusive probe is NOT a full green light → do NOT curate iOS blind: the `CertMateUnified/Sources/Resources/default_config.json` prune + `DeepgramServiceTests` guardrails are **HELD** (need a real-audio spot check + a VALID TestFlight build, both unavailable autonomously) and surfaced to Derek in `phase0-probe-results.md`. **Shipped this cycle (the plan's "allowed work while blocked"):** (1) **web nova-3 fallback protection** — `web/src/lib/recording/keyword-boosts.ts`: `trip time` boost 1.5→2.5 (it ranked 107th of 120 base+board entries and was DROPPED below web's 85-term cut, so the "tryptoid"→trip time garble never reached Deepgram on web; verified live) + garble-critical survival regression test + three stale-comment-cluster fixes (per-client caps are DISTINCT: iOS 100/2000 vs web 85/1800; live counts iOS 153+8=161, web 112+8=120). This is a targeted survival bump, NOT the banned equal-weight re-curation. (2) **web Flux path** in `web/src/lib/recording/deepgram-service.ts` behind a new `sttModel:'nova3'\|'flux'` ctor selector (default `nova3` so nova-3 is byte-unchanged; 20/20 nova-3 tests still green): `buildFluxURL` (`/v2/listen`, `flux-general-en`, `eot_threshold=0.7`, `eot_timeout_ms=5000`, `mip_opt_out=true`, equal-weight keyterms NO `:boost`), a Flux message handler mapping `TurnInfo`/{Update→interim, StartOfTurn→speechStarted, EndOfTurn(transcript)→final, EndOfTurn(empty)→utteranceEnd} onto the SAME delegate API as nova-3 (so recording-context feeds the EXISTING dispatchFinal/TranscriptGate/chime path — no parallel forwarder), `sendConfigure` with echo-validation + RTT (`onConfigureResult`), Error/Fatal/ConfigureFailure surfaced never dropped, 80ms/1280-sample audio batcher, nova-3 KeepAlive disabled on Flux (Flux rejects `{type:'KeepAlive'}` + silent PCM triggers spurious EndOfTurn), and an equal-weight `generateFluxKeyterms`/`appendFluxKeytermsToUrl` (PROVISIONAL curated ~40-term subset — the flip commit swaps in the FINAL curated iOS list). (3) **runtime kill-switch** — `web/src/lib/runtime-config.ts` (fetch `/runtime-config` once per RECORDING session with `{cache:'no-store'}`, `force:true` in `start()` after `primeTts()`; split constants `DEFAULT_STT_MODEL='nova3'` product default (may flip) vs `SAFE_STT_MODEL='nova3'` never-flip fail-safe; `resolveSttModel` normalises `nova-3`/`Nova3`/`nova_3`→`nova3`; fetch/parse failure or unrecognised value → SAFE + loud diagnostic) wired into `recording-context.tsx` via `activeSttModelRef` (reconnects reuse it, `stop()` clears it); a NET-NEW same-origin route `web/src/app/runtime-config/route.ts` at TOP-LEVEL `/runtime-config` (NOT `/api/*` — the prod ALB has a priority-10 `/api/*→backend` rule, so `/api/runtime-config` would 404 in prod and pin every session to the nova-3 fail-safe; `force-dynamic`, `Cache-Control:no-store`, pinned NetworkOnly in `sw.ts`; sits behind the middleware JWT gate, benign — authed→JSON, expired→/login HTML→fail-safe); `ecs/task-def-frontend.json` gains `DEEPGRAM_STT_MODEL=nova3` (infra-from-source); and a `frontend-taskdef` deploy target in `.github/workflows/deploy.yml` (exact single-file match via `list-files:json` + jq count guard; skips both Docker builds/scans + all backend steps/migrations; registers the frontend task def against the EXISTING image + rolls `eicr-pwa` only — ~3-5 min vs ~30 min, so the runtime var is actually flippable). The RUNTIME var (not a `NEXT_PUBLIC_*` build-time var, which is inlined at `next build` and un-flippable in this standalone ECS deploy) is what makes the kill-switch operable. Frontend-infra exception to parent §5.4 `web/`-only logged in parent §7 WS4. **The flip to Flux-default** = one commit flipping `DEFAULT_STT_MODEL` + the task-def value to `flux`, AFTER (a) the curated iOS list ships via a VALID TestFlight build and (b) the two inconclusive garbles pass a real-audio check. nova-3 stays the kill-switch fallback until a NAMED nova-3-removal follow-up. Gates: 1269 web tests (116 files) green (+~42 new: Flux service/builder + kill-switch matrix incl. fail-safe split + no-rebuild proof + `/runtime-config`-not-`/api/*` + middleware authed→JSON); `npm run build` green (`/runtime-config` = Dynamic route); typecheck 17≤21 baseline, lint identical baseline count — zero new. NO fuzzy/edit-distance garble correction anywhere (parent §3E, HARD RULE). | `web/src/lib/recording/deepgram-service.ts`, `web/src/lib/recording/keyword-boosts.ts`, `web/src/lib/runtime-config.ts` (new), `web/src/app/runtime-config/route.ts` (new), `web/src/lib/recording-context.tsx`, `web/src/app/sw.ts`, `ecs/task-def-frontend.json`, `.github/workflows/deploy.yml`, `web/tests/{deepgram-service-flux,runtime-config,runtime-config-route,runtime-config-middleware,keyword-boosts}.test.ts`, docs/ledger |
| 2026-07-02 | **Parity WS3 — voice behavioural catch-up (web-only, ZERO backend changes; `parity-ws3-voice-catchup-2026-07-02` /ep session).** Parent-plan items 1/2/3/6/7 + item 9 deliverables shipped; items 4/5/8 split to the NAMED pre-WS8 follow-up `parity-ws3b-voice-latency-2026-07` (dated ledger rows, never unowned). **Item 1 — capability hello:** `session_start` now carries `capabilities: { voice_latency: { version: 1, supports: ['low_conf_readback_v1'] } }` — the EXACT nested shape `parseVoiceLatencyCapabilities` requires (a bare array parses as v0/dormant; shape pinned by test). Advertised only after verifying the web apply path has NO local `reading.confidence` drop filter (the only client-side confidence is Deepgram transcript confidence). Activates PR #61's universal read-back for web users — until now the backend PRE-APPLY skipped every `< 0.5` reading for web sessions (silent loss, audio-first invariant #2). `regex_fast_v2`/`client_playback_telemetry` deliberately NOT claimed (plumbing absent — follow-up owns). TWO-PHASE: ledger row `partial` until post-deploy CloudWatch `voice_latency.startup_log` evidence from an authenticated web session. **Item 2 — read-back dedupe re-key:** new `web/src/lib/recording/confirmation-dedupe-key.ts`, literal port of iOS `buildConfirmationDedupeKey` (single-circuit legacy shape / multi-circuit sorted+djb2(text) / degenerate djb2(text+boardId); djb2 = BigInt UInt64-wrap over Unicode scalars, decimal render). Hash vectors in the test file were GENERATED by executing the backend mirror `src/extraction/ios-dedupe-key.js` (read-only) so client/server key drift fails deterministically. `Confirmation` wire type gains `circuits`/`board_id`; the D6 loop's old `<field>_none` fallback (which swallowed the second read-back on every board-level pair and multi-circuit broadcast — sessions C0C21546/84CE2125) is gone. **Item 3 — observation canonical wording:** `regulation_title`/`regulation_description`/`rationale` decoded on BOTH the initial extraction and `observation_update` (the second, unreachable `observation_update` case in the same switch DELETED — kept the `original_text`+`schedule_item` case with the empty-payload guard); persisted with iOS semantics (title/description assigned UNCONDITIONALLY on update so a table-MISS refinement CLEARS stale wording; rationale non-empty-overwrite-only); rendered on the observation card in iOS order (ref → title → description → italic 'Because {rationale}'). Initial-extraction rationale was ALSO silently dropped pre-change. **Item 6 — LIM audit:** web parsing/apply/display already iOS-identical; gap was zero tests → 9 regression tests (incl. word-boundary + the SHARED digit-prefix quirk, documented not 'fixed'). **Item 7 — TranscriptGate + chime:** `web/src/lib/recording/transcript-gate.ts` FULL literal port (digit / regex-hit / pending-ask / inResponseTo / bare-negation / observation-pattern / strong / earthing (threshold 1) / weak+≥3-content / identity ≥2 branches + verbatim trigger/stopword arrays); wired in `dispatchFinal` preserving the 2026-05-15 regex ask-answer skip via a NON-consuming `peekInFlightToolCallId()` computed FIRST; new non-mutating `peekPayloadForTranscript()`/`clearExpiredSlot()` on the in-flight tracker (raw `peekSlot()` has no stale-window check — a stale TTS question could force PASS-chime-then-null-send); ask-answer emission block + `setProcessingCount` increment moved INSIDE gate-PASS (the counter is only decremented by server frames — an always-run increment would stick the processing indicator on every reject); regex matcher now runs flag-INDEPENDENTLY for the gate signal while hint application stays behind `NEXT_PUBLIC_REGEX_HINTS_ENABLED`. `playSentForProcessingChime()` = sample-accurate `makeChimeWAVData` PCM port in `tones.ts` (absorbed from WS6's interim tour-local synth; tour switched to the shared helper, `lib/tour/tour-chime.ts` deleted). BEHAVIOUR CHANGE: non-qualifying chatter stops reaching Sonnet (iOS cost/noise win). 17 branch tests + 9 wiring tests. **Item 9:** `surge_*` sweep — surface complete vs iOS Fix D (fallback-removal parity verified: zero `board.spd_type`→`spd_type_supply` in web/src; no web supply CSV; PDF loads job server-side); one gap closed (Sonnet voice-apply routing test — surge relies on the `?? 'supply_characteristics'` default matching iOS's dedicated cases). EIC divert-to-comments VOICE apply added (`apply-extraction.ts` dedicated `comments` branch: EIC-only guard, newline-APPEND, EICR drop; form cell pre-existed and was NOT re-added; pre-fix a diverted note default-routed into `supply_characteristics` — invisible). `ask_user.context_board_id` RECLASSIFIED backend-internal across all four governance surfaces in one commit (neither client decodes it; answer path is `tool_call_id`-keyed on both — the WS0 classification was wrong). Also: `confirmation-dedupe-key.ts` uses `BigInt()` calls not literals (web tsconfig targets ES2017). Gates: 1200+ web tests green, build green, typecheck/lint at main baseline. Ledger: 7 rows → `match` + capability row pending-prod + 3 rows → named follow-up owner; INDEX-2026-07 WS3 gaps closed; files-map +5/-3 entries. | `web/src/lib/recording/{sonnet-session.ts,transcript-gate.ts,confirmation-dedupe-key.ts,in-flight-question.ts,apply-extraction.ts,tones.ts}`, `web/src/lib/recording-context.tsx`, `web/src/lib/types.ts`, `web/src/app/job/[id]/observations/page.tsx`, `web/src/hooks/use-tour.ts`, `web/src/lib/tour/{steps.ts,tour-chime.ts(deleted)}`, 9 test files, `web/docs/parity-ledger{.md,-files.json}`, `web/audit/{INDEX-2026-07.md,ws3-checklist-2026-07.md}` |
| 2026-07-02 | **Parity WS9 — client-side PDF render shipped (web-only, ZERO backend changes).** The web PDF tab's primary Generate now renders the certificate CLIENT-SIDE, exactly like iOS. (1) **Renderer (Step 0 spike, decision documented in `render/capture.ts`):** hidden-iframe + SVG foreignObject page capture at 3× (≈267dpi) assembled with pdf-lib (single new runtime dep, web workspace, dynamic-imported) — one PDF page per fixed-size `.page`/`.page-landscape` div, portrait+landscape merged in iOS order, mirroring `HTMLPDFRenderer.swift`. html2canvas rejected (cannot render the `writing-mode: vertical-lr` circuit headers); @react-pdf/pdfmake rejected (cannot consume HTML — would break the 1:1 iOS↔web template correspondence WS1 governance depends on). Trade-off: raster pages (text not selectable) vs iOS vector. Spike acceptance test `tests-e2e/pdf-renderer-spike.spec.ts` green on chromium+webkit. (2) **Template port:** `web/src/lib/pdf/template/` — line-for-line TS port of `EICRHTMLTemplate.swift` (2131 lines): all page builders, EICR 90-item + EIC 14-item inspection schedules with iOS auto-controls (TT/microgeneration/section-7), per-board landscape circuit schedule incl. sub-main section + legacy `board_info`→single-board fallback, guidance page, verbatim helpers, and even iOS's totalPages-estimate quirks reproduced. Wire decoders keyed to iOS CodingKeys with two documented leniencies (numeric-string coercion, ''-as-unset) because strict decode makes a job with `next_inspection_years:""` UNDECODABLE — a live bug on the WS9 EICR fixture (iOS cannot open job_1782978942222 as seeded; reseed should fix). (3) **Data graph:** `generate-certificate.ts` mirrors `PDFGenerator.swift:9-68` — job normalization verbatim; company settings + inspector/authoriser/designer/constructor profiles via existing api-client fetchers; logo/signature blobs inlined as data URIs; `try?`-style best-effort semantics. (4) **Page integration:** Blob into existing session `pdfBlob` state (preview/share/delete unchanged); `pdf_s3_key` stamped `local://<filename>` per iOS `PDFTab.swift:363`; server generate-pdf demoted to explicit "Generate on server (fallback)" action stamping `route://` with TODO(ws9-followup) for the post-field-validation debug-page flip (parent §6.5); compliance spec §4.3 retry implemented — failed render keeps the written attestation_ids and Try-again re-renders with NO re-prompt (current iOS re-presents the sheet — dated iOS spec-parity todo, vault todos-certmate.md). (5) **Acceptance:** iOS reference PDFs produced on the iPhone 17 Pro simulator through the real template/WKWebView pipeline from production fixture GETs; web renders PASSED the page-by-page diff (EICR 9/9, EIC 5/5, identical boxes/values/badges/footers) after two diff-caught fixes (body-font inheritance into the foreignObject wrapper; group-header cascade compensation — web-only rule dated in `css.ts`). Evidence `web/audit/ws9-pdf-fidelity-2026-07/`. Ledger: `pdf/pdftab-270` rewritten to the client path (match), `pdf/pdf-fidelity` stays `partial` with dated acceptance note until field validation, iOS-template-change⇒web-companion note added as the load-bearing drift-stop, 10 new files registered in `parity-ledger-files.json`, warner clean. 1145 web tests green (7 rewritten pdf-tab + 16 new). | `web/src/lib/pdf/**` (new), `web/src/app/job/[id]/pdf/page.tsx`, `web/tests/pdf-{tab,template,assemble}.test.*`, `web/tests-e2e/{pdf-renderer-spike,ws9-acceptance-render}.spec.ts`, `web/audit/ws9-pdf-fidelity-2026-07/README.md`, `web/docs/parity-ledger.md`, `web/docs/parity-ledger-files.json`, `web/audit/INDEX-2026-07.md`, `docs/reference/architecture.md`, hub `CLAUDE.md`, `web/package.json`, `package-lock.json` |
| 2026-07-02 | **Parity WS5 — design-system unification to iOS CMDesign (web-only, ZERO backend changes).** Web tokens regenerated from `CertMateDesign.swift` with the full mapping table committed at `web/audit/cmdesign-token-map-2026-07.md` (every CMDesign duplicate resolved by live-call-site grep; superseded tokens recorded in the notes column): text/status/soft-brand/transcript colors moved off Apple-system/Tailwind approximations to CMDesign values, Green/Blue accent scales + `--color-text-inverse` added, semantic radii (`--radius-input` 12 / `button` 14 / `card` 18 / `section-card` 16 / `hero` 22 / `cta-pill` 26), heights (`--h-input` 52, `--h-tabbar` 49, `--h-button-lg`/`--h-list-row` new). Spacing KEYS AND VALUES kept (user decision 2026-07-02) — the Tailwind v4 `max-w-*` override block survives, computed-verified `max-w-3xl`=768px. Component recipes ported in place: `.cm-card` rewritten as the full `cmCardStyle()` glass recipe (gradient border via padding-box/border-box), SectionCard → CMSectionCard spec (L1+blue-subtle bg, accent 20→8% gradient border, inset 3px accent→40% conduit stripe, radius 16/p-4), HeroHeader → radius 22 + blue-glow + `cmHeroShimmer` sweep, JobTabNav → iOS rail colours + single 3px blue→green gradient underline SLIDING between tabs (measured indicator + `--ease-spring`), shared field controls → CMFloatingTextField spec (L2 bg, 1.5px L3 border, GREEN focus + 12px glow, radius 12, h-52, 12px-medium floated label, 17px value — also kills iOS-Safari zoom-on-focus; the UPPERCASE `formLabel` style was deliberately NOT applied: zero live iOS call sites). Inspection section accents consolidated onto the shared iOS category map (`EICR_INSPECTION_SECTION_CATEGORIES` per `InspectionTab.swift:362-374`; `magenta` remapped). **Bug fixes:** styled-dialog double-offset (WS0 finding) fixed centrally — `.cm-dialog-content` animation moved off `transform` onto the standalone `scale` property so Tailwind v4's `translate` utilities are the single translation source; `terms/page.tsx` consumer duplicate cleaned same commit; Playwright regression `web/tests-e2e/dialog-centering.spec.ts` (288px measured offset pre-fix). Recording-chrome photo-chooser referenced three UNDEFINED CSS vars — mapped to real tokens. **Capture tooling:** `visual-baseline-capture.mjs` gains API_BASE_URL/OUT_DIR overrides + a localhost CORS intercept-and-fulfill shim + a HARD read-only guard (blocks all non-GET API methods) added after a live incident: a transient job-GET failure let the installation/supply auto-seed defaulters save a BLANK doc over the seeded parity EICR fixture — restored same-session from the twin EIC fixture; the underlying auto-seed-on-unhydrated-job data-loss bug is recorded in `web/audit/INDEX-2026-07.md` (P1, owner: next parity session). Ledger: 4 WS5 rows flipped (3 `match`, `tab-rail-form` `partial` with the dated swipe-paging accepted difference); INDEX WS5 gaps closed; MANIFEST EIC-vs-iOS deferred-review note added. Gates: 1131 web tests, typecheck/lint at main baseline, build green, Playwright dialog spec green both browsers (3 pre-existing record-spec failures reproduce identically on main). | `web/src/app/globals.css`, `web/src/lib/design-tokens.ts`, `web/audit/cmdesign-token-map-2026-07.md`, `web/src/components/ui/{card,section-card,hero-header,dialog,floating-label-input,labelled-select,multiline-field,select-chips}.tsx`, `web/src/components/job/job-tab-nav.tsx`, `web/src/components/recording/recording-chrome.tsx`, `web/src/app/terms/page.tsx`, `web/src/app/job/[id]/inspection/page.tsx`, `web/src/lib/constants/section-accents.ts`, `web/tests-e2e/{dialog-centering.spec.ts,visual-baseline-capture.mjs}`, `web/tests/section-card.test.tsx`, `web/docs/parity-ledger.md`, `web/audit/INDEX-2026-07.md`, `web/audit/visual-baseline-2026-07/MANIFEST.md` |
| 2026-07-02 | **Parity WS2 — PWA observation-photo auto-link: parked branch rebased + shipped (web-only, ZERO backend changes).** The 2026-05-13 L2 sprint branch `pwa-observation-photo-autolink-2026-05-13` (Phases 3–6) replayed onto main with its four commits' substance preserved via cherry-pick: `e880043d` forward-link in `applyObservations` (16 tests), `efe7449b` image-resize + `captureObservationPhoto` orchestration (10 tests), `b0730325` Photo button + camera/library chooser on recording chrome, `577f8107` `unassigned_photos` pool + From-Job picker (7 tests). **Real conflict set** (derived by dry-run `git rebase --onto` in a scratch worktree, contradicting the old HANDOFF's "heavy collision" prediction): commits 1–3 applied CLEAN including the Phase-4 orchestration against main's four post-branch extracted recording modules; only the Phase-6 commit conflicted, in ONE hunk of `recording-context.tsx` where main's lifecycle-actions ref-sync effect and the branch's pending-photo timer cleanup effect chose the same insertion point — resolved keep-both (Pattern C). Two semantic conflicts surfaced by tooling, fixed in follow-on commits: duplicate `toast` import (main gained the same import post-branch-point; TS2300) and unused `isWithinLinkWindow` import. **Contract fixes vs the CURRENT backend wire** (backend re-verified intact and untouched — PUT persists arrays incl. `[]` at `src/routes/jobs.js:785-793`, GET emits `string[]`-or-`null` at `:594`, pinned by backend tests): (1) `JobDetailSchema` in `web/src/lib/adapters/job.ts` extends a strip-mode `z.object`, so the undeclared `unassigned_photos` key was silently dropped by `api.job()` before JobProvider/the From-Job picker ever saw it — declared `z.array(z.string()).nullable().optional()` + 2 adapter regression tests (populated pool + null both survive the parse); (2) `JobDetail.unassigned_photos` retyped `string[]`→nullable in `types.ts` + picker-gate null-pool test; the two `as string[]`-`undefined` casts dropped so the real type flows to consumers. **iOS canon re-verified against CURRENT refs** (not May-iOS): 60 s window `DeepgramRecordingViewModel.swift:1094`, capture `:2257` (Case B reverse-link `:2276`, Case A pending + expiry-to-pool `:2317`), forward-link in `applySonnetObservations` `:7262`, recent-obs ref `:7275` — all matched; web's +10 s pool-promotion grace (link decision itself exactly 60 s) kept as the reviewed branch decision for in-flight uploads. **Two dated deliberate divergences** on ledger row `observations/obs-photo-autolink`: no CCU picker source (iOS `JobPhotosPickerSheet.swift:41` reads `job.ccuPhotoPath`; web has NO persisted CCU photo reference and the job wire neither emits nor accepts `ccu_photo_path` — frontend-only port impossible under the zero-backend rule) and the web-extra camera/library chooser (iOS camera-only, library a stated iOS follow-up; iPadOS Safari can't strictly enforce camera from `<input capture>`). Forward-link wiring audit: photo callbacks live at the ONLY observation-carrying `applyExtractionToJob` site (`recording-context.tsx:1976`); main's three post-branch synthetic apply sites carry only field_clears/circuit_updates; `onObservationUpdate` mirrors iOS `handleObservationUpdate`, which doesn't forward-link on iOS either. **Gates:** 99 files / 1099 web tests, 203 suites / 4952 backend tests, `next build` green; typecheck + eslint error sets identical to origin/main baseline (zero new errors — CI runs web tsc/eslint non-blocking, blocks on build+vitest, both green). **Ledger/INDEX:** row `observations/obs-photo-autolink` → `partial` (NOT `match`) with `last-verified` 2026-07-02; `parity-ledger-files.json` maps the row to the full 17-file feature surface incl. the Phase-1/2 files already on main (`pwa/job-cache.ts`, `observation-photo.ts`) and all tests; INDEX-2026-07 gap stays open as DEPLOYED/partial — flip to `match`/closed only after the iPad Safari device smoke passes (upload-during-resize race + iPadOS camera quirks are runtime-only; todo in vault `todos-certmate.md` 2026-07-02). Hub CLAUDE.md PARKED bullet rewritten as shipped-awaiting-smoke in the same commit. | web/src/lib/recording-context.tsx, web/src/lib/recording/capture-observation-photo.ts, web/src/lib/recording/observation-photo.ts, web/src/lib/recording/apply-extraction.ts, web/src/lib/image-resize.ts, web/src/components/recording/recording-chrome.tsx, web/src/components/observations/job-photos-picker-sheet.tsx, web/src/components/observations/observation-sheet.tsx, web/src/lib/adapters/job.ts, web/src/lib/types.ts, web/tests/observation-photo.test.ts, web/tests/apply-extraction-photo-link.test.ts, web/tests/capture-observation-photo.test.ts, web/tests/job-photos-picker.test.ts, web/tests/adapters.test.ts, web/docs/parity-ledger.md, web/docs/parity-ledger-files.json, web/audit/INDEX-2026-07.md, CLAUDE.md, docs/reference/changelog.md |
| 2026-07-02 | **Parity WS0+WS1 — audit refresh + drift-stop governance (first executable slice of the iOS↔Web Full-Parity Program; docs/CI only, zero backend changes).** **WS0 (audit):** web tab set/gating re-verified against iOS canon `JobDetailView.swift:472-536` — NO re-drift of the Wave-5 P0 class (only the stale header-comment line ref fixed in `job-tab-nav.tsx`). Wire-shape audit of ALL 11 backend PRs merged 2026-06-17→2026-07-02 (#58–#70), cross-checked log-vs-tree-diff with exact reconciliation, classified per file into backend-only / iOS-companion-shipped / dormant-behind-capability → `web/audit/ws3-checklist-2026-07.md` (key findings: `low_conf_readback_v1` universal read-back is DORMANT for web — the hello frame sends no capability array; `surge_*` namespace only partially on web; observation canonical wording undecoded). Ledger sweep: every parity-mapping row in `web/docs/parity-ledger.md` gains stable `id` + `last-verified` columns (367→396 rows; 29 new program gap rows for WS2–WS9; `backend` status RETIRED per the zero-backend rule; all "needs backend defaults endpoint" rows REWRITTEN — web already has named `CertificateDefaultPreset`s, the real gap is the frontend job-creation flow, WS6). New gap index `web/audit/INDEX-2026-07.md` supersedes `web/audit/INDEX.md` (inline corrections: `utterance_end_ms` is 1000 not 1500; T&Cs signature is a decided WS7 port, not "pending legal review"). Doc-staleness fixes in hub CLAUDE.md (iOS live STT = Deepgram Flux `/v2/listen`, web stays nova-3 until WS4; `queueSaveJob` IS wired at `job-context.tsx:159`); parked branch `pwa-observation-photo-autolink-2026-05-13` verified (4 commits `e880043d`..`577f8107`) + pushed to origin. **WS1 (governance):** MANDATORY web-companion rule added to hub CLAUDE.md + mirrored into `CertMateUnified/CLAUDE.md` (separate nested-repo commit `9a91c5d`, which also fixes its stale audio-first invariant #2, `Domain: certmate.uk`, both `../EICR_App/` doc paths, and the Recording Pipeline Nova-3→Flux line); new warn-only CI job `parity-ledger-warn` in `deploy.yml` (pull_request-only via job-level `if:`, `continue-on-error: true`, no `needs:` dependants — structurally unable to gate a deploy) running new `scripts/check-parity-ledger.mjs` against new `web/docs/parity-ledger-files.json` (73 files → 340 row-id refs; rows indexed by stable `id`, never line number). Fixture dry-runs prove stale-date / blank-date / invalid-date / id-missing-from-ledger / duplicate-id each emit `::warning::` while exiting 0, with blank-dated rows collapsed into ONE summary line (signal, not noise). Merge-gate verifications passed: `check-task-def-env-drift.sh` still runs for both services, `JWT_SECRET` still in `ecs/task-def-frontend.json`, and the ECS execution role still grants `secretsmanager:GetSecretValue` on the exact `eicr/api-keys` ARN (live IAM read — no gap to record). Reviewer footgun checklist + quarterly re-audit todo land in `~/.claude/agents/certmate-plan-reviewer.md` + `~/obsidian-vault/active/todos-certmate.md` (session-log-verified, not PR-gated). | web/src/components/job/job-tab-nav.tsx, web/audit/ws3-checklist-2026-07.md, web/docs/parity-ledger.md, web/audit/INDEX-2026-07.md, web/audit/INDEX.md, web/docs/parity-ledger-files.json, scripts/check-parity-ledger.mjs, .github/workflows/deploy.yml, docs/reference/deployment.md, CLAUDE.md, docs/reference/changelog.md |
| 2026-06-26 | **ElevenLabs model consolidation — items A + A2 (backend, ships via CI).** Investigated whether ElevenLabs deprecations force a CertMate migration: they do NOT — none of CertMate's models are on the July-9-2026 removal list (that list is v1-era only). This is consolidation + cost-accuracy, not a forced migration. **A:** the non-streaming TTS proxy `POST /api/proxy/elevenlabs-tts` (`src/routes/keys.js`, the iOS `APIClient.proxyElevenLabsTTS` + web `elevenlabs-tts.ts` fallback path) switched `model_id` `eleven_turbo_v2_5` → `eleven_flash_v2_5`. The streaming WS path (`elevenlabs-stream-client.js`) already defaulted to Flash, so every live TTS path is now one model. Flash is ElevenLabs' documented recommended real-time model; Turbo v2.5 is "superseded by" Flash. Contract-preserving for iOS/web: identical Archer voice (`Fahco4VZzobUeiPqni1S`), identical `voice_settings`, identical implicit `mp3_44100_128` output, `Accept: audio/mpeg` unchanged — so the audio byte contract is unchanged and NO client rebuild is needed. Win is first-byte latency (~75 ms vs ~250-300 ms). **A2 (per-model cost accuracy):** ElevenLabs bills per model in credits — Flash v2.5 & Turbo v2.5 are 0.5 credits/char = $0.05/1k chars = $0.00005/char; Multilingual v2 & eleven_v3 are 1 credit/char = $0.10/1k = $0.0001/char (verified against ElevenLabs' published per-character API pricing 2026-06-26). NOTE: Flash and Turbo are the SAME rate (Turbo dropped to 0.5 credits in ElevenLabs' Aug-2024 cut), so the turbo→flash live-path switch does NOT change live session cost — A2 is correctness + future-proofing, not a current correction. `cost-tracker.js`: added `ELEVENLABS_RATE_PER_CHAR_BY_MODEL` map + `DEFAULT_ELEVENLABS_MODEL_ID='eleven_flash_v2_5'`, kept `ELEVENLABS_RATE_PER_CHAR=0.00005` as the unknown-model fallback; replaced the single `elevenLabsCharacters` scalar with per-model `elevenLabsCharsByModel` buckets (`elevenLabsCharacters` is now a derived getter = sum of buckets, preserving the `toCostUpdate` wire shape); rewrote the `elevenLabsCost` getter to sum each bucket × its rate. Threaded an optional `modelId` (default Flash) through all THREE char accumulators — `addElevenLabsUsage`, `recordElevenLabsStreamingStarted`, `recordElevenLabsSpeculativeStarted` — their `active-sessions.js` wrappers (`recordElevenLabsUsageForSession`, `recordElevenLabsStreamingStartedForSession`), and the call sites: proxy `keys.js` passes `eleven_flash_v2_5`, streaming `keys.js` passes `client.modelId`, speculator `loaded-barrel-speculator.js` passes `client.modelId`. The regex fast-TTS route (`voice-latency-fast-tts.js`) has no cost attribution today and was left out of scope (no behaviour change). Tour-audio v3 (item B, iOS) is generated offline by the shell script and is NOT session-attributed, so it stays out of per-session accounting. Tests: full backend suite 4952 passed / 0 failed (19 skipped pre-existing); +8 new per-model cost-tracker tests (per-model rate map, mixed-model bucket summing, turbo==flash, unknown-model fallback, streaming+speculative modelId threading, wrapper threading). | src/routes/keys.js, src/extraction/cost-tracker.js, src/extraction/active-sessions.js, src/extraction/loaded-barrel-speculator.js, src/__tests__/cost-tracker.test.js, CLAUDE.md, docs/reference/changelog.md |
| 2026-06-16 | **Voice-feedback fixes — field session F1AC26FB (job_1781607199550, EIC, 78 Meadow Close Thatcham), 5 markers, /rp→/ep, backend PR #55.** **#4 (LIM):** `parseMegaohms` (and its byte-identical legacy twin `parseValue`) now accept the limitation sentinel — a LIM branch returns canonical `"LIM"` for the Deepgram garble set `lim/limb/limp/limit(ation|ed)/lynn/lym` (after saturation, before numeric); the same alternation is added to `MEGAOHMS_VALUE_GROUP`, `MEGAOHMS_BARE_SAFE_VALUE_GROUP` and the legacy `valueGroup` so named extractors ("live to live LIM") catch it. `coerceRecordReadingValue` widened to canonicalise `ir_live_live_mohm`/`ir_live_earth_mohm` LIM garbles, and `engine.js` canonicalises `ir_live_*` pending_writes (seed path + both drain points) via the same helper — closing the `record_reading`/speculator bypass. New per-slot no-progress cap in `engine.js`: 2nd consecutive unparseable answer emits a format hint, 3rd skips the slot and falls through to Sonnet, so ANY garble (not just LIM) can no longer loop a slot ask forever (the original failure: "LIM for live-to-live" re-asked indefinitely; repeat of 2026-02-18 + 2026-06-08 requests; LIM stored as sentinel string, NOT >999). **#3 (designation):** new `stripDesignationFiller` helper strips a leading article/filler phrase + trailing punctuation from the USER text only — `findCircuitsByDesignation` resolves "For the sockets." against a "Sockets" designation, and `buildCircuitRetryQuestion` + the stored `last_designation_attempt` use it so the re-ask never echoes raw "…for the For the sockets.?"; stale "dead code" comment on `schemas/ring-continuity.js` corrected (it IS the live ring path). (#3.4 — designation never crossing the wire to the server snapshot — DEFERRED `[contract]`.) **#5.2:** `validateCreateCircuit` rejects an implausible `circuit_ref` (>= 100 or > board-max + 20) with `implausible_circuit_ref` — stops the scratch circuit 999 "(temp)" Sonnet invented for a designation swap. **#2.2:** `dispatchRecordBoardReading` blocks `sub_main_cable_csa` on a job with no sub-board (`no_sub_board_for_sub_main`), redirecting to `main_switch_conductor_csa`. **Shared-prompt:** `sonnet_agentic_system.md` gains SWAP/REORDER (two `rename_circuit` calls, never a scratch ref — #5.1), TAILS→`main_switch_conductor_csa` (#2.1), and an earthing head/value garble line (#1.4, mirrored into `sonnet_extraction_system.md`); prompt-length regression caps bumped to absorb the additions. iOS companion (Defect #1 — feedback-capture dual-route + 20s auto-timeout + loosened earthing regex) on CertMateUnified PR #17. (#5.3 atomic swap tool DEFERRED `[contract]`.) Full backend suite 4773 green; ECS rollout COMPLETED. | `src/extraction/dialogue-engine/parsers/megaohms.js`, `src/extraction/insulation-resistance-script.js`, `src/extraction/record-reading-coercion.js`, `src/extraction/dialogue-engine/engine.js`, `src/extraction/dialogue-engine/helpers/circuit-resolution.js`, `src/extraction/dialogue-engine/schemas/ring-continuity.js`, `src/extraction/stage6-dispatch-validation.js`, `src/extraction/stage6-dispatchers-board.js`, `config/prompts/sonnet_agentic_system.md`, `config/prompts/sonnet_extraction_system.md` |
| 2026-06-04 | **Web — voice-feedback triage UI (PLAN-web-final §1.6.5) + Deepgram endpointing parity (§3.4).** Field-test-fixes-session-60754e4d sprint web slice. (1) New `/voice-feedback` list + `/voice-feedback/[id]` detail pages for the on-device "feedback ... end feedback" voice trigger. List view (`web/src/app/voice-feedback/page.tsx`, ~460 lines): status filter chips (All / Open / Reviewed / Actioned / Wontfix), client-side distinct-jobId dropdown derived from the loaded rows, free-text search debounced 300 ms, status pills (open=amber / reviewed=blue / actioned=green / wontfix=neutral via shared `Pill` primitive), optimistic "Mark reviewed" PATCH, 50-per-page offset pagination with Load more, admin-only "Show all users" toggle that switches between `voiceFeedbackList` and `voiceFeedbackAdminAll` endpoints. Detail view (`web/src/app/voice-feedback/[id]/page.tsx`, ~390 lines): full issue text + timestamped transcript window in a monospace list + authed-fetch "Open raw JSON" via a new `voiceFeedbackFetchRawBlob` helper (modelled on the existing `fetchPhotoBlob` pattern from `api-client.ts:480` — browsers can't attach our Authorization header to bare anchors, so we fetch a Blob and open via `URL.createObjectURL` with a 60 s revoke timer to dodge Firefox's eager next-tick revoke), status dropdown that PATCHes on change, review-note textarea that PATCHes on blur via a `noteCommittedRef` to dedupe duplicate blurs. New `VoiceFeedback*` types in `web/src/lib/types.ts` (`VoiceFeedbackStatus` union, `VoiceFeedbackListItem`, `VoiceFeedbackTranscriptEntry`, `VoiceFeedbackDetail`, `VoiceFeedbackListResponse`). Four new `api.voiceFeedback*` methods in `web/src/lib/api-client.ts`. New header-chrome `VoiceFeedbackBell` (`web/src/components/layout/voice-feedback-bell.tsx`) wired into AppShell between AlertsBell and InstallButton — open-count badge driven by `voiceFeedbackList({status:'open', limit:1})` with focus + visibilitychange refetch (AlertsBell pattern). Amber badge (status-processing token) rather than red (status-failed) — open feedback is "triage queue" not "alarm". Tests: new `web/tests/voice-feedback-page.test.tsx` (3 tests — list renders rows + correct first-call params; Open chip re-queries with `status=open`; Mark reviewed optimistically flips the pill, hides the button, calls `voiceFeedbackPatch(id, {status:'reviewed'})`). Test file lives at `web/tests/` not the plan's specified `web/src/app/voice-feedback/__tests__/` because `web/vitest.config.ts:56` `include` glob only picks up `tests/**/*.test.{ts,tsx}`. Logged as [ASSUMED] decision in `/ep` execution log. New deepgram-service URL-params test asserts both `endpointing=400` and `utterance_end_ms=1000` via the existing `wsFactory` test-seam. Stable mock singletons for `useRouter`/`useCurrentUser` to prevent the page's data-fetch effect from re-running on every render. (2) `web/src/lib/recording/deepgram-service.ts`: `endpointing` 300→400 ms (header comment too) to match iOS slice §3.3 — `rules/mistakes.md` "Keep web and iOS Deepgram configs in sync" lists `endpointing` explicitly. Web mirrors the iOS choice of unconditional 400 ms (option 2 of the plan) because option 1's script-state-aware 250/400 split required a Deepgram WS reconnect per dialogue-script entry, which would drop accumulated keyterm context. Backend slice ships routes + S3 proxy in a sibling PR — web pages will 404 their data fetches until that lands; PR review reads the UI from source, integration is a post-merge gate. v2 detail-view scope-down (TTS prompts said, tool calls fired, dialogue-script state at moment of feedback) deferred pending the optional `feedback_context JSONB` backend column. | web/src/app/voice-feedback/page.tsx, web/src/app/voice-feedback/[id]/page.tsx, web/src/lib/types.ts, web/src/lib/api-client.ts, web/src/lib/recording/deepgram-service.ts, web/src/components/layout/voice-feedback-bell.tsx, web/src/components/layout/app-shell.tsx, web/tests/voice-feedback-page.test.tsx, web/tests/deepgram-service.test.ts |
| 2026-06-04 | **Field-test fixes from sessions DC321DBC + 60754E4D — backend slice (PLAN-backend-final.md).** Eight phases shipped as one branch (`ep/field-test-fixes-backend-...`). **Phase 1.3 / 1.4:** new `case 'client_log_batch'` switch arm in `src/extraction/sonnet-stream.js` next to the existing `client_diagnostic` arm. Per-session realtime log buffer (`entry.realtimeLogBuffer`) drained via the new `realtime-log-sink.js` module on whichever of ~30 s tick, 100 KB threshold, ws_close, session_timeout, session_stop, or `gracefulShutdown` fires first. S3 keys use lexically-sortable timestamp + 4-byte random suffix (`session-logs/{userId}/{sessionId}/realtime/{ms}-{shortUuid}.jsonl`) so download/replay concatenates chronologically across ECS restarts and `putObject` never overwrites a prior batch. Audit-integrity hardening: strip client-provided `userId`/`sessionId`/`timestamp` per entry, re-attach server-authoritative values. Cost cap: 20 000 lines/session → downsampling mode (all error/warn, 1/10 info, 1/100 debug) so the streaming visibility stays alive on stuck sessions. Companion guardrail in `gracefulShutdown` (`src/server.js`) awaits `flushAllSessions(...)` BEFORE DB / queue / email close so SIGTERM during deploy doesn't drop the last 30 s of telemetry. **Phase 1.6.3 / 1.6.4 / 1.6.8:** new `migrations/012_voice_feedback.cjs` table indexed by `(user_id, created_at DESC)` + partial `(job_id) WHERE job_id IS NOT NULL`; `user_id` is `TEXT` (not UUID — matches existing `users.id` shape `"user_..."`). `src/db.js` gains `insertVoiceFeedback` / `listVoiceFeedback` / `getVoiceFeedback` / `updateVoiceFeedbackStatus` with status CHECK against `('open','reviewed','actioned','wontfix')` and `reviewed_at` auto-stamp on transition away from open. `POST /api/debug-report` extended to (a) accept `lastTranscriptWindow: [{ts,text}]` from iOS, (b) emit `Voice feedback captured` CloudWatch row, (c) best-effort insert into the new table (S3 write must remain durable on a DB hiccup). New `src/routes/voice-feedback.js` exposes GET `/` (owner), GET `/admin/all` (admin), GET `/:id` (owner-or-admin), PATCH `/:id`; route order matters — `/admin/all` MUST register before `/:id` or it's swallowed as `id="admin"`. Mounted in `src/api.js` next to `/api/feedback` under `requireAuth + requireConsent`. 24 new tests across `realtime-log-shutdown.test.js` (21) + `routes-voice-feedback.test.js` (19) + `routes-debug-report-cloudwatch.test.js` (5). **Phase 2.4:** prompt alias section ("client" / "customer" / "name" / "name of customer" → `client_name`). **Phase 4.0 / 4.2 / 4.3 / 4.4 / 4.5:** four new BILLING address slots (`client_address` / `client_postcode` / `client_town` / `client_county`) in `config/field_schema.json` so `record_board_reading` can write them; prompt guidance for "use site address for client too" → four writes copying site→client_*; dispatcher guard rejects address-shaped values in `client_name` with `validation_error.code = "client_name_looks_like_address"`; `CONFIRMATION_FRIENDLY_NAMES` entries added INSIDE the `Object.freeze` literal (post-declaration assignment throws TypeError in strict ES modules); 4 dispatcher tests + inverted prompt regression locking the client_* mention. **Phase 5.1 / 5.2 / 5.3:** new `HAS_COMPLAINT_OR_NEGATION` reason in `pre-llm-gate.js` BEFORE `HAS_DIGIT` so complaints that accidentally contain digits log with intent reason; bare `no` requires a continuation pronoun so "no problem"/"no signal"/"no spare" still block. `voice_latency.gate_forwarded_complaint` telemetry row on positive match. 13 new positive + 3 negative + 1 ordering invariant test. **Phase 6.1 / 6.2 / 6.3 / 6.4:** RCD entry guard skips script entry when `\bRCD\b` co-occurs with imperatives (delete/undo/cancel/fix/why/stop/remove/clear) OR denials (what are you / i didn't / that's wrong / that's not). Session-scoped `dialogueScriptDeferredSlots` Map keyed by `${schemaName}:${circuit_ref}` survives `clearScriptState` so a re-entry doesn't re-ask a slot the inspector already deferred; `nextMissingSlot` gains an optional `deferredSet` 4th arg, volunteered writes clear the deferred mark. Cancel-drain emits `cancel_pending_tts {prefix: "srv-{script}-"}` WS frame on any `*_script_cancelled` (generalised over rcd/ocpd/rcbo/irs/rcs) so iOS slice 7.1 can purge queued TTS in the same namespace. 18 new tests. **Phase 7.3 / 7.4:** new `applyConfirmationDebounce(...)` in `stage6-event-bundler.js` (1500 ms same-field-family suppression) wired in `stage6-shadow-harness.js` AFTER the mid-stream-canonical filter and BEFORE the bundlerEmittedCount calc; `voice_latency.confirmation_debounced` log when ≥1 confirmation is suppressed. 8 new tests. **Phase 8.1 / 8.2 / 8.3 / 8.4 / 8.5:** `setFieldForAllCircuits` schema gains `exclude_circuits: integer[]`; dispatcher dedupes + validates input, applies subtractive filter BEFORE scope check (so an excluded ref doesn't pollute `skipped[]`), surfaces `excluded_count` = inspector intent count (scope-orthogonal). Prompt edge-case bullet teaches "apart from / except / excluding / all but" → `set_field_for_all_circuits` with `exclude_circuits:[N]` (rcd_time_ms worked example). `intentHints.bulkExclude` hint set on the turn context after the gate when the regex fires, with `voice_latency.intent_hint_bulk_exclude` telemetry. 6 new tests. **Token-cap bumps:** combined 13350 → 14150, standalone 8250 → 9000. **Migration auto-applies** via the Fargate pre-service-update task wired into the deploy workflow per the 2026-05-29 changelog entry — no manual `npm run migrate:up`. | src/extraction/realtime-log-sink.js, src/extraction/sonnet-stream.js, src/server.js, src/storage.js, src/db.js, src/routes/recording.js, src/routes/voice-feedback.js, src/api.js, migrations/012_voice_feedback.cjs, config/field_schema.json, config/prompts/sonnet_agentic_system.md, src/extraction/confirmation-text.js, src/extraction/stage6-dispatchers-board.js, src/extraction/stage6-tool-schemas.js, src/extraction/stage6-dispatchers-circuit.js, src/extraction/stage6-event-bundler.js, src/extraction/stage6-shadow-harness.js, src/extraction/pre-llm-gate.js, src/extraction/dialogue-engine/engine.js, src/extraction/dialogue-engine/helpers/extraction.js, src/__tests__/realtime-log-shutdown.test.js, src/__tests__/routes-voice-feedback.test.js, src/__tests__/routes-debug-report-cloudwatch.test.js, src/__tests__/stage6-tool-schemas-board.test.js, src/__tests__/stage6-agentic-prompt.test.js, src/__tests__/pre-llm-gate.test.js, src/__tests__/dialogue-engine-rcd-entry-guard.test.js, src/__tests__/stage6-event-bundler-debounce.test.js, src/__tests__/stage6-dispatcher-bulk-exclude.test.js |
| 2026-05-22 | **CCU dewarp output width restored to 2048-fixed (was native ~5917 px) — silent env-var regression closed in code + guardrails added.** Production extraction `1779468040371-vwj60m` (Wylex NHRS12SL, Ciaran field test) returned 14 entries when CV correctly counted 16 modules — dropped the 2-pole main switch + a Lighting circuit (circuit 1 or 2 next to the main switch). Side-by-side with the morning extraction on the same physical board (`1779438896822-l64kfa`, 16/16) showed the failing photo was framed ~3% tighter (rail/image ratio 80.9% vs 78.1%), which placed the rightmost rail content closer to the image edge. With dewarp output at native resolution (~5917 px), gpt-5.5 missed the right-edge devices — same failure mode as 2026-05-13. Forensic trace through ECS task-def revisions identified the regression source: rev 185 (2026-05-13 18:38 UTC) was registered out-of-band with `CCU_DEWARP_OUTPUT_WIDTH=2048` as a manual hotfix (no source commit); rev 186 (2026-05-14 15:33 UTC) was the next CI-driven `register-task-definition` from `ecs/task-def-backend.json`, which had never carried the var, silently stripping it. The fix lived for ~21 hours and was lost. **Same failure mode as `JWT_SECRET` on 2026-04-19** (changelog row below) — out-of-band hotfixes dropped by subsequent CI deploys. **Permanent fix (commit `01c081e`):** moved the 2048 default into `src/extraction/ccu-single-shot.js` so even if `ecs/task-def-backend.json` doesn't carry the env var, the code default holds. Env var still tunable as escape hatch (`=N` for explicit width, `=native` for the failing pre-fix behaviour reserved for OCR experiments). Also reverted today's interim margin bump 0.10→0.15 (`fc2c848`) which would have dropped per-module pixel density too far when combined with the 2048 cap. Brief asymmetric retake-required quality gate (`53649b1`) shipped and reverted (`b98078e`) on UX grounds — inspector retake friction is a worse UX than an occasional miss; root-cause fix is upstream. **Guardrails:** new `MANDATORY — Infrastructure changes must come from source` rule at the top of this file, and new `scripts/check-task-def-env-drift.sh` invoked from `.github/workflows/deploy.yml` before every `register-task-definition` step — fails the deploy if any env var exists on the live task def but not in the corresponding source template. Verified end-to-end with extraction `1779474149576-4p3d6d` post-deploy: `vlmCount=16, cvCount=16, vlmCountAgreesWithCv=true, labelCoverage=12/12`. Cost $0.045, wall 22.9s. Both the home Wylex's chronic slot-8 sewage-pump-misread-as-blank ALSO didn't fire on this extraction — the lower-resolution input may incidentally fix the classification, will monitor. **Lesson logged for future Claude sessions:** any AWS CLI / console action mutating live infra must be paired with a source commit in the same session; the failure to do this is the loophole that bit us twice. | src/extraction/ccu-single-shot.js, ecs/task-def-backend.json, .github/workflows/deploy.yml, scripts/check-task-def-env-drift.sh, CLAUDE.md |
| 2026-05-20 | **CCU single-shot prompt hardened on label-to-slot alignment + docs corrected to current pipeline.** Field-test extraction `1779189537865-ctf46p` on 2026-05-19 (Ciaran's first session, Crabtree board) returned "Cooker" mis-attached to a 16 A B-curve MCB (slot 9) when the cooker was physically on slot 10's 32 A MCB. Root cause: `MODERN_PROMPT` in `src/extraction/ccu-single-shot.js` told gpt-5.5 to read the label "nearest" the slot — a loose constraint that lets the model pick a label centred on a neighbouring column. **Fix:** replaced the LABELS section in both `MODERN_PROMPT` and `REWIREABLE_PROMPT` with explicit vertical-column alignment ("DIRECTLY ABOVE or DIRECTLY BELOW the device, in the SAME VERTICAL COLUMN centred on the device's left-right midpoint"), tie-break rule (label on a column boundary → return null on BOTH slots, don't guess), multi-slot label rule (one wide sticker covering ≥2 devices → null on every slot it spans). Keeps existing guards: ignore circuit-number prefix, multi-line join, never copy from adjacent slots, abbreviation normalisation. Deliberately does NOT ask the VLM to return coordinates — prior experiment with VLM-returned x-positions degraded results. **Docs:** CLAUDE.md "Current Focus" + `docs/reference/architecture.md` CCU Photo Extraction Pipeline section corrected to describe the actual 2026-05-08+ pipeline (gpt-5.5 single-shot, no per-slot crops). Every prior Claude session had been reasoning about CCU failures through a stale per-slot-crop mental model because the docs hadn't been updated when the pipeline pivoted. Historical changelog rows untouched. Two other bugs surfaced by the same extraction (RCD type web-search overriding correct AC reading; phase-walking heuristic marking pre-RCD MCBs as rcd_protected) tracked separately as todos. | src/extraction/ccu-single-shot.js, docs/reference/architecture.md, CLAUDE.md |
| 2026-05-13 | **PWA observation_confirmation UX brought to iOS parity — AlertCard render dropped, voice-answer auto-dismiss wired, 500ms client burst buffer added.** Field session sess_mp4jg2mt_231n (user 82b54893, job_1778705595721, 2026-05-13 20:55-20:57 UTC) reproduced a two-symptom regression: inspector said "Observation. There is a crack in a socket in a bedroom" expecting the obs to land, and the PWA appeared to "crash" mid-session. Both pinned to Deepgram emitting the trigger and the description as two separate finals 1.3s apart (textPreview="Observation." at 20:55:51.015, then "There is a crack in a socket in a bedroom." at 20:55:52.268). The Stage 6 dispatcher (`src/extraction/sonnet-stream.js:3179`) has no batching layer — the first final kicked off Sonnet turn-3 with just "Observation." as input (usage_input 353 / output 148 tokens, finished 20:56:01.528), and the description got queued for turn-4 because `entry.isExtracting=true` when it arrived. Turn-3 correctly applied RULE 1 of `sonnet_extraction_system.md:292` (explicit trigger → extract directly) but had nothing to record, so it emitted `ask_user` with `reason=missing_context, context_field=observation_clarify, question="What's the observation?"` (toolu_013cZs3cG6, TTS'd at 20:55:55). Inspector tapped Reject on the PWA's AlertCard (tap_reject_entered at 20:55:59.834) sending 'no'. Turn-4 then drained "There is a crack…" alone, applied RULE 2 correctly, and emitted `observation_confirmation` ("Should I log that as an observation on the certificate?" toolu_01XsG9wEhQbSSehqc4SGgw3m at 20:56:51.471 — answer_outcome=timeout, 45s wait). But the PWA had disconnected at 20:56:02.905 (no client-side `sonnet_ws_close` diagnostic ever reached the server; six `/api/me` calls at 20:56:04-05 match the Next.js full-page-reload pattern) so the second question never delivered — inspector saw nothing happen and the observation was lost. Three web-only fixes in commit `34e3972`: (1) `web/src/components/recording/recording-chrome.tsx` no longer renders `<AlertCard>` — iOS canon defines `AlertCardView` + `AlertOverlayModifier` but `grep -rn "alertOverlay()\|AlertCardView("` across `CertMateUnified/Sources` finds zero call sites outside the file's own previews; `.alertOverlay()` is never applied to a production view, so iOS handles ask_user as TTS-out + voice-in only. (2) `web/src/lib/recording-context.tsx` dispatchFinal now removes the matching question from `questions[]` state immediately after `sendAskUserAnswered` fires (mirrors `DeepgramRecordingViewModel.swift:2108-2113` `alertManager.dismissCurrentAlert()`). The existing `dismissTimersRef` useEffect handles timer cleanup on state change. Match by `tool_call_id` (not index) so reordering is safe. (3) New 500ms `BURST_BUFFER_TIMEOUT_MS` + `burstBufferRef` + `dispatchFinalBurstBuffered` wrapper in `recording-context.tsx` — every Deepgram final is held for 500ms; a second final inside the window concats with ' ... ' (server's legacy `eicr-extraction-session.js:1440 _processUtteranceBatch` separator) and dispatches immediately. Both existing `dispatchFinal` call sites (naming-buffer-timeout fire + normal path) route through the wrapper; `teardownDeepgram` clears the timer alongside `pendingNamingBufferRef`. The dispatcher batching itself was kept out of the backend (CLAUDE.md immutable-during-PWA-only-work rule; adding a backend hold would add up-to-500ms worst-case latency to every iOS utterance). 500ms covers Deepgram-segmented sub-second pauses but won't catch this specific session's 1.3s gap — a larger window would slow every isolated utterance, so 500ms is the conservative perceptual-responsiveness pick (Sonnet's roundtrip is ~3s so worst-case +17% only when no second final arrives). Tests: 871/871 vitest pass; lint clean for touched files (5 pre-existing errors on main verified via git stash). | web/src/components/recording/recording-chrome.tsx, web/src/lib/recording-context.tsx, CLAUDE.md |
| 2026-05-09 | **`ask_user.context_field` enum widened to cover board-level fields — unblocks sub-board creation focus-asks.** Backend single-file change in `src/extraction/stage6-tool-schemas.js`: `CONTEXT_FIELD_ENUM` was previously only `circuit_fields` keys + sentinels (`observation_clarify`, `none`) + `null`. The multi-board sprint (2026-05-07/08) added `parent_board_id`, `feed_circuit_ref`, `board_type`, `sub_main_cable_*` to `board_fields` and shipped `add_board` + `record_board_reading` so Sonnet could WRITE them, but never widened the ask enum that gates CLARIFICATION asks for those same fields. Result: Sonnet could write a board field but couldn't legally ask about it. Reproduced end-to-end in field session CBC1C763 (2026-05-09 18:58 UTC, "Garage" sub-board): inspector said "new sub-board" → Sonnet TTS'd combined ask "designation, AND which circuit feeds it?" → inspector answered just "Garage" → Sonnet emitted focused follow-up `ask_user` with `context_field: "feed_circuit_ref"` → validator at `stage6-dispatch-validation.js:304` rejected as `invalid_context_field` (the enum had ZERO board keys) → focused question never reached TTS → Sonnet fell back to `add_board({board_type:"sub_main"})` with no parent / feed → add_board validator (correctly) rejected with `feed_circuit_ref_required` → Sonnet recovered & re-asked with `context_field: "none"` (legal) but inspector had already moved on to circuit 2 → ask landed `user_moved_on`, sub-board never created, inspector stopped session at $0.17. Pinned by 3 prior add_board attempts at the same root cause — the user reported "I've never been able to add a sub-board by any method." Compounding the symptom: iOS commit `753cc20` (today, 14:33 BST) is the FIRST iOS build that actually consumes `result.board_ops` from the wire — earlier builds decoded the array but silently dropped it, so even the 3 backend add_boards that DID succeed today (11:46, 16:08, 16:09 UTC via `single_main_fallback`) never rendered into `job.boards` on iPad. **Fix:** widened `CONTEXT_FIELD_ENUM` to the full union of `circuit_fields` ∪ `board_fields` ∪ `supply_characteristics_fields` ∪ `installation_details_fields` ∪ sentinels (filtered for `_ui_*` meta keys, deduped via Set, sorted, `null` appended). This mirrors `BOARD_FIELD_ENUM`'s shape — same input config, same dedupe rule — so the ask enum and the write enum cover the same namespace. Two existing schema-test assertions updated (the length check at `stage6-tool-schemas.test.js:260` was coupled to `circuit_fields_count + sentinels_count`); 13 new positive assertions split across two suites: schema test pins `feed_circuit_ref`/`parent_board_id`/`board_type`/sub-main cable trio AND `earth_loop_impedance_ze`/`prospective_fault_current`/`address`/`postcode`; dispatch-validation test uses `test.each` to confirm `validateAskUser({context_field: <key>})` returns null for all 10 of those keys. Full backend suite: 3292/3292 green (3 skipped, 130 of 131 suites — the 1 skipped is unrelated). Combined with iOS `753cc20` already on `main`, this closes the "sub-board never appears" symptom in both directions: backend can ask focused questions to fill required board fields BEFORE add_board fires, and iOS now consumes `board_ops` so successful add_boards land in `job.boards`. TestFlight build with iOS `753cc20` is the next step. | src/extraction/stage6-tool-schemas.js, src/__tests__/stage6-tool-schemas.test.js, src/__tests__/stage6-dispatch-validation.test.js, CLAUDE.md |
| 2026-05-07 | **Multi-board sprint Phases 2a + 4a shipped + Phase 5 handoff written; both Codex deal-breakers closed.** Phase 2a `ddde287`: `src/export.js` `CIRCUIT_FIELD_ORDER` and `CIRCUIT_HEADERS` extended with `board_id` / `is_distribution_circuit` / `feeds_board_id` (appended at end so legacy 29-column CSVs still parse — `parseCSV` maps by header name, not position). New `src/__tests__/export.test.js` with 4 unit tests pinning the round-trip; the `test.todo` placeholder in `jobs.test.js` becomes a cross-reference. Closes Codex deal-breaker #1 (CSV silently dropping multi-board fields). Phase 4a `7e588c8`: `src/routes/recording.js` `POST /api/recording/:sessionId/finish` gets an explicit "SINGLE-BOARD ONLY" docstring + a `logger.warn` on the `boards[0]` collapse when `jobData.boards.length > 1` (logs the dropped sub-board ids to CloudWatch). The whisper path is now formally scoped — multi-board jobs route through Stage 6 (`/api/sonnet-stream` WS), iOS picks the right path. Closes Codex deal-breaker #2. No behaviour change for any current iOS client. Phase 5 handoff: new sibling `PHASE5_HANDOFF.md` (≈340 lines) is the self-contained brief for the next session resuming Phase 5 — covers the Codex-corrected snapshot shape (`stateSnapshot.circuits` is a keyed object at `eicr-extraction-session.js:766`, NOT an array, so PLAN.md's `.find(...)` / `.push(...)` sketch is structurally wrong; right pattern is composite-string keys `'main::1'`), the 6+ file `circuits[0]` entrenchment audit with verbatim line refs, the recommended 6-slice commit order (5.1 `ensureMultiBoardShape` → 5.6 legacy bucket removal), and the deal-breaker #3 status (Stage 6 `board_ops` wire channel — still open, scheduled as Phase 6.0 before any new tool can usefully ship). Sprint-level `HANDOFF.md` updated to point at it. Phase 5 itself (Stage 6 state-model widening, 2-3 sessions) is the next concrete step. Plan/delivery log: `.planning-stage6-agentic/handoffs/multi-board-support-2026-05-07/PLAN.md`. | src/export.js, src/__tests__/export.test.js, src/__tests__/jobs.test.js, src/routes/recording.js, .planning-stage6-agentic/handoffs/multi-board-support-2026-05-07/PHASE5_HANDOFF.md, .planning-stage6-agentic/handoffs/multi-board-support-2026-05-07/HANDOFF.md, CLAUDE.md |
| 2026-05-07 | **Multi-board sprint Phase 4 shipped — `/api/analyze-ccu` board attribution + iOS `.addNewBoard` extraction mode.** Backend `EICR_Automation` `a40a9f3`: `src/routes/extraction.js` parses two new optional multipart fields (`board_id` string, `board_index` non-negative integer with `Number.isInteger` guard) and echoes both back as `analysis.attribution = { board_id, board_index }`. Always present (nulls when omitted) so iOS has a single decode path. Defensive parsing matches the existing `rail_roi` precedent — malformed input silently becomes null. 3 new tests in `ccu-route-merger.test.js` (happy / back-compat / malformed-index). iOS `CertMateUnified` `f9902cd`: fifth `CCUExtractionMode` case `.addNewBoard` (icon `square.split.2x1`, subtitle "Photograph a separate consumer unit (sub-board)"); `FuseboardAnalysis.attribution: BoardAttribution?` decoding; `APIClient.analyzeCCU(...)` + `JobViewModel.analyzeCCUPhoto(...)` grow `boardId: String?` + `boardIndex: Int?` parameters; `APIClientProtocol` + `MockAPIClient` updated; `FuseboardAnalysisApplier.applyAddNewBoard(...)` appends a fresh `BoardInfo` (default designation `"DB-N"`), leaves boardType/parentBoardId/feedCircuitRef/sub-main-cable fields nil for inspector to fill (Phase 2.3 validator tolerates), stamps circuits with the new board's id, and runs `applyBoardInfo(overwrite: true)` — the `idx == 0` guard means supply characteristics stay on main, never retroactively claimed by a sub-board photo. 4 new XCTests; all pass on Mac Catalyst. 46 pre-existing test failures in `FuseboardAnalysisApplierTests` verified via `git stash` to be unrelated to Phase 4. Phase 5 (Stage 6 state-model widening) is next — heaviest slice, 2-3 sessions. Phase 2a (CSV header hardening) + Phase 4a (recording.js scope decision) remain unblocked, parallelisable. Plan/delivery log: `.planning-stage6-agentic/handoffs/multi-board-support-2026-05-07/PLAN.md`. | src/routes/extraction.js, src/__tests__/ccu-route-merger.test.js, CertMateUnified/Sources/Models/CCUExtractionMode.swift, CertMateUnified/Sources/Models/FuseboardAnalysis.swift, CertMateUnified/Sources/Services/APIClient.swift, CertMateUnified/Sources/ViewModels/JobViewModel.swift, CertMateUnified/Sources/Models/FuseboardAnalysisApplier.swift, CLAUDE.md |
| 2026-05-07 | **Multi-board sprint Phase 3 shipped — PDF sub-main section (iOS-only).** `CertMateUnified` commit `df4311c`. Inside the per-board landscape page loop in `Sources/PDF/EICRHTMLTemplate.swift` (line 1472, `for board in job.boards`), the existing 4-row Board Details table is now followed — when `board.boardType == .subDistribution || .subMain` — by a single-row "Distribution Circuit (Sub-Main)" section showing: Fed from (parent designation, looked up via `parentBoardId`), Feed circuit (`feedCircuitRef`), Cable material (`subMainCableMaterial`), Live conductor CSA (`subMainCableCsa`), CPC CSA (`subMainCpcCsa`). Cable-length cell is intentionally omitted (Phase 1 dropped the field at the model layer). Implementation splits the existing single `html += """..."""` heredoc into two around the new conditional, keeping rendered HTML byte-identical for non-sub-boards. Reused existing `red-bar-small` heading + `board-detail-table` styling so the new section blends into the page. 4 new XCTests in `EICRHTMLTemplateTests.swift` — happy path, single-main-board absence, `.subDistribution` coverage, and a Phase 1 cross-check that no cable-length row renders. Build verified via `xcodebuild ... -destination 'generic/platform=iOS Simulator' build` → BUILD SUCCEEDED. Tests verified via Mac Catalyst (`xcrun simctl create` hangs in "creation state" on this Mac, flagged separately) → 25/25 pass. Phase 4 (`/api/analyze-ccu` board attribution + iOS `.addNewBoard` mode) is next; Phase 4a (recording.js single-board scope decision) is also unblocked and could land in parallel. Plan/delivery log: `.planning-stage6-agentic/handoffs/multi-board-support-2026-05-07/PLAN.md`. | CertMateUnified/Sources/PDF/EICRHTMLTemplate.swift, CertMateUnified/Tests/EICRHTMLTemplateTests.swift, CLAUDE.md |
| 2026-05-07 | **Multi-board sprint Phase 2 shipped — backend schema parity + hierarchy validator.** Four backend commits on `main`: (1) `1059f39` widens `packages/shared-types/src/circuit.ts` to mirror iOS — new `BoardType` union, `BoardInfo` gains 18 fields (already on iOS but missing from shared-types since the multi-board iOS work landed: `designation`, `supplied_from`, polarity/phases confirmation, RCD trip time, main-switch/overcurrent/SPD ratings, notes, plus the 4 multi-board hierarchy fields and the sub-main cable trio), `Circuit` gains `board_id` / `is_distribution_circuit` / `feeds_board_id`, `Board.designation`/`location` made optional, canonical `ze_at_db` added alongside legacy `zs_at_db` alias. tsc green across `shared-types`, `shared-utils`, `web`. (2) `ebb6183` adds three regression tests to `src/__tests__/jobs.test.js`: PUT preserves multi-board fields in `extracted_data.json` (intercepts `mockUploadText`), GET surfaces them on response (stubs `mockDownloadText`), `test.todo` for circuit-level fields pinned to Phase 2a CSV-header hardening. Pins Phase 1's `sub_main_cable_length` deletion. (3) `ef56e25` adds new module `src/extraction/board-hierarchy-validator.js` exposing `validateBoardHierarchy(boards, circuits) → { ok, errors }` with 4 error codes (`parent_not_found`, `circular_reference`, `multiple_main_boards`, `feed_circuit_not_found`); wired into `src/routes/jobs.js` PUT handler immediately after the auth check, gated on `if (boards)` so single-board legacy saves are unaffected. 13 unit tests + 1 route-level test asserting no S3 upload happens on rejection. (4) `c21820b` extends `config/field_schema.json` with 9 entries — 2 in `circuit_fields` (`is_distribution_circuit`, `feeds_board_id`), 7 in `board_fields` (`board_type`, `parent_board_id`, `feed_circuit_ref`, `sort_order`, `sub_main_cable_material`, `sub_main_cable_csa`, `sub_main_cpc_csa`). CSA fields typed as `text` to match iOS `CMUnitTextField` UI; `sub_main_cable_material` mirrors `Constants.conductorMaterials` exactly. `sub_main_cable_length` intentionally absent (Phase 1). One deviation from the literal plan: `Circuit.board_id` was added explicitly in 2.1 (plan listed only `is_distribution_circuit` + `feeds_board_id`) — it's the foundation of the multi-board model and is already used by the 2.2 test and the 2.3 validator. Tests: 1 todo, 29 passed in the two newly-touched suites. Phase 3 (PDF sub-main section) is next — iOS-only single session. Plan/delivery log: `.planning-stage6-agentic/handoffs/multi-board-support-2026-05-07/PLAN.md`. | packages/shared-types/src/circuit.ts, src/__tests__/jobs.test.js, src/extraction/board-hierarchy-validator.js, src/routes/jobs.js, config/field_schema.json, CLAUDE.md |
| 2026-05-07 | **Multi-board sprint Phase 1 shipped — `sub_main_cable_length` dropped from iOS `BoardInfo`.** First slice of the multi-board / sub-main support sprint (`.planning-stage6-agentic/handoffs/multi-board-support-2026-05-07/`, plan locked 2026-05-07). Removed the field from `Sources/Models/BoardInfo.swift` (model property, `CodingKeys` case, init parameter, init body assignment, custom decoder line, custom encoder line) and the corresponding "Cable Length" `CMUnitTextField` row in `Sources/Views/JobDetail/BoardTab.swift` sub-main cable section — Cable Material, Live CSA, and CPC CSA remain. BS 7671 distribution-circuit documentation requires material + CSA (live + cpc), not length; the field appeared in the data model but was never specified by the standard nor used by inspectors during field testing. `Codable.decodeIfPresent` is tolerant of stale `sub_main_cable_length` keys, so any old job snapshots load cleanly with the new struct shape — no migration script. Backend `packages/shared-types/src/circuit.ts` `BoardInfo` interface never carried this field, so no wire-format break. Commit `723b3f3` on `CertMateUnified` `main`; build verified via `xcodebuild -scheme CertMateUnified -destination 'generic/platform=iOS Simulator' build` → `BUILD SUCCEEDED`. Diff: 2 files, 2 insertions, 7 deletions. Phase 2 (backend `shared-types` + `field_schema` + hierarchy validator) is the next concrete step, separate session because it crosses repos. | CertMateUnified/Sources/Models/BoardInfo.swift, CertMateUnified/Sources/Views/JobDetail/BoardTab.swift |
| 2026-05-06 | **BS-EN canonical aligned across schema, lookup, parser, picker, and migration (Option B — prefixed form).** Pre-fix the system carried four drifting canonical forms for `ocpd_bs_en` / `rcd_bs_en`: schema bare-digit (`60898-1`), CCU `BS_EN_LOOKUP` bare-digit, `device-lookup-table.js` BS-EN-prefixed-WITH-suffix (`BS EN 60898-1`), and `parseBsCode` + iOS `Constants.swift` BS-EN-prefixed-NO-suffix (`BS EN 60898`). The drift hadn't surfaced as a user bug (most CCU values get dictated over before save, and the resolver digit-normalises) but every change in the area was harder to reason about. The 2026-05-06 alignment sprint (handoff: `.planning-stage6-agentic/handoffs/bs-en-alignment-2026-05-06/PLAN.md`) chose **Option B** (prefixed canonical, no `-1` sub-clause) because that's what's printed on UK certs and what inspectors say aloud. Single backend commit `4611a2e` flipped: `config/field_schema.json` (`ocpd_bs_en` + `rcd_bs_en` options), `BS_EN_LOOKUP` in both `src/routes/extraction.js:257` AND legacy `src/extract.js:215`, `src/extraction/device-lookup-table.js` (32× `-1` strips), `src/extraction/dialogue-engine/parsers/bs-code.js` (drops AFDD `BS EN 62606` and `BS 4293` patterns; maps `BS 88-2`/`88-3` legacy UK refs onto harmonised `BS EN 60269-2`; new explicit 60269-2 pattern), VLM prompt strings in `ccu-geometric.js` + `ccu-sliding-window.js` (was telling the model to write the `-1` suffix form), Sonnet enum reference in `routes/extraction.js`, `slotsToCircuits` fallbacks (the `61008` / `61009` / `60898-1` literals at `extraction.js:2104`/`2123`), `generate_pdf.js` web-PDF defaults, and `scripts/normalise-bs-en-values.js` mapping table inverted to target the prefixed canonical (with explicit folds for the pre-alignment bare-digit form, AFDD → `N/A`, and `BS 4293` → `BS EN 61008` so historical rows never surface as picker-empty). 7 backend test files updated; 1 test file (`dialogue-engine-bs-code-parser`) gains explicit coverage for the new 88-2 → 60269-2 mapping + AFDD/4293 returning `null`. Companion iOS commit (`d4d6db1` on `CertMateUnified`) flips `Constants.swift` `ocpdBsEnOptions` + `rcdBsEnOptions` and aligns the `FuseboardAnalysisApplier` test fixtures from `BS EN 61009-1` → `BS EN 61009`. New diagnostic script `scripts/audit-bs-en-values.js` (commit `90cbc4d`, read-only) walks `job_versions.data_snapshot` and reports the value-distribution histogram + top-20 unknowns, used as a pre-flight before running the production migration. Backend tests: 2977/2977 green. Production data migration (running `scripts/normalise-bs-en-values.js --apply` against the prod DB) is **deferred** to coordinate with Derek's next field test — single-user deployment so the window is trivially small. | config/field_schema.json, src/routes/extraction.js, src/extract.js, src/extraction/device-lookup-table.js, src/extraction/dialogue-engine/parsers/bs-code.js, src/extraction/ccu-geometric.js, src/extraction/ccu-sliding-window.js, scripts/normalise-bs-en-values.js, scripts/audit-bs-en-values.js, CertMateUnified/Sources/Models/Constants.swift, CLAUDE.md |
| 2026-05-06 | **Chitchat pause feature shipped — backend stops sending to Sonnet during true chitchat windows; iOS shows a wake banner.** Production motivator: 60-min jobs frequently include 10-15 min of chitchat (site banter, phone calls, walking between rooms). Forwarding those turns to Sonnet at full conversation context bills the dominant cost driver (input-token spend scales with conversation length, not just the new turn) for zero extraction value. **Architecture**: per-session state on the active-session entry (`turnsSinceExtraction`, `paused`, `pausedAt`, `replayBuffer`). New module `src/extraction/chitchat-pause.js` owns the pure state-machine — `ensureChitchatState`, `recordTurn`, `enterChitchatPause`, `exitChitchatPause`, `bufferTranscript`, `drainReplayBuffer`, `isWakeWordTranscript`. Module is intentionally framework-agnostic; `sonnet-stream.js` owns WS plumbing + session lookup. **Engagement signal** = at least one of: Sonnet extracted a reading, captured an observation, emitted a question, the session has a pending ask_user round-trip, OR iOS regex (`TranscriptFieldMatcher`) caught a value on this turn. The last bullet mitigates Derek's "if regex extracts well, Sonnet would go offline" concern — regex-only sessions never accumulate the counter. **Threshold** = `CHITCHAT_PAUSE_THRESHOLD = 10` consecutive zero-engagement turns. **Wake triggers** (four): (1) WAKE_REGEX match on transcript text — `\b(?:resume\|carry\s+on\|continue\|wake\s+up\|go\s+on\|back\s+to\s+it\|certmate.{0,15}?(?:resume\|listen\|on))\b/i` — bounded "certmate" form catches "CertMate, resume" / "CertMate listen" without false-firing on prose mentioning the brand; (2) iOS regex hit (`msg.regexResults` non-empty); (3) manual `chitchat_resume` WS envelope from the iOS Resume button; (4) existing `session_resume` WS envelope (Deepgram doze recovery — audio coming back also wakes Sonnet). **Replay buffer** capped at 30 s by time (not size) via `CHITCHAT_REPLAY_HORIZON_MS = 30_000`; eviction at append-time AND drain-time so a slow wake doesn't resurrect already-stale text. On wake from any source, buffer drains in chronological order, space-joined, prepended to the wake utterance before forwarding to `handleTranscript` so a value spoken right at the pause/wake boundary lands in the next Sonnet turn. **Cache keep-alive** delivered by existing infrastructure: `EICRExtractionSession._sendCacheKeepalive` already runs a 4-min cache-refresh timer (`CACHE_KEEPALIVE_MS = 4 * 60 * 1000`) for the lifetime of `session.isActive`, with `cache_control: ephemeral 5m` on the system blocks. The chitchat-pause helpers DO NOT call `session.pause()` (that's the Deepgram doze path with its own `PAUSE_KEEPALIVE_BUDGET_MS` cap), so the cache keepalive runs uninterrupted through chitchat windows. Documented as load-bearing invariant in `chitchat-pause.js` header + 3 guard tests verifying `enterChitchatPause` / `exitChitchatPause` never touch session lifecycle methods. **Hooks in `sonnet-stream.js`**: (a) `case 'transcript'` — when paused, check wake word + regex hit; otherwise append to buffer + suppress. (b) `case 'session_resume'` — also clear chitchat pause if active. (c) New `case 'chitchat_resume'` — manual Resume button. (d) Both extraction-result paths (sync `handleTranscript` ~L3550 and async `session.onBatchResult` ~L2152) call `recordChitchatTurn` after the existing "Extraction result" log. (e) Non-paused transcripts with `regexResults.length > 0` reset the counter immediately at transcript-receipt time (don't wait for Sonnet result). **iOS banner** (`Sources/Views/Components/ChitchatPauseBanner.swift`) — top-of-screen overlay rendering only when `recordingVM.chitchatPaused` is true; copy "Listening paused — Say 'resume', 'carry on' or a value to wake" + brand-blue Resume button; distinct from existing Deepgram doze indicator (`isPaused`) and phone-call interruption (`isInterruptionPaused`). New WS message types decoded in `Sources/Services/ServerWebSocketService.swift`: `chitchat_paused` / `chitchat_resumed`. Outgoing helper `sendChitchatResume()`. View model gains `var chitchatPaused: Bool` (auto-tracked by `@Observable`) + `resumeFromChitchatPause()` which optimistically clears the flag locally + sends the WS envelope. Default no-op delegate methods in protocol extension keep test mocks compiling unchanged. **Costs**: input-token spend on long chitchat windows is the dominant saving target. For a 60-min job with 10 min of chitchat in the middle: previously ~10 min of transcript-laden turns × full conversation context = significant Sonnet input-token cost; with chitchat pause those turns are dropped entirely and the cache stays warm via the 4-min keepalive (~$0.0045/probe × 1 probe per 4 min = ~$0.067/hour idle, but only during pause, capped by Deepgram doze for very long pauses). **Tests**: 47 chitchat-pause unit tests covering counter increment/reset, threshold trigger, ask-user blocking, idempotent enter/exit, full wake-word table, replay-buffer chronological drain, append-time + drain-time horizon eviction, regex-hint engagement signal, regex-only sessions never pause, and the slice-3 session-lifecycle guards. **Slices** (4 commits across two repos): backend `b0d977a` (slice 1, state machine + WS), `c580f42` (slice 2, replay buffer + regex-hit wake), `663c135` (slice 3, docs + guard tests); iOS `f6a29f0` (slice 4, banner UI). Plan + delivery log at `.planning-stage6-agentic/handoffs/chitchat-pause-2026-05-06/PLAN.md`. CI: backend deployed via runs `25448898663` / `25451622312` / `25451779043`. iOS not yet on TestFlight — local-build-first per Derek's preference; ready for `./deploy-testflight.sh` whenever Build 343+ is wanted. | src/extraction/chitchat-pause.js, src/extraction/sonnet-stream.js, src/__tests__/chitchat-pause.test.js, CertMateUnified/Sources/Services/ServerWebSocketService.swift, CertMateUnified/Sources/Recording/DeepgramRecordingViewModel.swift, CertMateUnified/Sources/Views/Components/ChitchatPauseBanner.swift, CertMateUnified/Sources/Views/JobDetail/JobDetailView.swift, CertMateUnified/CertMateUnified.xcodeproj/project.pbxproj, CLAUDE.md, .planning-stage6-agentic/handoffs/chitchat-pause-2026-05-06/PLAN.md |
| 2026-05-06 | **Stage 6: BS-code parser gains Lev-1 fuzzy fallback to break OCPD/RCD/RCBO re-ask loop.** Production session C4467E35 (1 Plowley) reproduced an infinite "What's the BS number?" loop on OCPD: inspector said "BS 60898" three times, Deepgram dropped the leading "0" and emitted "BS 6898", `parseBsCode`'s strict `\b60898\b` pattern never matched, dialogue engine treated "no answer" and re-asked. The earlier `8dfe660` / `72d01a8` / `e5a5bc8` enum-resolver fixes only covered the `ask_user` dispatcher path — the dialogue engine has its own per-slot parser that's independent. **Fix in `src/extraction/dialogue-engine/parsers/bs-code.js`**: after the exact-pattern miss, extract the longest 4-6 digit run (regex `\d[\d-]*\d|\d+`), strip non-digits via `digitsOnly`, compare to every canonical's digit form via standard O(m\*n) Levenshtein-1 DP with rolling two-row memory. Returns canonical only when EXACTLY ONE target sits at distance ≤ 1 — ambiguity (e.g. "6100" tied between "61008" and "61009") deliberately falls through so the engine re-asks rather than guessing. Lev-1 covers all three primitive Deepgram drift modes: insertion ("6898" → "60898"), deletion ("610008" → "61008"), substitution ("60008" → "61008"). Conservative on length: only attempts a match when the candidate has 4-6 digits, ruling out fragments like "6" or long phone-style numbers that are clearly not BS codes. **`FUZZY_TARGETS` list** mirrors PATTERNS digits: 60947-2, 60947-3, 60898, 61008, 61009, 62606, 62423, 88-2, 88-3, 3036, 1361, 4293. Covers OCPD, RCD, and RCBO BS slots — all three schemas in `src/extraction/dialogue-engine/schemas/{ocpd,rcd,rcbo}.js` share `parseBsCode` so the fuzzy fallback applies uniformly. Canonical strings unchanged ("BS EN 60898", "BS EN 61009", "BS 3036" etc.) so iOS picker (`Constants.swift` `ocpdBsEnOptions` / `rcdBsEnOptions`), existing job data, derivation matchers (`helpers/derivations.js` → `bsCodeDigits`), and 50+ existing tests all keep working unchanged. The schema-options vs canonical-output divergence introduced by `e5a5bc8` is a separate cleanup (would require iOS Constants alignment + DB normalisation via `scripts/normalise-bs-en-values.js`); not in this fix's scope. **Tests**: 24 new unit tests in `dialogue-engine-bs-code-parser.test.js` covering exact-pattern paths, all three Lev-1 primitives, ambiguity gate, length bounds (4-6 digits), exact-match precedence over fuzzy. 1704/1704 stage6 + dialogue-engine + bs-* tests green. Commit `c36f75a`, deployed via CI run `25447365093`. | src/extraction/dialogue-engine/parsers/bs-code.js, src/__tests__/dialogue-engine-bs-code-parser.test.js, CLAUDE.md |
| 2026-04-17 | Web rebuild Phase 7b — iOS "Add to Home Screen" hint on `/settings`. **Closes Phase 7b.** Final scope-exclusion item from `PHASE_7A_HANDOFF.md` §"Scope exclusions": "No iOS 'Add to Home Screen' hint — Safari never fires `beforeinstallprompt`, a dismissible banner on `/settings` is the planned accommodation." Without this, iPhone/iPad users have no pathway to discover that CertMate is installable — Chrome/Edge/Android get the `<InstallButton />` in the AppShell header (driven by the `beforeinstallprompt` event captured in `install-store.ts`), but Safari desktop + iOS deliberately omit that event per Apple platform policy, so an iOS user would otherwise go their entire app lifetime without ever learning they can pin CertMate to their Home Screen. **`web/src/components/pwa/ios-install-hint.tsx`** is a dismissible banner that guides the user through the iOS install flow: Share icon → Add to Home Screen → Add. **Platform detection**: `/iPad|iPhone|iPod/.test(navigator.userAgent) && !('MSStream' in window)` — the `MSStream` guard filters out old Windows Phones that spoofed iOS user-agents (vanishingly rare now, but the guard costs nothing and removing it would be a regression-shaped hole). Non-iOS devices already have `<InstallButton />` so double-prompting would be redundant noise. **Already-installed check**: two signals combined — `(navigator as IOSNavigator).standalone === true` (iOS-specific property, typed via a local interface extension rather than a global `declare module` pollute so every `navigator` access across the codebase doesn't gain an unused optional) OR `window.matchMedia('(display-mode: standalone)').matches` (the cross-browser standard that iPadOS 16+ reports reliably). Either one firing suppresses the banner. **Dismissal persistence**: `localStorage` key `cm_pwa_ios_hint_dismissed:v1` — the `:v1` suffix is deliberate: the rule is "show once per user per campaign", not "never again forever", so a future marketing push can reset the pool by bumping to `:v2` without a data-migration step. The try/catch around `setItem` handles private-mode Safari + quota-exceeded (falls through silently; worst case the banner reappears on next navigation, which is strictly better than a thrown exception hitting the error boundary). **First-paint behaviour**: `useState(false)` + effect that flips to `true` only after all the platform gates pass. Defaulting to `false` means SSR renders the no-show case and the client never flashes the banner for a frame on Chrome/desktop before detection runs. **Mount site**: `/settings` page, inserted between the hero profile section and the TEAM SectionGroup. Deliberately **NOT the dashboard** — the dashboard is already dense with hero + recent-jobs list + setup grid, and adding an install prompt there would push Recent Jobs below the fold on a phone for the primary daily task. `/settings` is a low-traffic page by inspector-workflow standards (they hit it once to configure, then rarely), so the banner surfaces at a moment when the user is already in "configure my app" mode and receptive to an install nudge. **Visual design**: `<aside role="region" aria-label="Install CertMate on your iPhone">` — semantic landmark for screen readers so users can jump to or skip the banner. Brand-blue-tinted surface (`color-mix(in srgb, var(--color-brand-blue) 10%, var(--color-surface-2))`) with brand-blue border at 30% opacity — matches the 6b LinkCard accent treatment so the banner feels like part of the settings surface rather than a foreign nag. Plus icon in a blue 12px-rounded tile at 10×10, matching the `LinkCard` icon tile sizing on the same page. Numbered ol with inline Share icon on step 1 (`text-[var(--color-brand-blue)]` so the visual token matches what the user will see in Safari's toolbar), keyword-bolded "Add to Home Screen" and "Add" on step 2. Dismiss is a 32×32 × icon button at top-right with `aria-label="Dismiss install hint"` — 32px ≥ WCAG 2.1 AA / Apple HIG 44pt minimum tap target when accounting for the surrounding padding. **No auto-dismiss-on-install** — we'd need to re-check standalone status on `visibilitychange` or poll, but iOS "installs" don't fire a web-platform event at all; the banner is suppressed on the next navigation after install via the standalone check at mount. One-cycle staleness is acceptable for a once-in-a-lifetime flow. **Settings page integration** (`web/src/app/settings/page.tsx`): one import, one render site between hero and TEAM. The banner self-suppresses in every case where it shouldn't render, so the integration site doesn't need conditional logic — this keeps the settings page free of platform-specific branching and the decision to show/hide lives in one place (the component itself). **Scope exclusions closed by this commit**: all four items from `PHASE_7A_HANDOFF.md` §"Scope exclusions" that were tagged "7b" are now landed — (1) user-initiated SW update handoff (`ce8323a`), (2) IDB read-through cache (`2d3527f`), (3) AppShell offline indicator (`a85487f`), (4) iOS ATHS hint (this commit). **Still deferred**: no outbox / mutation queue (7c — next phase), no offline job-edit UI polish (7d), no push notifications / periodic sync / share-target, no Sentry. **Verification**: `npm run typecheck` clean (main + SW), `npm run lint` at 0 errors / 6 warnings baseline (unchanged from `a85487f`), `npm run build` succeeds — `/settings` still prerenders static. Manual DevTools checks: toggle device emulation to iPhone 14 → banner visible on first `/settings` visit; tap × → banner hidden, localStorage row set; reload → banner stays hidden; Application → Local Storage → clear `cm_pwa_ios_hint_dismissed:v1` → banner returns. On a simulated Pixel 7 → banner never renders (UA doesn't match iOS regex). Phase 7b now closed. | web/src/components/pwa/ios-install-hint.tsx, web/src/app/settings/page.tsx, CLAUDE.md |
| 2026-04-17 | Web rebuild Phase 7b — AppShell offline indicator. Follow-up to the IDB cache (`2d3527f`); closes the next item in `PHASE_7A_HANDOFF.md` §"Scope exclusions" — "No offline indicator in AppShell — the `navigator.onLine` + retry-on-online UX is 7b." Adds a small amber pill to the AppShell header right-cluster that appears whenever the browser reports `navigator.onLine === false`. Pairs with the 7b IDB cache so the inspector knows WHY the list/detail looks the same as last time — the cache may be showing stale data, and the pill is how they know connectivity is the reason. **`web/src/lib/pwa/use-online-status.ts`** is a minimal hook that wraps `navigator.onLine` + `online`/`offline` window events. Returns a plain boolean — most callers just want "am I online right now", and keeping it small avoids tempting consumers to track transient "just came back" state across remounts (which is easy to get wrong). SSR-safe: defaults to `true` (optimistic) on the server and first client render because `navigator` doesn't exist there; the `useEffect` flips to `navigator.onLine` on mount, so a genuinely-offline user sees the pill within one tick. Defaulting to `false` would flash the pill on every cold render even for online users — worse UX. Plain `useState` rather than `useSyncExternalStore` because this is a binary that changes at most a handful of times per session; the subscription cost of the external-store pattern isn't warranted. **`web/src/components/pwa/offline-indicator.tsx`** renders the pill: rounded-full border + 15%-opacity background fill in `--color-status-processing` (amber #ff9f0a, same token as `processing` status pills so the design-system palette stays coherent), `WifiOff` lucide icon, "Offline" text on `sm+` / icon-only below. **Amber, not red** — deliberate: red (`--color-status-failed`) reads as "something broke" / destructive; amber reads as "degraded — be aware". Offline with a cached render is degraded, not broken; the SWR from the IDB cache commit keeps everything browsable. Red would be panic-inducing for a state the system handles gracefully. **Icon-only below `sm`** — the mobile header is 56px tall with Logo + user-name + InstallButton + Sign-out already in the right cluster; forcing the label at 320px wide iPhone SE would push Sign-out off-screen. `title` + `aria-label` always carry the full "You are offline. Showing cached data; changes will not sync until your connection returns." string so hover and screen readers aren't starved by the responsive hide. `role="status"` + `aria-live="polite"` announces transitions to assistive tech without interrupting in-flight speech — deliberately NOT `assertive` because this is informational, not an action the user must take RIGHT NOW. Returns `null` when online, so zero DOM footprint in the common case. **Mounted in `web/src/components/layout/app-shell.tsx`** as the **first** item in the header right-cluster (before user-name → InstallButton → Sign-out) so the inspector's eye lands on connection state before anything else — the SWR cache may be showing stale data and the pill is how they know. Placement inside the existing `flex items-center gap-3` means the 3-unit gap already absorbs the pill's appear/disappear without layout shift for neighbours. **No "back online" confirmation toast** — Serwist's `reloadOnOnline: true` flag (set in `next.config.ts` since 7a) already triggers `window.location.reload()` when the browser fires `online` after being offline. By the time any toast would render, the page is already reloading — the toast would flash for a frame or two and disappear. The visible pill disappearing IS the confirmation. **`navigator.onLine` truthiness caveat** (documented in both files): `true` just means the device has a network interface, NOT that requests succeed. Captive-portal wifi, hotel DNS hijack, and ISP blocks all look "online" to the browser. That's why the UI uses this hook for the visible pill ONLY — actual retry logic should be driven by failed fetches (the SWR paths in `2d3527f`'s dashboard/job-layout), not `onLine` transitions. **Scope exclusions still deferred** (from PHASE_7A_HANDOFF.md §"Scope exclusions"): no iOS "Add to Home Screen" hint on `/settings` (next 7b commit — Safari never fires `beforeinstallprompt` so a dismissible banner on the settings hub is the planned accommodation), no outbox / mutation queue (7c), no offline job-edit UI polish (7d), no Sentry. **Verification**: `npm run typecheck` clean, `npm run lint` at 0 errors / 6 warnings baseline (unchanged from `2d3527f`), `npm run build --webpack` succeeds. Manual DevTools check (Network → Offline → switch off): amber "Offline" pill appears in header; switch on → pill disappears + Serwist `reloadOnOnline: true` reloads the page. Tested at 375px (iPhone mini) and 390px (iPhone 14) — icon-only rendering keeps Sign-out visible; at `sm` (640px+) the "Offline" label surfaces. | web/src/lib/pwa/use-online-status.ts, web/src/components/pwa/offline-indicator.tsx, web/src/components/layout/app-shell.tsx, CLAUDE.md |
| 2026-04-17 | Web rebuild Phase 7b — IDB read-through cache for jobs (offline dashboard + offline job detail). Follow-up to the 7b kickoff (`ce8323a`); closes the biggest scope item in `PHASE_7A_HANDOFF.md` §"Scope exclusions" — "No IDB read-through cache — offline jobs will 404 to `/offline`. That's 7b." Adds stale-while-revalidate reads at the two top-level fetch sites (dashboard job list, job detail layout) backed by a new IDB database at `certmate-cache` so a previously-visited dashboard + any previously-opened job now render from local cache when the network is unreachable (subway, basement CCU cupboard, flaky mobile data, backend outage) instead of falling through to the branded `/offline` shell. **`web/src/lib/pwa/job-cache.ts`** is a vanilla IDB helper — no `idb` package dependency because the surface is tiny (2 stores × 5 operations) and 7c's outbox is the first commit with a real case for a richer wrapper. Database name `certmate-cache`, version 1, two object stores: `jobs-list` (keyPath `userId`, one record per user holding `{userId, jobs: Job[], cachedAt}`) and `job-detail` (keyPath `key`, composite `${userId}:${jobId}` so a single store scales to all users × jobs without an explosion of per-user stores as tenants grow). Module-scoped `dbPromise` caches the open handle for the tab lifetime; on rejection it nulls out so the next call retries instead of being permanently stuck. `onblocked` (concurrent schema upgrade in another tab) surfaces as a reject rather than hanging forever — callers fall back to network-only. **Six exports** — `getCachedJobs(userId)` / `putCachedJobs(userId, jobs)` for the list, `getCachedJob(userId, jobId)` / `putCachedJob(userId, jobId, detail)` for a single job, `clearJobCache()` wipes both stores on sign-out. Every export is SSR-safe via `typeof indexedDB === 'undefined'` early-return so callers can `await` without branching on environment. Errors inside transactions are **swallowed to `null`/`void`** and logged to console — the cache is a best-effort optimisation; a failed read must never break the page because the network fetch is always also in-flight. `wrapRequest<T>` + `wrapTransaction` convert the IDB event API into promises so call sites stay linear (four-deep callback chains would be unreadable). **Dashboard (`web/src/app/dashboard/page.tsx`)** now runs cache-read + network-fetch in parallel. `getCachedJobs(user.id)` fires immediately; if it resolves with data BEFORE `jobs` has been set (the `jobs === null` guard prevents a late cache resolve clobbering fresh network data), it paints the shimmer-replaced list and flips a local `hadCache` flag. The network `.then` always overwrites and fire-and-forget `void putCachedJobs(...)` writes back — ignoring the promise deliberately, the UI already has the fresh data and a cache write failure is logged inside the helper. **Error handling diverges from 7a**: if the network fetch fails but we already painted from cache, we **don't** surface the error banner. The inspector can still browse their cached jobs; the forthcoming AppShell offline indicator (separate 7b commit) will communicate staleness. Only show an error when there's nothing to paint at all. **Job layout (`web/src/app/job/[id]/layout.tsx`)** mirrors the same pattern for `api.job(userId, jobId)`. A cached paint means `<JobProvider>` mounts with realistic data so the inspector can start reviewing observations/circuits/etc. while the network catches up — and the fresh fetch's `setJob` triggers `JobProvider`'s `useEffect([initial])` which resets `isDirty=false` cleanly, so local edits between cache-paint and network-hit aren't silently lost (they just get replaced by the server snapshot, same contract as today). Both sites intentionally omit `job`/`jobs` from the effect deps array with an `eslint-disable` comment explaining why — including them would cause a fetch loop since every `setJob(fresh)` would retrigger the effect. **Shared-device security (`web/src/lib/auth.ts`)**: `clearAuth()` now fires `void clearJobCache()` after localStorage + cookie wipe. Jobs can contain the inspector's notes, site address, and observations text, so if user A signs out and user B signs in, user B must not be able to render A's jobs offline. Fire-and-forget because the caller is about to `router.replace('/login')` — the tab stays alive long enough for the readwrite transaction to commit before navigation. We don't `indexedDB.deleteDatabase()` (would force a schema-upgrade dance on next open and can block under concurrent tabs); a `.clear()` per store in one transaction is faster and safe. **Not partitioned by user at DB level** — one shared DB, records keyed by userId — because a hostile browser profile can read any origin-scoped IDB regardless of partitioning, so per-user DBs wouldn't add meaningful protection; `clearAuth()` is the only durable wipe. **Why vanilla IDB, not React Query's persister**: the app doesn't use React Query despite it being installed — data-fetching is imperative `useEffect` + state throughout Phases 3–6. Introducing RQ here would balloon the diff across every page that reads a job and obscure the actual SWR change. If 7c/7d want to migrate, that's a separate refactor. **Why no update-after-save wiring**: grepped for `api.saveJob` — it's defined in api-client but never called from the UI (Phase 4 shipped the recording/extraction path but stopped short of the debounced save flush). Building a `putCachedJob`-on-save hook now would be speculative code for a hypothetical future caller per CLAUDE.md §"Don't design for hypothetical future requirements"; when saves land, the caller wires `putCachedJob` in the same commit. **Scope exclusions still deferred** (from PHASE_7A_HANDOFF.md §"Scope exclusions"): no AppShell offline indicator (`navigator.onLine` + retry-on-online UX — next 7b commit), no iOS "Add to Home Screen" hint on `/settings` (Safari doesn't fire `beforeinstallprompt` — next 7b commit), no outbox / mutation queue (7c), no offline job-edit UI polish (7d), no Sentry. **Verification**: `npm run typecheck` clean (main + SW), `npm run lint` at baseline 0 errors / 6 warnings (unchanged from `ce8323a`), `npm run build --webpack` succeeds — all routes still prerender, Serwist bundler still emits `public/sw.js`. Manual DevTools verification steps (Application → IndexedDB → `certmate-cache`): visit `/dashboard` → expect one row in `jobs-list` keyed by userId; open any job → expect one row in `job-detail` keyed `{userId}:{jobId}`; Network → Offline → reload `/dashboard` → list still renders from cache (no `/offline` fallback); sign out → both stores empty. | web/src/lib/pwa/job-cache.ts, web/src/app/dashboard/page.tsx, web/src/app/job/[id]/layout.tsx, web/src/lib/auth.ts, CLAUDE.md |
| 2026-04-17 | Web rebuild Phase 7b kickoff — user-initiated service-worker update handoff. Mandatory first commit of Phase 7b per `web/PHASE_7A_HANDOFF.md` §"Kickoff checklist for 7b" — replaces 7a's `skipWaiting: true` with a postMessage-driven prompt BEFORE any other 7b feature lands, because the second-ever prod deploy would otherwise hot-swap the bundle under an active inspector mid-edit (the old tab's JS chunks would 404 as soon as the new SW took over). Flow: new deploy ships `/sw.js` → browser installs it, it sits in `waiting` (old SW still controls the page) → `SwUpdateProvider` detects it and raises a sonner toast "New version available — Reload" (persistent, `duration: Infinity`, dismissible) → user taps Reload → `registration.waiting.postMessage({ type: 'SKIP_WAITING' })` → new `message` listener in `sw.ts` calls `self.skipWaiting()` → SW activates → `clientsClaim: true` (retained from 7a) claims the open tab → browser fires `controllerchange` → provider calls `window.location.reload()` ONCE (guarded by `reloadedRef` so a spec-noncompliant double event can't double-reload during a save). **Why not call `reload()` immediately on click** — reloading before the new SW has activated would just re-serve the old SW to the fresh page and the toast would re-appear; waiting on `controllerchange` guarantees the matching client bundle loads. **Detection covers two races**: (A) page-load scan via `navigator.serviceWorker.getRegistration()` — catches the case where the user closed the tab before the previous deploy's SW finished installing and has now reopened it with a worker already in `waiting`; (B) live `updatefound` → `statechange: installed` while `navigator.serviceWorker.controller` is non-null — catches in-session upgrades. The `controller != null` guard is the first-install distinguisher — a fresh SW landing for a user who's never had one should NOT show a "new version" toast, so the prompt only fires when there was already a previous controller. `toastShownRef` dedupes in case both paths observe the same worker (registration resolves after `updatefound` already fired). `'serviceWorker' in navigator` early-return keeps the provider inert on older Safari and privacy-mode Firefox; Serwist is already `disable`'d in dev (see `next.config.ts`) so this effectively only runs in prod builds. **Toast placement**: `<Toaster position="bottom-right" theme="dark" richColors closeButton />` mounted in root `layout.tsx` (bottom-right clears both the mobile AppShell bottom nav AND the floating `RecordingOverlay` mini-pill that Phase 4 introduced; a bottom-centre or bottom-left toast would overlap). `richColors` so any future `toast.success/error/warning` calls pick up semantic accent without per-call styling — the bare `toast('New version available', ...)` in `SwUpdateProvider` deliberately uses the default dark theme since "new version" is neither success nor warning. `theme="dark"` pins to the app's dark palette (layout already forces `<html class="dark">`). **sw.ts changes**: removed `skipWaiting: true` from `new Serwist({...})` (comment expanded to explain why), kept `clientsClaim: true` / `navigationPreload: true`, appended `self.addEventListener('message', ...)` that calls `void self.skipWaiting()` only when `event.data?.type === 'SKIP_WAITING'`. Listening directly (rather than relying on Serwist's own event handling) keeps the contract explicit: the worker skips waiting because a logged-in client asked for it, never on its own schedule. **eslint config fix** — `public/sw.js` (the Serwist-bundled output) was being picked up by eslint and threw 1 error + 106 warnings on third-party bundler code; `.gitignore` already excluded it from git but eslint's `globalIgnores` didn't. Added `public/sw.js`, `public/sw.js.map`, and defensive `swe-worker-*.js` / `workbox-*.js` glob patterns (same shape as `.gitignore`'s coverage) so lint stays at the documented baseline of 0 errors / 6 warnings. Pre-existing issue — the 7a lint run happened before any Serwist bundle had been emitted to `public/`. **Why this is a separate commit and not bundled with any 7b feature**: the handoff mandates `skipWaiting` gets replaced "BEFORE any other 7b work lands" — reviewers can assess the update-handoff mechanism in isolation, and if we shipped IDB read-through in the same commit, the diff would be too large to verify the SW lifecycle change is correct. Verified: `npm run typecheck` (main + `tsconfig.sw.json`) clean, `npm run lint` back to 0 errors / 6 warnings baseline, `npm run build --webpack` emits `public/sw.js` with the `SKIP_WAITING` handler bundled (grep confirmed 2 occurrences), all 7a routes still listed (`/manifest.webmanifest` static, `/offline` static, error boundary wired). **Scope exclusions** (from PHASE_7A_HANDOFF.md §"Scope exclusions", still deferred): no IDB read-through / offline job list (next 7b commit), no offline indicator in AppShell, no iOS "Add to Home Screen" hint on `/settings`, no outbox / mutation queue (7c), no offline job-edit UI (7d), no Sentry. Only the kickoff checklist is closed in this commit. | web/src/app/sw.ts, web/src/app/layout.tsx, web/src/components/pwa/sw-update-provider.tsx, web/eslint.config.mjs, CLAUDE.md |
| 2026-04-17 | Web rebuild Phase 7a — PWA foundation. Single focused commit that makes the web client installable, version-skew-safe, and resilient to mid-session network drops, without adding any offline read-through or outbox plumbing (those are deliberately deferred to 7b/7c/7d). Installed `@serwist/next@^9.5.7` + `serwist@^9.5.7`; `next.config.ts` wrapped with `withSerwist({ swSrc: 'src/app/sw.ts', swDest: 'public/sw.js', cacheOnNavigation: false, reloadOnOnline: true, disable: NODE_ENV === 'development' })` — `cacheOnNavigation: false` because the SW drives navigation caching itself with stricter rules than Serwist's default. `web/src/app/manifest.ts` is a typed `MetadataRoute.Manifest` export (served at `/manifest.webmanifest`): `start_url: '/dashboard'`, `scope: '/'`, `display: 'standalone'`, `id: '/'` for stable install identity, `theme_color` + `background_color` both `#0a0a0a` (surface-0 token) — aligned with the iOS status-bar tint, Android task-switcher card, and the app's top edge. Icons: geometric SVG masters at `web/scripts/icons/` (rounded-22% brand-blue gradient w/ "CM" glyph rendered as paths, not `<text>`, so build hosts can't drift on font availability) rasterized by `npm run pwa:icons` (sharp-based `scripts/generate-pwa-icons.mjs`) into `public/icons/icon-{192,384,512}.png`, `public/icons/icon-maskable-512.png` (70% safe zone inside full-bleed background for Android adaptive-icon crop), `public/apple-icon-180.png` (**flattened onto `#0066FF` — iOS rejects transparency**), and `public/favicon-32.png` + `public/favicon.svg`. **Service worker `web/src/app/sw.ts`** is authored (not auto-generated) so we control every route. Five runtime-cache rules in priority order: **(1) NetworkOnly** for `/_next/app/*`, `Next-Action` header, and RSC flight payloads (detected via `RSC: 1` header OR `?_rsc=` query) — **never cached**, because a stale flight payload is the #1 cause of mid-session "Failed to find Server Action" errors after a deploy. **(2) StaleWhileRevalidate** for `/_next/static/*` (hash-named, immutable) into `static-<BUILD_ID>` with `ExpirationPlugin` (maxEntries 200, maxAge 30d). **(3) CacheFirst** for fonts (`.woff2/.woff/.ttf/.otf`). **(4) CacheFirst** for `/icons/*`, `/apple-icon*`, `/favicon*`. **(5) NetworkFirst** for public navigations **only** (`/`, `/login`, `/legal*`, `/offline`) with 3s timeout and `/offline` fallback. Auth-gated pages (`/dashboard`, `/job/*`, `/settings/*`) deliberately fall through to default NetworkOnly — a shared device must never replay one user's HTML for another. Build-ID-versioned cache names read `process.env.NEXT_PUBLIC_BUILD_ID` (webpack inlines at build); fallback `local-${Date.now()}` for local prod-builds. Custom `activate` listener purges any cache whose name doesn't end in the current BUILD_ID, preserving `serwist-*` metadata caches. `skipWaiting: true` + `clientsClaim: true` + `navigationPreload: true` — skipWaiting is **safe for the first deploy only** (no prior SW in prod); Phase 7b MUST replace with postMessage-driven skipWaiting + "New version available" toast **before any other 7b work lands** (see PHASE_7A_HANDOFF.md §"Kickoff checklist for 7b"). `web/src/app/offline/page.tsx` is a branded fallback reusing the login page's `cm-orb` + `cm-glass` aesthetic; copy is deliberately truthful ("Reconnect to continue") — "your work will sync automatically" is aspirational and belongs in 7c. `web/src/app/error.tsx` is the root error boundary: if the error matches `'Failed to find Server Action'` OR `digest?.includes('NEXT_SERVER_ACTION')`, auto-reloads once — but with a 30s `sessionStorage` timestamp guard so a consistently-failing action doesn't pin the user in a reload loop. After 30s the fallback card renders instead of reloading again. Logs `error.digest` to console (quotable in bug reports). `web/src/lib/pwa/install-store.ts` is a Zustand slice holding the deferred `BeforeInstallPromptEvent`; the event type interface is declared here because lib.dom doesn't include it. `InstallPromptProvider` (renders null) is mounted at root layout so Chrome's `beforeinstallprompt` is captured **even on `/login`** — mounting it in AppShell would miss the event for users who land on login, sign in, and cruise straight to the dashboard. `preventDefault()` suppresses Chrome's built-in banner; we prefer a low-key ghost/sm `<InstallButton />` in the AppShell header (between user-name span and Sign-out). Button returns null on Safari (never fires the event), once installed, or before the event arrives. Click handler awaits `userChoice` before clearing the store so the button doesn't flicker mid-dialog. `web/src/middleware.ts` gained a CLAUDE.md-mandated guardrail: every non-static page response now carries `Cache-Control: no-cache, no-store, must-revalidate` — forces the browser HTTP cache to always revalidate HTML so the client bundle can never outlive its matching server routes. Static files (dot in path) and `/_next/*` are already early-returned. Independent of the SW's caching. `viewport.themeColor` in `layout.tsx` changed from `#0A0A0F` → `#0a0a0a` (align with manifest + surface-0 token); prevents a visible 1px tint mismatch between the iOS status bar (viewport) and standalone chrome (manifest). `metadata.icons` added so Next emits `<link rel="icon">` + `<link rel="apple-touch-icon">` pointing at the generated PNG/SVG files (explicit URLs, not relying on `app/icon.tsx` conventions, so raster assets live in `public/` with the SW's CacheFirst `icons` rule applied). **TypeScript gotcha**: `sw.ts` uses `lib: ["webworker"]` which overrides `WorkerNavigator` onto `navigator` and breaks `mic-capture.ts`. Fixed with `tsconfig.sw.json` (dedicated SW config) + `src/app/sw.ts` added to the main `tsconfig.json` exclude. Minimal inline `declare const process` shim in sw.ts so webpack's build-time `NEXT_PUBLIC_BUILD_ID` replacement typechecks without pulling `@types/node` into the worker build. `build` script switched from `next build` to `next build --webpack`: Next 16 defaults to Turbopack but Serwist doesn't support it yet (see serwist/serwist#54); webpack build is still a first-class Next target with zero regression. `.gitignore` now covers `/public/sw.js`, `/public/sw.js.map`, `/public/swe-worker-*.js(.map)`, `/public/workbox-*.js(.map)` (Serwist 9 bundles to a single sw.js but the workbox/swe-worker patterns are defensive for future Serwist versions). Verified clean: `npm run typecheck` (main + SW) passes, `npm run lint` at baseline (0 errors, 6 warnings — unchanged from `bc11914`), `npm run build --webpack` succeeds with Serwist bundler log confirming sw.js emission and all 7a routes (`/manifest.webmanifest` static, `/offline` static, error boundary wired). **Scope exclusions** (all in PHASE_7A_HANDOFF.md §"Scope exclusions"): no IDB read-through (7b), no outbox/mutation queue (7c), no offline job-edit UI (7d), no "New version available" toast (7b first-commit), no iOS ATHS hint (7b), no push/periodic-sync/share-target, no offline indicator in AppShell (7b), no Sentry. | web/next.config.ts, web/package.json, web/.gitignore, web/tsconfig.json, web/tsconfig.sw.json, web/scripts/icons/*.svg, web/scripts/generate-pwa-icons.mjs, web/public/icons/*.png, web/public/apple-icon-180.png, web/public/favicon-32.png, web/public/favicon.svg, web/src/app/manifest.ts, web/src/app/sw.ts, web/src/app/offline/page.tsx, web/src/app/error.tsx, web/src/app/layout.tsx, web/src/middleware.ts, web/src/lib/pwa/install-store.ts, web/src/components/pwa/install-prompt-provider.tsx, web/src/components/pwa/install-button.tsx, web/src/components/layout/app-shell.tsx, web/PHASE_7A_HANDOFF.md, CLAUDE.md |
| 2026-04-17 | Web rebuild Phase 6c — system-admin user management. Final sub-phase of Phase 6; ports iOS `AdminUsersListView` / `AdminEditUserView` / `AdminCreateUserView`. New sub-tree at `/settings/admin/users` gated by the JWT `role` check in `middleware.ts` (already in place since 6a) plus a belt-and-braces `isSystemAdmin(user)` client guard per page that renders a friendly "Not authorised" state rather than a 403 flash — same pattern as the 6b dashboard. No backend changes: all five endpoints (`GET /api/admin/users`, `POST`, `PUT /:userId`, `POST /:userId/reset-password`, `POST /:userId/unlock`) already live behind `requireAuth + requireAdmin` at the mount point in `src/api.js`. API client gained five methods (`adminListUsers`, `adminCreateUser`, `adminUpdateUser`, `adminResetPassword`, `adminUnlockUser`) — `adminListUsers` always sends limit+offset so the backend returns the `Paginated<AdminUser>` envelope, matching the `companyJobs` pattern from 6b (backend falls back to a bare array if no pagination params are present). `AdminUser` type was already declared in 6a pre-work so types.ts didn't change. `web/src/app/settings/admin/users/page.tsx` is the paginated list (50/page) — cards with avatar initial, name/email, last-seen hint, and a cluster of pills (Admin / User system role, company-role if non-employee, Inactive if `!is_active`, Locked if `locked_until > now`). Lockout comparison reads `nowMs` from a `useState(() => Date.now())` initializer — React's `react-hooks/purity` rule disallows calling `Date.now()` directly in render, and the lazy state initializer is the sanctioned escape hatch (the value is stable per mount, and the list re-fetches on `window.focus` so an expired lockout clears within one refresh cycle, which is plenty of freshness). Rows are whole-card `<Link>`s to `/settings/admin/users/[userId]`; no inline actions, which match iOS and avoid mis-tap hazards on narrow rows. Top-right "+ New user" button mirrors the Staff list pattern. `web/src/app/settings/admin/users/new/page.tsx` is the create form: three SectionCards (Account / Roles / Company) with FloatingLabelInputs for name, email, password, company_name, company_id; a local `LabelledSelect` primitive (14px label + native `<select>` — built inline rather than adding a new UI primitive per handoff §"Components") for system-role and company-role. Password client-validation shows an inline "At least 8 characters required" hint in error-red below the field so the admin doesn't need to wait for a 400 round-trip; 409 duplicate-email surfaces as "A user with this email already exists." matching the 6b invite sheet. Unlike `inviteEmployee` (which generates the password server-side and shows it once), the admin chooses the password here — same "hand off out-of-band" model since the backend doesn't send email. Company-ID is a free-form UUID field with an "advanced" hint because the handoff §"Scope exclusions" explicitly defers a company picker. `web/src/app/settings/admin/users/[userId]/page.tsx` is the edit + reset + unlock page. Loads the row by scanning paginated pages until found (capped at 20 pages / 1000 users — well beyond any single tenant; backend has no `GET /api/admin/users/:id` and 50-per-page is plenty). Self-edit guard: if `userId === currentUser.id`, the System Role select and Account-active toggle are disabled with a hint banner "You can't demote or deactivate yourself." — the backend enforces both at `admin-users.js:129-135` and 400s anyway, but the client-side disable avoids a round-trip error and signals the intent. Save-patch conditionally omits `role` + `is_active` when editing self so we never even send values that would bounce. If the admin edits their OWN row, we call `refreshCurrent()` on success so `useCurrentUser` re-hydrates and the settings hub header re-renders with the new name/email. Reset-password is a modal sheet — copy of the 6b `InviteEmployeeSheet` pattern, two-phase (form → success state) with an info line "All existing sessions for this user have been signed out." (backend `incrementTokenVersion` invalidates live JWTs at `admin-users.js:180`). NO copy-to-clipboard reveal — the admin typed the password, they don't need it read back. Unlock is inline on the Security SectionCard, rendered only when `locked_until && new Date(locked_until) > nowMs` (same `useState` trick); fires `window.confirm('Unlock this account?')` → POST → `load()` refresh, matching the iOS "confirm then toast" flow without adding a toast infra. Reset + Unlock are separate endpoints because bundling them into the generic PUT would force typing a password just to flip `is_active`. **No delete button** — backend has no delete endpoint and iOS doesn't surface one either; `is_active: false` is the documented soft-delete. Settings hub (`/settings/page.tsx`) drops the `disabled` + `disabledLabel="Coming in Phase 6c"` props on the Manage Users LinkCard to go live. Build verified clean: `next build` lists all three new routes (`/settings/admin/users` static, `/settings/admin/users/new` static, `/settings/admin/users/[userId]` dynamic); `tsc --noEmit` clean; lint at baseline (0 errors, 6 pre-existing warnings — unchanged from `6e85e9e`). **Scope exclusions** (from handoff §"Scope exclusions"): no delete UI, no audit-log surfacing (backend logs every admin action via `logAction` at `admin-users.js:98/140/182/213` but there's no read endpoint — deferred to Phase 7+), no company-picker UI (raw UUID field only), no bulk actions, no email notifications, no failed-login-attempts counter display (Locked badge is enough signal). Phase 6 now closed. | web/src/lib/api-client.ts, web/src/app/settings/admin/users/page.tsx, web/src/app/settings/admin/users/new/page.tsx, web/src/app/settings/admin/users/[userId]/page.tsx, web/src/app/settings/page.tsx, CLAUDE.md |
| 2026-04-17 | Web rebuild Phase 6b — company settings + company-admin dashboard. Ships two new routes: `/settings/company` (branding details form, visible to any authenticated user but read-only for non-admins so inspectors can verify the stamp on their certs) and `/settings/company/dashboard` (3-tab Jobs/Team/Stats view, company-admin only). Backend: added **two** new routes to `src/routes/settings.js` — `POST /api/settings/:userId/logo` (multipart, field `logo`, 10MB, PNG/JPEG, rejects SVG to avoid embedded-script risk when the PDF generator inlines the image) and `GET /api/settings/:userId/logo/:filename` (auth'd read with basename path-traversal guard and `Cache-Control: private, max-age=300`). Route pattern deliberately mirrors the signature upload/download pair — two-step save (upload bytes → get S3 key → merge onto `company_settings.logo_file` → PUT settings blob) because inlining base64 in the settings JSON would bloat reads by ~200KB per cert-generation and bust cache on every save. Types: added `CompanyMember`, `CompanyJobRow`, `CompanyStats`, `InviteEmployeeResponse`, `Paginated<T>` to `web/src/lib/types.ts` — `Paginated<T>` is generic so the same envelope shape can be reused when admin surfaces need pagination in 6c. API client gained five methods: `companySettings` / `updateCompanySettings` (full-blob PUT, no server-side merge), `companyUsers`, `companyJobs` (always sends limit+offset so response shape is consistent), `companyStats`, `inviteEmployee` (returns the plaintext `temporaryPassword` the admin must copy once), `uploadCompanyLogo`, `fetchLogoBlob` (same split-key → basename trick as signatures). New `web/src/components/settings/logo-uploader.tsx` handles the image picker + auth'd blob preview — the `<LogoPreview>` subcomponent uses the same `URL.createObjectURL` + revoke-on-unmount lifecycle as `ObservationPhoto`. Preview box is **~200×60px with white background** to mirror the actual PDF header slot so admins see a realistic rendering. Remove button clears the in-memory key via `onUploaded(null)` but does NOT delete the S3 object — orphans are cheap and there's no delete endpoint for logos (adding one is scope creep). `web/src/app/settings/company/page.tsx` is 4 SectionCards (Branding / Address & Contact / Registration + save bar); fields all disabled for non-admins but still visible — pure hiding would force inspectors to switch to the iOS app just to check the registration number before producing a cert. `web/src/app/settings/company/dashboard/page.tsx` wraps 3 tabs in the existing `SegmentedControl` primitive: **Jobs** (paginated 50/page — client always sends limit+offset so the backend consistently returns the `Paginated<T>` envelope; backend would return a bare array if we passed neither), **Team** (invite sheet with two-phase UI — form first, then success state with copy-to-clipboard password and "only shown once" warning; on close, `setResult(null)` explicitly nulls the temp password so it never outlives the modal), **Stats** (3-card count grid + jobs-by-status breakdown, no charts per handoff §"Scope exclusions"). Dashboard page applies a **triple-layer role gate**: middleware JWT check (blocks system-admin-only paths), belt-and-braces `isCompanyAdmin(user)` client guard with a friendly "Not authorised" state rather than a bare 403 flash, and a defensive "No company linked" state for system admins without a `company_id` so `stats` / `users` / `jobs` don't blast requests that would 404. The `InviteEmployeeSheet` treats the returned `temporaryPassword` as secret-adjacent PII — never logged, never persisted, cleared on close, rendered in a `<code>` block with a copy button that uses `navigator.clipboard` (silently falls back to on-screen copy if the clipboard API is blocked). Hub page (`/settings`) updated: Company section now renders live for every user (not just admins) with a contextual subtitle ("branding…and logo" vs "branding and contact info (view-only)"); the admin-only `LayoutDashboard` link card sits below the Details card so the dashboard is one click away without cluttering the non-admin view. **Scope exclusions** (from handoff §"Scope exclusions"): no logo cropping / aspect-ratio picker, no trend charts, no email invitations, no audit log, no bulk actions — all deferred. | src/routes/settings.js, web/src/lib/types.ts, web/src/lib/api-client.ts, web/src/components/settings/logo-uploader.tsx, web/src/app/settings/page.tsx, web/src/app/settings/company/page.tsx, web/src/app/settings/company/dashboard/page.tsx, CLAUDE.md |
| 2026-04-17 | Fix `next build` failure on `/login`. The page is a client component that calls `useSearchParams()` at the top level to read the `?redirect=` query param. Next.js App Router refuses to statically ship a page using `useSearchParams()` unless the hook is wrapped in a `<Suspense>` boundary — without one, the entire route is forced into client rendering and the build bails out with "should be wrapped in a suspense boundary". Split `LoginPage` into a presentational shell (default export) and a `LoginForm` inner component that actually calls the hook; wrapped `LoginForm` in `<Suspense fallback={<LoginCard disabled />}>`. Next now pre-renders the shell (orbs, card chrome, logo, heading) at build time and streams the form in on the client — `/login` moved from the "forced dynamic, build-breaking" bucket to the statically-prerendered bucket (confirmed in the `next build` route table: `○ /login`). Fallback is the same card with disabled inputs and a disabled button so there's zero layout shift while the search params resolve (effectively instant). Chose this over `export const dynamic = 'force-dynamic'` (loses static benefit for the most-hit page) or reading `window.location.search` in a `useEffect` (clunkier, and breaks SSR-safety assumptions the rest of the codebase leans on). Orbs extracted into `AmbientOrbs`, card into `LoginCard` — both reused by the fallback so the refactor didn't duplicate markup. | web/src/app/login/page.tsx, CLAUDE.md |
| 2026-04-17 | Web rebuild Phase 6a — `/settings` hub + Staff (inspector profiles + signatures). Pre-work: extended `User` with `company_id` + `company_role`, added `InspectorProfile` / `CompanySettings` / `AdminUser` types so the remaining 6b/6c commits can land without churning `types.ts` again. New `web/src/lib/roles.ts` exports `isSystemAdmin` / `isCompanyAdmin` — the entire settings tree calls these instead of inlining `user?.role === 'admin'`, so when the role model evolves (e.g. per-tenant scopes) it's a one-file change. New `web/src/lib/use-current-user.ts` hydrates from `getUser()` for instant first paint then revalidates via `api.me()` — necessary because the `cm_user` localStorage snapshot is frozen at login and a mid-session demotion would otherwise still render admin chrome. Middleware now decodes the JWT payload and redirects non-admins away from `/settings/admin/*`; component-level guards (Phase 6c) are belt-and-braces — the JWT `role` claim is signed by the backend so middleware is the first-line defence without a server round-trip. Backend: added `GET /api/settings/:userId/signatures/:filename` (mirrors the photo download pattern) because the upload route returns an S3 key but there was no auth'd read endpoint — browsers can't attach the bearer header to a bare S3 URL. Guards against path-traversal with a basename check; sets `Cache-Control: private, max-age=300` since signatures are PII. API client gained four methods: `inspectorProfiles`, `updateInspectorProfiles` (full-array PUT — backend has no per-profile endpoint, last-writer-wins matches iOS), `uploadSignature` (multipart field name `signature` to match multer config; wraps raw Blobs in a File so multer writes a sensible on-disk name), and `fetchSignatureBlob` (splits the stored S3 key to just the basename — iOS stores the full key on the profile). New `web/src/components/settings/signature-canvas.tsx` is an HTML5 canvas with Pointer Events, DPR-aware sizing, and quadratic-curve smoothing that mirrors the iOS `SignatureCaptureView` stroke style exactly (2.5px black ink, rounded joins). Imperative handle (`getBlob` / `clear` / `hasContent`) keeps the parent form in control of when uploads fire — we don't auto-POST on every stroke (iOS doesn't either). If an `initialSignatureFile` is passed, the canvas loads it as a background image via the auth'd blob fetch + `URL.createObjectURL` (revoked on unmount) so strokes drawn on top produce a merged PNG on export. Routes: `web/src/app/settings/layout.tsx` reuses `AppShell` (no sub-nav yet — settings tree is shallow), `web/src/app/settings/page.tsx` is the hub with a hero avatar + role pills and role-gated link cards; Company/Admin cards render conditionally even though they route to placeholders ("Coming in Phase 6b/6c") so the gating logic is correct from day one and 6b/6c only need to flip a prop. `web/src/app/settings/staff/page.tsx` ports `InspectorListView` — gradient hero with stacked avatars, card list with inline trash icon + confirm modal (swipe-to-delete is an iOS gesture that doesn't translate; web idiom is explicit), empty-state with UserPlus icon. Refreshes on `window.focus` rather than subscribing to a pub-sub — settings mutations are rare enough. `web/src/app/settings/staff/[inspectorId]/page.tsx` is the shared add/edit form: `new` triggers a blank profile with `is_default: true` when it's the first-ever profile (so there's always a default); existing-id loads the profiles array, finds the row, and splits equipment into a collapsible section (auto-expanded if any slot is populated — matches iOS). Save flow is **two-step**: if the canvas has a fresh blob, upload first to get the S3 key, THEN PUT the full profiles array with the key merged in. Order matters — if we did the array save first and upload failed, the profile would point at a non-existent signature. Setting `is_default` on any profile unsets default on all others client-side (mutex) so the PUT body always reflects exactly one default. Sticky bottom save bar matches the rest of the rebuild's primary-action placement; settings is out of the recording tree so there's no z-index collision with `RecordingOverlay`. **Scope exclusions**: no billing/Stripe, no email invitations (backend doesn't send), no audit-log UI, no logo cropping — all deferred per the handoff §"Scope exclusions". | src/routes/settings.js, web/src/lib/types.ts, web/src/lib/roles.ts, web/src/lib/use-current-user.ts, web/src/lib/api-client.ts, web/src/middleware.ts, web/src/components/settings/signature-canvas.tsx, web/src/app/settings/layout.tsx, web/src/app/settings/page.tsx, web/src/app/settings/staff/page.tsx, web/src/app/settings/staff/[inspectorId]/page.tsx, CLAUDE.md |
| 2026-04-17 | Web rebuild Phase 5d — LiveFillView. Ports iOS `LiveFillView.swift` so the inspector sees every Sonnet-extracted field populate in real time with a brand-blue flash that fades to transparent over 2s. New `web/src/lib/recording/live-fill-state.ts` is a module-level singleton `LiveFillStore` (Map<fieldKey, timestamp>) subscribed via `useSyncExternalStore`. The two hooks it exports — `useLiveFillStore` (snapshot with `lastUpdatedSection` / `lastUpdatedAt` for auto-scroll) and `useIsFieldRecent(key)` (per-field boolean) — give leaves independent subscriptions so a single `markUpdated` call only re-renders the fields whose recency actually flipped. A self-scheduling 3050ms cleanup sweep prunes stale keys (+50ms headroom over the 3s window) and re-emits so the flashing stops; bundling pruning into one delayed sweep avoids a per-key timer storm when large extractions land. Extended `applyExtractionToJob` to return `{patch, changedKeys}` — changedKeys is computed by diffing the pre-patch job against the patch (not derived from the readings array, which would drift if the extractor adds new fields). Scalar sections emit `section.field` keys, circuit cells emit `circuit.{id}.field`, newly-created circuit rows emit both `circuit.{id}` (whole-row flash) and one key per filled cell, new observations emit `observation.{id}`. `hasValue` guard suppresses flashes on no-op reassignments. New `web/src/components/live-fill/live-field.tsx` is a CSS-only primitive (two variants: `LiveField` for scalars, `LiveFieldWide` for long strings) that toggles `data-recent="true|false"` — the CSS `transition: background-color 2s ease-out` re-triggers automatically, no keyframes. `@media (prefers-reduced-motion: reduce)` shortens to a 200ms fade. Empty values render `—` so cells keep layout (spatial-memory parity w/ iOS). New `web/src/components/live-fill/live-fill-view.tsx` renders the full form in iOS section order (Installation → Extent only-if-EIC → Supply → Board → Circuits → Observations only-if-EICR) with `data-section` attributes targeted by a `scrollIntoView` `useEffect` that fires off the `lastUpdatedAt` trigger (so the same section updating twice still scrolls). Section entrance stagger (`cm-live-section` + nth-child delays) in globals.css keeps the initial render calm. Mount site is **Option A** from the handoff: a new `<JobBody>` component in `web/src/app/job/[id]/layout.tsx` reads `useRecording().state` and swaps tab `{children}` for `<LiveFillView>` whenever recording state is `active`/`dozing`/`sleeping`. TranscriptBar + FloatingActionBar + RecordingOverlay stay mounted at their usual positions — no z-index juggling. `liveFill.reset()` fires on `start()` and `stop()` so previous sessions never bleed into a new one. `role="status"` + `aria-live="polite"` on the root so screen readers announce new content. **Scope exclusions** (from handoff §"Scope exclusions"): typing-per-character animation, CCU slot-correction grid, landscape-specific compact layout, per-observation fade-in. Defer until inspectors ask — the background flash already conveys the "just filled" signal. | web/src/lib/recording/live-fill-state.ts, web/src/components/live-fill/live-field.tsx, web/src/components/live-fill/live-fill-view.tsx, web/src/lib/recording/apply-extraction.ts, web/src/lib/recording-context.tsx, web/src/app/job/[id]/layout.tsx, web/src/app/globals.css, CLAUDE.md |
| 2026-04-17 | Web rebuild Phase 5c — observation photos. New `web/src/components/observations/observation-photo.tsx` fetches auth'd image bytes via `api.fetchPhotoBlob` → `URL.createObjectURL` → revokes on unmount (necessary because `<img src>` won't attach the bearer token the photo endpoint requires). New `web/src/components/observations/observation-sheet.tsx` is the add/edit modal — code chip row (C1/C2/C3/FI), location, description, remedial action, and a photo grid with **two buttons** (Camera + Library) matching iOS `EditObservationSheet.swift`. Camera input sets `capture="environment"` for iOS Safari rear-camera hint; Library input omits `capture` for the photo-library picker. Uploads fire immediately on file-select (eager — matches iOS so cancels don't need orphan cleanup; S3 lifecycle handles stragglers); delete also hits the backend before mutating local state so failures don't silently drop filenames. Renamed `ObservationRow.photo_keys` → `photos` for iOS parity (`Observation.photos` JSON key) — safe rename since the field was dormant (grep confirmed zero call-sites). Added three API methods: `uploadObservationPhoto` (multipart POST `/api/job/:userId/:jobId/photos` under field name "photo"), `deleteObservationPhoto` (DELETE), and `fetchPhotoBlob` (bespoke `fetch` — the shared `request` helper returns JSON/text only). Observations page Add button now enables a blank sheet; cards are clickable + keyboard-focusable to edit; inline preview shows up to 3 thumbnails + `+N` chip for overflow. Backend routes (`src/routes/photos.js` upload + delete) already live — **no backend changes**. Accepts HEIC directly via backend `IMAGE_MIMES`, so no client-side conversion on iOS. **Not ported:** bulk upload / drag-drop, photo annotations (iOS doesn't have them either). | web/src/lib/types.ts, web/src/lib/api-client.ts, web/src/components/observations/observation-photo.tsx, web/src/components/observations/observation-sheet.tsx, web/src/app/job/[id]/observations/page.tsx, CLAUDE.md |
| 2026-04-17 | Web rebuild Phase 5b — document extraction on the Circuits tab. New `web/src/lib/recording/apply-document-extraction.ts` folds `/api/analyze-document` responses onto a JobDetail patch across installation, supply, board, circuits, and observations with the **fill-empty-only** 3-tier priority guard applied everywhere (stricter than iOS `CertificateMerger` which overwrites on installation/supply/board). Circuits match by case-insensitive `circuit_ref`; unmatched extracted circuits append as new rows tagged with `board_id`. Observations dedupe by (`schedule_item` + `code`) OR (`location` + first-50-char lowercased text prefix), matching iOS `CertificateMerger.swift:100-122`. Exported `parseObservationCode` from apply-extraction.ts for reuse. New `api.analyzeDocument(photo)` posts multipart; Circuits rail Extract button now opens a library-first image picker (no `capture` hint — inspectors usually photograph docs ahead of time) and renders spinner + merge-summary hint + error banner on the same card-surface pattern as 5a. Images only for now: backend hard-codes `image/jpeg` data URL at `src/routes/extraction.js:1425`, so PDFs deferred to a follow-up that needs either pdfjs-dist client render or a backend JPEG conversion step. | web/src/lib/recording/apply-document-extraction.ts, web/src/lib/api-client.ts, web/src/lib/types.ts, web/src/app/job/[id]/circuits/page.tsx, web/src/lib/recording/apply-extraction.ts, CLAUDE.md |
| 2026-04-17 | Web rebuild Phase 5a — CCU photo capture + GPT Vision analysis on the Circuits tab. New `web/src/lib/recording/apply-ccu-analysis.ts` ports iOS `FuseboardAnalysisApplier.hardwareUpdate`: matches circuits by `circuit_ref`, merges OCPD/RCD hardware via a non-empty-only `mergeField`, preserves all test readings on matched circuits, and appends unmatched existing circuits that have readings so data is never lost. Board patch synthesises a main board if the job has none; SPD fields split between `board.spd_*` (device-level) and `supply.spd_*` (supply-section fallbacks derived from the main switch). RCD-type normalisation mirrors iOS (AC/A/B/F/S/A-S/B-S/B+ only); unresolved RCD-protected circuits auto-generate "What is the RCD type for circuit X?" inspector questions. New `api.analyzeCCU(photo)` posts multipart to `/api/analyze-ccu`. Circuits rail CCU button now opens a camera-first file picker (`capture="environment"` — iOS Safari rear-camera hint with library fallback), shows spinner + merge summary + dismissible question chips. Reuses `hasValue()` from apply-extraction as the shared 3-tier priority guard. | web/src/lib/recording/apply-ccu-analysis.ts, web/src/lib/api-client.ts, web/src/lib/types.ts, web/src/app/job/[id]/circuits/page.tsx, web/src/lib/recording/apply-extraction.ts, CLAUDE.md |
| 2026-04-17 | Start ground-up web frontend rebuild on branch `web-rebuild`. Archive previous `web/` to `_archive/web-legacy/`. Phase 0 complete: fresh Next.js 16 + React 19 + Tailwind 4 scaffold at `web/` with CertMateDesign tokens ported to CSS custom properties (brand #0066FF / #00CC66, surface L0–L4, SF Pro Rounded stack), base UI primitives (Button, Card, Logo), and Playwright visual-verification harness (`npm run verify` screenshots every route at mobile + desktop for side-by-side compare against iOS reference shots in `web/_reference/ios-screenshots/`). Lessons from `../transcript-standalone/` codified in README for Phase 4 (AudioWorklet + direct Nova-3 + resampled ring buffer + no proxy fallback). | web/*, _archive/web-legacy/*, CLAUDE.md |
| 2026-03-04 | Add /api/analyze-document endpoint: GPT Vision extracts all EICR/EIC fields from photos of previous certificates, handwritten notes, or typed test sheets. Returns { success, formData } envelope matching extract-transcript shape. iOS app gets new "Extract Doc" button in recording overlay bar and CircuitsTab. Supports camera, photo library, and file picker (images + PDFs). | src/routes/extraction.js, iOS: CircuitsTab.swift, JobDetailView.swift, RecordingOverlay.swift, JobViewModel.swift, APIClient.swift |
| 2026-02-28 | Fix extraction quality regression: raise COMPACTION_THRESHOLD 6000→60000 to effectively disable compaction for normal sessions. The 6000 threshold caused compaction to fire after ~15-20 utterances, replacing full conversation history with a dry summary — destroying Sonnet's ability to infer circuit assignment from recent conversational flow. With prompt caching (1h TTL, cache reads at 10% rate), full history costs ~$0.25-0.35/session. 60000 threshold preserves full context for all normal inspections. | eicr-extraction-session.js |
| 2026-02-23 | Fix compaction cost blowout: 5 guards on compact() (min messages, min tokens, no-new-turns, failure backoff, 120s rate limit), increase max_tokens 2048→4096, client-side 120s rate limit on session_compact handler | eicr-extraction-session.js, sonnet-stream.js, eicr-extraction-session.test.js |
| 2026-02-23 | Fix audio loss during VAD warm-up: remove premature ring buffer reset, add reconnect audio queue (5s cap), extract shared chunk handler, increase reconnect timeout to 5s, flush queued audio after reconnect. Fix server connection failures: add /api/health/ready readiness endpoint (DB/Deepgram/Anthropic checks), add iOS pre-flight connectivity check with NetworkMonitor + server health, dropped-audio logging in DeepgramService | SleepManager.swift, DeepgramRecordingViewModel.swift, DeepgramService.swift, APIClient.swift, api.js |
| 2026-02-23 | CCU extraction prompt v2: 4-step structured methodology (physical scan, label mapping, extraction, cross-check), RCD waveform type identification, device-face amp reading enforcement, questions-for-inspector TTS, ported to live /api/analyze-ccu endpoint, wired questions into batch pipeline | src/analyze_photos.js, src/routes/extraction.js, src/process_job.js |
| 2026-02-22 | 5-star Phase 8: OpenAPI spec, Swagger UI, pre-commit hooks, dev setup guide, 7 ADRs, CLAUDE.md cleanup | docs/api/openapi.yaml, src/api.js, .husky/, docs/DEVELOPER_SETUP.md, docs/adr/, CLAUDE.md |
| 2026-02-21 | Inspector profiles settings page: full CRUD (name, position, org, enrolment number, signature upload), Inspectors tab in settings nav | frontend/src/app/settings/inspectors/page.tsx, frontend/src/app/settings/layout.tsx |
| 2026-02-21 | Prompt injection guardrail: transcript delimiters + data-vs-instruction rule; remove incomplete-reading WAIT (Sonnet asks immediately, 2s TTS debounce); fix Dockerfile missing files | eicr-extraction-session.js, docker/backend.Dockerfile |
| 2026-02-20 | Web iOS feature parity: live Deepgram streaming, Sonnet extraction, sleep/wake, transcript highlighting, alert TTS, LiveFillView, CCU upload, recording controls -- full pipeline port from iOS to Next.js PWA | frontend/src/lib/recording/*.ts, frontend/src/components/recording/*.tsx |
| 2026-02-20 | CCU photo analysis: revert to GPT-5.2 (Gemini 3 Pro truncating at ~146 tokens), keep v3 prompt, add finishReason guard | api.js |
| 2026-02-20 | Remove silence check (redundant with inline questionsForUser, saves ~$0.20/session) | DeepgramRecordingViewModel.swift, ServerWebSocketService.swift, sonnet-stream.js, eicr-extraction-session.js |
| 2026-02-19 | Deepgram auto-sleep: 3-tier power saving (Active/Dozing/Sleeping), Silero VAD wake, ring buffer replay | SleepManager.swift, AudioRingBuffer.swift, DeepgramService.swift |
| 2026-02-18 | Regex extraction restored alongside Sonnet (3-tier priority) | TranscriptFieldMatcher.swift, DeepgramRecordingViewModel.swift, sonnet-stream.js |
| 2026-02-17 | Server-side Sonnet multi-turn extraction | sonnet-stream.js, eicr-extraction-session.js |
| 2026-02-15 | Session optimizer v3, URL-based review | session-optimizer.sh, analyze-session.js |
| 2026-02-14 | iOS PDF generation, LiveFillView all fields | EICRHTMLTemplate.swift, LiveFillView.swift |

---

## Hub-CLAUDE.md Changelog migration — 2026-06-09

The hub `CLAUDE.md` was bloated to 53 k chars (over the 40 k soft-limit auto-loaded into every Claude session). The full Changelog table from the hub is preserved verbatim below so no detail is lost. The hub now carries one-line summaries only; this section is the authoritative long-form copy for entries between 2026-04-17 and 2026-06-04. Some dates already had entries above; those are NOT removed — both copies remain, since the hub authors may have captured a different angle on the same date than the original changelog row.

### Hub table (verbatim, two-column shape)

| Date | Summary |
|------|---------|
| 2026-06-04 | **iOS Fix D — reconcile voice path with Fix B's `spd_*` canonicalisation (D1 + D2).** Post-Fix-B (`345648b0`) audit found two legacy iOS surfaces still routing inspector *"main fuse"* / *"supply fuse"* / *"cutout"* vocabulary onto the `mainSwitchBsEn` / `mainSwitchCurrent` slots — producing a dual-write where the regex / alias path sets the consumer-unit-isolator slot while Sonnet's canonical `spd_bs_en` write goes to the SPD slot, leaving both PDF cells populated with the same value (the DNO cutout is NOT the consumer-unit isolator). **D1 (`a62000e`):** drops `main_fuse_bs_en` / `main_fuse_type` / `main_fuse_rating` / `main_fuse_current` / `supply_fuse_rating` from the `applySonnetReadings` apply-switch case heads at `DeepgramRecordingViewModel.swift:5400/5407`; adds the five canonical `spd_*` keys to `Self.supplyFields` so Sonnet's `circuit == -1` writes via `record_board_reading` rescue through (silent-drop fix per the SUPPLY vs MAIN SWITCH DISAMBIGUATION prompt block — same bug class as the 2026-04-27 Ivydene Road `ir_test_voltage_v` drop); adds `Self.deprecatedSupplyAliases` static + drop guard around `:4748` that warns `deprecated_main_fuse_alias_dropped` + `continue`s instead of buffering (any backend regression now surfaces in CloudWatch instead of silently mirroring into the wrong slot); retargets the four `spd_*` apply-switch `displayKeyword` strings + adds four lowercase `keywordAliases` entries so transcript highlighting matches inspector vocabulary; appends a same-file (SE-0169) test seam at file bottom (`_test_applySonnetReadings`, `_test_applyRegexMatches`, `_test_pendingReadingsCount`). New `Tests/CertMateUnifiedTests/Recording/DeepgramRecordingViewModelAliasTests.swift` adds parameterised drop-guard tests across all 6 legacy keys (`(a)` no slot mutated, `(b)` not buffered, `(c)` warn payload preserves field name + circuit), per-slot rescue tests for the 5 spd_* additions, and a dual-write regression test feeding both `main_fuse_bs_en` and `spd_bs_en` in one batch (proves only `spdBsEn` populates). `SchemaCoverageRescueTests` adds 5 spd_* members to `mustBeRescued` and a `testDeprecatedAliasesDisjointFromSupplyFields` invariant lock that prevents a future contributor "tidying up" the deprecated list from silently moving an spd_* into the drop guard. Pre-existing test infra blockers also fixed in this commit (folded in to make D1's xcodebuild test gate work): `Tests/CertMateUnifiedTests/Mocks/MockAlertManager.swift` gains the three voice-latency Phase 1 protocol stubs (`markFastPathPending`, `markFastPathFailed`, `playFastPathAudio`) added on 2026-05-26; `project.yml` gains `SWIFT_OBJC_BRIDGING_HEADER: "Sources/CertMateUnified-Bridging-Header.h"` so XcodeGen regenerations don't lose the bridging header (the original pbxproj had it but project.yml didn't, so the first xcodegen of the session dropped it and AudioEngine.swift failed to compile). **D2 (`b54cb75`):** splits four supply-side regex patterns at `TranscriptFieldMatcher.swift:642/646/651/655` into SPD vs main-switch variants — `mainFuseBsEnPattern` widened to REQUIRE a `bs\s*(?:en\s*)?` prefix (no `?` after — preventing dual-write with the rating branch) + digit-anchored `[0-9][0-9A-Za-z\s\-]{0,30}?` capture (preserves *"88-2"* / *"88 type gG"* / *"60898"* verbatim per the prompt's strip-only-leading-BS rule) + value-verbatim assign (no enum-coerce for SPD path); `mainSwitchBsEnPattern` keeps the bare-number capture + bsEnMap enum-coerce for the closed-enum Constants picker; `mainFuseRatingPattern` / `mainSwitchRatingPattern` split with explicit anchor (the original generic `current rating` alternative dropped to prevent bare-utterance silent misrouting); `mainFuseBsEnLimPattern` / `mainFuseLimPattern` likewise split. `SupplyUpdates` struct gains `spdBsEn` + `spdRatedCurrent` staging slots + extended `isEmpty` gate (without the `isEmpty` extension, supply-only transcripts silently no-op the apply block). New apply-path consumer blocks in `DeepgramRecordingViewModel.swift` after line 4369 wire the staging slots into `supplyCharacteristics.spd*` via the existing `applyRegexValue` helper — without these the staging fills but never reaches the form during the ~1-2s pre-Sonnet window. 14 new positive + negative tests in `TranscriptFieldMatcherTests.swift` (CutoutVocabulary / ServiceFuseVocabulary / NoBsPrefixDoesNotMatchValueBranch / LimGuardFiresOnSupplyPath / BareCurrentRatingNoLongerMatches et al — REQUIRED dual-write negatives plus inspector-vocabulary coverage). 3 new regex-apply round-trip tests in `DeepgramRecordingViewModelAliasTests.swift` use D1's `_test_applyRegexMatches` seam. **D3 (locked D3-KEEP, no commit):** per-board PDF cell at `EICRHTMLTemplate.swift:1518` (`Main Switch BS (EN): {board.mainSwitchBsEn}`) stays unchanged — terminology is semantically correct (consumer-unit isolator); the SPD section at line 821 (and alternate at 1153) renders `supply?.spdBsEn` and already disambiguates via its prominent "Supply Protective Device" header. The voice-path dual-write Fix D1+D2 close affects the supply-row cells at lines 866 + 1196 (`supply?.mainSwitchBsEn`), NOT the per-board cell at 1518 (which is populated by the CCU pipeline + manual board-detail entry, not the voice path). **D4 (deferred):** `FuseboardAnalysis` naming divergence audit (`spdRatedCurrentA` / `spdShortCircuitKa` with units in CodingKey suffix vs live-voice-path `spd_rated_current` / `spd_short_circuit`) pending fresh CCU CloudWatch payload audit per Sequencing step 5. **iOS-only — no backend changes**, per Fix B's already-shipped server-side canonicalisation. **`xcodebuild build-for-testing` proven green during D1 commit;** D2 build verification blocked this session by CoreSimulatorService `POSIXErrorDomain 12 "Cannot allocate memory"` (system at ~57 MB free of 17 GB; D2 source changes are structurally minimal additions on the proven D1 base). User-to-rerun `xcodebuild test` after freeing memory + before merge. Concurrent session shipped `8b51418` (tts-dedup confirmation key) on the same branch in parallel — preserved cleanly via stash-pop. NOT yet pushed; NO TestFlight deploy without explicit user approval per standing rule. Plan: `.planning/plan-voice-correctness-d-ios-reconcile-2026-06-04-final.md`. |
| 2026-06-03 | **Voice fix: three F03B590C defects closed — FIELD-AMBIGUITY field menu dropped, "main fuse" routes to `spd_*`, speculator skips off-enum round-1 synth.** Field-test session `F03B590C-7BDA-41BB-AD99-5B27A9CBFF76` (2026-06-03 20:03-20:04 UTC) against the just-deployed observation-correctness sprint surfaced three distinct voice-side defects. Fix A (`e0d5da92`, prompt + test): turn 8 inspector said *"My net is 16 mil"* (Deepgram garble of "mains earth"); Sonnet asked *"…is it R1+R2, Zs, IR, polarity, or number of points?"* — the 5-field menu inside FIELD-AMBIGUITY RULE and inside RULE 5's allow-list example covered only 5 of ~25 circuit fields and ZERO board/supply fields, so the inspector's free-form *"Its main earth."* didn't fit. Canonical ask shape rewritten to a single open question (*"For circuit N, what was that reading for?"* / *"What was that '<value>' for?"*); RULE 5's duplicate menu deleted; prompt-wide regex regression-lock `/Zs.*R1.*IR.*polarity.*number of points/i` added in `stage6-agentic-prompt.test.js` Group 12 + new Group 13 Fix B coverage. Fix B (`345648b0`, prompt + confirmation table + tests): turn 9 inspector said *"Main fuse is BS 1361"* — Sonnet wrote `record_board_reading {field:"main_switch_bs_en", value:"BS 1361"}` (WRONG field — `spd_bs_en` is the canonical home; `field-name-corrections.js:105-108` already maps `main_fuse_bs_en` → `spd_bs_en` but the prompt never named the boundary) AND TTS spoke *"main switch BS EN BS 1361"* (doubled BS + wrong device vocabulary). New SUPPLY vs MAIN SWITCH DISAMBIGUATION prompt block (between ZE/ZS DISAMBIGUATION and OBSERVATIONS) teaches the boundary with explicit value-kind mapping (BS → spd_bs_en, amps → spd_rated_current, kA → spd_short_circuit, type-alone → spd_type_supply) plus the "strip the leading BS" rule that prevents the doubled-BS TTS. Five new `CONFIRMATION_FRIENDLY_NAMES` entries (`spd_bs_en` → "main fuse BS EN", `spd_rated_current` → "main fuse rating", `spd_short_circuit` → "main fuse breaking capacity", `spd_type_supply` → "main fuse type", `main_switch_bs_en` → "main switch BS EN") matching the inspector-vocabulary aliases iOS already uses in `DeepgramRecordingViewModel.swift:5232`. Token caps bumped per the plan's measured re-verification: standalone 7500 → 7850, combined 12500 → 12950. Fix C (`03feaece`, speculator gate + tests): also turn 9 — the dispatcher rejected the off-enum `"BS 1361"` at 20:04:24.366 but the Loaded Barrel speculator's `onToolUseStreamed` hook had already started ElevenLabs synth and shipped TTS *"main switch BS EN BS 1361"* at 20:04:24.812 before the round-2 canonical `"1361 type 1"` landed at 20:04:25.748. Round-2 bundler emit at 20:04:27.291 was correctly deduped at iOS but the act of dispatching it disturbed the audio session and truncated TTS #1 mid-speech (`audio_finalizer_timeout_fired:true`, empty `ios_playback_ack[]`). New gate in `loaded-barrel-speculator.js`'s `onToolUseStreamed` (just after the `coerceRecordReadingValue`/`coerceRecordBoardReadingValue` coercion, before `_speculate(...)`) checks `BOARD_FIELD_VALUE_ENUMS` / `CIRCUIT_FIELD_VALUE_ENUMS` from `stage6-dispatch-validation.js`; when the coerced value is off-enum, returns early after emitting `voice_latency.speculator_skipped_enum_field` to CloudWatch. Policy is VALUE-AWARE (not field-aware) so the speculator's latency win is preserved for legitimate enum values — only the round-1 values the dispatcher would reject are skipped. `makeSpeculator` test helper extended to thread a logger so tests can spy on the new log row. Schema-lock tests in `stage6-dispatch-validation-enum.test.js` pin `main_switch_bs_en` IN `BOARD_FIELD_VALUE_ENUMS`, `ocpd_bs_en`/`rcd_bs_en` IN `CIRCUIT_FIELD_VALUE_ENUMS`, and `spd_bs_en` INTENTIONALLY absent from the BOARD map (it's `type:"text"` — no enum constraint means no round-1/round-2 split for `spd_bs_en` writes; the speculator must keep firing pre-synth on `spd_bs_en` for the latency win). Fix D (iOS TTS echoes inspector's spoken term + form/PDF/code reconcile to `spd_*` namespace) deferred — requires TestFlight cycle. PR #47 merged via `27500d9d`; CI run 26916177650; deployed task def `eicr-backend:289`. 4459/4459 active backend tests passing (1 pre-existing CCU autocorr statistical flake unrelated). Plan: `.planning/plan-voice-correctness-2026-06-03b-final.md`. Manual field-test gate pending: (a) *"My net is 16 mil"* (Fix A — single open ask), (b) *"Main fuse is BS 1361 type 1"* (Fix B — routes to `spd_bs_en` with TTS *"main fuse BS EN 1361 type 1"*), (c) *"Main switch is BS 1361"* (Fix C — `voice_latency.speculator_skipped_enum_field` log row + no round-1 TTS leak + single round-2 TTS). |
| 2026-05-31 | **Voice fix: RCBO walk-through asked "What's the BS number?" twice for two distinct fields + Deepgram defer garbles ignored.** Field repro session E8C6B716-547A-454C-A507-5D3079F7E24D (~22:14 UTC): inspector auto-pivoted into the RCBO walk-through via "Our type is a c for circuits 4 to 7" → engine asked "What's the BS number?", inspector replied "61008" (the BS code for a stand-alone RCD). Engine wrote `ocpd_bs_en` = "BS EN 61008" but the existing mirror derivation on that slot was gated on `value: '61009'` only, so `rcd_bs_en` stayed empty; `nextMissingSlot` then picked it and the engine re-emitted the IDENTICAL TTS prompt "What's the BS number?" — same wording, different field, no audible distinction. Inspector reasonably concluded the system had lost their answer and tried four times in 12 s to defer with Deepgram-clipped phrasings ("you filled in.", "in later.") that the engine's `deferTriggers` regex set didn't recognise; the prompt looped every ~6 s and the session ended at "Oh, fuck off." then "I give up. Stop." (cancel, 3 of 7 RCBO slots saved). Two-part fix: (1) `applyDerivations` (`src/extraction/dialogue-engine/helpers/derivations.js`) gains "unconditional" semantics — a derivation with `value` omitted matches every write to that slot; rcbo.js's `ocpd_bs_en` mirrors to `rcd_bs_en` unconditionally (was `{ value: '61009', mirrors: ['rcd_bs_en'] }`); `rcd_bs_en` becomes `volunteeredOnly: true` so it's never auto-asked but the namedExtractor still harvests the volunteered "RCD BS code is 61009" form, with a symmetric unconditional mirror back to `ocpd_bs_en`; `rcd_bs_en.question` reworded to "What's the RCD's BS number?" as defence-in-depth. (2) `rcd.js` `deferTriggers` widened — leading-"later" anchor now tolerates ≤ 2 lead words so "in later.", "and later.", "uh later." defer (≥ 3 lead words still rejects to keep "I'll deal with that later" out of the defer path); new short-reply pattern (≤ 30-char prefix + 20-char tail) catches `leave it/that/them`, Deepgram garbles `filled in` / `filed in` / `fill it in`, and `skip for/until later`. Slot-level `acceptsDeferAnswer` gate still applies (only `rcd_bs_en` opts in). Two commits: `104735e2` RCBO mirror, `684d7ffa` defer-trigger widening. New test files `src/__tests__/dialogue-engine-rcbo-bs-mirror.test.js` (12 tests) + `src/__tests__/dialogue-engine-rcd-defer-garbles.test.js` (18 tests). Tracked-as-follow-up scope note (in defer test file): pre-existing verb-prefix patterns (`fill…later` / `do…later` / `(?:come\s+)?back…later`) still match anywhere in the utterance not just at the end, so "come back to it later but right now the BS is 60898" still defers today — separate fix. 4190/4190 backend tests green. |
| 2026-05-31 | **Voice fix: RCD focus loop swallowed observations + blocked "later" defer.** Two distinct bugs in the same field-test thread. (1) Inspector says "Observation: the RCD cover is cracked." intending to log a defect; RCD schema's trigger regex (`rcd.js:107` `\bRCD\b...`) matched the bare mid-sentence "RCD", `runEntry` captured the turn, and the engine emitted "What's the BS number?" — locking out `record_observation`. Fix in `processDialogueTurn` (`src/extraction/dialogue-engine/engine.js`): when no script is active AND `OBSERVATION_PATTERN` (`src/extraction/pre-llm-gate.js:147`) matches, short-circuit to `{handled: false}` so Sonnet records the observation. (2) Once in the RCD loop, inspector's reply "later" to "What's the BS number? Or do you want to fill that in later?" never reached the engine's `deferTriggers` (`rcd.js:148` matches bare `^\s*later[.!?]?\s*$`) — the pre-LLM gate blocked it `LOW_CONTENT` (1 content word, no weak trigger) because dialogue-engine server-side asks deliberately bypass `pendingAsks` (`sonnet-stream.js:~1429`, the `srv-*` toolCallIdPrefix path) so the gate had no signal that a question was on the wire. Fix surfaces `session.dialogueScriptState?.active` to the gate as a new bypass flag `hasActiveDialogueScript` (slotted between `hasPendingAsk` and `inResponseTo`, new `GATE_REASONS.BYPASS_DIALOGUE_SCRIPT_ACTIVE` for CloudWatch); when true the gate forwards unconditionally so the engine's defer/skip/cancel/topic-switch parsers all get a chance to fire on terse replies ("later", "AC", "skip", "blank"). Wired at the gate call site in `sonnet-stream.js`'s `handleTranscript` + added `had_active_dialogue_script` to the `gate_blocked` telemetry payload. Two commits: `45662e0c` gate bypass, `4725afbb` engine observation guard. New test file `src/__tests__/dialogue-engine-observation-prefilter.test.js` + 6 new tests in `src/__tests__/pre-llm-gate.test.js`. 4160/4160 backend tests green. |
| 2026-05-29 | **Prod fix: iOS "Issue certificate" failing with `failed to record attestations`.** Inspector tapped both attestation toggles and got the red error from `IssueCertificateSheet.swift:214`. CloudWatch showed `cert_attestations_accept_failed { error: 'relation "cert_attestations" does not exist' }` — migrations `010_account_consents.cjs` and `011_cert_attestations.cjs` (authored alongside the route code in commit `02e13380`) had never been applied to `eicr-db-production`. CI/CD had no migration step, so `npm run migrate:up` was a manual gate someone had to remember; nobody did. The consent endpoint was silently failing the same way (catch + warn lets the request through) but only cert-attestations surfaced a user-visible 500. Hotfixed by temporarily opening the RDS SG to my Mac IP, running `node-pg-migrate up` (applied 010 + 011), then revoking. Permanent fix: `scripts/migrate-from-secrets.js` fetches `eicr/database` from Secrets Manager and runs `node-pg-migrate up`; `docker/backend.Dockerfile` now `COPY`s `migrations/` and the script into the image; `.github/workflows/deploy.yml` runs the just-registered backend task-def as a one-off Fargate task with a `containerOverrides` command, waits for it to stop, and fails the deploy on non-zero exit before updating the service. Picked Fargate-task-with-override over GH-runner-public-access because the migration then runs from inside the VPC with the same IAM + network the backend already uses — no SG churn per deploy, no GH-runner IP-whitelisting. 11/11 routes-cert-attestations tests still green. |
| 2026-05-28 | **Voice latency: widen early-terminate + Loaded Barrel to cover board readings.** Field telemetry across DFE90C4F / A80BB3AF / BE8D791F (2026-05-27 → -28) showed clean single `record_board_reading` and multi-record turns paying ~1.4-2.5 s of round-2 Sonnet wall despite being structurally as safe as the single `record_reading` turns the predicate already early-terminated. Two commits: `ba7d8e21` widens `shouldEarlyTerminate` (`src/extraction/stage6-early-terminate.js`) to allow N≥1 records of `{record_reading, record_board_reading}` with per-tool bucket-size invariants (`perTurnWrites.readings.size` matches record_reading count, `boardReadings.size` matches record_board_reading count) and a record_reading-only multi-board guard; `b604e7a6` extends Loaded Barrel's Phase 2.D `onToolUseStreamed` hook (`src/extraction/loaded-barrel-speculator.js`) to also fire on `record_board_reading`, recovering the ~500-1000 ms pre-synth lead time board readings previously paid by waiting for `onSnapshotPatch` post-dispatch. Same coercion (`coerceRecordReadingValue`), same friendly-name gate (board fields like `earth_loop_impedance_ze`, `prospective_fault_current` are already in `CONFIRMATION_FRIENDLY_NAMES`). 4066 backend tests passing (44 in early-terminate, 31 in speculator). |
| 2026-05-28 | PWA fix: Circuits tab silently empty for legacy single-board jobs. `parseCSV` (`src/utils/jobs.js:48`) writes `''` for missing cells, so re-saved pre-multi-board CSVs come back with `board_id: ''` on every legacy circuit; the Circuits-tab filter at `web/src/app/job/[id]/circuits/page.tsx:185` used `c.board_id == null` which doesn't catch the empty string, so when `boards.length === 1` (selector chips hidden) all circuits silently vanish from the tab while still rendering on Overview. Repro: `36 Wittenham Ave` (formerly `34 Wittenham`, recorded Feb 23 before the multi-board `board_id` column existed). Same predicate was used at 7 other sites in `web/src/lib/recording/apply-ccu-analysis.ts` (names_only / full_capture / hardware_update / append_rail) — all swapped for a shared `isUnscopedBoardId(id)` helper that treats `null`/`undefined`/`''` as unscoped. Backend (`src/`) untouched per the iOS-shared-backend rule; CSV column round-trip stays as-is. 988 web tests pass. |
| 2026-05-27 | Prod data-corruption fix: address (+ postcode, client name/phone/email) silently flipping to `[REDACTED]` after every job open. `redactPiiInPlace` in `src/logger.js` was recursively mutating sub-objects in place on the live `extractedData.installation_details` ref passed via logger meta in `routes/jobs.js:507-513`; next auto-save persisted the redacted value to S3 + `jobs.address` DB column. Logger rewritten copy-on-write (`5bf304ac`) + call-site cleaned (`d5adb2e3`) + read-only recovery script `scripts/audit-redacted-job-addresses.js` (restores via `job_versions.data_snapshot` — pre-corruption snapshots are preserved because auto-version saves run BEFORE the PUT overwrite). |
| 2026-05-22 | CCU dewarp output width restored to 2048-fixed (silent env-var regression: an out-of-band hotfix was dropped by the next CI deploy). Permanent fix moves the default into `src/extraction/ccu-single-shot.js`; `scripts/check-task-def-env-drift.sh` guardrail wired into the deploy workflow. Commit `01c081e`. |
| 2026-05-20 | CCU single-shot prompt hardened on label-to-slot alignment (explicit vertical-column rule, tie-break to null, multi-slot label rule). Architecture docs corrected to describe the actual 2026-05-08+ gpt-5.5 single-shot pipeline (no per-slot crops). |
| 2026-05-13 | PWA `observation_confirmation` brought to iOS parity — AlertCard render dropped, voice-answer auto-dismiss wired, 500ms client burst buffer in `web/src/lib/recording-context.tsx` to coalesce Deepgram split finals. Commit `34e3972`. |
| 2026-05-09 | `ask_user.context_field` enum widened in `src/extraction/stage6-tool-schemas.js` to the full union of circuit/board/supply/installation fields + sentinels. Unblocks sub-board creation focus-asks (Sonnet could write board fields but not legally ask about them). |
| 2026-05-07 | Multi-board sprint Phases 2a + 4a shipped (`ddde287` CSV header round-trip in `src/export.js`; `7e588c8` recording.js single-board scope warning). Phase 5 handoff written; both Codex deal-breakers closed. |
| 2026-05-07 | Multi-board sprint Phase 4 — `/api/analyze-ccu` board attribution + iOS `.addNewBoard` extraction mode. Backend `a40a9f3`; iOS `f9902cd`. |
| 2026-05-07 | Multi-board sprint Phase 3 — PDF sub-main section (iOS-only): conditional 5-cell row in `EICRHTMLTemplate.swift` for `.subDistribution` / `.subMain` boards. Commit `df4311c`. |
| 2026-05-07 | Multi-board sprint Phase 2 — backend schema parity + hierarchy validator. Four commits: `1059f39` shared-types, `ebb6183` PUT/GET tests, `ef56e25` `board-hierarchy-validator.js` + jobs.js PUT wiring, `c21820b` `field_schema.json`. |
| 2026-05-07 | Multi-board sprint Phase 1 — `sub_main_cable_length` dropped from iOS `BoardInfo` (not required by BS 7671). Commit `723b3f3`. |
| 2026-05-06 | BS-EN canonical aligned to Option B (prefixed form, no `-1` suffix) across schema, lookup, parser, picker, and migration. Backend `4611a2e`; iOS `d4d6db1`; diagnostic `scripts/audit-bs-en-values.js` (`90cbc4d`). Prod migration deferred to next field test. |
| 2026-05-06 | Chitchat pause — backend stops forwarding to Sonnet after 10 zero-engagement turns; four wake triggers (wake-word regex, iOS regex hit, manual resume, Deepgram doze recovery) with a 30 s replay buffer. iOS banner + Resume button. Backend `b0d977a` / `c580f42` / `663c135`; iOS `f6a29f0`. |
| 2026-05-06 | Stage 6: BS-code parser gains Lev-1 fuzzy fallback to break OCPD/RCD re-ask loop ("BS 6898" → "BS EN 60898" via single insertion; ambiguous matches fall through). Commit `c36f75a`. |
| 2026-04-30 | **Prod hotfix: CCU extraction failing for every user with "Unexpected non-whitespace character after JSON at position 70".** Production was returning 502 to iOS on every `/api/analyze-ccu` request (observed against a Wylex NHRS12SL retried 7+ times in ~7 min). Stage 2's VLM was emitting its primary `{"rail_bbox":{...}}` object plus trailing content (whitespace + a second JSON object — likely a follow-up `{"main_switch_center_x":...}` block from prior prompt conditioning even though those fields were dropped from the schema in `84dc97e`). The old `extractJson` in `src/extraction/ccu-geometric.js` sliced from first `{` to last `}` and handed both objects to `JSON.parse`, which threw at position 70 — the start of the second concatenated object. With single-shot retired (`3087553`) the per-slot pipeline is the only path, so a parser failure now bubbles up as 502 with no safety net. **Fix:** replaced the brace heuristic with a balanced-brace walk that tracks string + escape state and stops at the matching close brace, ignoring all trailing content. Also accepts a bare ` ``` ` fence (not just ` ```json `) and includes a truncated raw-text excerpt in any future parse error so the next failure is self-diagnosing. The same brittle slice in `classifySlots`'s array parser was replaced by the shared `extractJson` walker. Added a regression test that feeds the exact production failure shape (object + `\n\n` + second object); reproduces the V8 error on the old code. 44/44 ccu-geometric tests + 157/157 CCU tests green. |
| 2026-04-29 | **CCU pipeline cleanup — legacy Stage 2 path retired** (branch `ccu-drop-single-shot`, follow-up commit). Now that tighten-and-chunk has been the sole runtime path for a day, the legacy `populated_area_start_x` / `populated_area_end_x` Stage 2 prompt + body have been deleted (`MODULE_COUNT_PROMPT` constant, the dispatch `if (isCcuStage2GroupsEnabled())`, and ~280 lines of populated-area chunking logic). The `getModuleCountFromGroups` function was renamed back to `getModuleCount` (no disambiguation needed). `CCU_STAGE2_GROUPS` env var retired (no longer read anywhere — removed from task-def is a follow-up deploy task). Dead return fields dropped: `mcbGroups`, `upstreamRcds`, `populatedAreaStartX/EndX`, `stage2Source`. `lowConfidence` now also surfaces Stage 2's CV-vs-bbox count drift gate (was previously orphaned). Net: −359 lines, 2407 tests green. |
| 2026-04-29 | **CCU single-shot Sonnet retired; per-slot is the only path** (branch `ccu-drop-single-shot`, 5 commits). 2026-04-29 production audit on a Wylex NHRS12SL showed total wall-clock 47.1 s with single-shot the long pole at 46.5 s — running in parallel with the per-slot pipeline only to produce 5 board-level metadata fields the per-slot pipeline didn't already cover. Extended `classifyBoardTechnology` (`src/routes/extraction.js`) prompt+parser to also return `board_manufacturer`, `board_model`, `main_switch_rating`, `spd_present` (max_tokens 200→400, ~5 s, ~$0.01). Dropped `main_switch_width` + `main_switch_center_x` from the Stage 2 `MODULE_COUNT_PROMPT_GROUPS` (CV-based pitch detection has been more reliable than the VLM's main-switch estimate since 2026-04-29; the prod cross-check disagreement was 42-67 % every retry). Pitch cross-check rewritten to compare CV's own `moduleCountFromCv` against bbox-derived `moduleCount` (lowConfidence on drift > 1). Ripped out the 130-line single-shot prompt + `singleShotPromise` + AbortController + JSON parser + max-tokens handler + `CCU_GEOMETRIC_V1` kill switch. `analysis` now built directly from classifier output + `slotsToCircuits` over Stage 3/4 merged slots. SPD presence promoted to Stage 3 authority (any `cls === 'spd'` slot). `mainSwitchSide` chain rewritten — Stage 3 `main_switch` slot index → rewireable `mainSwitchOffset` → Stage 1 `mainSwitchPosition`. New `extraction_source: "classifier-only"` for Stage 3/4-empty fallbacks. Failures in classifier or `prepareGeometry` now return 502 (no safety net). Expected wall-clock ~21 s (Stage 4 long pole), cost ~$0.04 vs ~$0.10. iOS API contract unchanged — same response keys. 2391 tests green. |
| 2026-04-22 | **CCU per-slot crop-and-classify shipped to prod** (main HEAD `613d54b`, CI run `24805037373`). 10 commits fast-forwarded: `94bf88d` rewireable pipeline module → `ab195b0` main-switch force-tag → `1301110` route-handler wiring → `dba8098` architecture docs → `54ab93b` merger unit tests → `778e907` Stage 4 label pass + drop single-shot fallback in merger → `1ba46e4` label-confidence gate → `11f40e4` rewireable reliability pass (Stage 1 samples 3→5, Stage 2 retry, Stage 3 crop 1024→1536 + colour disambiguation) → `f2e304d` Codex P1+P2 fixes (modern coord space, `mainSwitchOffset` inline-edge numbering, post-merge enrichment) → `613d54b` Stage 3/4/classifier/single-shot parallelism (~10-15s + ~3s saved). 464 tests passing. Cost ~$0.10-0.12/extraction (3-4% of £3/cert margin). iOS TestFlight companion on main `f812061` — adds `slots[]`, `extraction_source`, `boardTechnology` decoding on FuseboardAnalysis. Reviews: Claude (`.planning/ccu-per-slot-claude-review.md`) + Codex (`.planning/ccu-per-slot-codex-review.md`). Training-data-loop plan at `.planning/ccu-review-training-loop-plan.md`. Stage 6 kept out of this deploy as requested. |
| 2026-04-22 | **CCU extraction — per-slot crop-and-classify primary (branch `ccu-per-slot-primary`, superseded above).** `/api/analyze-ccu` runs a cheap `board_technology` classifier first (~3s, ~$0.01), routes to either the existing DIN-rail-targeted `extractCcuGeometric` or the new bakelite-carrier `extractCcuRewireable` (`src/extraction/ccu-geometric-rewireable.js`, 820 lines, 31 unit tests). Both return per-slot classifications; a new `slotsToCircuits` merger builds `circuits[]` from Stage 3 slots, using single-shot's labels by circuit number, with per-slot confidence fallback (<0.7 → defer to single-shot at that position). Stage 3 output was previously computed-then-dropped (Phase C never wired) — this sprint made it authoritative. Verified E2E on the 2026-04-22 Wylex photo: pre-fix single-shot returned 5 circuits with Shower on the wrong carrier; new pipeline returns 6 circuits with all ocpd_type="Rew" / ocpd_bs_en="BS 3036". Flag `CCU_GEOMETRIC_V1` defaults ON; set to "false" on the task-def to kill-switch back to pure single-shot. Cost: ~$0.03 → ~$0.08-0.09 per extraction (1.5-2.5% of £3/cert margin). Three commits: `94bf88d` rewireable module, `ab195b0` main-switch force-tag fix, `1301110` route handler. iOS `FuseboardAnalysis` gains `slots[]` + `extraction_source` decoding (additive, no UI wiring yet — tap-to-correct grid is future iOS work). Sprint plan: `.planning/ccu-per-slot-sprint.md`. |
| 2026-04-22 | **CCU extraction — rewireable fuse board support (shipped prod, commit `8da2292`).** Single-shot CCU prompt in `/api/analyze-ccu` previously forced every position into MCB/RCBO and rejected BS 3036 rewireable fuses. Added board_technology classification (Step 1b), per-position fuse-carrier branch (Step 2b, colour→rating mapping white/blue/yellow/red/green), and widened output schema to accept `ocpd_type: Rew\|HRC` + `ocpd_bs_en: BS 3036\|BS 1361\|BS 88-2`. `BS_EN_LOOKUP` gets `REW → BS 3036` fallback. `field_schema.json` `ocpd_type` options aligned with iOS `Constants.swift`. iOS `FuseboardAnalysis` gets `boardTechnology` decode field (commit `4bed04a` on `stage6-agentic-extraction`). CI run `24799705740` green. |
| 2026-04-19 | **Prod fix: web recording failed with "Deepgram WebSocket error".** Backend mints Deepgram credentials via `/v1/auth/grant` (JWT access tokens) as of `248953b` (2026-04-18, P0-10 — master-key fallback was correctly removed). The web client (`web/src/lib/recording/deepgram-service.ts:107`) was still advertising `['token', apiKey]` as the WS subprotocol — that scheme is valid only for raw master API keys; Deepgram 401s the upgrade for JWTs and browsers surface it as a generic `WebSocket error` with no body. iOS already fixed this path (`DeepgramService.swift:228-230` comment: "JWT+Token=401, JWT+Bearer=connected"). Fixed by switching the subprotocol to `['bearer', apiKey]` and expanding the comment block at the call site so the next migration can't re-trip it. The 2026-03-31 hotfix `550278e` (return master key directly) masked this; correctly removing that bypass in `248953b` re-surfaced it on web. `jest-websocket-mock` echoes any subprotocol the client offers, so the 15 existing deepgram-service tests still pass without change. |
| 2026-04-19 | **Prod hotfix #2: same PWA login bounce, recurrence.** Local `./deploy.sh` re-registered `eicr-pwa` from `ecs/task-def-frontend.json` (revs 33 & 34) — the morning's fix only lived in the registered task def, not in the source template, so the secret was dropped again. Hotfixed live by registering `eicr-pwa:35` = rev 34 + JWT_SECRET. **Permanent fix (commit `9b5b809`):** added the `secrets: [JWT_SECRET]` block to `ecs/task-def-frontend.json` itself (shared by `deploy.sh:93` + `.github/workflows/deploy.yml:442`), so every future register-task-definition emits the secret. No CI guard added — template is small, fields stable; revisit if a third regression occurs. |
| 2026-04-19 | **Prod hotfix: PWA login bouncing back to /login.** Rebuilt PWA middleware (Wave 4 D4) HMAC-verifies the JWT and fail-closes when `NODE_ENV=production` AND `JWT_SECRET` is unset — every authenticated nav 307'd to `/login` with no error. Phase 8 cutover shipped the new middleware but never injected `JWT_SECRET` into the `eicr-pwa` task def (old frontend didn't need it). Fixed by registering `eicr-pwa:32` with a `secrets` entry referencing `eicr/api-keys:JWT_SECRET::`, + attaching inline policy `eicr-exec-secrets-access` to `eicr-ecs-execution-role` granting `secretsmanager:GetSecretValue` on that one ARN (the execution role — which ECS uses for secrets-to-env injection — had no SM perms; the task role did, because backend fetches secrets at runtime from Node). Rollout clean after the IAM add. Pre-flight check added to `deploy-runbook.md`. |
| 2026-04-18 | **Web Phase 8 — production cutover.** PR derek570/EICR-#1 merged `web-rebuild` (154 commits) into `main` (merge commit `9202351c`). CI deploy pipeline built/pushed ECR images, registered ARM64 task defs, force-new-deployment on `eicr-pwa` (rev 30) + `eicr-backend` (rev 27); services-stable + smoke green (certmate.uk/ → 307 /login, /api/health 200, /manifest.webmanifest + /sw.js served). Three pre-flight fixes shipped in the PR: `4b77316` CI `npm run build` (webpack flag via package.json not bare next build), `4cef21f` `output: 'standalone'` in next.config.ts (Dockerfile needs `.next/standalone` to exist), `07f79e6` Next 16 @next/swc platform-binary lockfile patch. **Closes the web rebuild.** |
| 2026-04-18 | Web Wave 2a — test harness (vitest 4 + jsdom + RTL + fake-indexeddb) stood up; D12 ApiError JSON-envelope parsing (lifts `{error: "..."}` to `.message`, preserves `.body`; 401 classifiers switched from regex to `.status`); 32 regression tests (5 suites) backfilling Wave 1 fix surfaces. Wave 2b (D2 adapters) deferred. |
| 2026-04-18 | Web Phase 7d — offline-sync UI (OfflineIndicator pending/failed pills, JobRow Pending chip, `/settings/system` admin page, discard/requeue helpers, BroadcastChannel change notifier). **Closes Phase 7.** |
| 2026-04-17 | Web Phase 7c — offline mutation outbox + replay worker (IDB v2, exp backoff, FIFO). |
| 2026-04-17 | Web Phase 7b — iOS Add-to-Home-Screen hint on `/settings`. **Closes Phase 7b.** |
| 2026-04-17 | Web Phase 7b — AppShell offline indicator (amber pill via `navigator.onLine`). |
| 2026-04-17 | Web Phase 7b — IDB read-through cache (offline dashboard + job detail via `certmate-cache`). |
| 2026-04-17 | Web Phase 7b kickoff — user-initiated SW update handoff (sonner toast + `SKIP_WAITING`). |
| 2026-04-17 | Web Phase 7a — PWA foundation (Serwist, manifest, icons, offline/error boundary, `InstallButton`). |
| 2026-04-17 | Web Phase 6c — system-admin user management. **Closes Phase 6.** |
| 2026-04-17 | Web Phase 6b — company settings + company-admin Jobs/Team/Stats dashboard. |
| 2026-04-17 | Fix `next build` on `/login` (wrap `useSearchParams` in `<Suspense>`). |
| 2026-04-17 | Web Phase 6a — `/settings` hub + Staff (inspector profiles, signatures). |
| 2026-04-17 | Web Phase 5d — LiveFillView (brand-blue flash as Sonnet fields populate). |
| 2026-04-17 | Web Phase 5c — observation photos (auth'd blob fetch, camera+library pickers). |
| 2026-04-17 | Web Phase 5b — document extraction on Circuits tab (`/api/analyze-document`). |
| 2026-04-17 | Web Phase 5a — CCU photo capture + GPT Vision on Circuits tab. |
| 2026-04-17 | Start ground-up web rebuild on branch `web-rebuild`. Phase 0: Next 16 + React 19 scaffold. |
| 2026-03-04 | Add `/api/analyze-document` (GPT Vision for EICR/EIC fields from photos/PDFs). |
| 2026-02-28 | Raise `COMPACTION_THRESHOLD` 6000→60000 — preserves Sonnet's conversational context. |
| 2026-02-23 | Compaction cost guards (5 checks) + `max_tokens` 2048→4096 + 120s rate limit. |
| 2026-02-23 | Fix audio loss during VAD warm-up; add `/api/health/ready`; iOS pre-flight check. |
| 2026-02-23 | CCU prompt v2 (4-step methodology, RCD waveforms, device-face amp enforcement). |
| 2026-02-22 | 5-star Phase 8: OpenAPI spec, Swagger UI, pre-commit hooks, ADRs. |
