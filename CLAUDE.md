# EICR-oMatic 3000

> **MANDATORY — Every code change must update docs and be committed.**
> - Changed a pipeline step, field, or architecture? Update the matching [reference file](docs/reference/).
> - Hub CLAUDE.md is an **index only** — add detail to reference files, not here.
> - Add a row to the [Changelog](#changelog) for any user-facing or architectural change.
> - Delete stale content rather than commenting it out. Keep every file under its target line count.
> - **Commit automatically after each logical unit of work — do NOT wait to be asked.** Small, focused commits with detailed messages explaining both what changed and WHY the code exists.

> **MANDATORY — Backend (`src/`, `config/prompts/`, `packages/shared-types`, `packages/shared-utils`, RDS, S3) is SHARED with iOS and web. Changing it is ALLOWED in any wave — no separate mandate, no permission step. The ONE invariant is that the wave ENDS with every client correct.**
> - **Transient mid-wave breakage on either client is fine.** A wave that leaves one client wrong when the work stops is not. Do not gate a backend change on "but iOS sees this" — ship it, then ship the client half in the same wave.
> - **Never leave a cross-client wave half-shipped when Derek next goes out to inspect.** Backend deploys in ~30 min; iOS needs a TestFlight build he actually installs. Before declaring a wave done, confirm the client halves are on the shipped versions — that check is the whole of the remaining risk.
> - **Capability gates (`board_clear_v1`, `lim_ranged_write_v1`, `low_conf_readback_v1`) are a judgement call, not ceremony.** Reach for one when a wave will realistically end with clients on different versions, or when a new wire shape would confuse an older build. Skip it when both halves ship together — a gate you flip in the same wave is pure overhead.
> - **Why (2026-07-26, Derek — supersedes the 2026-04 freeze):** the old rule froze the backend behind an explicit per-wave mandate because a regression would hit iOS users mid-inspection. **Derek is the only user, and he runs these waves over a weekend and does not touch the app until the whole batch is finished.** The harm the freeze priced in does not exist; the coordination tax it charged on every cross-platform fix was real, and it stranded genuine fixes (feedback id 101 — a bogus `ze` that voice could not clear, persisting into a legally-significant certificate — sat behind an ungranted mandate).
> - **Still binding, and unchanged:** infra changes come from source (separate rule below); `main` is PR-only; iOS remains canon for the DATA CONTRACT when the two clients disagree about shape — that is a design tiebreak, not a permission gate.
> - **Pure-frontend state** (anything that doesn't traverse the WS / HTTP boundary) was never covered by this rule and still isn't.

> **MANDATORY — Infrastructure changes must come from source.**
> - If a fix requires editing an ECS task definition, IAM policy, secret, env var, or any other live AWS resource, the canonical change goes into the corresponding source-controlled file (`ecs/task-def-backend.json`, `ecs/task-def-frontend.json`, `.github/workflows/deploy.yml`, etc.) AND is committed in the same session.
> - **Direct `aws ecs register-task-definition`, AWS console edits, or any out-of-band CLI action is never the canonical change.** At most it's an emergency stopgap, and MUST be followed by a source commit before the session ends. If you're tempted to apply something live without a commit, stop and update the source instead.
> - **Why:** any subsequent CI deploy re-registers the task def from the source template. Live-only fixes get silently dropped on the next deploy, with no warning. This has bitten us twice — `CCU_DEWARP_OUTPUT_WIDTH=2048` (2026-05-13, dropped 2026-05-14) and `JWT_SECRET` (2026-04-19, see changelog). Both cost field-test time + a re-investigation that traced the regression back to a missing commit.
> - **How to apply:** before running any `aws ecs ...`, `aws iam ...`, or AWS-console mutating action, ask: "does this change persist to source?" If no, stop and fix the source first. If yes, run it AND commit the source change.
> - **Guardrail:** `scripts/check-task-def-env-drift.sh` runs in CI before every `register-task-definition` call and fails the deploy if any env var exists on the live task def but not in the source template. Bypass via `[skip-drift-check]` in the commit message only for emergencies, and follow up with a real source commit immediately after.

> **MANDATORY — Web companion required for every client-visible change.**
> - *Every* plan/wave that changes voice UX, wire shapes, or client-visible behaviour MUST contain a **"Web companion"** section: either the web change ships in the same wave, or a dated `web/docs/parity-ledger.md` row **with an owner** + a todo records the deliberate lag. "Deferred with no owner" is not an allowed state.
> - **Why:** the iOS+backend ship loop (field feedback → plan → TestFlight + ECS) had no web step; between 2026-06-17 and 2026-07-01 ~8 voice waves shipped backend+iOS companions and ZERO web companions, leaving MANDATORY audio-first behaviour (universal read-back) dormant for web users. This rule is the WS1 drift-stop of the iOS↔Web Full-Parity Program.
> - **How to apply:** when writing or reviewing a plan, ask "does a web user see this change?" (new wire fields, changed frames, spoken UX, visible behaviour). If yes and the plan has no Web-companion section, add one — or add the dated ledger row + todo — before the plan converges. CI warns on PRs touching files whose ledger rows are >30 days unverified (`scripts/check-parity-ledger.mjs`).

> **MANDATORY — Parallel workstreams over shared test files: re-run `main`'s full suite between merges.**
> - When two or more PRs/workstreams run in parallel and touch OVERLAPPING test files (or the shared test harness — `web/tests/setup.ts`, `web/vitest.config.ts`), do NOT merge each on green-in-isolation. After a merge to `main`, re-run the FULL suite on `main` (backend Jest AND `npm test --workspace=web`) BEFORE the next PR merges.
> - **Why:** WS5 and WS7 both touched the terms tests; each PR was green in isolation, but their interaction only surfaced on the post-merge `main` run — which then SKIPPED the deploy (deploy jobs depend on `Test Frontend` passing). Green-in-isolation ≠ green-after-merge when the shared harness is what changed.
> - **How to apply:** the local gate is `.husky/pre-push` (now runs both suites on a Node warned to match CI's 20 — see [docs/reference/deployment.md](docs/reference/deployment.md) § Local Node version). After any merge into `main` that touched test infra or shared test files, watch the `main` CI run to green before merging the next; if it goes red, fix-forward before the next merge — don't stack.

Automated EICR/EIC certificate creation for electrical inspectors using an iOS-first workflow.

## Project Overview

1. **Photo Capture** - Inspector photographs consumer unit (CCU) via iOS app
2. **CCU Analysis** - GPT Vision extracts circuit data from consumer unit photos
3. **Document Extraction** - GPT Vision extracts certificate data from previous certificates, handwritten notes, or photos
4. **Voice Recording** - Inspector dictates test readings and observations into iOS app
4. **Live Transcription** - Deepgram Flux transcribes speech in real time on BOTH clients (`flux-general-en` on `/v2/listen`, direct WebSocket). Web flipped to Flux in production 2026-07-03 (`ff620997`, `DEEPGRAM_STT_MODEL=flux` runtime kill-switch — nova-3 remains the fail-safe fallback only)
5. **Live Extraction** - Server-side Sonnet 4.5 extracts structured certificate data via multi-turn conversation
6. **Review & Edit** - Inspector reviews populated certificate in iOS app tabs
7. **PDF Generation** - Generate complete EICR/EIC PDF certificates

## Audio-First Design Principles

CertMate is evolving into an **audio-first, hands-free** tool. The inspector works in **AirPods**, walking the installation with the phone pocketed and **no eyes on the screen** — they dictate readings and hear them read back. Treat the spoken channel as the **primary UI**; the on-screen grid is the secondary/visual mirror.

The following are **MANDATORY** product invariants. They override older guidance that optimised for screen-first use or for minimising TTS chatter, and they span both backend (extraction/confirmation synthesis) and iOS (TTS playback):

1. **Every dictated reading is read back aloud — exactly once. Never silently entered into the UI.** A value that only appears on screen is invisible to a hands-free inspector, so every applied reading/correction MUST produce one spoken confirmation. *Exactly once* — not zero (silent entry) and not twice (the double-confirm bug). This holds for ALL apply paths, including client-initiated reassignments, not just server-extraction turns.
   - **Exception (by design):** automatic derivations and side-effect ticks — e.g. polarity auto-ticked from Zs, mirror-derived fields — are computed consequences, NOT dictated readings, and do **not** get a spoken confirmation.
2. **Structurally complete readings are WRITTEN regardless of self-reported confidence, and read back aloud — never silently dropped.** A structurally complete dictated reading (field + circuit/board scope + value) is written at whatever confidence and read back; the inspector verifies by ear and corrects by speaking. Ask ONLY for structural gaps, contradictions, invalid/out-of-range values, or true non-values. The live model is Haiku 4.5, whose self-reported `confidence` is not a trustworthy gate, so we do NOT gate behaviour on it (`CONFIRMATION_MIN_CONFIDENCE` is now only the loaded-barrel speculator's pre-synth cost gate; the `< 0.5` write decision is a capability-gated PRE-APPLY rollout step in the dispatcher, not a behavioural confidence threshold). This supersedes BOTH the older "suppress low-confidence confirmations to cut noise" stance AND the interim "low-confidence readings ASK" stance — a dropped reading is invisible to a hands-free user.
3. **Latency is a first-class concern.** The dictate→confirm loop is conversational; perceived latency between speaking and hearing the read-back directly shapes usability. Optimise for low perceived latency and treat regressions as bugs, not cosmetics.

## Tech Stack

| Component | Technology |
|-----------|------------|
| iOS App | SwiftUI (CertMateUnified) |
| Transcription | Deepgram Flux `flux-general-en` (`/v2/listen`, direct WebSocket) on BOTH clients — web live since 2026-07-03 via the `DEEPGRAM_STT_MODEL` runtime kill-switch (nova-3 = fail-safe fallback only) |
| Live Extraction | Claude Sonnet 4.5 (server-side multi-turn via WebSocket) |
| CCU Photo AI | GPT Vision (consumer unit analysis) |
| Document Extraction AI | GPT Vision (certificate/notes data extraction) |
| Backend | Node.js (ES modules) — API, WebSocket, S3 |
| PDF (iOS) | WKWebView HTML->PDF (EICRHTMLTemplate.swift) — **iOS app uses this, NOT the server generators** |
| PDF (web) | CLIENT-SIDE since 2026-07-02 (WS9): TS port of the iOS template + foreignObject capture + pdf-lib Blob (`web/src/lib/pdf/`) — **any EICRHTMLTemplate.swift change needs a web-template companion** (ledger row `pdf/pdf-fidelity`) |
| PDF (server) | Python ReportLab + Playwright — **FALLBACK/DEBUG-ONLY** (web "Generate on server (fallback)" action; flips behind the debug page after field validation) |
| Web Frontend | Next.js (App Router, PWA) |
| Cloud | AWS ECS Fargate, S3, RDS PostgreSQL, Secrets Manager |

## Monorepo Structure

npm workspaces with 3 packages:

| Workspace | Path | Purpose |
|-----------|------|---------|
| Backend | `src/` | Express API + WebSocket server |
| Web | `web/` | Next.js frontend (PWA, dashboard, recording, editing) |
| shared-types | `packages/shared-types/` | TypeScript types (`@certmate/shared-types`) |
| shared-utils | `packages/shared-utils/` | Shared utilities (`@certmate/shared-utils`) |

## Quick Commands

### Development

```bash
npm start                          # Backend (port 3000)
npm run dev --workspace=web        # Web (port 3001)
```

### Testing

```bash
npm test                           # Backend tests
npm test --workspace=web           # Web tests
```

### Linting

```bash
npm run lint                       # ESLint
npm run format                     # Prettier
```

### Deploy

**ALWAYS deploy via GitHub Actions.** Push to `main` → CI runs tests, builds ARM64 Docker images, pushes to ECR, deploys to ECS (~30 min end-to-end). Monitor with `gh run watch <run-id> --exit-status` (single long-poll connection — no polling). Run history: https://github.com/derek570/EICR-/actions

**Auto-DELIVER at the end of a work session — do NOT wait to be asked. Delivery is PR-only: `main` is PR-protected (field-replay corpus gate, 2026-07-16 — Derek's repo-wide PR-only decision).** When changes are committed locally and `npm test` is green, open a PR and merge it via `gh pr merge --merge` after the required checks pass — for EVERY end-of-session delivery, backend AND web-only waves AND docs commits included. Never `git push origin main` directly. CI handles the deploy from the merge.

- Branch from `main`, commit per logical unit, `git push -u origin <branch>`, `gh pr create`, then `gh pr merge <branch> --merge` once `Test Backend (Node.js)` / `Test Frontend (Next.js)` / `npm Audit Security Scan` are green. Watch the post-merge deploy with `gh run watch <id> --exit-status`.
- **Why PR-only now:** the field-replay corpus gate is merge-blocking, and a merge-blocking gate is a `main` branch-protection ruleset — necessarily repo-wide (GitHub has no path-scoped PR enforcement). A direct push would bypass the gate.

**When NOT to auto-deliver:**
- `npm test` (backend Jest) or the web vitest suite is failing — fix first; do not ship red.
- A pre-push hook (secrets scan, full test suite, `replay:field-corpus:prepush`) fails — investigate; do not bypass with `--no-verify`.
- The user explicitly said "don't push/merge" for THIS task.
- Schema / migration changes that need coordination with an iOS TestFlight cycle — merge the backend FIRST and wait for ECS rollout (`gh run watch`) before kicking off iOS auto-push, so iOS hits a backend with the new shape live.

Default is auto-PR-then-merge; the exclusions above are the only reasons to hold.

Do **not** use the local `./deploy.sh` quick-deploy script even though it exists in the repo. Docker Desktop is not kept running on the dev Mac, so the script fails immediately, and its `tee`-wrapped invocation masks the failure as exit 0. CI is the only deploy path that works reliably.

iOS TestFlight: `~/Developer/EICR_Automation/CertMateUnified/deploy-testflight.sh` — same auto-push-at-end-of-work policy applies; see `CertMateUnified/CLAUDE.md` § TestFlight Deployment.

> Full details: [docs/reference/deployment.md](docs/reference/deployment.md) (AWS), [docs/reference/deploy-testflight.md](docs/reference/deploy-testflight.md) (iOS)

### Check Status

```bash
aws ecs describe-services --cluster eicr-cluster-production --services eicr-frontend eicr-backend --region eu-west-2 --query "services[*].{Service:serviceName,Running:runningCount,Status:deployments[0].rolloutState}" --output table
aws logs tail /ecs/eicr/eicr-backend --region eu-west-2 --since 10m
gh run list --limit 5
```

## iOS Recording Pipeline (v3)

```
iOS (16kHz PCM) -> DeepgramService (direct Deepgram Flux WS, flux-general-en /v2/listen)
    -> transcript -> NumberNormaliser -> TranscriptFieldMatcher (instant regex)
    -> ServerWebSocketService (wss://backend/api/sonnet-stream) + regex hints
    -> Backend: multi-turn Sonnet 4.5 extraction (with regex context)
    -> results + questions + cost updates back to iOS
```

**Field priority (3-tier):** Pre-existing (CCU/manual) > Sonnet > Regex
**Dual extraction:** Regex provides instant ~40ms field fill; Sonnet overwrites with higher accuracy 1-2s later. Regex hints (field names only) sent to backend as Sonnet context.

> Full details: [docs/reference/ios-pipeline.md](docs/reference/ios-pipeline.md)

## AWS Configuration

> Replace `<ACCOUNT_ID>` with your AWS Account ID.

| Resource | Value |
|----------|-------|
| Region | eu-west-2 (London) |
| Domain | certmate.uk |
| ECS Cluster | eicr-cluster-production |
| ECR Backend | `<ACCOUNT_ID>`.dkr.ecr.eu-west-2.amazonaws.com/eicr-backend |
| RDS Database | eicr-db-production.cfo684yymx9d.eu-west-2.rds.amazonaws.com |
| Backend Memory | 2048 MB / 512 CPU |

> Full table: [docs/reference/architecture.md](docs/reference/architecture.md)

## Environment Variables

Cloud keys loaded automatically from AWS Secrets Manager: `eicr/api-keys` (all API keys as a single JSON object) and `eicr/database` (DB credentials). No local `.env` needed for cloud deploys.

> Full details: [docs/reference/architecture.md](docs/reference/architecture.md#environment-variables)

## Certificate Types

- **EICR** - Electrical Installation Condition Report (periodic inspection)
- **EIC** - Electrical Installation Certificate (new installations)

## Commit Rules
- **Auto-commit after every logical unit of work.** Do NOT wait for the user to ask — commit immediately when a meaningful change is complete (a bug fix, a feature addition, a refactor, a config change, etc.). Multiple small commits are always better than one large commit.
- **Commit messages must be detailed and explain the WHY, not just the WHAT.** Every commit message should answer:
  1. **What** changed (a brief summary line)
  2. **Why** the change was needed (what problem existed, what was broken, what feature was missing)
  3. **Why this approach** (why the code is written the way it is — design decisions, trade-offs, alternatives considered)
  4. **Context** — flag any deliberate UI/layout decisions, note if a change fixes a problem caused by a previous refactor, mention if a pattern was chosen for consistency with existing code
- Use multi-line commit messages: a short subject line, then a blank line, then a detailed body paragraph.
- If a change touches multiple concerns, split into separate commits — one per concern.
- Never batch unrelated changes into a single commit.

## Development Notes

- All Node.js uses ES modules (`"type": "module"` in package.json)
- Backend routes split into 14 modules in `src/routes/`
- Route registry: `src/api.js` (197 lines) mounts all routes + legacy aliases
- API documentation: Swagger UI at `/api/docs`
- Pre-commit hooks: eslint + prettier via lint-staged, secrets detection
- Pre-push hooks: full test suite

## Reference Documentation

Detailed docs split into focused reference files:

| Document | Contents |
|----------|----------|
| [architecture.md](docs/reference/architecture.md) | Tech stack, containers, AWS config, environment vars, AI models, costs |
| [ios-pipeline.md](docs/reference/ios-pipeline.md) | Recording pipeline v3, debug runbook (7-step), S3 paths, common issues |
| [field-reference.md](docs/reference/field-reference.md) | All UI fields (29 circuit columns), CSV mapping, field schema, sync rules |
| [deployment.md](docs/reference/deployment.md) | AWS deploy commands, cloud status, troubleshooting |
| [field-replay-corpus.md](docs/reference/field-replay-corpus.md) | Field-replay correctness gate: captured sessions as a merge-blocking regression corpus (lanes, authoring, gate-state machine, trusted evidence, governance, PII) |
| [deploy-runbook.md](docs/reference/deploy-runbook.md) | Quick AWS ECS deploy reference (env, secrets, steps) |
| [deploy-testflight.md](docs/reference/deploy-testflight.md) | iOS TestFlight deploy script, ASC credentials, onnxruntime patch |
| [pwa-replay-harness.md](docs/reference/pwa-replay-harness.md) | Web-pipeline replay harness: seams, trace, iOS differential, generated field sweep, CI lanes, corpus process |
| [vad-investigation.md](docs/reference/vad-investigation.md) | Dated journal: VAD sleep/wake investigation + hybrid VAD decisions |
| [changelog.md](docs/reference/changelog.md) | Verbatim commit-body-level changelog (full history beyond hub summary) |
| [file-structure.md](docs/reference/file-structure.md) | Directory tree, key files |
| [deployment-history.md](docs/reference/deployment-history.md) | Implementation phases 1-8, resolved items archive |
| [DEVELOPER_SETUP.md](docs/DEVELOPER_SETUP.md) | Full developer setup guide (all platforms) |
| [ADRs](docs/adr/README.md) | Architecture Decision Records (7 ADRs) |
| [OpenAPI](docs/api/openapi.yaml) | OpenAPI 3.1 spec (served at /api/docs) |

## Documentation Sync Rules

When modifying UI fields: update `config/field_schema.json` + [field-reference.md](docs/reference/field-reference.md). When adding extractable fields to Sonnet: (1) add to prompt in `eicr-extraction-session.js`, (2) add case in `applySonnetReadings()`, (3) add keyword boosts in `default_config.json`.

> Full sync checklist: [docs/reference/field-reference.md](docs/reference/field-reference.md#keeping-this-documentation-in-sync)

## Current Focus / Active Work

- **⚡ CCU pipeline (live):** single-shot `gpt-5.5` over the whole image via `src/extraction/ccu-single-shot.js`. **No per-slot cropping.** Stage-3/Stage-4 per-slot pipeline (`ccu-geometric.js`, `ccu-label-pass.js`) is LEGACY FALLBACK only, gated behind `CCU_USE_SINGLE_SHOT=false`. In-scope failure modes: gpt-5.5 mis-counts in long identical-MCB runs, label-column mis-alignment, post-merge enrichment overrides, `slotsToCircuits` phase-walking heuristics. NOT in scope: CV crop accuracy / slot crop boundaries (not in live path). Full details: [docs/reference/architecture.md#ccu-photo-extraction-pipeline](docs/reference/architecture.md#ccu-photo-extraction-pipeline).
- **Web rebuild in production** since 2026-04-18 (PR #1, merge `9202351c`). certmate.uk serves Next 16 / React 19 PWA client from `web/`.
- **Live in production:** Deepgram auto-sleep (3-tier Active/Dozing/Sleeping), server-side Sonnet v3 multi-turn extraction.
- **Next candidates:** Playwright E2E coverage for offline-sync. (`queueSaveJob` IS wired into JobProvider's save path at `web/src/lib/job-context.tsx:159` — verified 2026-07-02; the earlier "no production caller yet" note was stale.)
- **SHIPPED 2026-07-02 (WS2), awaiting iPad field smoke:** PWA observation-photo auto-link — the parked 2026-05-13 branch was rebased onto main and merged. Ledger row `observations/obs-photo-autolink` stays `partial` until the iPad Safari device smoke passes (todo in vault `todos-certmate.md`); two dated deliberate divergences on the row (no CCU picker source — zero-backend; web-extra camera/library chooser).
- **OPEN FOLLOWUP 2026-06-05 — voice-latency Phase 2.2 (deferred from PR #52, merged).** Surface proactively on any voice-latency or field-test discussion. Pick server `FINALIZER_TIMEOUT_MS` widen vs iOS Apple-native `local_fallback` emit once 1–2 field sessions hit the deployed code. Runbook: [CertMateUnified/.planning-stage6-agentic/handoffs/voice-latency-correlation-fix-2026-06-05/FOLLOWUP.md](../CertMateUnified/.planning-stage6-agentic/handoffs/voice-latency-correlation-fix-2026-06-05/FOLLOWUP.md).

## Changelog

Recent changes — one line each. **Full commit-body-level detail in [docs/reference/changelog.md](docs/reference/changelog.md)**; use `git log` for everything else.

| Date | Summary |
|------|---------|
| 2026-07-30 | **PLAN-2B §3.3 — deterministic multi-description resolution (feedback id 104; backend-only, zero wire-schema change).** Paired with the plural prompt steer already on `main` from merged PR #133, a pending circuit write now resolves quantified and enumerated descriptions against an effective-board-only census, including unnamed circuit refs; explicit counts may fan out exact/substring candidate sets only when the count agrees, while wrapped leading retractions, fuzzy, ambiguous, malformed-compound and count-mismatch shapes ask. Whole and embedded designations use unique exact separator equivalence (`&`/`+`/`and`/`plus`/comma); raw exact names such as *"Two Lighting Circuits"* own their span before quantifier stripping; plain empty-residue counts retain the shipped scalar path while `all`/`both` never collapse to one ref. The registered `mdr-*` clarification uses an anchored ref grammar, so timer chatter preserves the exact ask on both answer channels; it validates explicit refs, retains direct-answer utterance epochs, reconciles candidate capacity correctly, and gives cancellation/incomplete paths one truthful terminal. PLAN-2A notices still require a surviving sibling, including the narrow cancelled-turn designation exception; all-unmatched replies retain zero partial-success notices. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-30 | **PLAN-2A §3.1 — partial-failure notices: a turn that half-succeeded now SAYS SO (backend-only; feedback id 112; ZERO wire change — the notice rides the existing field-nil confirmation channel both clients already speak).** *"Zs for circuits 5 to 10 is 0.4"* on a job with no circuits 7 or 8 wrote and read back four circuits and dropped two in COMPLETE SILENCE. The inspector's only evidence is the ABSENCE of two read-backs among four — inaudible by construction, Audio-First #1 in the zero-times direction. Every existing net declines CORRECTLY: four confirmations defeat marker-②'s zero-speech-intent predicate; `producedNothing` requires `(orphanToolCalls === 0 \|\| allRejected)` and an `is_error:false` SKIP is neither, so even a skip-ONLY turn cannot reach A3; no ask ⇒ F7 stands down (each pinned by test, not changed). Mechanism = plan B's stage-at-the-dispatcher shape but a deliberately SEPARATE third notice regime: a partial failure is not a refusal, it must NOT enter plan B's coverage arbitration (an entry inside `coveredUnion` would suppress the generic `REJECTED_PROMPTS` line and change plan B's behaviour — and because the accumulator is separate, `coveredUnion`/`rejectedSetFullyCovered` iterate `mandatoryNotices` ONLY — exclusion is STRUCTURAL, not a class-marker filter — so an all-rejected turn stays byte-identical), and it needs a drain rule plan B has no analogue for. Own accumulator/cursor/`route`, and its OWN ordinal terminals from attempt 4 — **Codex cycle 2 BLOCKER: the regime shipped with none and the "each turn's partial failure is a fresh fact" reasoning was wrong.** Re-dictating the same range at a genuinely missing circuit re-renders the same `(reason, label, ref-set)` sentence, and 3 variants mod 3 means the 4th render inside 30 s wraps to variant 0 → swallowed by the client's text-keyed `field: null` dedupe while marker-② is already suppressed by the queued prompt = a silent chimed turn, i.e. the very class 2A exists to close. `PARTIAL_FAILURE_TERMINALS` (one ordinal + label bearing line per family) render from attempt 4 off a 30 s-windowed counter on its OWN `session.partialFailureRepeats` namespace (never `refusedOps`, whose keys drive plan B's rotation + ordinals), so attempts 1–3 stay byte-identical and past the wrap the ordinal itself makes every render distinct. **Distinctness is STRUCTURAL, because the variant IS the identity's own repeat count** — the cycle-2 mini-review then found the first fix (shared per-family cursor + one-step per-identity skip) unsound: a cursor advanced by every identity gives `X, X, Y, X` → 0, 1, 2, 0, so X's third render byte-repeats its first while still below the terminal, and a one-step skip cannot close that class. Now the Nth render of an identity takes variant N-1 and, once the pool is EXHAUSTED (not a separate constant — a constant can drift below the pool, silencing a variant, or above it, wrapping), the ordinal terminal: same-identity renders can never collide, different identities may share an index but never bytes, and two boards share a key so they share the counter. All 32 interleavings of two identities over 5 renders test-pinned. That "never bytes" guarantee needed one further fix — the cycle-2b mini-review found the key re-deriving the target set in PARALLEL with the renderer and DISAGREEING with it: the grammar gives refs PRECEDENCE (any ref present ⇒ the scope flag never reaches the sentence) while the key appended `|scope` whenever a scope target existed at all, so `[scope, circuit 4]` and `[circuit 4]` — both reachable, `record_reading` staging a circuit target while `set_field_for_all_circuits` stages a scope one into the same aggregate — render IDENTICAL bytes under two keys, two counters, both at repeat 1, second swallowed (ditto `[4, 4]` vs `[4]`, which the descriptor de-duplicates and the key did not). The key is now derived FROM the rendered descriptor (`reason::fieldLabel::subject`), so identical bytes IMPLY an identical key by construction and no future grammar edit can reintroduce the disagreement — one derivation, not two. Deliberately the LABEL not the canonical field (the label is what is spoken) and deliberately NOT `aggregate.key` (which carries the board the sentence never names). One LAST hole in that same class, found by the mini-review OF that fix: identical-bytes-imply-identical-key holds only while every template renders `fieldLabel` RAW, and the `write_failed` terminal opened on `capitaliseFirst(fieldLabel)` — a NON-INJECTIVE transform over a schema carrying a live case-colliding pair (`ring_r2_ohm` → `r2 (ohm)`, `r2_ohm` → `R2 (ohm)`, both circuit fields on the same ring-continuity dictation, so both reachable inside one 30 s window): two identities, two counters, ONE spoken string, second swallowed. Closed by REMOVING the transform (the terminal now leads on the ordinal), not by mirroring it in the key — mirroring would leave the next transform-bearing line free to reopen it — and pinned by a sweep rendering every family's 3 variants + terminal against every case-colliding label pair the schema actually holds. 4 families × 3 byte-distinct variants, singular/plural target grammar, labels from `field_schema.json` only. Headline byte-exact: **"Circuits 7 and 8 weren't found — no Measured Zs (ohm) recorded for them."** SIX previously-silent producers: `record_reading`'s `circuit_not_found` (`wrong_board` deliberately excluded — different class), the `lim_ranged_write_v1` + `low_conf_readback_v1` capability skips (the DEFAULT shape on a dark client), the four ask auto-resolve write failures (new explicit `stagePartialFailureNotice` callback), and `set_field_for_all_circuits`' LIM gate (SCOPE-level target) + `!bucket` miss (exclusion test hoisted above staging). Staging is TRUSTED-LABEL ONLY — schema label + `Number.isInteger(circuit)`, else stage NOTHING (never a raw model string to TTS). Drain (AFTER plan B's coverage arbitration settles — rule (1) reads the arbitrated `allRejected` — and before F7/marker-②, gated `confirmationsEnabled ∧ chimeObserved ∧ !cancelled`): (1) `allRejected` ⇒ suppress; (2) SUBTRACT targets with a surviving same-slot write — the model retries within a turn, and announcing a failure it then fixed is worse than silence — sourced from `projectReadingWinners`, **NEVER a raw `readings` Map scan** (board-ambiguous post-A2), canonical identity BOTH sides (alias retry subtracts; drift-locked to P5's `CLEAR_WIRE_EXEMPT`), subtracting REGARDLESS of `derived:true`; (3) speak the residual AGGREGATED per `(field, reason, board)` so six misses are ONE line. NO ask-coverage suppression — churn circuit-breaker fired at refine round 6; the double-mention is ACCEPTED, dated, test-pinned. Skip-only turns drain and suppress marker-②; `spare_circuit`/`no_rcd` stay designed-silent; capable clients byte-identical. **Bug the tests caught:** rule 3 pushed onto an un-materialised `pendingVoicePrompts`, so the `TypeError` vanished into the net's own fail-open catch and the turn spoke nothing — the very defect the net removes, order-dependently (a session with an earlier prompt worked). 125 new tests (66 + 8 for the cycle-1 third-loop wiring + 8 for the cycle-2 repeat ladder + 31 for the cycle-2-mini-review exhaustive interleave sweep + 2 for the cycle-2b descriptor-keyed identity + 10 for the cycle-2b2 raw-label sweep), 6848 backend green, corpus 9/9. NO recorded fixture BY DESIGN (model-side utterance shape ⇒ synthetic + live lane). **Live probe REQUIRED, PENDING (ep-digest):** the id-112 utterance ⇒ 4 read-backs + ONE notice naming 7 and 8. Web companion: none (dated ledger row `recording/partial-failure-notices`, owner Derek). Follow-on chunks: 2B §3.3 answer resolver (id 104), 2C §3.5 postcode token, 2D §3.6 `KNOWN_FIELDS`. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-30 | **PLAN-1 (feedback-2026-07-27 wave) — dialogue-engine correction paths: value-first ring amends + retained-value negation machine (ids 109/110b), RCD intent-gated entry + narrative veto (id 113), IR volunteer-both voltage ask + exactly-once wire emit (id 105). Backend `src/` only; ZERO wire change — both clients benefit identically.** **Group A:** the ring `awaiting_confirmation` ladder accepted only label-before-value corrections — `"…circuit 13, 0.85 on the lives"` drew 5g's re-read of the EXISTING values, `"0.85 on the lives"` exited engine-silent, `"No. 0.85"` discarded the value. The legacy twin's 2026-05-21 bidirectional design is ported via a two-candidate `namedExtractorCandidates` hook (`helpers/extraction.js` — smaller field↔value gap wins, field-first ties, byte-matching the twin's ff[1]/vf[2] rule; mandatory value-first connector so circuit/way numbers are never mis-classified; non-hook slots byte-identical); the widening reaches every extractor consumer (call-site matrix pinned) and the orphan-net reparse now masks `circuit N` spans via the same shared `maskCircuitSpans`. New retained-value machine: `"No. 0.85"` retains the value (schema's digits-only `pendingValuePattern`) through `handleNegation`'s P1 bookkeeping while asking which slot is wrong; the slot answer applies it via the 5c write path — a two-turn correction; cleared on every exit AND mid-episode continuation; 5g now PRESERVES the per-episode reask latch (resetting it re-opened the feedback-91 byte-identical-repeat silence class). Bare unnamed triples + sentinel words stay model-bound (dated known limitations; `parseOhms` docstring corrected). **Group B:** bare `RCD` anywhere entered the script and hijacked descriptive sentences (incl. the general-condition utterance — a capability-gap mirage). Entry now intent-gated IN the trigger regexes (circuit clause OR enumerated intent term `test|trip|milliseconds|ms|button|x1|x5|times one/five`, both directions, stem-only, no fuzzy) AND NOT the new narrative veto (`no/without RCD protection|RCD absent|not RCD protected` — third exclusion beside the P1 pair, outranks circuit scope, gates entry + 5g re-entry); circuit stays capture group 1 everywhere, `reparseSingleCompleteReading` tightens identically, ICD parity. Fail-forward trade Derek-confirmed: intent-less RCD mentions reach the model, which owns `record_reading` and can ask. **Group C:** volunteer-both-then-circuit-answer finished the IR script silently (the 30 s re-ask was armed only by an ask that was never emitted) AND stranded the drained LL/LE writes un-emitted (inverse Audio-First). Resolution now records span metadata (additive `parseCircuitDigitWithSpan` + `matchedDesignation`), reads the RAW reply, and parses the voltage from the masked remainder — bare `"56"` resolves the circuit and draws the voltage ask (never *"Did you say 56 volts?"*), `"circuit 4, tested at 500"` stays byte-identical; a null parse emits the ask + stamps `voltage_phase_entered_at` (arming the existing re-ask machinery); a `flushWritesOnce` latch on every enumerated exit makes the drained writes reach the wire exactly once (incl. the confirm-gate drain-turn flush for non-standard voltages). Schema-scan pin: exclusive slots == `{ir_test_voltage_v}`. 72-test correction-paths suite + 3 replay-parity value-first scenarios (twin needed ZERO edits; retained-value + RCD table engine-only by design); 6703 backend green, corpus 9/9. **Live probe REQUIRED, PENDING (ep-digest):** Derek's verbatim general-condition sentence ⇒ one `record_board_reading{general_condition}`, one read-back, zero RCD questions. Web companion: none (dated ledger row `recording/dialogue-correction-paths`, owner Derek). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-29 | **Plan E-ship — MAIN EARTH vs Ze prompt precedence ladder + anchored detector aliases, extracted standalone from held PR #126 (backend prompt + `src/extraction/stage6-pending-value.js`; feedback id 100(a), session C06B9904; ZERO wire change — both clients benefit identically).** Derek dictated *"Main earth is 16"* (a conductor CSA in mm²) and the model wrote `earth_loop_impedance_ze` (an impedance in ohms) — two quantities, two certificate boxes. The full plan E (main-earth-steer, 2026-07-25) went CODEX-HELD at execution: its C4 concurrent-state ask-answer routing half (semantic-slot identity, re-injection lifecycle, generation supersession, reservation/rollback) churned across four review cycles while THIS half — the prompt precedence ladder + detector aliases — was review-quiet since round 2. Ships only the review-quiet half, per the held run's own recommendation. **Prompt (one batched edit, shared region, both flag renders):** an explicit PRECEDENCE ladder — "Ze at the board"/"Ze at DB" keeps its `ze_at_db` routing ABOVE bare Ze; a bare-Ze anchor or an ohms unit ⇒ `earth_loop_impedance_ze`; an explicit size anchor ⇒ `earthing_conductor_csa` (bare "conductor" explicitly NOT a size anchor — material/continuity/bonding keep their homes); genuine conflict ⇒ ONE ask naming the unit. Example-8 gains the re-opened-field-choice exception (a reply naming a different field's vocabulary re-maps from the reply's own words). The ladder judges each value by its OWN clause only, requires ONE anchor from EACH family (Ze vs size) for a genuine conflict — never same-family reinforcement like "16 mm² CSA" — and an explicitly-named adjacent field (material/continuity/bonding) always outranks a bare unit token, so an invalid PASS/FAIL value is asked for rather than written or misread as Ze. Caps base 17753 / combined 22991 (+100 over measured, P8 precedent — bumped from the held branch's original 17612/22850 across four Codex diff-review cycles, which restructured the ladder into an explicit 3-tier priority (adjacent field name beats the conflict test beats the ladder) after finding each ordering fix had introduced a new contradiction — the churn circuit-breaker then applied at cycle 4 (two further findings are the exact adversarial phrasing this plan already reserves for Derek's live ear-verification, logged as follow-ups, not patched further)). **Detector:** `DETECTOR_ALIASES` gains the two anchored earth phrases ONLY (`main earth`, `main earthing conductor` → `earthing_conductor_csa`; a bare "earthing conductor" alias stays deliberately absent — first-hit-wins would seize material/continuity) with per-alias `notFollowedBy` exclusions compiled as immediate lookaheads; `ze at the board`/`ze at db` ordered before bare `ze`. Two DECIDED-pin test hunks ported from the held branch's later review cycles as the evidence the two shipped commits are standalone-safe on the tested pendingValue-class dispatcher path (an explicit ohms unit doesn't re-route the detector; a detector-labelled CSA reply never reaches the deterministic value writer there — it falls through to the model, where the prompt ladder resolves it correctly). **Known limitation (pre-existing, not introduced or worsened by this diff — Codex-investigated, confirmed unrelated to any file this diff touches):** an ORDINARY concrete-field ask (e.g. a pending Zs ask) answered on the direct `ask_user_answered` channel does not get this same guard and can still misroute an alternate-field reply — this is the exact gap the C4 re-plan (`sticky-wrong-field-replan`) exists to close. **NOT shipped (stays on held branch `ep/main-earth-steer-2026-07-25-...` / PR #126 for a future re-plan, `sticky-wrong-field-replan`):** the C4 concurrent-state machinery — the shared semantic-slot ask-answer comparison, ordinary-ask overtake/re-injection lifecycle, generation supersession, and reservation/rollback. Zero piece-3/4 code references under `src/` or `config/` (rg-verified: `buildReplySlot`, `isSameSemanticSlot`, `canonicalReadingFieldIdentity`, `reinjectionReservations`, `lastConfirmationsEnabled`, `ordinary_ask_overtake`, `generation_superseded`). 6613 backend green (0 failed, 19 pre-existing skips), corpus 9/9, prompt suite 78/78 (Group 20 pins). **Live probes REQUIRED, recorded PENDING:** groups 1–6 (main-earth positive pair, blocking "Ze is 0.35" regression, "the earth is 16" ambiguity, adversarial precedence, adjacent-field set) need Derek's post-deploy voice verification — never marked complete headlessly; ep-digest entry filed. Web companion: none (dated ledger row `recording/main-earth-steer-prompt-detector`, owner Derek). PR #126 left OPEN with a comment linking this PR as the shipped half. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-29 | **Plan A1b — board-clear CLIENT contract + the `board_clear_v1` advert flip (wave-END plan of the feedback-2026-07-25 batch; backend fence + web + iOS; the A2-multiboard iOS companion shipped first as its own CertMateUnified PR to satisfy the dependency gate).** A1a shipped `clear_board_reading` server-side deny-first with `BOARD_CLEAR_SCOPE_MAP={ze,pfc,manufacturer}` failing CLOSED for every other enum member; this plan ships the CLIENT halves and flips the advert. **Backend:** the capability is read LIVE at DISPATCH via `getActiveSessionEntry(sessionId)?.voiceLatency?.capabilities?.hasBoardClearV1` (never the turn-start ctx snapshot — the reconnect re-parse mutates the sibling entry mid-turn; refine rounds 7–11 RETIRED the deferred-mutation fence for eager mutation + this one live read); the mid-session capability DOWNGRADE is pinned STRUCTURALLY IMPOSSIBLE (web `session_resume` never re-parses — invariant test; iOS re-advertises its static set on its process-private sessionId) with the `stage6.capability_changed_on_reparse` TRIPWIRE (zero expected occurrences) instead of downgrade machinery; A1a's stale enum-re-narrow comment corrected (the enum STAYS 78 — the scope map is the clearable gate). **§2 contract gate:** the committed full field→scope manifest `tests/fixtures/test-contracts/board-clear-scope-keys.json` (sha256 `c3272245e8cc…`) is deep-equal drift-tested against the live map (keys AND scope values) on ALL THREE route-map surfaces — backend, web `BOARD_CLEAR_ROUTE_MAP`, iOS `Stage6BoardClearRouting` — with a byte-identical committed copy in CertMateUnified (LOCAL-only drift test + pinned digest; delivery-time digest comparison recorded in the exec log before the flip). **Web:** the `field_corrected` BOARD frame (circuit:null + non-null `board_id`) now DECODES (previously silently dropped) and routes through the tested map: global `ze` clears ATOMICALLY under both supply aliases + every `boards[].ze` mirror + any retained `board_info` copy (a section-only clear leaves a stale Ze the tabs still render); `pfc` both aliases; `manufacturer` two-leg on canonical-main (`board_info` + `boards[canonicalMainIndex]`), `boards[]`-only on a sub-board, FAIL-CLOSED on an explicit-unmatched id. **iOS:** `Stage6FieldCorrected.circuit` becomes optional with the decode rule circuit-less+`board_id` decodes / scope-less REJECTED (deliberately-rewritten decode test); `handleFieldCorrected` routes the three fields (full-sweep ze incl. every `BoardInfo.ze`; one-leg canonical-main manufacturer — iOS `board_info` is a decode fallback, not a store) via A2-multiboard's tri-state resolution (explicit-unmatched = no-op, never `boards.first`). **A2-multiboard iOS companion (PRE-flip gate work, own PR):** `CanonicalMainBoard` (the three-surface attribution rule), decode-time missing-id provenance (`BoardInfo` missing wire id → empty string), tri-state fail-closed guards on the reading/circuit-update/mark_distribution paths, `CircuitUpdate.from_ref` lossless rekey, djb2 `boardId` fold in BOTH circuit dedupe branches, `buildJobStateForServer` manufacturer, PDF orphan attribution to canonical main, fixture literal re-mirror. Kill-switch `BOARD_CLEAR_DISABLED` remains the rollback — never un-advertising. Ledger row `recording/board-clear-client` `partial` until the wave-end installed-TestFlight-build check. Follow-up `board-clear-scope-map-expansion` logged (grow the map field-by-field under the same gate). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-28 | **Plan A2-multiboard — a per-turn write JOURNAL makes multi-board voice writes DECIDABLE, and every winner is addressed to the board the server actually mutated (backend + web = ONE PR; iOS = a separate CertMateUnified PR riding the wave build; the wire gains only ADDITIVE-OPTIONAL `board_id` keys, omitted exactly where they were omitted before).** A2-core (PR #127) shipped `replaces_cleared` for SINGLE-board jobs and explicitly DEFERRED multi-board delivery. This closes that deferral and the family of same-shaped defects underneath it: **a circuit ref, a field key or a Map key that is unique per BOARD was being treated as unique per JOB.** **Item 9 (foundation)** — an append-only, monotonically-sequenced write JOURNAL (`stage6-per-turn-writes.js`) beside the existing `perTurnWrites` Maps, with ONE staging/removal helper pair every producer routes through (all four `readings.set` producers, both `boardReadings.set` producers, both same-turn clear→write collapses). The raw Map key is board-AMBIGUOUS by construction (`record_reading` omits `board_id` in the common case; `record_board_reading` has none in its schema at all), so plain `Map.set` was LWW by RAW key and silently destroyed the earlier board's write — `select_board A → record manufacturer → select_board B → record manufacturer` read A's value back aloud then vanished it (spoken-but-not-written, Audio-First #1, on a board the second utterance never targeted); removal ran the same hazard in reverse. A journal rather than a re-keyed Map because `perTurnWrites.readings` MUST stay a raw-key Map (the bundler's Map-type guard, its two iteration sites, the harness size check, the speculator snapshot/diff and `decodeReadingKey` all depend on today's semantics); `WRITE_SEQUENCE` is NON-ENUMERABLE per the existing shape-lock contract, so wire bytes are byte-identical. **Item 2** — the bundler projects both Maps LWW per EFFECTIVE slot (field + circuit + effective board), which makes the collision DECIDABLE and therefore makes A2-core's `cands.length > 1` fail-closed-UNFLAGGED branch obsolete: it is REMOVED with the `REPLACES_CLEARED_AMBIGUOUS_PROJECTION` Symbol, its emitter and both call sites (declining to stamp was right while undecidable, but it left web holding the STALE value on a real replacement — the defect the marker exists to prevent). The `length === 1` shape is KEPT so a future duplicate-producing regression fails silent-unflagged rather than stamping arbitrarily. Circuit designations are re-keyed by `(effective_board_id, circuit_ref)` at both build and resolve sites — on the bare ref, the raw-Map last writer's name won for BOTH boards and a sub-board circuit was read back with the MAIN board's name. **Item 1** — surviving the bundler ≠ landing on the right row: a projection-time pass reads the `EFFECTIVE_CIRCUIT_SLOT` marker each winner already carries and stamps `board_id` on the wire reading, DELIBERATELY ASYMMETRIC — FLAGGED readings ALWAYS (the collapse manifest keys on the effective board, so an unenriched flagged reading is unresolvable by a fail-closed client), ORDINARY readings only when the turn spans ≥2 distinct effective boards (single-board traffic stays byte-identical, the speculator's null-board cache entries keep hitting, no latency for a field carrying no information — Audio-First #3). An explicit model `board_id` is never overwritten; enrichment runs BEFORE `synthesiseConfirmations` so a flagged confirmation inherits it; speculation stays safe because `validateAgainstConfirmations` compares TEXT only. Web consumes it via `ensureBoardScopedRow` (exact `(board_id, ref)` → first-come claim on an unscoped legacy row → new scoped row), every branch non-destructive: an unscoped reading is still ref-only, a board web has no INDEPENDENT evidence of falls back to ref-only and logs `apply_circuit_reading_unevidenced_board` (declining is never a skip), and a new scoped row seeds `indexByRef` only when nothing owns the ref, so a scoped sibling can't re-point the ref-only `field_clears`/`circuit_updates` loops. **Item 5** — "which board record IS the main board?" was four hand-rolled `!board_type || board_type === 'main'` copies and, at the seam that mattered, NOT AT ALL: the web Board tab rebuilt `board_info` from `boards[0]` on every persist, but it also ships Move left/right, so index 0 is a UI-ORDERING ARTEFACT — one reorder on `[main, sub]` and the Overview strip, the PDF **and the backend's own `_applyTopLevelBoardInfo` ingest** all start reading the SUB-board as the main board (same defect in `confirmRemove`). One shared `findCanonicalMainBoard` rule now backs both derivations; it is deliberately NOT `getMainBoardId`, whose `boards[0].id` fallthrough is LOAD-BEARING (something must own the legacy bare-numeric namespace on a `[sub, off_peak]` snapshot) — a test pins the divergence. The backend half is a CONSOLIDATION not a fix (said plainly), its value being the LOCK plus one tightening: a retained `existingMain` must itself be ID-BEARING. **Item 6** — an empty-string `board_id` is a REAL shape (legacy CSV writes `''`; the bundler's omit-guard is `!= null`), and the backend had no single answer for what it means: routing helpers used a NULLISH fallback `''` survives (composing the key `'::4'`), `validateBoardScope` rejected it as `wrong_board`, and the calculators let it through as a phantom EXPLICIT target — the same call simultaneously routable and unroutable, decided by which helper ran first. Normalised ONCE at the dispatcher boundary before validation, via shared `isUnscopedBoardId`/`normaliseBoardScopeInput`; the boundary is ENUMERATED, with `select_board`, `clear_board_reading` and `record_board_reading` still REJECTING an injected empty id (for them "unaddressable board" must not become "whatever board is current") and three pins locking the exemption. `loaded-barrel-cache.js` carried the same disagreement internally, leaving legacy unscoped entries unreachable by the very invalidations that stop stale audio being served over a corrected reading. Web's half replaces A2-core's ENVELOPE-WIDE gate with P4b PER-READING row resolution (`resolveFlaggedReplacementRow` DECLINES rather than picks). **Item 3** — circuit-topology ops reach the clients on the board the server actually mutated, LOSSLESSLY: an `EFFECTIVE_OP_BOARD` stamp taken at dispatch (the only place the resolution is known) fixes `select_board sub-b → delete circuit 2` deleting MAIN's circuit 2 on the client, and `legacyShapeCircuitOps` fixes a metadata-free `create_circuit` reaching NEITHER client (the meta fold skips nulls and the carrier was then deleted). **Item 4** — the loaded barrel must not serve audio for a replaced value: the speculator runs BEFORE projection, so an entry for a slot P5 later collapses is parked under the NULL board while the confirmation carries the enriched one, and `validateAgainstConfirmations` compares TEXT ONLY (never board id, never cache key) so it judges the stale entry VALID and leaves it servable for its 15 s TTL — a WRONG-AUDIO path, closed by an INVALIDATE-ONLY `reconcileCollapsedReplacements` pass ordered ahead of the drift check. **Item 7** — the BOARD-scope twin: web consumed the flag in exactly ONE place (the circuit-row path), so a collapsed BOARD replacement arrived bare and every fill-only gate skipped it ("manufacturer recorded as Wylex" spoken, certificate unchanged). A board value lives in TWO places written by two INDEPENDENT loops across three gates, so `planFlaggedBoardReplacements` decides ONCE before either leg runs — local bypasses would let the halves diverge, which is WORSE than either failing cleanly because it looks like it worked. Rules: BOARDLESS ⇒ SECTION-ONLY and that is a SUCCESS (`ze`/`pfc` are `'global'`, so a flagged global reading carries a NULL board BY CONSTRUCTION); SUB-BOARD ⇒ `boards[sub]` only with `board_info` WITHHELD (it is the MAIN-board summary the PDF and the backend's ingest read as "the board"); ORPHAN ⇒ NEITHER leg, logged. Round 2's circuit-0 APPEND/MERGE arms were verified UNREACHABLE on both clear channels and LOCKED AS SUCH by a test rather than implemented. **Item 8** — "All circuits" must be measured against the group's OWN board: the census did `Number(key)` and `Number('garage::3')` is NaN, so every sub-board circuit was skipped outright, and the count it did produce was keyed to the SESSION's board while a confirmation group belongs to whichever board the DISPATCHER resolved — so the one phrasing whose entire job is to assert completeness could assert it against a DIFFERENT board's population, an INAUDIBLE LIE. New `totalCircuitsByBoard` folds both key namespaces in, attributed most-specific-first; every branch FAILS CLOSED (an over-claim is an inaudible lie, an under-claim is a less elegant line) and the scalar's own arithmetic is UNCHANGED. **Item 10** — two boards routinely share circuit refs, so identical spoken lines on main and garage produced the SAME per-circuit dedupe key and the SECOND read-back was silently swallowed (Audio-First #1 in the zero-times direction); `board_id` is now folded into the HASHED STRING (`text + (boardId ?? '')`) rather than added as a key segment, so an ABSENT id hashes byte-identically to the pre-item-10 key and every single-board confirmation keeps the key it has today. Token precedence is deliberately UNCHANGED (the `dedupe_token` IS the operation identity and is already replay-stable across boards; folding would break the §A1a contract), pinned on both mirrors. Regression inventory closed with archive legs 4d-iv (ordinary-write LWW dedup — the dedup is a property of the PROJECTION, not the marker; the reverse dictation order proves SEQUENCE decides) and 4e (`apply_circuit_reading_lim_ambiguous_ref_skipped` was the one telemetry event asserted by no test); web's 4d-iv consumer leg is a PAIR whose second half is a CHARACTERISATION LOCK proving the backend dedup is LOAD-BEARING. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-28 | **Plan A2 — `replaces_cleared`: stop web silently dropping a spoken replacement reading (backend + shared-types + web; iOS is a TEST-ONLY pin in CertMateUnified; ADDITIVE-OPTIONAL wire field, NO capability gate).** The marker is **omitted when false**, so every pre-A2 turn is byte-identical and only a COLLAPSED turn carries the one new key — that, plus *verified* unknown-key tolerance on every existing consumer (iOS `ExtractedReading` declares explicit `CodingKeys` so `Codable` ignores it; web `JSON.parse`s permissively; the replay oracles project readings down to circuit/field/value), is what stands in for a capability gate. It is NOT literally "zero wire-shape change" — a collapsed turn does hand an older client a key it has never seen. P5 (2026-07-23) collapses a same-turn `clear_reading` + `record_reading` for one circuit slot server-side, so web receives a BARE write against a cell it still believes the user owns — and `applyCircuitReadings`' 3-tier gate is fill-only AND source-agnostic, so it SKIPPED it. The assistant SPOKE the replacement and backend + iOS stored it; web alone kept the stale value (the INVERSE Audio-First violation — spoken-but-not-written). **This corrects P5's own changelog claim** that both clients saw a coherent stream / needed no web companion (struck through in all three docs, epistemic split preserved: the gate defect is source-confirmed, the stale-WRONG-value-vs-blank-cell field symptom is INFERENCE — no web capture of that turn exists). **Backend:** the bundler indexes each projected reading as a `replaces_cleared` candidate keyed on the SAME slot identity the P5 collapse matches on and SPLIT the same way (`EFFECTIVE_CIRCUIT_SLOT`-stamped vs Symbol-less raw-key), so a stamp can only ever land on a write the collapse predicate itself considered; on a collapse it stamps the sole candidate. Derived/mirror writes are EXCLUDED from candidacy (designed-silent computed consequences, never the audible replacement); **calculator `::calc::` writes REMAIN candidates** — since F/U-1 (2026-07-19) a calc result is explicitly-requested and spoken, so clear→calculate carries identical exposure to clear→dictate. >1 candidate ⇒ **fail-closed-UNFLAGGED**: stamp NOTHING (web behaves exactly as today) and record the slot on a non-enumerable `REPLACES_CLEARED_AMBIGUOUS_PROJECTION` Symbol the harness turns into `stage6.replaces_cleared_ambiguous_projection` on BOTH lanes — the only observability a silent-by-design branch can have; the bundler itself stays PURE. **New named egress seam `projectExtractionResultForWire`** (`sonnet-stream.js`): the raw-result→wire projection was an inline destructure-spread duplicated per egress site, so a wire-contract test had to re-implement it and could pass while production drifted; both surviving live sites now call the one hoisted spread — deliberately NEVER an allowlist, which would silently drop the very `field_corrections`/`cleared_readings` frames the P5 machinery depends on the moment a key is added upstream. **Web:** decodes the marker and bypasses the fill-only gate ONLY when board cardinality ≤ 1 — web's apply is REF-ONLY (`circuit_ref` carries no board scope), so on a multi-board job two boards' "circuit 1" are indistinguishable and an overwrite could clobber the WRONG board. Cardinality is the UNION of the job's board registry + board ids on existing rows + board ids on EVERY reading in the envelope + every board id NAMED by a `board_ops` entry (that reading term is what makes an incoming reading for an unknown board defer rather than guess). Three further terms come from the Codex review, all the same wrong-board class reached by different doors: `board_ops` rides the SAME envelope but the caller applies it AFTER the readings (`onBoardOps` fires after `onExtraction`), so at apply time `job.boards` does not yet reflect a board this very turn is creating — an `add_board` therefore declines outright (an op can create a board without naming a usable id); and a board id the server NAMES this turn can never vouch for ITSELF, so only web's own `boards[]` + the scopes of its EXISTING rows count as independent evidence and anything naming a board outside that set declines — whether that is an op (a bare `select_board` onto an unknown id) or the flagged READING's own `board_id` (an envelope carrying no ops at all). Both read as cardinality 1 on count alone, and both mean web's registry is out of sync with the server's, so the single flat "circuit 1" row may not be the one the server means. That third door needed ONE narrowing (Codex cycle 4), or it would have re-opened the very defect A2 closes on the commonest job shape there is: when web has NO board identity of its own at all — no `boards[]`, every row unscoped, i.e. the legacy flat single-board job — the backend has SYNTHESISED its default main board and stamps that literal id (`DEFAULT_MAIN_BOARD_ID`, `'main'`) on the reading, which web has never seen and would read as an unknown board, decline, and keep the stale value. That one id is seeded as independent evidence **only** when web's own evidence is empty (a job that HAS a registry naming something else is a real mismatch and still defers), and it is seeded into the independence snapshot and NOT the cardinality set — so it can never inflate the count: a `sub-1` on the same legacy job still declines, and a `main` + `sub-1` envelope still declines on cardinality. It is a hand-mirrored literal (web cannot import from `src/`), pinned by a test. A FOURTH door (Codex cycle 5) is the same class from the opposite side: web's own `applyBoardOpsToJob` appends an `add_board` board WITHOUT materialising the implicit main its existing flat rows belong to (the backend does synthesise one), so the turn AFTER "add the garage board" leaves `boards: [sub-1]` beside UNSCOPED main-board rows — cardinality 1 — and a write that OMITS `board_id` (which the backend does whenever the model relies on the current board) carries no id for the reading term to catch either, so the bypass would have put SUB's reading into MAIN's circuit 1. A registry SUB board that NO existing row is scoped to, while unscoped rows exist, now defers (`implicit_unregistered_board`). That fourth door needed its own narrowing (Codex cycle 6), for exactly the cycle-4 reason: the Board tab synthesises a sole `boards[0]` from legacy `board_info` (`board_type: 'main'`, a fresh uuid) and persists it on any edit WITHOUT scoping the existing circuits, while the Circuits tab deliberately renders those unscoped legacy rows under the selected board — so a genuinely single-board job routinely holds a registry board that owns no row, and declining it would have kept the stale value and reopened the defect. Only a CHILD board fires the term, because only a child implies a parent scope nothing in the envelope names; a TOP-LEVEL board that owns no row owns the unscoped ones. `BoardType` is a CLOSED union (`main | sub_distribution | sub_main | off_peak`), so the split is exhaustive rather than a guess — and Codex cycle 7 caught it landing on the wrong side of that union: `off_peak` is a SIBLING of main (both fed straight from the supply mains; the Board tab clears `parent_board_id` for both and the hierarchy validator accepts one of each side by side), so a legacy flat job whose sole board the user relabelled "Off-Peak Board" was being declined exactly as the main-typed shape had been. Top-level is now type `'main'`, `'off_peak'`, **or absent** (absent mirrors the backend's own predicate at `stage6-multi-board-shape.js:54` — legacy records carry no type), plus a declared `parent_board_id` as independent proof of a child whatever the type says. A table-driven test pins every member of the union so a new member fails loudly instead of silently inheriting a side. `add_board` defaults to `board_type: 'sub_distribution'`, so the cycle-5 close is untouched. **A FIFTH door (Codex cycle 8) is that whole registry test being necessary but not sufficient, and the missing evidence is the ROWS.** It only fires on a board owning NO row, so a child board owning SOME row — just not the target one — walks past it while unscoped rows sit beside it: two scopes, cardinality 1, bypass, wrong-board write. Worse, the type reasoning cannot even see the commonest producer of that shape, because `applyAddNewBoardMode` deliberately leaves `board_type` UNSET for the inspector to fill in on the Board tab, so a CCU-appended SUB board reads as top-level and `isSub` is false. Both are one defect: a job whose ROWS carry more than one scope cannot be resolved by bare `circuit_ref` whatever the registry claims, so scope count is the distinct row `board_id`s plus one if any row is unscoped, and >1 defers (`mixed_row_scopes`, logged separately so a routine mixed-scope job is distinguishable from the rarer unmaterialised-parent bug). It is deliberately BLIND to which board is which: `append_rail` (new rows scoped to the SAME board the unscoped legacy rows belong to — safe) and `add_new_board` (a DIFFERENT board — a wrong-board write) produce byte-identical mixed scoping, so it declines both. That costs a stale value on an `append_rail` job — the pre-A2 status quo, recoverable by re-dictating — where guessing costs a silent overwrite of a cell the inspector never spoke about, which no read-back can catch by ear. The row term also subsumes the ambiguous CCU `off_peak`-append shape noted at cycle 7, which now defers on its rows rather than bypassing on its type. **Known residual, owned by the deferred multi-board plan:** if web has NO board evidence at all while the backend is silently on a non-default board and omits `board_id`, nothing in the envelope can reveal it — closing that needs the server to state the effective board, which is that plan's job. **Every declined case FALLS THROUGH to the unchanged gate — never a new skip** (a fail-closed skip would manufacture a fresh spoken-but-not-written case); the three declines (`_multiboard_deferred` / `_orphan_ref` / `_duplicate_ref`) are observability-only. **iOS genuinely needs no change and gets none** — `ExtractedReading` uses explicit `CodingKeys`, so the key decodes inertly; that is now PINNED rather than assumed by `DeepgramRecordingViewModelReplacesClearedTests` (CertMateUnified `7f022e7`, test-only, rides the next wave build; iOS suite 1530/0). Both client tests assert against the SAME backend-egress fixture (`tests/fixtures/test-contracts/replaces-cleared-circuit.json`, produced by the REAL harness→validator→projection chain): web IMPORTS it, so it cannot drift; the iOS test embeds a hand-mirrored copy of the payload, which CAN drift with nothing detecting it (follow-up: generate the Swift literal or hash-compare it in CI). 6457 backend green; 1510 web green (new consumer suite RED-proven 12/15 → 38/38); corpus 9/9. Multi-board delivery DEFERRED to the A2-multiboard hardening plan (ledger row `recording/replaces-cleared-write`, owner Derek). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-28 | **Plan B — honest refusal: structural impossibilities draw a dispatcher-authored refusal, never a "say it again" apology (backend-only; feedback id 101 secondary / session C06B9904; ZERO wire change — refusals ride the existing field-nil channel both clients render).** The repro was five rejected `clear_reading` attempts on `earth_loop_impedance_ze` (`field_not_clearable` — NOT unknown-tool), each drawing a generic retry-inviting apology: the A3 orphan net classifies an all-error tool set as `allRejected` and speaks `REJECTED_PROMPTS` before marker-② ever evaluates, so any refusal branch living only at marker-② was structurally UNREACHABLE. Fix generalises A1a's `mandatoryNotices` seam — **never infer refusals from error envelopes at the net**: the rejecting dispatcher stages the refusal at dispatch time with CALL-level coverage (`coveredToolCallIds`), and a coverage arbitration BESIDE the A3 classification (independent of `VOICE_ORPHAN_PROMPT`) suppresses the generic prompt ONLY when every rejected id is covered. Recovery-first: `reparseSingleCompleteReading` still writes a structurally complete reading regardless of coverage (refusals drain additively beside the read-back); PARTIAL coverage → A3 owns the whole set (covered notices stamped `drain:false` — one generic line, never refusal+generic); FULL coverage also suppresses the ENTIRE orphan branch incl. `session.orphanContext` (which would have reinjected the honestly-refused request next turn as "not understood" — the exact loop this plan kills). NEW shared `src/extraction/refusal-notices.js` (dependency-downstream-only; A1a's five families + djb2 rotation moved VERBATIM with a compat re-export, byte-parity pinned). `dispatchClearReading`'s `field_not_clearable` now classifies THREE ways: **board-coverable** (canonical-vs-canonical against `CLEAR_BOARD_READING_FIELD_ENUM`'s canonical image — the enum's own members are mixed-spelling, so raw `earth_loop_impedance_ze`/`prospective_fault_current` AND canonical `ze`/`pfc` all route) → BRIDGE to the newly-extracted pure `classifyBoardClear` (dark/kill-switched/EICR/unclassified ⇒ that A1a family with BRIDGE-pool wording + coverage — the headline "Delete Ze" finally gets an honest denial; CAPABLE ⇒ `wrong_tool_clear` on the SAME scope-conditioned `boardSlotKey` a `clear_board_reading` success stamps, reconciled away at net-0 by a same-slot success OR already-empty outcome, both dispatch orders; the bridge NEVER mutates); **known circuit field** ∈ `CIRCUIT_FIELD_ENUM − CLEAR_READING_FIELD_ENUM` ⇒ `unsupported_clear` (label only from `field_schema.json`, slot discriminator — circuit + injective board ORDINAL, never designation — embedded in the rendered string); **off-schema** ⇒ the `model_contract` off-schema route (no trusted label; the raw string NEVER renders — leak safety). Unknown-tool calls (BOTH envelope shapes — composer string-form + `createWriteDispatcher` object-form, via a bound staging callback at both harness composition sites; an `unknown_tool`-errored `ask_user` now COUNTS as rejected in `allRejected`) ⇒ the non-blaming internal-snag family — a hallucinated tool is a protocol error, never "can't do that". Wording contract: every B-staged family/route has its OWN ≥3 byte-distinct pool + an ordinal-bearing TERMINAL from attempt 3 (per-slot `session.refusedOps`; count advances once per ACCEPTED family+slot notice — coalesced duplicates only append coverage; 30 s prune matches the client dedupe window; `model_contract::unknown_tool` / `::offschema_clear` keep SEPARATE ordinals); A1a DIRECT denials keep exact rotation byte-identically for attempts 1–5 and gain a per-family TRUTHFUL terminal from attempt 6 (the one sanctioned A1a rendering change — closes the latent wrap-to-identical-string silence; family-wide session-lifetime `_mandatoryNoticeDirectCount`, ordinal worded as a family count with the label carried separately). The whole rendered inventory (`renderedNoticeInventory`) joins BOTH client-dedupe distinctness sweeps. Recoverable rejections (`circuit_not_found`, ranges…) stage NOTHING — today's paths byte-identical (fail-audible). 30 new tests incl. a separate `VOICE_ORPHAN_PROMPT=false` integration file; 6389 backend green; corpus green as the general gate — NO recorded fixture BY DESIGN (the C06B9904 "Delete Ze" replay is A1a's and its outcome legitimately changes at A1b; a B fixture lands when a real captured turn contains a B-family refusal). Web companion: none (dated ledger row `recording/honest-refusal-notices`, owner Derek). Known pre-existing follow-up (declined widening, logged): A3's `REJECTED_PROMPTS` is a 3-string rotation that can wrap byte-identical inside 30 s for UNCOVERED rejections. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-28 | **Plan F — dialogue-script entry accepts a LEADING circuit + a cross-utterance delete is never swallowed (backend-only; feedback ids 98 + 93, session 2D8E432D; ZERO wire change — both clients benefit identically).** **id 98:** "Circuit 10, ring continuity lives are 0.61…" entered UNSCOPED (every entry pattern captured `circuit N` only AFTER the trigger) and asked "Which circuit…?". NEW clause-start leading-circuit patterns placed FIRST in all four pattern files (live ring + IR schemas, both legacy test twins) — `(?:^|[.?!][ \t]+)` horizontal-whitespace anchor, `circuit[ \t]*(\d{1,3})`, ≤20-char clause-bounded gap to each FILE'S OWN trigger vocabulary (per-file garble divergences preserved: schema `insurance` vs twin `international`, twin ring-only terse); circuit stays capture group 1 everywhere. All SIX `detect*` readers reworked first-match → COLLECT-across-all-patterns returning `{matched, circuit_ref, scope_conflict}` — with leading patterns first, a first-match reader would silently pick the leading circuit on "Circuit 10, ring continuity for circuit 13", the exact silent-winner class. Conflict branches on EVERY path (initial entry, active-episode different-entry, AND `awaiting_confirmation` — its 5a preflight filters out the CURRENT circuit, so `{current, other}` used to silently switch): skip the designation fallback, queue volunteered values on an UNRESOLVED episode (they drain onto the `which_circuit` answer), emit the existing ask; confirmation-mode purges the stale confirm prompt first (Audio-First purge contract). Circuit-span MASKING before extraction on every conflict path, engine + BOTH twins (NEW `maskCircuitSpans` in the IR twin) — "Circuit 5, ring continuity earths for circuit 3 are 1.19" would otherwise capture 3 as the CPC value, parity-invisibly. **id 93:** "delete" then "recontinuity readings for circuit 13." 6 s apart as two STT finals — the entry guard tests only the trigger utterance (P1 closed the same-utterance case only), so the delete was discarded and the script asked its confirmation instead of the model reaching `clear_reading`. sonnet-stream now OWNS a one-shot destructive token: arrival stamped ONCE per accepted transcript (enumerable server-owned Symbol — survives the `{...msg}` queue/requeue spreads, never client-suppliable); ARM at the model-commit seam immediately before `runShadowHarness`, after every pending-ask early-return (an ask-answered "delete" arms NOTHING; `{handled:true, fallthrough:true}` script exits DO arm — the exact repro shape); CONSUME exactly once at each terminal disposition (gate-block/ask-resolution returns, or post-dequeue BEFORE the three wrappers — never at arrival: delete + trigger both queued behind an in-flight extraction is the round-4 FIFO case); window = ORDERED ARRIVAL DELTA ≤ `CROSS_UTTERANCE_DESTRUCTIVE_WINDOW_MS` 12 000 ms (the repro gap is 6 s — the 5 s same-text veto constant would fail the reported bug; consumption-time `now` banned so queue latency can't expire a live token). One `suppressDestructiveEntry` verdict reaches ALL THREE wrapper calls; the engine skips ENTRY for schemas carrying an `entryExclusionPattern`. The live IR schema GAINS the ring-shape destructive-verbs-only `entryExclusionPattern` (same-utterance parity). P1's contracts + live probes byte-unregressed; twins deliberately get NO cross-utterance machinery (engine-only test class). Verification lane: dialogue-engine unit (39) + replay parity (+5 scenarios) + REAL sonnet-stream ingress (12, incl. the FIFO both-queued suppression, the re-stamp discriminator, and byte-exact Symbol survival through queue + `_drainedRetry` requeue) — the field-replay corpus CANNOT observe dialogue ingress (`dialogue_answer_ingress` exclusion); corpus still green 9/9 as the general gate. 6359 backend green. Web companion: none (zero wire change; dated ledger note, owner Derek). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-27 | **iOS misheard-clarification net retired (wave plan C; feedback ids 96(a) + 97-stacking, session 2D8E432D; iOS-only — CertMateUnified PR #38 + this docs companion).** The client-side 6.0 s "did I mishear" heuristic is deleted: it raced a variable-latency multi-round server (already widened once; the bug returned) and stacked a second uncoordinated "say it again" on top of the backend orphan apology, mis-read as a disconnect. Every net-eligible utterance (decimal ⇒ digit ⇒ gate-forward) is already delivered to the server audibility nets (marker-①/②, F7, P4), which own the reply — pinned by a RED→GREEN discriminating iOS test. Shared reconnect-suppression state KEPT (helper renamed `armPostReconnectGrace`; two-fresh-VM regression pin). ACCEPTED dated regression (owner: client chime-silence watchdog, PLAN-C/P4d lineage): a lost server response on a decimal electrical utterance is now silent instead of drawing a usually-wrong prompt. Web never had the net — deliberate negative-parity ledger row `recording/misheard-clarification-net` (do NOT port). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-27 | **Plan A1a — `clear_board_reading`: the board/supply-scope clear the model never had (backend-only; SHIPPED MUTATION-DARK — deny-first `board_clear_v1` capability gate at the DISPATCHER, kill-switch `BOARD_CLEAR_DISABLED`; feedback id 101 / session C06B9904).** "Delete Ze" ×3 drew only "say that again" apologies — `clear_reading` is circuit-scope-only, so a board/supply clear had NO possible tool and the bogus `ze` (the id-100(b) divide) persisted into the certificate: data integrity, not UX. New tool `clear_board_reading{field, reason}` (NO board_id — `select_board`-then-clear, the write path's two-step); enum DERIVED at load (`BOARD_FIELD_ENUM` 84 − 5 structural keys − the `earth_loop_impedance_ze` wire-alias dupe = **78**, literal pinned ONLY in the test → a new schema key fails closed). Tool advertised UNCONDITIONALLY (prompt headings now TOOLS (17)/(19); the prompt latches before capabilities parse, so a capability-conditional toolset is impossible) — enforcement is `denyBoardClear` at the dispatcher (P3 `denyLim` shape), threaded to BOTH dispatcher lanes (a lane-asymmetric capability would diverge the shadow clone). Every denied/refused/already-empty/unknown-scope outcome is a SOFT SKIP + a **mandatory notice on NEW net 0 (`perTurnWrites.mandatoryNotices`)** — an ADDITIVE channel drained before F7/marker-② so a MIXED turn speaks the notice ALONGSIDE a sibling read-back (`voiceNotices`' whole-turn fallback gate would drop it — the round-11 BLOCKER); five rotating families on a per-session STRICT MONOTONIC CYCLE (djb2-per-turn collides; identical repeats client-dedupe within 30 s — how attempts 2/3 would go silent). Clears sweep the field's WHOLE alias set by SCOPE (write path stores RAW spellings; storage bucket depends on `currentBoardId` at write time): global `ze`/`pfc` sweep `circuits[0]` + every `boards[i]` + every alias; board-scoped `manufacturer` clears one record; unclassified fields fail closed pending A1b. ONE `EFFECTIVE_BOARD_SLOT` identity drives mechanism A (dispatcher write-delete), mechanism B (bundler clear→write collapse — P5's twin; deferring recreates marker-T10 at board scope) and the #31 gate re-source (bare-field membership silently ate a legitimate cross-board clear's speech). Wire: `field_corrected{circuit:null, board_id, reason:'clear_reading'}`, POST-canonicalisation name (PFC rides as `pfc`). §3.4c: ONE ordered frame LEDGER (extraction → current_board_changed → observation_update → field_corrected → VCR) for BOTH immediate send and reconnect flush, Symbol-backed FRAME-level cursor → suffix-only replay, VCR exactly once (the flush previously never re-sent standalone `field_corrected` frames — a clear during socket loss left the client stale). Corpus fixture `frc_e94d9854…` ("Delete Ze" → "Ze cleared" exactly once; `purpose: triage` — on unfixed main the failure is `unknown_tool`, a different class, so no honest RED proof exists); validate 9/9, corpus 9/9; ~65 new tests, 6300+ backend green. Infra from source (`BOARD_CLEAR_DISABLED=false` + replay pin). Ledger row `recording/board-clear-denial-notice` (partial, owner Derek — web hears the NEW denial notice via the existing field-nil channel, zero web code). A1b owns the client sweep + advert flip, AFTER A2-multiboard. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-27 | **Stale Flux docs corrected + WS4 default-flip completed (docs + web-only; rides the freeze-lift PR).** Hub CLAUDE.md (2 places), `architecture.md`, and `ios-pipeline.md` claimed web was still on Nova-3 "until WS4" — web has run Deepgram Flux in production since 2026-07-03 (`ff620997`, task-def `DEEPGRAM_STT_MODEL=flux`, an emergency flip fixing nova-3's partial-sentence sends). That flip changed ONLY the task-def and missed the code half its own design specified: `DEFAULT_STT_MODEL` in `web/src/lib/runtime-config.ts` stayed `'nova3'`, so a dropped/blank env var would have silently reverted web to nova-3 (reintroducing the partial-sentence bug) with zero diagnostic. `DEFAULT_STT_MODEL` now `'flux'` (missing-env branch matches the shipped default); `SAFE_STT_MODEL` deliberately stays `'nova3'` — the kill-switch's fail direction on fetch failure/unrecognised value, and those paths log loudly. Ledger row `recording/flux-migration` de-staled (Flux IS live; remaining `partial` = final curated iOS keyterm list + device ear-check). Zero prod behaviour change (the env var is set explicitly). |
| 2026-07-25 | **Plan D — the read-back must speak the value that was STORED: the impedance clamp becomes SERVER-AUTHORITATIVE (backend + tests + corpus fixture; feedback id 100(b) / session C06B9904; SAFETY-CRITICAL, highest in the 2026-07-25 wave). Backend cross-platform mandate GRANTED (Derek 2026-07-25).** Deepgram Flux drops decimal points, so both clients carried a `clampImpedance` that divided an out-of-band impedance by 10 then 100 — CLIENT-side, AFTER the server had already spoken. On a TN-C-S installation Derek dictated "Main earth is 16 now."; the server wrote the raw `16` and synthesised "Ze 16" from it, the client stored `1.6`. The inspector heard a value 10× the one in the certificate — undetectable by ear, the exact harm Audio-First #1 exists to prevent. Fix: ONE authoritative actor. New `src/extraction/impedance-clamp.js` clamps at every **pre-write** seam (`coerce → clamp → validate`) so the snapshot, the emitted `readings` entry and the spoken confirmation all come from the SAME clamped number, and the correction is **named aloud** ("Ze recorded as 1.6 — I corrected 16 to 1.6") so a WRONG correction is catchable by ear. **A hand-maintained JS mirror, not an import** — `packages/shared-utils` publishes raw TS with no build output, the root manifest doesn't depend on it, Jest runs `transform: {}`, and `Dockerfile.backend` never copies `packages/`; follows the `ios-dedupe-key.js` four-way-mirror precedent with a reciprocal drift test. **Bands:** `ze`/`earth_loop_impedance_ze`/`ze_at_db`/`zs_at_db` → `[0.01,5]` Ω, widening to `[0.01,200]` on TT; `r1_r2_ohm`/`r2_ohm`/`ring_r1|rn|r2_ohm` → `[0.01,2]` Ω, earthing-independent. **`measured_zs_ohm` DELIBERATELY EXCLUDED** (the range gate allows 100 Ω — a long final circuit legitimately reads tens of ohms); PR 2 removes it from web's map too, closing a pre-existing silent-divide defect. **Earthing FAILS SAFE:** `resolveBoardAwareEarthing` reuses the `resolveBoardAwareZe` ladder (target board record → `circuits[0]` origin supply), scalars only, **TT wins across sources** (the TT band is a superset — believing TT can only leave a value alone, believing not-TT can divide a good rod-earth reading); an UNRESOLVED arrangement never divides a `ze`; the supply-merge ingest was widened so the arrangement is seeded from turn 1 even with no `job_state_update`. **Same-round ordering (Codex lens 3):** the clamp reads the COMMITTED snapshot and `runToolLoop` dispatches a round's records sequentially, so "Ze is 16 on a TN-C-S system" (Ze emitted first, in utterance order) would decline to divide while the reverse phrasing clamped — the default `sortRecords` hook is now a THREE-way stable partition (`record_board_reading{earthing_arrangement}` → other writes → `ask_user`), mirrored in the loop's emergency sort fallback (predicate INLINED — the loop must not depend on the dispatcher module) and pinned by a drift-lock test. The hoist is BOUNDED BY BOARD CONTEXT (Codex mini-review): `add_board`/`select_board` mutate `snapshot.currentBoardId` and `record_board_reading` carries no `board_id`, so those two pin in place as SEGMENT BOUNDARIES and the partition applies within each segment — an unbounded hoist would stamp "add the garage board, earthing is TT, Ze is 16"'s `TT` on the ORIGIN supply. `out_of_range` writes unchanged with nothing spoken — no new rejection path (Audio-First #2). **Correction transport is a Symbol** (`IMPEDANCE_CLAMP_CORRECTION` on the `perTurnWrites` entry — invisible to `JSON.stringify`, so it reaches the synthesiser and cannot leak to a client even via a new wire boundary); all **four** independent confirmation-text producers (bundler, Loaded Barrel speculator, `voice-latency-fast-tts.js`, dialogue engine) render the clause byte-identically through the one shared `buildConfirmationText`. **Every seam patched incl. the dispatcher-bypassing ones:** 3 dispatcher pre-write seams, both speculator entry points, orphan recovery, the fast-TTS route (lowest-latency ⇒ most-often-HEARD, never reaches a dispatcher), and dialogue Seams A/B/C **plus Seam D** — `dispatchStartDialogueScript`'s `perTurnWrites` backfill (Codex full review) built a FRESH entry with no Symbol, so the bundler read a clamped seed back with a BARE line; stored and spoken still agreed (the safety invariant held) but the read-back that lands FIRST did not name the correction, which is the whole point of naming it. It now CONSUMEs the engine's correction ledger — consuming is what keeps the clause exactly-once across both orderings. That consume is itself GATED on deliverability (Codex cycle 3): `safeSend` silently returns on a closed socket and reports no success signal, so an unconditional consume emptied the ledger on a turn that spoke NOTHING and the order-5g re-emit then read the clamped value back BARE — a `canDeliver` predicate mirroring `safeSend`'s own gate leaves the clause owed until something can actually speak it. The speculator's SAME-ROUND ordering gap (stream-time snapshot ⇒ unclamped speculation) is a documented SAFE MISS, not a bug: `validateAgainstConfirmations` invalidates on the clause-bearing text mismatch, `keys.js` MISSes, the correct line is synthesised fresh, and the unclamped number is never audible — pinned by a test because that safety depends on the clause being part of the TEXT IDENTITY. **`server_impedance_clamp` on `session_ack`** — additive top-level field (`speech_epochs` precedent, ZERO wire-SHAPE change; an older client keeps its own clamp and stays self-consistent), advertised in `live` `STAGE6_TOOL_CALLS_MODE` **only** so the documented mode rollback can't leave a client trusting an unclamped server. Telemetry `stage6.impedance_clamp_applied` (per-seam, before/after values logged deliberately — a row without the numbers can't audit a suspect correction); dialogue Seams A/B don't emit it yet (no logger param — bounded follow-up). Recorded fixture `frc_51be8bec…` RED-proven `reading.op_ze_clamped` → `required_green`, asserting BOTH halves (clamped literal `1.6` written AND the spoken line byte-exact — a "contains 1.6" matcher would tolerate the very drift under lock). 6196 backend green; corpus 8/8. Web companion = PR 2 (`measured_zs_ohm` removal + the clamp becoming an inert no-op); iOS (`session_ack` decode + capability latch + ownership-aware speak) sequenced with wave plans C and G. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-24 | **P8 — batched model-decision prompt steers: garble-adjacent value writes (id 88) + observation C-coding deciding-facts checklist (id 83). Backend PROMPT-ONLY (`config/prompts/sonnet_agentic_system.md`), ONE batched edit, ZERO wire change; both clients benefit. LAST in the feedback-2026-07-22 wave — probes measure RESIDUAL model behaviour.** Model-decision bugs → recorded-lane fixtures can't lock them; verification is LIVE probes / the nightly live lane. **Steer 1 (id 88, folded into Example 8):** a field anchor + UNAMBIGUOUS circuit/board scope + a schema-valid value → WRITE even when a garble ("n o", a stray mid-utterance "no", filler) sits between anchor and value — a mid-utterance garble is not a negation ("Zs on the cooker is n o 0.55" → `record_reading`, not a `missing_value` ask). Preserves missing-scope/contradiction/invalid-range handling; guards survive — a leading-"no" reply with no in-utterance anchor+scope ("No. It was 0.63") stays a correction, and a "no"+value reply to a pending `ask_user` resolves that ask. Value-less Example-8 ask (marker-② steer) NOT weakened. **Steer 2 (id 83, INSIDE the AMBIGUOUS C2/C3 SEVERITY block — reuses its bounded clarification budget + `clarification_chain_id`):** a compact THREE-class checklist (Derek widened v1 enclosure-holes-only → enclosure penetrations/holes · basic protection at accessories/CUs · main protective bonding); TWO classes share ONE ACCESS LADDER (accessible TO TOUCH → C1; potential access → C2; tool/lockable or no danger → C3), bonding has its OWN tree (for an extraneous part, absent/ineffective or <6mm² → C2; thermal damage independently → C2, or C1 if immediate present danger; sound ≥6mm² no-thermal-damage → **C3 at most, never C2**), a FACT-WEIGHTING cue (weight the deciding fact, ASK it first, never blanket-default), per-class BS 7671 citations verified at execution (416.2.2 top-surface IP4X vs 416.2.1 side IP2X; 411.3.1.2 + 544.1.1/Table 54.8 bonding; 701.415.2 bathroom supplementary / BPG4 Q2.4; 512.2 environmental IP ≠ 416.2). **Codex diff-review fix (cycle 1):** the plan's un-emittable "NC"/"NO observation" outcomes (`record_observation.code` enum is C1/C2/C3/FI only; "NO observation" conflicts with RULE 1a + false-fires the D2 net) were reworked to "C3 at most, never C2" — anti-over-code intent preserved, every explicit observation records a valid code, no silent-drop path. **Budget (binding gate):** net +745/+745 tokens (minimised via the shared ACCESS LADDER + terse prose); a REVIEWED measured cap bump on BOTH `stage6-agentic-prompt.test.js` caps was the EXPECTED path (the ~250 target is structurally unreachable with three cited classes) — base `renderedOn<=16790` (measured 16705), combined `combinedRenderedOn<=22025` (measured 21943), each measured+~100. Steers land in the SHARED region (outside A1 marker blocks) → BOTH flag renders grow (prod is flag-ON); stale "flag-off byte-identical to pre-A1" comments softened in tests + `eicr-extraction-session.js` + `architecture.md`. P4 decline-ack rider DROPPED (P4's net owns it — double-speak risk); P5 "no clear-before-replace" rider OMITTED (contradicts correction rules; P5 covers it). New Group 0b + Group 18 pins assert both flag renders; 89/89 prompt suites + full backend green. Web companion: none (prompt-only, no wire change). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-24 | **P7 — observation dedupe demoted to server-id keying (marker ④; feedback id 82, session 36731498). Client-only cross-platform: iOS (P7-shared TestFlight build) + web (this PR); ZERO backend change.** Spoken-but-not-written (inverse Audio-First): two same-vocabulary observations ("small hole in the SIDE" / "…in the TOP" of the enclosure) — the second backend-created with a DISTINCT `observation_id`, its confirmation SPOKEN, then the client text-similarity dedupe (>0.7 word overlap / 40-char prefix) false-positive-swallowed it (~0.8 overlap) so it never rendered. The backend owns observation identity (stable id + its own BPG4/RULE-6/D2 dedupe), so the client gate wrongly rejected a server-authoritative write. Fix (both clients — iOS `applySonnetObservations`, web `applyObservations`): key dedupe on `observation_id`. A re-seen id is an IDEMPOTENT REPLAY (P4d reconnect preserves ids) → a PURE NO-OP: SKIP creation side-effects (append, pending-photo, `recentObservationId`/reverse-link, `observation_added`) AND schedule projection, and fill NOTHING (filling "absent" fields from the same replayed frame could only restore a field an authoritative `observation_update` cleared to nil — never overwrite a since-refined field; authoritative changes remain `observation_update`'s job); a non-nil id not seen applies; a nil id retains the text gate for id-less rows ONLY. `observation_update` made id-first, scoped-fuzzy (fuzzy only no-`server_id` legacy rows + stamp the id; NEVER a different-id row). Web `applyObservations` returns append/replay metadata so photo/reverse-link gate on a real APPEND and schedule projects from accepted creations/fills, not the generic `changed` flag or the raw frame. iOS diagnostic `observation_apply {observation_id, dedupe_bypassed_reason}` on `client_diagnostic` for one release. iOS 6 apply + 3 update-handler tests + web 10 new (incl. a DISCRIMINATING clear→replay schedule regression) green; RETAINED M9/exact-dedupe nil-id web tests pass unchanged; iOS-canon respected (backend untouched). Parity ledger `recording/observation-id-dedupe` (partial until side-hole/top-hole device smoke). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-24 | **P2 — read-back exactly-once: value-aware confirmation dedupe key (id-84) + iOS fast-path double-read-back fix (id-87). Cross-platform: backend telemetry mirror + web (this PR) + iOS (own TestFlight build); session 2ACE7677.** Two Audio-First #1 violations in OPPOSITE directions in the client confirmation-dedupe machinery. id-84 (correction SWALLOWED → ZERO speaks): the single-circuit reading-confirmation dedupe key was value-LESS (`{field}_{circuit}`) AND session-permanent, so a spoken correction ("Zs 0.83" → "No. It was 0.63.") reached the wire as a different-text confirmation whose key collided with the original forever → the client deduped and spoke nothing. Fix: fold djb2 of the confirmation TEXT into the single-circuit key (`{field}_{circuit}_{djb2(text)}`, matching the multi-circuit branch) so a correction (different text) speaks and a genuine duplicate (same text — the model re-recording circuit 4) still dedupes. Token precedence unchanged; the old "measured fields stay bare for the correction-TTS cross-match" contract is retired (cross-match intentionally dropped — worst case an extra read-back, never silence). Backend `ios-dedupe-key.js` is the TELEMETRY-ONLY mirror (`buildPerCircuitDedupeKey` gains a `text` param) so `expected_dedupe_key` stays byte-truthful — documented backend-immutability exception, ZERO wire change. Derek round-1 decisions: TEXT-HASH (not direct-value), NO new TTL. id-87 (double read-back, iOS-only) + iOS A1-1/Bug A' land in the P2 TestFlight build (fast-path VALUE + effective-board suppression identity, generation-scoped supersession, grouped-confirmation aggregate coordinator, fail-closed canonicaliser; client-initiated `correctionDedupeKey` made value-aware + routed through the §A1b reserve/playback/discard lifecycle). Docstrings on all platforms rewritten (they documented the OPPOSITE). Backend 29/29 + web 32/32 dedupe green. Parity ledger `recording/readback-dedup-value-aware` (partial until device smoke). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-24 | **P6 — backend dictation-transcript normaliser (backend-only; feedback ids 89 + 80A; ZERO wire change).** There was NO backend normalisation of the raw dictation transcript. id 89 ("Z s on the heating was 0.67" took two goes): Flux spelled the earth-loop field token as spaced "Z s", which the "zs"-substring anchor (`reading-transcript-anchor.js`) misses → the reading didn't anchor, the model no-op'd on try 1. id 80A ("A hundred MΩ"): the ARTICLE word-number "a hundred" (iOS/web digit-ise "one hundred"+compounds, not the article form) produced no digit → the megaohms parser found nothing. The prior ingest point (`:3714`) sits AFTER the ask-answer anchor/gate/classifiers and is rebuilt from raw for the `in_response_to` case — so a normaliser there missed exactly id-80A. New pure `src/extraction/transcript-normalise.js` — `normalise(text)→{text,rules_hit[]}` with TWO evidence-backed, ENUMERATED, word-boundary rules (NO fuzzy — §3E ban): `a_hundred` "a hundred"→"100" (compound "a hundred and fifty" left UNTOUCHED — out of scope, no corruption); `zs_field_token` context-gated "Z s"/"Zed s"→"Zs" gated on a reading-shaped same-clause (closed connector/scope set + numeric/sentinel value) so names/designations/postcodes ("Z S Electrical", "designation Z S 1") are NOT collapsed. Rule ORDER is load-bearing (a_hundred FIRST so its digit output satisfies the Zs gate on one pass); single-pass fixed point, idempotent. The unit-garble rule was DROPPED as speculative (units already aliased by `parseBareMegaohmsWithUnit`; megaron in no session/fixture/source). Applied at TWO seams with a raw/canonical split — `msg.text` is NEVER mutated (recorded-corpus fixtures + reverse-race dedupe keys keep the raw garble; no live raw-transcript S3 sink on this path, only `cost_summary.json`). Seam A (top of `handleTranscript`) routes canonical to both content anchors (both sides canonical → cross-seam dedupe holds either order), the gate, BOTH classifyOvertake calls (transcript-overtake stays UN-annotated), detectStructuredReading, `transcriptText`, the three dialogue-script `rawReplyText` args (normalised but UN-annotated), and runShadowHarness; non-string `msg.text` falls back byte-identical. Seam B (`ask_user_answered`) keeps `sanitiseUserText` on RAW user_text and routes `canonicalAnswerText` to the model-facing consumers (shape check/new-command gate/detectStructuredReading/resolvePayload/synthetic-reinjection); the two dedupe-ledger keys (reverse-race lookup + recentAskAnswers anchor push) use one RAW-based `canonicalUserTextForAnchor` matching Seam A; the reinjected synthetic is already canonical so Seam A re-normalises to a no-op. Telemetry `stage6.transcript_normalised {rules_hit, seam}` (rule IDs only, leak-filter); a JSON-invisible Symbol at Seam A dedupes the log across queue/drain/retry re-entries (exactly once). Tests: unit + production-ingress (raw→canonical proof — the direct replay runner bypasses these seams; asserts canonical reaches gate/harness/wrappers, the REAL anchor flips false→true, the REAL megaohms parser parses "100", Seam B routing, paired-frame dedupe both orders, one telemetry row across queue+drain) + a REAL insulation-resistance-engine ingress test (live engine records `ir_live_live_mohm=100` from raw "A hundred megaohms"). Hardened over a Codex diff-review (3-lens cycle 1 + per-fix mini-reviews + cycles 2–3): reading-lead-in Zs gate (newline-safe, name-safe 60-char bound), compound/decimal-safe "a hundred" guard, enumerable telemetry Symbol, raw-based Seam-B dedupe keys, raw hard-length cap preserved on the overtake sanitiser. 6080 backend green. Web companion: none (zero wire change; web rides the same backend ingest). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-24 | **C1 — Sonnet observation-tier model router wired onto the LIVE recording path (backend-only; DARK behind new `OBSERVATION_TIER_ROUTING`, default OFF; the router chunk of the field-feedback-2026-07-16 F5 remainder / marker ⑤).** `runLiveMode` hard-coded `model: SHADOW_MODEL` (= `SONNET_EXTRACT_MODEL` = Haiku in prod) into `runToolLoop`, so ALL observation C1/C2/C3/FI severity coding ran on Haiku — the `callWithRetry` tiered router (`eicr-extraction-session.js:2798`) escalating observation turns to `OBSERVATION_EXTRACT_MODEL` (Sonnet) was DEAD on the live path. Now `runLiveMode` computes `selectedModel` ONCE before the loop, gated on the flag: OFF ⇒ byte-identical to pre-C1; ON ⇒ an observation-shaped RAW utterance routes the WHOLE loop (incl. any blocking `ask_user` continuation) to `OBSERVATION_EXTRACT_MODEL`. Rollback = flip the flag OFF (task-def + CI redeploy), NEVER edit `OBSERVATION_EXTRACT_MODEL`. Classify on RAW `msg.text` (`options.rawInspectorTranscript`), NOT the enriched `transcriptText` (`sonnet-stream.js:3758` prepends `[In response to TTS question…]` → a bare "yes" could false-escalate; server-context isolation); threaded at both non-test `runShadowHarness` callers + the field-replay harness (`session-builder.mjs`) so replay/bench route identically. NEW `allowRound1ModelOverride` on `runToolLoop` locks observation-tier turns against the round-1 `VOICE_LATENCY_ROUND1_MODEL` override (reading turns keep it). ONLY the LIVE call site changes (`:1288`); the `mode==='shadow'` comparison call stays on `SHADOW_MODEL`. Cost flows via `toolLoopOut.model`; PII-safe `stage6.observation_tier_routing` telemetry proves the seam for the post-flip P8 probes. Flip prerequisite: the web observation-processing cue (ledger `recording/observation-processing-cue`; iOS already live) ships first — no numeric latency gate (Derek: masked by cue, not gated). `OBSERVATION_TIER_ROUTING` pinned in `PINNED_FROM_TASK_DEF` + task-def (`false`). The deterministic ambiguity ask-gate (C2) is a SEPARATE spike-first brief, not this plan. 10 new routing tests + replay-parity; 6042 backend green. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-23 | **P4 — answered-ask decline-acknowledgment net (backend-only; feedback id 85 / session 2ACE7677; ZERO wire change).** The assistant asked a clarify question about a reading, the inspector answered "No. Don't worry.", and the session went COMPLETELY SILENT — an answered turn with zero audible output (violates "chime is a promise" / Audio-First #1). marker-② treats the earlier spoken ask as audibility evidence (its `emittedAskToolCallIds.size===0` term is correct for the spoken-but-UNANSWERED class), §D2 is observation-clarify-only, and the F7 pre-emission net covers only the attempted-but-NEVER-emitted ask — so an ANSWERED-then-silent turn slipped every net. New backend net (after marker-②, before the §A4 drain) keyed on `outcome.answered===true`: a NEW resolution-time `onAskAnswered` observer (the ONLY hook carrying the answered outcome — `onAskRegistered`/`onAskUserStarted` fire pre-answer) fired from the dispatcher initial-await + pvr broker feeds a per-turn ask-lifecycle ledger; the net fires when the LAST ask emitted this turn was ANSWERED and zero post-answer speech survived, speaking ONE field-null ack. Keying on the LAST emitted ask makes it mutually exclusive with a later follow-up ask; `timeout`/`user_moved_on` (answered:false) and `srv-*` engine asks (never onAskAnswered'd) are excluded by construction; DEBOUNCED confirmations are DELIBERATELY excluded from the post-answer predicate (removed before crossing an audible channel — suppressing on them repeats the defect); `!isAudibleText(spoken_response)` is the A1 mutual-exclusion term (under `VOICE_AGENTIC_ANSWERS` the model may answer via `answer_user`, making the net a backstop). TWO-variant wording (Derek): affirmative DECLINE family ("No problem, moving on.") for brush-off replies (`classifyDeclineReply` — substring match, not exact-whole-string `CANCEL_PHRASES`, so "No. Don't worry." classifies), plain "Okay."-class ANSWERED family otherwise — string-distinct from every apology family, NEVER a "didn't catch that" apology (the answer was understood). `CANCEL_PHRASES` sub-plan DROPPED (both reviewers): exact-whole-string can't catch the repro, `'leave it'` already present, a cancel returns answered:true + emits nothing (predicate 2 covers it). Oracle (plan Option B): recorded fixture `frc_85ace7677…` declares the original ask_user frame AND the ack (`field_null_fallback`/`text_exact`) via a scoped `fixture-schema.mjs` relaxation (the §A4-drained ack is TOKENLESS — a token-bearing matcher would fail even on FIXED main); RED-proven EXACTLY `audibility.output.out_decline_ack` → `required_green`, corpus 7/7. Web companion = no web code (rides the existing field-nil channel). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-23 | **P3 — accept "LIM" (limitation) as a value for numeric reading fields (cross-platform: backend + shared-utils + web; iOS on its own TestFlight build; feedback id 86 / session 2ACE7677).** The live model wrote `record_reading{measured_zs_ohm,"LIM"}` and validation rejected it (`value_out_of_range`) — Derek: "LIM is a legitimate reading, shortfall limitation." Nine fixes over ONE `NUMERIC_READING_FIELDS` allow-set (six ranged + eight ungated numerics): range-gate LIM acceptance + `validateNumericReadingValue`; four-form coercion (`lim/limb/limp/limitation`, near-matches `limit/limited/lynn/lym` rejected); dialogue-script ingestion (shared `parseLimSlot` + `namedExtractor` alts + kA allow-set + `normaliseDialogueSlotWrite` at 3 drain sites + every broad matcher narrowed + `resolveValueAnswer` broadened to all numeric fields); bulk-path coerce+validate; sentinel-aware identity-derivation guard (`shared-utils/circuit-derivations.ts`, all 3 targets incl. the `ze` param, + iOS mirror); OCPD-rating→max-Zs invalidation via a before/after transition helper (web + iOS; NOT breaking-capacity); enhanced read-back "…recorded as LIM — limitation" via ONE shared `buildValueSpokenTail` (speculator byte-identical; `rcd_operating_current_ma` → "RCD operating current"); NEW `lim_ranged_write_v1` capability gates LIM ACCEPTANCE (backend deny-first: record_reading + bulk + speculator skip until advertised; IR fields excluded — pre-P3 behaviour; web advertises same wave; iOS on its TestFlight build; server kill-switch `LIM_RANGED_WRITE_DISABLED` forces deny even for capable clients — the real rollback); iOS `CircuitsTab` result-status returns `.incomplete` when either Zs operand is non-numeric. DEFERRED (in the /ep execution log): the dialogue active-slot capability gate + dialogue-script write-time LIM read-back — the model-driven Zs paths (the feedback-86 vector) are fully covered. Closed-enum LIM = separate future plan (flagged to Derek). 5931 backend + 1452 web tests green. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-23 | **P5 same-turn clear→write ordering-wipe collapse (backend-only; marker T10 / feedback ids 80B + 81; ZERO wire-SHAPE change).** A dictated "IR L to L 100" on circuit 3 was read back aloud AND written server-side, then SILENTLY un-written on the client — the turn carried both a `clear_reading` and a `record_reading` for the SAME slot, and because the extraction envelope (write) is emitted BEFORE the `field_corrected` frame (clear), the stale clear landed last and wiped the write (server ended with the value, client empty — the INVERSE Audio-First violation). write→clear was ALREADY collapsed for circuit slots by `dispatchClearReading`'s same-turn delete; the MIRROR (clear→final-write) is closed by a PURE projection-time collapse in `bundleToolCallsIntoResult` that drops any `clear_reading` field-correction (+ `cleared_readings` + synthesised "cleared" confirmation) whose circuit slot has a SURVIVING `readings` write. Slot identity `(raw field, circuit, effective_board_id)` is stamped at DISPATCH on a non-enumerable `EFFECTIVE_CIRCUIT_SLOT` marker across all four `readings.set` producers (producer-specific board resolution; never the `'*'` broadcast) + a shared `rawCircuitSlot` fallback; `dispatchClearReading`'s delete became effective-board-aware so a mixed-spelling write→clear can't escape it. Circuit slots ONLY (`boardReadings` excluded — out of P5 scope); matching on RAW keys before A2 (`r2_ohm` exempt); bundler stays PURE (Loaded Barrel append-only); collapse metadata → `stage6.same_turn_clear_write_collapsed` telemetry; spoken output unchanged (#31 untouched). Corpus: new `clear_then_write` fail-closed schema shape + joint oracle (replacement present AND zero same-slot clear) with the A2 field mapping DYNAMICALLY injected (import-graph regression enforces no static `src/extraction` import); recorded fixture `frc_4687948e…` RED-proven `reading.op_ir_ctw` → `required_green`; corpus 6/6. ~~Web companion = no web code (frames dropped/reordered server-side, both clients coherent).~~ **CORRECTED 2026-07-28 by plan A2 — the "both clients coherent" half was WRONG.** Dropping the clear leaves a BARE write against a cell web still holds the OLD value in, and `applyCircuitReadings` is fill-only and source-agnostic (source-confirmed by reading it), so web SKIPPED the replacement the assistant had just spoken. A2 adds the `replaces_cleared` marker + a single-board-gated web bypass; iOS was genuinely unaffected and is now pinned rather than assumed. (That the field-visible symptom was a stale WRONG value rather than a blank cell is INFERENCE from the wire shape — no web capture of that turn exists.) 2529 stage6 + 209 field-replay + 22 collapse-matrix tests green. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-23 | **A1 agentic-voice — the model gets a VOICE: `answer_user` + `inspect_session_state` tools + gate borderline-forward (backend + one web `{force:true}` VCR companion commit; ZERO iOS change; ZERO wire-shape change — answers ride the existing `voice_command_response` frame).** The live model was structurally mute for answers (question turns → marker-① "didn't catch that") and the server gate silently dropped non-matching turns after the client chime. Now: questions about the session are ANSWERED aloud (≤2 sentences, leak-filtered, at-most-once; `inspect_session_state` reads state outside the recent_3 snapshot per the Derek-approved completeness appendix `docs/reference/inspect-session-state-policy.md`); borderline turns forward to the model instead of dropping (`BORDERLINE_FORWARD`, no server question word-lists — the model decides); a failed answer stages a FIXED fallback in BOTH confirmation-toggle states (the feature owns its own audibility; marker nets extended for mutual exclusion — never double-speak). ONE master flag `VOICE_AGENTIC_ANSWERS` (session-latched: prompt render + toolset + gate atomically; flag-off prompt render byte-identical to pre-A1 → shipped DARK in PR 1 with task-def `false`; PR 2 = the flip, the single cache-invalidation event). 5716 backend + 1430 web green; corpus 5/5; ledger row `recording/assistant-answers` (partial until ear-verified probes). Follow-ups: A2 client question lane (fast-follow), A3 mid-script, A4 wake phrase, A5 numeric-gate check. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-22 | **P1 ring-script-hardening — ring-continuity confirmation loop hardened (backend-only; session B4C45F25 feedback 90/91/92, the wave's P1).** Entry guard now per-schema opt-in (`entryExclusionPattern`; RCD verbatim, ring destructive-verbs-only so delete requests reach the model while question-form entries keep working); canonical `awaiting_confirmation` order 0–5h: confirmation-gated delete/clear preflight exits with a FIXED server-note antecedent + raw reply so the MODEL keeps `clear_reading` ownership (both live probes PASSED: 3× clear on entry AND confirm paths); per-episode at-most-once negation re-ask + anchored pending-slot correction machine (`No.` → `R1.` → `0.85`) + negated-positive guard + 2-miss audible cap exit — never a byte-identical repeat into the client 30s dedupe (the feedback-91 loop); masked 5a different-circuit routing with negation polarity + `overwriteVolunteered` closes the wrong-circuit amend corruption (feedback 92) incl. the enumerated `re-?continuity` Flux garble; raw-reply contract (`rawReplyText`) stops the annotated "All correct?" question false-matching `detectPositive`; Audio-First purge contract on every confirmation-abandonment exit (5f positive finish exempt); reading-like replies NEVER consumed by the miss cap. Legacy twin mirrored + 6 new replay byte-parity scenarios; 75-test pinned suite; two-layer delete integration contract; 5630 backend tests passed / 0 failed; corpus 5/5 (Phase-0: no B4C45F25 capture exists — vault follow-ups: corpus boundary to dialogue ingress, iOS queued-alert tool-call-id purge). ZERO new wire SHAPE / zero client-decoder change (existing-shape cancel_pending_tts + ask_user_started frames are emitted on the new paths; server-side for both clients). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-20 | **PLAN-C Phase 4 part 2 (P4d) — the ask/question/voice-command emit-site epoch matrix (backend-only; completes the Phase-4 wire contract, stacks on P4c #106).** The OTHER direction of the response-epoch contract: when a chime arms the client chime-silence watchdog and the server answers with an *ask* (not a written reading), that ask's TTS must carry the arming utterance's response epoch (`utterance_id`) so the watchdog disarms on the spoken question instead of false-firing a 20s native apology. Eight emit-site rows, enumerated exhaustively (a MISSED site = an audible ask with no epoch = a watchdog false-fire once clients arm). **Row 1 (dialogue engine):** `buildScriptAsk`/`buildScriptConfirm`/`buildScriptInfo` (`helpers/wire-emit.js`) gain a REQUIRED creation-time `responseEpoch` arg (sentinel default → THROWS on a missed nested thread, so a dropped epoch is a loud test failure not a silent null; explicit null allowed); a single captured snapshot is threaded parallel to `now` through EVERY nested engine fn to all 18 builder call sites (NEVER derived in `safeSend`); `ASK_STARTED_OBSERVER` reports the stamped id as a send-time backstop. Epoch SOURCES: sonnet-stream active-path turns pass `msg.utterance_id`; the shadow-harness resume/entry hooks pass `responseEpochRef.current`; the `start_dialogue_script` dispatcher reads a live `responseEpochRef` stashed on the session at the top of `runLiveMode` (cleared in its finally, same pattern as `activeTurnTranscript`). **Rows 2-3 (legacy ring/IR scripts):** their own builders stamp the epoch too (null-default, not the REQUIRED sentinel — these files are no longer on the live path, so a throw would add abort risk with zero benefit; carried for wire-contract completeness + the Codex table check). **Row 4 (dispatcher):** the initial + pvr `ask_user_started` QUESTION frames carry `responseEpochRef.current` (distinct from P4c which stamps the ANSWER-side confirmations). **Rows 5-8 (legacy sonnet-stream/batch cluster):** row 8 — `_processUtteranceBatch` combinedOptions now carries the LAST NON-EMPTY buffered `utteranceId` and `_extractSingle` preserves it as `result.utterance_id` (the SOURCE, covers sync + batch); row 5 — a new `stampQuestionsWithUtteranceId` clones each `question` with the epoch BEFORE enqueue (onBatchResult/sync use `result.utterance_id`, orphan-review uses `consumedUtteranceId`); row 6 — `voice_command_response` (sync + onBatchResult) carries it; row 7 — the reconnect replay STRIPS `spoken_response`/`action` from the extraction frame (iOS ignores them there) and emits a SEPARATE `voice_command_response` carrying the buffered epoch (SANCTIONED behaviour change — a buffered spoken_response that was effectively dropped on reconnect now speaks). All stamping is non-empty-string-only and the epoch KEY is OMITTED (never `null`) when absent, so a no-epoch frame is byte-identical to pre-P4d. Codex diff-review (3 parallel lenses + a per-fix mini-review + a cycle-2 verify) hardened four things: the internal engine helpers now default `responseEpoch` to the REQUIRED sentinel (a missed nested thread THROWS, caught a live RCD bulk-apply miss), the reconnect replay sends the audible `voice_command_response` LAST and halts-on-failure + re-queues undelivered entries in FIFO order (never loses/reorders/double-speaks the spoken reply; `dispatchObservationUpdates` gains a `failFast` flag for the flush), and `result.utterance_id` is omitted-when-absent. ~36 new tests incl. a real-session end-to-end A/B (buffer A then B → the real batched result carries B on extraction/question/live-VCR/reconnect-replay), the sync-transcript VCR+question, the periodic orphan-review consumedUtteranceId stamp, and the reconnect send-failure re-queue/FIFO/obs-failFast cases; 5539 backend green; corpus 5/5; lint 0 errors. ZERO client change (all fields additive-optional). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-19 | **F/U-2 + F/U-3 — specific voice notices for rename-to-same and wholly-already_set calculator turns (backend-only).** Both successful noops previously drew the generic marker-② apology (reads as failure). New dispatcher→harness voice-notice channel (`perTurnWrites.voiceNotices`; the notices are the SPECIFIC-FIRST branch of the marker-② net itself — they fire ONLY when zero other speech-intent owns the turn, so a corrected rename or a sibling calc read-back always supersedes a stale notice; Codex r1); wording ROTATES per turn + the >4 scope carries the count (client 30 s text-keyed field-nil dedupe would swallow byte-identical repeats — Codex r1; residual same-variant-same-scope repeat = accepted D-2 class): "Circuit N is unchanged — I didn't catch a new name or number for it." / "Zs for circuit 4 is already recorded — say a new reading to replace it." Mixed/partial calc outcomes deliberately keep the existing paths. 11 unit + composition/integration tests; 5492 backend green; corpus 5/5; zero wire change. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-19 | **F/U-5 — backend consumes the PWA `board_info` containers (backend-only; Codex-hardened, 4 cycles).** The top-level `board_info` container — the primary board-fields carrier for single-board web jobs and the ONLY container the web regex layer writes voice board fields to — was never read by the seeder or the job-state merge: board fields were invisible from turn 1 and mid-recording web board edits silently dropped. Fix (shared `_applyTopLevelBoardInfo`): FIELDS → `circuits[0]` (the `record_board_reading`/serialiser/resolver bucket — a record-targeted merge would let a stale seeded `ze` shadow a later dictated correction), identity → main record; the accompanying boards[] is CLASSIFIED (multi-board / sole-sub-board mirrors skipped — never stamp a reordered sub-board onto main; a sole MAIN board still takes fields — the regex-apply shape); measurements fill-only (dictated wins, pinned end-to-end through `calculate_zs`), facts client-authoritative (`FACT_FIELDS` gains the real Board-tab property keys); `flattenBoardRecord` for the documented nested shape; `board_info` joins `MERGE_SKIP_KEYS`. 23 tests; 5479 backend green; corpus 5/5; zero wire change. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-19 | **F/U-1 — explicit `calculate_zs`/`calculate_r1_plus_r2` results are now SPOKEN ("Circuit 4, Zs calculated as 1.21"), never silently written (backend-only).** The 2026-06-18 `::calc::` read-back exemption is retired for calculator results — post-PR-#99 the calc tools are explicit-intent-only, so every computed result is an explicitly-requested value the hands-free inspector must hear (beep-then-silence on probe B was the bug; model prose never reaches TTS, so the read-back is deterministic bundler synthesis). Distinct "calculated as" phrasing keeps derived values ear-distinguishable from meter readings; calc-ness is a grouping dimension (calc vs dictated same-value never merge; fan-outs collapse to one "Circuits 1 to 3, Zs calculated as X" line); mirrors/polarity ticks stay silent (`derived: true`). marker-② predicate-5 designed-silent exemption REMOVED as unreachable-by-design (fail-audible: a computed turn whose read-back is lost downstream now draws the catch-all apology — regression-pin test; a debounced calc read-back is predicate-4 already-heard evidence). Partial batches speak computed circuits; per-reason skip wording = F/U-3; wholly-empty calc turns still fire marker-②. Zero wire change (rides `confirmations[]` — no client companion needed). Codex riders (3 cycles): the loaded-barrel speculator now speculates calc writes WITH the calculated phrasing (servable cache hits instead of guaranteed drift-waste under prod `VOICE_LATENCY_LOADED_BARREL=true`), skips `derived:true` mirrors entirely, and ONE shared fan-out identity — `buildFanoutGroupKey` in `confirmation-text.js` — replaces THREE hand-rolled twins (speculator bucket lookup, its back-link, the bundler group key; the back-link twin had already diverged → suppress-but-never-abort, zero prior abort-path coverage, now pinned dictated→dictated + calc→calc). New required_green fixture `frc_9aa68a43…` (audible twin of the F/U-4 fixture per its pinned note) RED-proven `audibility.output.out_zs_spoken`; corpus 5/5; 5456 backend green. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-18 | **F/U-4 — supply Ze/PFC canonical-key contract fix + board-aware Ze resolution + PWA job-state ingestion repairs (backend-only; Codex-hardened over SIX review cycles to a clean verdict).** Core: the seeder + updateJobState supply merge stored Ze/PFC under legacy wire aliases (`circuits[0].ze`/`pfc`) while the calculators and dictated readings use canonical `earth_loop_impedance_ze`/`prospective_fault_current` — job-seeded supply Ze was INVISIBLE to `calculate_zs` (every call skipped `no_ze` while the model-facing snapshot SHOWED the Ze). Fix = supply-scope-only canonicalisation (`SUPPLY_MERGE_KEY_ALIASES` + `normaliseSupplyIngest`: canonical snake → camel → short-alias precedence; strict finite-numeric SCALAR validation — injection text/arrays dropped; all three container spellings; plain-record + `Object.hasOwn` + `__proto__`-safe merge) — deliberately NOT a bucket-wide rename (bare `ze` is a distinct board_fields key). Review-driven riders: (1) **board-aware Ze resolver** in BOTH calculators (iOS-canon chain `board.ze → earth_loop_impedance_ze → ze_at_db → zs_at_db → origin supply`; present-but-invalid board Ze = terminal `no_ze`, never a silent fallback to the WRONG measurement; whitespace never coerces to 0) — without it the seeded origin Ze would have silently mis-computed sub-board Zs; (2) calculator board-target validation (`board_id:'*'` → `board_id_star_unsupported`, unknown → `board_not_found`, `{code,field}` envelope; resolved target id threaded through selector+bucket, write path wire-preserved); (3) **PWA `job_state_update` frames were a silent NO-OP** (web nests the job under `jobState`; backend read top-level keys only) — new `unwrapJobStateFrame` (plain-record-only, null-sentinel → skip-and-warn, never the schedule-clearing envelope fallback); (4) PWA snake-case `supply_characteristics` shape now actually ingested. ~50 targeted tests (two dedicated suites) + recorded fixture `frc_a5f8cfeb…` RED-proven `reading.op_zs_calc` → `required_green` (corpus 4/4; `chime_observed:false` deliberate — the audible calc contract is open F/U-1). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-18 | **marker-② — a chimed turn with zero audible output ALWAYS speaks (backend-only; numeric-gate-redesign wave): catch-all audibility net + calculate_zs prompt steer.** Closes the "tool ran, didn't error, emitted nothing audible" class found by live prod test 8/8 ("Zs for circuit 4." → `calculate_zs` succeeds with `computed:[]` → beep-then-silence; a successful tool call defeats A3/marker-① `producedNothing`, `is_error:false` defeats the M1 net, no attempted ask defeats F7). New FINAL net in `runLiveMode` (after A3/D2/F7, before the §A4 drain): rotating `CATCHALL_AUDIBILITY_PROMPTS` apology when a chimed, confirmations-enabled, non-cancelled turn ends with ZERO speech-intent (audible confirmations + emitted asks + current-gen queued prompts + NEW per-turn produced-then-DEBOUNCED evidence `debouncedConfirmationCountThisTurn`) and no designed-silent write. Exemption classified by dispatcher OUTCOME, never tool name, and WHOLE-TURN: exempt only when EVERY tool call is a FULLY-computed calculator success (`ok:true ∧ computed.length>0 ∧ skipped===[]` — the `::calc::` read-back-exempt write; Phase-0 source check CORRECTED the plan's "computed is audible" assumption) AND the loop ledger is clean (not aborted/cap-hit, zero per-round errors, attempted==accumulated — thrown dispatchers/padded results are invisible to `tool_calls`); partial batches, mixed turns, malformed shapes and `computed:[]` (missing-input AND already-recorded) all FIRE (never silent, fail-closed). `isAudibleText`/`isCurrentGenPrompt` hoisted to shared `runLiveMode` scope. Recorded fixture `frc_b6ec5356…` RED-proven `audibility.turn` (net stashed) → flipped `required_green` (gate_state-only, per transition-immutability); corpus 3/3. 24 new unit tests incl. marker-① mutual exclusion, mixed/partial/ledger fail-closed cases (incl. abnormal-stop + truncated-ledger), generic-transport-failure + apology-family string-distinctness. Generic live tool-loop failures no longer early-return an empty (silent) extraction — they take the F7 cancellation finalization, so a transport error still speaks one apology. Phase-4 steer (`sonnet_agentic_system.md`, same PR): a bare value-less "Zs for circuit N" is an INCOMPLETE READING → ONE `ask_user` (`missing_value`/`number`) even with inputs present (meter wins); `calculate_zs`/`calculate_r1_plus_r2` reserved for explicit compute intent, explicit-with-inputs path PRESERVED — verification = the three post-deploy live probes A/B/C (REQUIRED after the ECS rollout, not yet run at merge time; model behaviour is live-lane, not fixture-lockable). ZERO client change (apology rides the existing field-nil channel; ledger row `recording/catchall-audibility-apology`); the per-client chime-silence WATCHDOG is the separate PLAN-C wave. Follow-ups logged: silent successful-calc read-back (F/U-1), `rename_circuit` same-ref no-designation silent edge (F/U-2), specific already-recorded wording (F/U-3). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-17 | **marker-① — a chimed no-op ALWAYS speaks (backend-only; EICR #97). Numeric-chatter gate HELD after Codex review — see below.** Closes the chime-then-silence-via-model-no-op class (keystone `frc_c55c996…`). Field session 36731498 turn 4: "Circuit 2 is upstairs lights" → Flux garble "Chuck it too is upstairs lights" → the model no-op'd (one `end_turn`, zero tools) after a chime had fired → beep-then-silence (Audio-First #1). The A3 orphan net (already the `producedNothing` net) had a content gate `(carriesValue \|\| carriesObservation)` the digit-less garble slipped; loosened to also fire on a chimed no-content turn via a threaded `options.chimeObserved` (production sets `true` at the sole post-gate `runShadowHarness` call site — gate-pass ⟺ chime; recorded lane from the fixture's `chime_observed`). Rotating `NOOP_AUDIBILITY_PROMPTS` (5 phrasings, no "reading" noun → no cross-dedupe, burst margin). Keystone flipped `expected_red → required_green` (RED→GREEN proven live; fixture change minimised to `gate_state` + `expected_failure_id` removal per the transition-immutability contract, Codex #4). Apology rides the existing field-null confirmation channel the client already renders (F7 apologies) → NO client change. **Numeric-chatter gate HELD (not shipped):** a client-side letter-free bare-number block was built (web + iOS #34) but Codex review found it can DROP a real reading — a bare-value repeat ("0.35") that completes a server-side `orphanContext` the client can't see would be blocked → Audio-First #2 violation. The gate can't be done safely client-side alone; reverted from this wave and surfaced to Derek for a cross-platform design (backend "orphan pending" signal). Consequence for now: bare-number chatter draws a marker-① apology (safe — never silent, never dropped). 5371 backend green; corpus gate 2/2; 3 gate-machinery unit tests migrated to an answer-turn RED (the net now heals the old non-answer no-op vehicle). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-17 | **Field-replay correctness gate — accident-class MVP + first keystone fixture ① (backend test-infra + CI; ZERO backend-behaviour change, ZERO client runtime change).** The missing live-exercise gate: replays REAL captured field sessions through the REAL `runShadowHarness` and asserts audibility/write/clarification invariants as a MERGE gate — a fix for a field bug must pass that bug's captured transcript before it can ship. **Re-scoped from the original governance-heavy foundation** (held as draft #92 after a Codex review found 14 BLOCKERs concentrated in the enforcement/evidence/oracle core): built vertically instead — one proven slice at a time. **Slice 1 ships the deterministic runner (recorded lane, the WHOLE blocking gate: expected_red must fail with exactly its target id, required_green must pass) + the first REAL on-disk fixture** — keystone ① (session `36731498` turn 4: a Flux garble the model no-op'd → chime-then-silence), which reproduces the bug end-to-end and confirms `audibility.turn` RED. The signed-commit governance, trusted-run evidence, HMAC source commitments, nightly live lane, and per-fixture attestation requirement are **deferred to `field-replay-hardening-followups`** (malice-hardening the threat model defers) — removed from the shipping gate, primitives retained. Delivery is PR-only. 5406 backend green. Full detail: [field-replay-corpus.md](docs/reference/field-replay-corpus.md).
| 2026-07-15 | **F7 hardening wave (harness-first; backend-only, ZERO client) — audibility-invariant sweep + pre-emission ask-audibility net (#16) + extraction-watchdog re-arm & generation cancellation (#14).** Origin: field-feedback-2026-07-14 Codex out-of-scope findings. **Item 1 (#17):** two-lane Stage-6 audibility-invariant sweep — the class-killer for the 28 review findings the PWA harness can't cover; the NINE invariant-(a) suppression cases shipped `test.failing` (RED on the audibility assertion, verified not a timeout) and flipped GREEN by Item 2 (RED→GREEN in one PR); enum-driven pre-emit vs emission-required classification asserts DISJOINT + union == `ASK_USER_ANSWER_OUTCOMES`. **Item 2 (#16):** closes chime-then-silence — a per-turn `emittedAskToolCallIds` Set fed by `onAskUserStarted` (fired only on a successful send: initial / `pvr` / dialogue-engine `safeSend` via the `ASK_STARTED_OBSERVER` symbol — single choke point); a pre-emission net (after §D2, before A4 drain, `confirmationsEnabled`-gated) queues ONE fixed field-null apology when an ask was attempted, emitted-set empty, nothing audible survives; §D2 tightened to require the continuation ∈ emitted set; step-3b FAST-FAIL of closed-WS/throwing-send/`fallbackToLegacy` (no 45s dead-air); `tool_call_id` on `toolLoopOut.tool_calls`; A4-drain trim; two telemetry rows + `generationId` (minted in `sonnet-stream.js`) on them + `ios_send_attempt`. **Item 3 (#14):** per-turn extraction-watchdog CONTROLLER — an `askChainObserved` latch (via `onAskRegistered`) extends the 30s no-ask deadline to `EXTRACTION_WATCHDOG_ABSOLUTE_MS` (195000ms); REAL cancellation via one `AbortController` per generation threaded through `runShadowHarness`→`runToolLoop` (signal + `throwIfStage6Cancelled` guards + SDK abort canonicalisation) + `rejectAll('timeout')`, NEVER force-clearing `isExtracting` (removes the concurrent-extraction bug); one shared FATAL discriminator (`stage6-control-flow-errors.js`) rethrown before every generic recovery; on cancellation `runLiveMode` finalizes a partial (every applied write still read back once, skips only `toolLoopOut`-dependent blocks) via inline `cancelled` guards; two watchdog telemetry rows; comment sweep 20s→45s. 5207 backend tests green. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-15 | **D2 mutation-to-chain correlation (own wave; backend, ZERO client).** Closes the multi-observation beep-then-silence path (field-feedback-2026-07-14 Codex cycle-5 #4). The §D2 dropped-net evaluated only the globally-latest answered clarify chain and let ANY later `record_observation` qualify it, so in a two-answered-chain turn one write suppressed the whole fallback and the earlier chain never retired. Fix: `record_observation` now carries a nullable-required `clarification_chain_id` (schema + prompt echo, ONE cache-invalidating edit; `null`=direct); the net GROUPS answered clarify asks by chain id (latest anchor per chain) and correlates each successful write (`is_error!==true` AND body `ok===true`, internal-catch parser) to its chain. Mutation-id LENIENT on both edge cases (Derek): id-less (D-1a) / unknown-non-null (D-1b) qualifies every evaluated chain whose anchor precedes it (avoids false apology→re-dictation→duplicate); only a non-null id matching a DIFFERENT chain fails to qualify this one. COLLAPSED single count-aware fallback (per-chain identical texts client-swallowed by the A1(b) 30s field-nil TTL); every non-null chain retires once. Telemetry: extended `dropped_net` + new `lenient_qualification` INFO row; raw model-controlled id NEVER logged (leak-filter bypass) — only `mutation_id_kind` + server-minted ids. Shared pure `normaliseObsClarifyChainId`; net resolves against the turn's anchor map, never broker membership. Zero-client: dispatcher returns exactly `{ok:true, observation_id}`; chain id never enters extractedObservations/perTurnWrites/legacy wire/logs (pinned). Known-limitation D-2 (dated, ACCEPTED): same-wording D2 fallback repeated within 30s may client-dedupe. Test matrix 1-16. 5159 passed / 19 skipped (+28 over 5131 baseline). Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-15 | **Field-feedback 2026-07-14 WEB COMPANION (deliverable 3; web-only, ZERO backend).** A1(a) token-aware dedupe key (`DEDUPE_TOKEN_FIELDS`, `Confirmation.dedupe_token`; vectors regenerated from the backend mirror, Swift-identical) + A1(b) 30s field-nil apology TTL with AGELESS queue reservations + shared forget helper clearing all three stores (`confirmation-dedupe-store.ts`, tts-queue `onPlaybackStarted` hook — closes the F7/F10 swallowed-apology class on web); C4 `zedi`→Ze + `icd`→RCD trip-time aliases (matcher + backend-facing normaliser; keyword-boosts untouched, exact iOS 67ffb9d mirror); A2 replay-harness mock-lane pin (`field_corrected` canonical `r1_plus_r2` clears r1_r2_ohm / exempt `r2_ohm` arrives raw — RED-proofed); B1 verified NO web dual-address ask (no gap, ledger row); B2 observation-processing cue DEFERRED with owner Derek (`recording/observation-processing-cue` — base cue never ported, own wave). Ledger: 1 row updated + 4 new; files-map extended. 1431 web tests green. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-14 | **Field-feedback wave — session 6B6FE011 (10 reports F1–F10): five chime-then-silence paths closed, two wrong-writes fixed, garble aliases, two Derek-decided product changes (backend PR; iOS + web companions follow).** Headline invariant (Derek, F7): *"There should always be a follow-up TTS if there has been a beep."* (A1a) five text-op confirmation fields carry a backend `dedupe_token` (replay-stable operation identity) so identical-text repeats of DISTINCT operations stop client-deduping (F2 rename-correction, F7/F10 apologies); token-aware debounce; `ios_send_attempt` telemetry moved post-debounce covering all five fields. (A2) clear_reading wire canonicalised (F5 — "cleared" spoken, cell stayed populated) with `r2_ohm` exempt (canonical `r2` would MIS-clear R1+R2 on build-418) + a SEMANTIC clearer round-trip audit gating the deploy. (A3) orphan net catches digit-less observations (F3/F9) with an observation-flavoured apology. (A4) `context_field:"none"` inverted asks get a write-or-reask GUARANTEE (F8): pendingValue captured at registration, field-name replies resolved and dispatched through the normal read-back path, deterministic `pvr-*` broker re-asks (register-before-send), typed schema-aware fresh-reading detector guards both answer channels, audible apology terminal — never silence. (B3) leak filter sanitises `suggested_regulation` to its regulation token instead of rejecting the call (F6 ~9s retry). (C1) conservative fuzzy designation matching (length-aware Levenshtein + strict margin, ask-answer path only) + prompt designation-outranks-ambient/no-phantom-circuit steering. (C4) ICD→RCD, triptan, Zedi→Ze enumerated aliases (no broad fuzzy — §3E stands). (D1) observations professionally REWORDED (BS 7671 tone, fact-preserving) — Derek's F6 ask. (D2) ambiguous C2-vs-C3 → ONE targeted factual ask naming the deciding fact (never "C2 or C3?"), three-way outcomes, per-OBSERVATION clarification-chain ask budget + post-answer net. (INV-2) ElevenLabs bytes/char anomaly telemetry (F1 garbled synth). 5,100+ backend tests green. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-08 | **PWA replay harness shipped (Waves 2–7) + A4 feedback capture port (Wave 6) — web/scripts/tests/docs + CI lanes; ZERO backend.** The composition-level test harness the A1–A4 bug class demanded: B1 injection seams at the RecordingProvider composition root (prod-neutral, `test-services.ts`); behavioural-trace capture from 3 seams; mock/live backend modes; scenario runner over the voice-latency YAML schema (+`mock_frames`/`expect.web`); **keystone proof** — A1/A2/A3 reverted on a scratch branch went RED on all three bug classes against the hand-authored `sess_mrbnds2d_jczh` fixture, GREEN unmodified (the first RED attempt caught the fake bypassing the real Flux mapping — fixed). iOS differential (fetch→convert→replay→diff, strict/loose lanes, iOS stale-circuit-hint quirk = WARN; corpus: B1916AD6 hand-authored-initial-state PASS 0 strict fails + A02B018D empty_fallback); 7-invariant D1 library; generated 116-field sweep from `field_schema.json` (clean+garble+chitchat per field, ZERO voice-coverage gaps) + garble/chitchat corpora seeded from real sessions. **A4**: `feedback-capture.ts` — iOS `TranscriptProcessor` HEAD port (sentence-opener trigger, 3 exit paths incl. the 'and feedback' garble close, 30s pre-trigger window, `POST /api/debug-report`, stop auto-close, 'Feedback logged' ack; NO PR-#17 timeout); new ledger row `recording/voice-feedback-capture` (untracked gap). CI: per-PR path-filtered mock lane (deterministic, zero tokens) + advisory nightly live lane (Haiku env, £10/month cap; PENDING `ANTHROPIC_API_KEY` repo secret — vault todo). Full detail: [pwa-replay-harness.md](docs/reference/pwa-replay-harness.md) + [changelog.md](docs/reference/changelog.md). |
| 2026-07-08 | **PWA live-bug fixes A1–A3 (pwa-replay-harness Wave 1; web-only, ZERO backend).** Field session `sess_mrbnds2d_jczh` root causes fixed: (A1) web Flux `EndOfTurn`-with-transcript now fires `onFinalTranscript` + `onUtteranceEnd` (iOS canon) — the missing utterance-end left `isInspectorSpeaking` stuck true so EVERY FIFO confirmation deferred forever (universal read-back dead on web since the 2026-07-03 Flux flip); composition regression pins Flux→FIFO defer-resume. (A2) orphan classifier ports the iOS `supplyFields` rescue (`non-circuit-fields.ts`, verbatim ~77-entry copy + drift guard vs the real route map) — section fields (client_name…) no longer produce a false "which circuit?" ask that preempt-flushed the read-back. (A3) regex apply layer gains a value-equality freshness gate (`computeFreshRegexWrites`, BOTH env paths; hints-OFF uses a per-session shadow baseline) — cumulative-window re-hits no longer chime/send on chitchat or reset the backend chitchat counter (iOS canon `applyRegexValue` equality → gate `hasRegexHit`). Fixtures `sess_mrbnds2d_jczh.yaml`/`a3-gate-consequence.yaml` committed separately (Wave-3 keystone pins; web sessions have no S3 debug_log — analytics-upload gap logged). A4 (feedback capture port) = Wave 6. 1383 web tests green. Full detail: [changelog.md](docs/reference/changelog.md). |
| 2026-07-08 | **CCU quality gate: classifier confidence softened to a corroborated soft signal (backend, zero client).** Field failure: a Contactum board pinned Stage-1 confidence at 0.82 on three different photos (the score is a property of the BOARD — model unidentifiable — not the photo) while extraction was perfect (13/13 circuits), so `classifier_low_confidence < 0.85` rejected every retake forever. Now: hard floor 0.65 (below = wrong-pipeline risk, always fail); 0.65–0.85 passes when the extraction corroborates itself (≥ 3 OCPD circuits, ≤ 50% null ratings). Also fixes the latent dead `too_many_nulls` check — the OCPD filter never matched live merged circuits (no `device_kind` key); now recognised via `ocpd_bs_en`/`is_rcbo`/`ocpd_rating_a`. Deliberately NOT fixed via board-model identification (too much hardware variance within one model number — Derek). The rejected 13-circuit extraction was recovered from the S3 training log into the live job. 4,974 backend tests green. |
| 2026-07-06 | **Web TTS FIFO queue + `cancel_pending_tts` — iOS AlertManager Phase 7.1 → web (web-only, ZERO backend; deployed, `match` pending device ear-verify).** Root-caused from a live PWA session (`sess_mr8qrvcm_20jn`): a two-circuit turn read back only the LAST circuit (the second `speakConfirmation` cancelled the first 5ms in) and a deferred "which circuit?" question was stranded (phantom SpeechStarted cleared `isInspectorSpeakingRef` WITHOUT draining `deferredTtsRef`). Both are the unshipped iOS TTS FIFO (WS3-FU `parity-ws3b-voice-latency-2026-07` item 8). Fix = **two paths** mirroring iOS Phase 7.1 scope: (1) new `web/src/lib/recording/tts-queue.ts` — a framework-free confirmation FIFO pump (injected player, last-mile deferral gate re-checking `shouldDeferPlayback` POST-fetch/pre-`audio.play()` per iOS `playOrDeferQueueHead`, drop-oldest overflow MAX 6, `preemptFlush`/`purge(prefix)`/`reset`, and a synchronous `onDiscarded` un-record so a confirmation discarded before it plays is re-speakable — never a permanent zero read-back, Audio-First #1); `elevenlabs-tts.ts` gains `prepareElevenLabs` (fetch/play split for the last-mile gate; `speakElevenLabs` unchanged for the direct path). (2) `tts.ts` split: `speakConfirmation()`→FIFO enqueue via a thin ElevenLabs-primary/native-fallback player (echo-suppression + per-head `ttsWindow` intact); `speak()` (ask_user/alerts/tour) stays DIRECT and PREEMPTS the FIFO (iOS `speakWithTTS`+`deferredTTS`), NOT merged into it; token-guarded `activeAudioOwner` (`direct`|`queue`) so barge-in `cancelSpeech({resetQueue:false})` cuts only a direct-owned prompt (both audio backends) and never a queue-owned confirmation; `resetQueue:true` teardown resets the queue FIRST (stray-`onEnd` post-stop guard). (3) `recording-context.tsx`: shared `handleInspectorStoppedSpeaking` drains the deferred prompt (via `speakDirectPrompt`, stays cancellable) AND resumes a deferred confirmation head from BOTH `onUtteranceEnd` and the phantom-reset (the Symptom-2 fix); defer gate = `isInspectorSpeaking || isDirectAudioActive()`. (4) `cancel_pending_tts` (iOS Phase 6.3) — backend already emitted it (`engine.js:1020-1024`), web had ZERO handling: `sonnet-session.ts` decode + `clearInFlightToolCallIdByPrefix`, `in-flight-question.ts removeByToolCallIdPrefix`, pure `tts-prompt-helpers.ts handleCancelPendingTts` silences the direct prompt (NOT gated on the audio window) + clears ask state (SonnetSession/tracker/questions/dismiss-timers). Deliberate divergence: voice-command `spoken_response` stays on the FIFO (rare/low-stakes). ~90 new tests (tts-queue, prompt-helpers, decode, serial-through-`speakConfirmation`, tour regression); full web suite green. Ledger: `recording/tts-fifo`→`match`, new `recording/cancel-pending-tts`→`match`. |
| 2026-07-03 | **Test-harness hardening — make "green locally" mean "green in CI" (web-only test infra + CI hook + docs; ZERO backend).** Roots the WS7 CI-only failure class. (A) **Node pin** — root `.nvmrc`=20 + `web/package.json` `engines >=20 <21` + WARN-level `web/scripts/check-node.mjs` wired as web `pretest` (fires on every `npm test --workspace=web`), so a local web run on a non-20 major (dev box is v25) no longer goes silently green; scoped to `web/` (NOT `engine-strict`, which would block backend install on 25). (B) **Vitest hygiene** — `restoreMocks`/`clearMocks`/`unstubGlobals`/`unstubEnvs`/`isolate` on in `web/vitest.config.ts` + global `afterEach(vi.useRealTimers())` in `web/tests/setup.ts` (the flags don't restore fake timers) + a new `harness-leak-lock.test.ts` regression guard. (C) **Fragile overrides converted** — direct `localStorage.{setItem,getItem,removeItem}=fn` (the pattern a real jsdom Storage silently ignores → the WS7 bug) → `vi.spyOn`; `globalThis.fetch=fn` → `vi.stubGlobal` across terms-gate/terms-page/api-client/runtime-config/ws6-pending-ccu-queue. (D) **Local==CI gate** — `.husky/pre-push` now sources nvm best-effort + WARNs via check-node + runs BOTH backend Jest AND `npm test --workspace=web` (was backend-only, so the web suite that broke was never gated locally); keeps the load-bearing Homebrew-PATH fallback for GUI git. Governance MANDATORY rule added to both hubs: parallel workstreams over shared test files must re-run `main`'s full suite between merges (the WS5×WS7 gap that skipped WS7's deploy). 1319 web tests green, parallel + serial. Builds on PR #81's already-merged unconditional `setup.ts` shim. |
| 2026-07-03 | **Parity WS7 — mobile "indistinguishable" PWA chrome + T&Cs signature + circuit keyboard accessory shipped (web-only, ZERO backend; deployed, `partial` until iPhone A2HS device smoke).** (a) **Circuit-cell keyboard accessory bar** (LIM / N/A / prev / next / Done) across ALL THREE surfaces (card / sticky-table / desktop-schedule) via shared `circuit-keyboard-accessory.tsx` + `circuit-focus-fields.ts` — 13 iOS focusable fields (tokens on all but ref/designation) + 12 web-extra keyboard fields (dated divergence: prev/next but NO LIM/N/A, iOS renders them as dropdowns); `visualViewport` positioning, blur-survival `preventDefault`, cross-circuit prev/next wrap; card auto-expands collapsed cards on cross-circuit Next; LIM wires to WS3 `circuits/lim-sentinel-display` (PR #76). 34 tests. (b) **T&Cs acceptance-signature port** — 7th attestation, completion 6→7, Accept gated on a signature, `termsAcceptanceSignature` PNG data URL persisted signature-first/all-or-nothing (storage-throw rolls back all terms keys + blocks the redirect); `hasAcceptedCurrentTerms()` unchanged (existing users don't re-sign); client-side only. 27 tests. (c) **Haptics** — `haptic('heavy')` gate-pass + `haptic('light')` job tab-rail (the two live iOS `UIImpactFeedbackGenerator` sites; `AppTabBar` legacy → no app-shell haptic). (d) **Standalone chrome** — `globals.css` `overscroll-behavior:none` + tap-highlight transparent + scoped `user-select`/`touch-callout` (inputs kept selectable) + `.p*-safe` env helpers; AppShell header `pt-safe`+`min-h-14` notch clearance; pull-to-refresh suppressed. (e) **`BrandedSplash`** root `loading.tsx`. `manifest.ts`/`layout.tsx` audited already-correct. Deferred: view-transition push/pop (needs Next experimental `viewTransition` flag). Ledger: 6 rows → `partial` (`last-verified` 2026-07-03), then `match` after device smoke EXCEPT `crosscutting/uiimpactfeedbackgenerator` (permanent `partial` — no iPhone Safari Vibration API); 15 duplicate ledger IDs de-duped (shipped copies kept); parent §7 WS3 row corrected (was stale `NOT STARTED`); files-map extended. ~76 new tests; full web suite green. |
| 2026-07-03 | **Parity WS4 — web Deepgram Flux migration + runtime kill-switch shipped BEHIND the switch (web-only + frontend-infra; zero backend), WS4 PARTIAL/BLOCKED.** One coordinated wave with the iOS keyterm sprint. **Phase 0 (gate):** built a net-new synthetic TTS→Flux probe (Archer TTS → two `flux-general-en` `/v2/listen` WS with/without keyterms); result = MECHANISM VALIDATED (LIM garble "lion"→"LIM" corrected by equal-weight `keyterm=`) but insulation+trip-time INCONCLUSIVE (synthetic voice too clean; the "RCD" utterances were a flash-TTS artifact, not a keyterm failure). Per the plan an inconclusive probe is NOT a full green light → iOS `default_config.json` curation HELD (needs a real-audio spot check + TestFlight — surfaced to Derek, `handoffs/.../phase0-probe-results.md`), Flux-default flip HELD. **Shipped (allowed-while-blocked):** (a) web nova-3 FALLBACK protection — `trip time` boost 1.5→2.5 in `keyword-boosts.ts` (it ranked 107/120 and was dropped by web's 85-cut; the "tryptoid" garble) + stale-comment fixes; (b) full web Flux path in `deepgram-service.ts` behind an `sttModel:'nova3'\|'flux'` selector — `buildFluxURL` (`/v2/listen`, `flux-general-en`, eot 0.7/5000, `mip_opt_out`), TurnInfo→delegate mapping (feeds the EXISTING dispatchFinal/gate/chime path, no parallel forwarder), Configure w/ echo-validation + RTT, Error/Fatal/ConfigureFailure surfaced, 80ms/1280 batcher, KeepAlive disabled on Flux, equal-weight `generateFluxKeyterms` (PROVISIONAL curated ~40, no `:boost`); (c) RUNTIME kill-switch `DEEPGRAM_STT_MODEL` — `web/src/lib/runtime-config.ts` (fetch once per recording session; split `DEFAULT_STT_MODEL` product-default vs `SAFE_STT_MODEL='nova3'` never-flip fail-safe; value normalisation) + a NET-NEW top-level `web/src/app/runtime-config/route.ts` at `/runtime-config` (NOT `/api/*` — prod ALB routes `/api/*` to backend; force-dynamic, no-store, NetworkOnly-pinned in `sw.ts`) + `ecs/task-def-frontend.json` `DEEPGRAM_STT_MODEL=nova3` + a `frontend-taskdef` deploy target in `deploy.yml` (register task def against the existing image + roll `eicr-pwa` only, ~3-5 min, no rebuild). DEFAULT stays `nova3`; the flip = ONE commit flipping `DEFAULT_STT_MODEL`+task-def to `flux` after the curated iOS list ships via TestFlight. Frontend-infra exception to `web/`-only logged in parent §7 WS4. Gates: 1269 web tests green, build green (`/runtime-config` = Dynamic route), typecheck/lint at main baseline (zero new). Ledger: `recording/flux-migration`→`partial`, new `recording/flux-kill-switch`→`match`. NO fuzzy garble correction (parent §3E). |
| 2026-07-02 | **Parity WS3 — voice behavioural catch-up shipped (web-only, zero backend).** Items 1/2/3/6/7/9: (1) web `session_start` now advertises `capabilities: { voice_latency: { version: 1, supports: ['low_conf_readback_v1'] } }` (exact parser shape pinned; verified NO client reading-confidence drop filter first) — activates universal read-back for web users after prod verification; `regex_fast_v2`/`client_playback_telemetry` deliberately NOT claimed. (2) Read-back dedupe re-keyed to the full iOS `buildConfirmationDedupeKey` (new `confirmation-dedupe-key.ts`, djb2-UInt64 BigInt, hash vectors generated from the backend mirror; `Confirmation` type +`circuits`/`board_id`). (3) Observation canonical BS 7671 wording end-to-end — `regulation_title`/`regulation_description`/`rationale` decoded (both paths; duplicate dead `observation_update` case deleted), persisted (update-MISS CLEARS stale wording), rendered in iOS card order. (6) LIM sentinel audit: web already iOS-identical — 9 regression tests added, zero runtime gaps. (7) **TranscriptGate full literal port** (`transcript-gate.ts`, all branches + trigger arrays) wired in `dispatchFinal` with non-mutating ask peeks (`peekPayloadForTranscript`/`clearExpiredSlot`), `playSentForProcessingChime()` (sample-accurate `makeChimeWAVData` port) on gate PASS only; tour switched to the shared helper, tour-local synth deleted. BEHAVIOUR CHANGE: web chitchat no longer reaches Sonnet. (9) `surge_*` sweep complete (voice-apply routing pinned; fallback-removal parity verified); EIC divert-to-comments voice apply added (EIC-only newline-append; form cell pre-existed); `ask_user.context_board_id` RECLASSIFIED backend-internal (neither client decodes it — checklist/ledger/files-map corrected). Items 4/5/8 (fast-path TTS, playback telemetry, TTS FIFO) NOT shipped — owned by named follow-up `parity-ws3b-voice-latency-2026-07` (dated ledger rows). Ledger: 7 rows → `match`, capability row `partial` pending post-deploy CloudWatch `voice_latency.startup_log` evidence (two-phase). |
| 2026-07-02 | **Parity WS9 — client-side PDF render shipped (web-only, zero backend).** The PDF tab's primary Generate now renders CLIENT-SIDE: line-for-line TS port of `EICRHTMLTemplate.swift` (2131 lines — page builders, 90+14-item inspection schedules, per-board landscape circuit schedule, guidance page) + `PDFGenerator.swift:9-68` data-graph mirror (job normalization; company/inspector/authoriser/designer/constructor via existing api-client fetchers, logo/signatures inlined as data URIs) + Step-0-spiked renderer (hidden-iframe SVG foreignObject page capture at 3× + pdf-lib merge — chosen over html2canvas (no vertical-lr) and vector re-authoring (breaks 1:1 template correspondence); raster-not-vector text is the documented trade-off). Blob integrates into the existing session `pdfBlob` state; `pdf_s3_key` stamped `local://<filename>` (iOS scheme, PDFTab.swift:363); server generate-pdf demoted to explicit "Generate on server (fallback)" (debug-page flip after field validation; endpoint untouched). Spec §4.3 retry implemented (failed render → Try-again re-uses written attestation_ids, NO re-prompt; iOS lacks this — dated todo). Acceptance diff vs simulator-produced iOS reference PDFs PASSED (EICR 9/9, EIC 5/5 pages — `web/audit/ws9-pdf-fidelity-2026-07/`); found+fixed serif-fallback and group-header cascade bugs. Ledger: `pdf/pdftab-270`→rewritten, `pdf/pdf-fidelity` stays `partial` (dated note) until field validation; 10 new template/render files registered in `parity-ledger-files.json`. Fixture bug logged: EICR fixture `next_inspection_years:""` makes the job UNDECODABLE on iOS. 1145 web tests green. |
| 2026-07-02 | **Parity WS5 — design-system unification to iOS CMDesign shipped (web-only, zero backend).** Tokens regenerated from `CertMateDesign.swift` (mapping table `web/audit/cmdesign-token-map-2026-07.md`; colors/radii/heights to CMDesign values, accent scales added; spacing keys+values KEPT per user decision — `max-w-*` override verified 768px). Signature recipes ported in place: `.cm-card` glass recipe, SectionCard → CMSectionCard (conduit gradient stripe, gradient border, radius 16), hero shimmer+glow (radius 22), tab rail → iOS colours + sliding 3px blue→green gradient underline, field controls → CMFloatingTextField spec (green focus glow, radius 12, h-52, 17px values — kills iOS-Safari zoom-on-focus; UPPERCASE formLabel deliberately skipped: zero live iOS call sites). Inspection accents consolidated onto `InspectionTab.swift:362-374` category map. Fixed the WS0 styled-dialog double-offset centrally (`scale` property animation; terms consumer cleaned; Playwright regression) + recording-chrome undefined CSS vars. Capture script gains local-recapture support + a HARD read-only guard after an incident: a transient GET failure let auto-seed defaulters save a blank doc over the seeded parity EICR fixture (restored same-session from the twin EIC fixture); the underlying auto-seed-on-unhydrated-job DATA-LOSS bug is open in `web/audit/INDEX-2026-07.md` (P1 — surface to Derek). Ledger: 3 rows → `match`, `tab-rail-form` → `partial` (dated swipe-paging accepted difference); INDEX WS5 gaps closed; MANIFEST EIC-vs-iOS deferred-review note. |
| 2026-07-02 | **Parity WS6 — workflow completion shipped (web-only, zero backend).** All six WS6 items: (1) CCU **Add Off-Peak Board** mode, full path — 6th mode-sheet tile (iOS copy/visibility), `applyAddOffPeakBoardMode` via a shared appended-board applier (`board_type='off_peak'`, designation "Off-Peak Board", sibling of main, supply untouched), localStorage last-mode guard 5→6, circuits-page hint/success/post-apply selection, Board tab `off_peak` option + parent-clear on type flip (`off_peak` already in the wire `BoardType` union — no backend). (2) **Pending-CCU queue** — IDB `pending-ccu-extraction` store (`certmate-cache` v5), persist-before-upload, ONE idempotency key per capture reused on every retry (`analyzeCCU` sends `X-Idempotency-Key`; backend `withIdempotency('ccu')` already live), 409-inflight honours `Retry-After: 5`, **terminal 422 retake** (entry dropped + retake card — web previously had zero `retake_required` handling), circuits-page banner (thumbnails, Retry/Retry-All, auto-retry on `online`) + Overview "photos waiting to upload" pill; CCU photos ONLY (doc path test-pinned queue-free). (3) **Tour v11** — 2+9=11 steps incl. new `job-tone` "conversational + tone" step playing a sample-accurate `makeChimeWAVData` port synthesised LOCALLY in `lib/tour/tour-chime.ts` (WS3 unmerged at ship — symbol-checked; WS3 item 7 owns the switch to `playSentForProcessingChime()`); Defaults narration shortened + Observations step de-staled per the 2026-06-30 iOS revision. (4) **Offline dirty-guard** audited: guard pre-existed (`job-context.tsx:121-138`); newer-`updated_at`-while-dirty regression tests added. (5) **Doc-extraction parity VERIFIED** (12-file cap, image+PDF, whole-batch failure surfacing) — no code change. (6) **Job-creation defaults ladder** — after `createJob`: fetch detail → 0 presets: literal `applyStandardDefaults` port / 1: auto-apply / 2+: `PresetPickerSheet` with Skip → persist (`queueSaveJob` + cache warm) → navigate. Ledger: 10 WS6 rows → `match` (dated), INDEX-2026-07 WS6 gaps closed, parity file-map extended; parent program §7 WS6 → DONE. |
| 2026-07-02 | **Parity WS0 item 4 — visual baseline captured (tests-e2e/audit only, zero backend, zero web/src).** Web COMPLETE: all 14 screens ×2 viewports vs production via authenticated `web/tests-e2e/visual-baseline-capture.mjs` (programmatic login; T&Cs/tour gates pre-seeded device-locally). iOS EICR set captured on simulator via temporary XCUITest driver (reverted; pbxproj regen = CertMateUnified #31). 10 iOS EIC screens still blocked: `deduplicateApiJobs` drops same-address jobs — EIC fixture invisible on iOS (bug logged, not fixed). 3 bugs recorded: iOS address-dedupe; web login rejects `company_id:null` (schema `.nullable()` missing); all web dialogs double-offset (Tailwind v4 `translate` + `.cm-dialog-content` transform). Evidence for WS6 off-peak + WS3 obs-wording gaps. `web/audit/visual-baseline-2026-07/MANIFEST.md`; PR #72. |
| 2026-07-02 | **Parity WS2 — PWA observation-photo auto-link rebased + shipped (web-only, zero backend).** The parked 2026-05-13 branch (4 commits `e880043d` forward-link / `efe7449b` capture handler + image-resize / `b0730325` Photo button / `577f8107` unassigned pool + From-Job picker, substance preserved via cherry-pick) replayed onto main. Real conflict set was ONE keep-both hunk in `recording-context.tsx` (the old HANDOFF's heavy-collision prediction was stale; `apply-extraction.ts` merged clean). Two contract fixes vs the current backend wire: `unassigned_photos` declared in strip-mode `JobDetailSchema` (`adapters/job.ts` — was silently dropped by `api.job()`) and retyped `string[] \| null` in `types.ts` (GET emits `null` on blank-slate jobs); + duplicate-toast-import and unused-import rebase cleanups. iOS canon re-verified against CURRENT refs (`DeepgramRecordingViewModel.swift:1094/2257/7262`); two dated deliberate divergences on ledger row `observations/obs-photo-autolink` (no CCU picker source — no web-persisted `ccu_photo_path`, zero-backend rule; web-extra camera/library chooser). Gates: 1099 web + 4952 backend tests, build green; typecheck/lint at main baseline (zero new). Ledger row `partial` + INDEX-2026-07 gap open pending iPad Safari device smoke (upload-during-resize race is runtime-only); todo in vault todos-certmate.md. |
| 2026-07-02 | **Parity WS0+WS1 (docs/CI only, zero backend).** WS0 audit refresh: tab gating re-verified vs `JobDetailView.swift:472-536` (no re-drift); wire-shape audit of PRs #58–#70 → `web/audit/ws3-checklist-2026-07.md`; ledger sweep adds stable `id` + `last-verified` columns (367→396 rows, 29 program gap rows WS2–WS9, `backend` status retired, backend-defaults claims rewritten frontend-only); `web/audit/INDEX-2026-07.md` supersedes `INDEX.md`; parked obs-photo branch pushed to origin; Flux/queueSaveJob doc fixes. WS1 governance: MANDATORY web-companion rule (hub + CertMateUnified `9a91c5d`, which also fixes invariant #2 + domain + `../EICR_App/` paths); warn-only `parity-ledger-warn` CI job (PR-only, `continue-on-error`, no dependants) + `scripts/check-parity-ledger.mjs` + `web/docs/parity-ledger-files.json`; fixture dry-runs prove all warn conditions exit 0; drift-check/JWT_SECRET/IAM merge-gate verifications passed. Reviewer checklist + quarterly re-audit todo in user-level agent + vault. |
| 2026-06-26 | **ElevenLabs model consolidation A + A2 (backend, live).** A: non-streaming TTS proxy (`POST /api/proxy/elevenlabs-tts`, `keys.js`) switched `eleven_turbo_v2_5` → `eleven_flash_v2_5` — the streaming WS path already ran Flash, so every live TTS path is now one model. Contract-preserving for iOS/web (same Archer voice, same default `mp3_44100_128`, same `voice_settings`); audio bytes unchanged, no client rebuild. Flash & Turbo bill identically (both 0.5 credits/char since ElevenLabs' Aug-2024 Turbo cut), so live cost is unchanged — the win is lower first-byte latency. A2: `cost-tracker.js` ElevenLabs accounting made per-model — `ELEVENLABS_RATE_PER_CHAR_BY_MODEL` map (Flash/Turbo $0.00005/char, Multilingual-v2/v3 $0.0001/char), `ELEVENLABS_RATE_PER_CHAR` kept as the unknown-model fallback, `DEFAULT_ELEVENLABS_MODEL_ID = eleven_flash_v2_5`. Single `elevenLabsCharacters` scalar replaced by per-model `elevenLabsCharsByModel` buckets; `elevenLabsCharacters` is now a derived getter (sum of buckets, back-compat for `toCostUpdate`); `elevenLabsCost` getter sums each bucket × its rate. `modelId` threaded through all three char accumulators (`addElevenLabsUsage`, `recordElevenLabsStreamingStarted`, `recordElevenLabsSpeculativeStarted`), their `active-sessions.js` wrappers, and the call sites (`keys.js:650` proxy → flash, `keys.js:259` streaming → `client.modelId`, `loaded-barrel-speculator.js` → `client.modelId`), each defaulting to Flash. Fast-TTS route (`voice-latency-fast-tts.js`) has no cost attribution today — left out of scope. 4952 backend tests green (+8 new per-model cost tests). v3 is the offline tour-audio model (item B, iOS) and is NOT session-attributed. |
| 2026-06-25 | **Observation-feedback follow-ups (#49 + #52 Fix B, obs-followups-2026-06-23 plan).** #49 — EIC observation-handling made PROACTIVE: the state-snapshot prefix now carries a `CERTIFICATE TYPE: EIC` line (`_computeSnapshotParts`, emitted UNCONDITIONALLY for EIC even on an empty session — before the `isEmpty` early-return, so the steer is present from turn 1; flips empty-EIC `buildStateSnapshotMessage` null→non-null), and RULE 0 in `sonnet_agentic_system.md` gains a proactive clause keyed off it (don't call `record_observation` on an EIC → go straight to the comments ask; reactive reject path stays as defence-in-depth). EICR path byte-unchanged (line is EIC-only → cache untouched). #52 Fix B — canonical BS 7671 `regulation_title`/`regulation_description` now carried END-TO-END on every `observation_update` path: `renameObservationsForLegacyWire` + both refinement (BPG4) payloads + the RULE-6 edit payload (`dispatchObservationUpdates`), each running its own `lookupRegulation(ref)` (import added to `sonnet-stream.js`), null-fallback on a table MISS. Backend-only this cycle; iOS card render (`SonnetObservation`/`JobObservation`/`ObservationUpdate` + `ObservationCardView`) ships next TestFlight. 4904 backend tests green. |
| 2026-06-18 | **Universal read-back + conversational correction (Option B, never-clear)** — backend Phase A. (1) FINAL read-back no longer gates on the model's self-reported confidence; every applied reading is read back (`stage6-event-bundler.js`); `CONFIRMATION_MIN_CONFIDENCE` is now only the loaded-barrel speculator gate. (2) Confirmation debounce re-keyed field+circuit+board+value (was field-only, which dropped same-field different-circuit second read-backs). (3) Auto-derivations (`::calc::` / `derived:true` mirror) exempt from read-back. (4) Prompt CONFIDENCE SCORING rewritten to diagnostic-only — structurally complete readings WRITE at any confidence (supersedes the "low-confidence ASK" invariant 2 stance). (5) bare-`no`/`nope`/`nah` forward through the pre-LLM gate. (6) Rolling ~3-turn context window fed to the live model so it resolves a bare "no" from the read-backs it spoke; bare "no" after a read-back → apologetic re-ask, NEVER `clear_reading` (value persists until a clear replacement overwrites). (7) `low_conf_readback_v1` capability PRE-APPLY gate on `< 0.5` writes; `context_board_id` threaded through `ask_user` schema + all 3 auto-resolve write sites + ask-budget key. NOTE: the §6 round-1 `tool_choice:any` no-op allowance was dropped during the PR #60 rebase — PR #60 removed the round-1 tool_choice force entirely, so the model already no-ops by default. iOS Phase B (drop `< 0.5` client filter, relax `TranscriptGate`, advertise capability) is a follow-on TestFlight cycle. |
| 2026-06-18 | **Audio-First Design Principles** added to hub + iOS CLAUDE.md — product direction shift to hands-free AirPods use (no eyes on screen). Three MANDATORY invariants: (1) every dictated reading read back aloud exactly once, never silently entered into the UI — all apply paths, with auto-derivations (polarity tick, mirrors) exempt by design; (2) low-confidence readings ASK rather than silently drop (supersedes the `CONFIRMATION_MIN_CONFIDENCE` suppress stance); (3) latency is first-class. Supersedes older screen-first / minimise-TTS-chatter guidance. Spans backend extraction/confirmation synthesis + iOS TTS. |
| 2026-06-16 | Voice-feedback fixes (field session F1AC26FB, 5 markers, /rp→/ep, 6 backend commits — PR #55): `LIM` now a first-class IR sentinel (`megaohms.js` parser + legacy twin + IR-field coercion) + per-slot no-progress cap in `engine.js` closing the IR re-ask loop (repeat of 2026-02-18); designation filler-strip (`stripDesignationFiller`) + clean retry echo in the dialogue engine; `implausible_circuit_ref` guard in `validateCreateCircuit` (stops scratch circuit 999); `sub_main_cable_csa` dispatcher guard on single-board jobs; shared-prompt tails + swap/reorder + earthing-garble steering. iOS companion (Defect #1 feedback dual-route + 20s capture timeout + loosened earthing regex) on CertMateUnified PR #17. `[contract]` #3.4 (designation not crossing wire) + #5.3 (atomic swap tool) deferred. |
| 2026-06-12 | PUT board-hierarchy gate rearchitected: invalid hierarchies are now deterministically REPAIRED (cleared dangling pointers, demoted duplicate mains) + persisted + echoed as `hierarchy_repairs`, never rejected — the reject gate had made job_1778443465217 permanently unsyncable for a week. Strict validation stays on the interactive add_board path. `repairBoardHierarchy` in `board-hierarchy-validator.js`. |
| 2026-06-12 | Voice-feedback fixes (session 15B88D6B, 4 field reports + /rp-style retrospective review, 11 backend commits): gate cert-identity weak triggers + 2-word identity threshold (`pre-llm-gate.js`, mirrored on iOS) so spoken client-name corrections forward; bonding check-field PASS coercion + `bonding_conductor_continuity` mirror derivation + early-terminate parity fix; `rename_circuit` `source_not_found` recovery hint; prompt MAIN PROTECTIVE BONDING + MERGED/STUTTERED NAMING sections. Companion iOS commits `27ca1d2`..`06376de`. |
| 2026-06-04 | iOS Fix D (`a62000e` + `b54cb75`) — voice path reconciled with backend Fix B's `spd_*` canonicalisation; legacy `main_fuse_*` aliases dropped from `applySonnetReadings`; supply-side regex split SPD-vs-main-switch. iOS-only. |
| 2026-06-03 | Voice fix (PR #47, three F03B590C defects): FIELD-AMBIGUITY menu dropped, `"main fuse"` routes to `spd_*`, speculator skips off-enum round-1 synth. 4459 backend tests. |
| 2026-05-31 | Voice fix: RCBO walk-through "BS number?" loop closed via unconditional mirror derivation + Deepgram-garble defer triggers. Commits `104735e2` + `684d7ffa`. |
| 2026-05-31 | Voice fix: RCD focus loop swallowed observations + blocked "later" defer. Engine observation guard + dialogue-script gate bypass. Commits `45662e0c` + `4725afbb`. |
| 2026-05-29 | Prod fix: `cert_attestations` table missing in prod — migrations 010/011 never applied. Permanent fix: CI-gate Fargate one-off task runs `node-pg-migrate up` before every service update. |
| 2026-05-28 | Voice latency: widened early-terminate predicate + Loaded Barrel `onToolUseStreamed` to cover `record_board_reading` (~500-1000 ms pre-synth lead recovered). Commits `ba7d8e21` + `b604e7a6`. |
| 2026-05-28 | PWA fix: Circuits tab silently empty for legacy single-board jobs. Shared `isUnscopedBoardId(id)` helper across 8 call sites treats `null`/`undefined`/`''` as unscoped. |
| 2026-05-27 | Prod data-corruption fix: address etc. silently flipping to `[REDACTED]` on every job open. `redactPiiInPlace` in `src/logger.js` was mutating the live ref; rewritten copy-on-write (`5bf304ac` + `d5adb2e3`). |
| 2026-05-22 | CCU dewarp output width restored to 2048-fixed (env-var regression dropped by next CI deploy). Default moved into `src/extraction/ccu-single-shot.js`; `scripts/check-task-def-env-drift.sh` wired into CI. Commit `01c081e`. |
| 2026-05-20 | CCU single-shot prompt hardened: explicit vertical-column rule, tie-break to null, multi-slot label rule. Architecture docs corrected to describe live 2026-05-08+ pipeline. |
| 2026-05-13 | PWA `observation_confirmation` brought to iOS parity — AlertCard render dropped, voice-answer auto-dismiss, 500 ms client burst buffer. Commit `34e3972`. |
| 2026-05-09 | `ask_user.context_field` enum widened to full union of circuit/board/supply/installation fields + sentinels. Unblocks sub-board creation focus-asks. |
| 2026-05-07 | Multi-board sprint Phases 2a + 4a — CSV header round-trip (`ddde287`); recording.js single-board scope warning (`7e588c8`). |
| 2026-05-07 | Multi-board sprint Phase 4 — `/api/analyze-ccu` board attribution + iOS `.addNewBoard` extraction mode. `a40a9f3` + iOS `f9902cd`. |
| 2026-05-07 | Multi-board sprint Phase 3 — PDF sub-main section (iOS-only) at `EICRHTMLTemplate.swift`. Commit `df4311c`. |
| 2026-05-07 | Multi-board sprint Phase 2 — backend schema parity + `board-hierarchy-validator.js`. `1059f39` / `ebb6183` / `ef56e25` / `c21820b`. |
| 2026-05-07 | Multi-board sprint Phase 1 — `sub_main_cable_length` dropped from iOS `BoardInfo` (not required by BS 7671). Commit `723b3f3`. |
| 2026-05-06 | BS-EN canonical aligned to Option B (prefixed form, no `-1` suffix) across schema/lookup/parser/picker/migration. Backend `4611a2e`; iOS `d4d6db1`. |
| 2026-05-06 | Chitchat pause — backend stops forwarding after 10 zero-engagement turns; four wake triggers + 30 s replay buffer. Backend `b0d977a` / `c580f42` / `663c135`; iOS `f6a29f0`. |
| 2026-05-06 | Stage 6 BS-code parser gains Lev-1 fuzzy fallback to break OCPD/RCD re-ask loop. Commit `c36f75a`. |
| 2026-04-30 | Prod hotfix: CCU extraction 502 on every request — `extractJson` brace heuristic choked on concatenated VLM JSON. Replaced with balanced-brace walker. 44/44 ccu-geometric + 157/157 CCU tests green. |
| 2026-04-29 | CCU pipeline cleanup — legacy Stage 2 path retired (branch `ccu-drop-single-shot`). `populated_area_*` Stage 2 prompt + body deleted; `CCU_STAGE2_GROUPS` env var retired. −359 lines, 2407 tests. |
| 2026-04-29 | CCU single-shot Sonnet retired; per-slot is the only path. `classifyBoardTechnology` extended to cover 5 board-level metadata fields. Wall ~21 s vs 47 s; cost ~$0.04 vs $0.10. 2391 tests. |
| 2026-04-22 | CCU per-slot crop-and-classify shipped to prod (HEAD `613d54b`, CI `24805037373`). 10 commits including rewireable pipeline + Stage 4 label pass + Codex P1+P2. iOS companion `f812061`. 464 tests. |
| 2026-04-22 | CCU per-slot primary (`ccu-per-slot-primary`, superseded above). `slotsToCircuits` merger over Stage 3 slots; flag `CCU_GEOMETRIC_V1` defaults ON. Commits `94bf88d` + `ab195b0` + `1301110`. |
| 2026-04-22 | CCU extraction — rewireable fuse board support (`8da2292`). board_technology classification + per-position fuse-carrier branch (BS 3036). |
| 2026-04-19 | Prod fix: web Deepgram WebSocket subprotocol switched from `['token', apiKey]` → `['bearer', apiKey]` for JWT auth (`248953b` removed master-key bypass). Tests unchanged via jest-websocket-mock echo. |
| 2026-04-19 | Prod hotfix #2: PWA login bounce recurrence — local `deploy.sh` re-registered without `JWT_SECRET`. Permanent fix added secret to `ecs/task-def-frontend.json` (`9b5b809`). |
| 2026-04-19 | Prod hotfix: PWA login bouncing — `JWT_SECRET` missing from `eicr-pwa` task def; added via `eicr-pwa:32` + execution-role IAM grant on the SM ARN. Pre-flight check added to `deploy-runbook.md`. |
| 2026-04-18 | Web Phase 8 — production cutover (PR #1, merge `9202351c`). Three pre-flight fixes: `4b77316` CI build flag, `4cef21f` `output: 'standalone'`, `07f79e6` @next/swc lockfile patch. **Closes web rebuild.** |
| 2026-04-18 | Web Wave 2a — vitest test harness + D12 ApiError JSON-envelope parsing + 32 regression tests. |
| 2026-04-18 | Web Phase 7d — offline-sync UI (pending/failed pills, `/settings/system` admin page, discard/requeue, BroadcastChannel). **Closes Phase 7.** |
| 2026-04-17 | Web Phase 7c — offline mutation outbox + replay worker (IDB v2, exp backoff, FIFO). |
| 2026-04-17 | Web Phase 7b — iOS Add-to-Home-Screen hint on `/settings`. **Closes Phase 7b.** |
| 2026-04-17 | Web Phase 7b — AppShell offline indicator. |
| 2026-04-17 | Web Phase 7b — IDB read-through cache (`certmate-cache`). |
| 2026-04-17 | Web Phase 7b kickoff — SW update handoff (sonner toast + `SKIP_WAITING`). |
| 2026-04-17 | Web Phase 7a — PWA foundation (Serwist, manifest, icons, offline boundary, `InstallButton`). |
| 2026-04-17 | Web Phase 6c — system-admin user management. **Closes Phase 6.** |
| 2026-04-17 | Web Phase 6b — company settings + company-admin dashboard. |
| 2026-04-17 | Fix `next build` on `/login` (wrap `useSearchParams` in `<Suspense>`). |
| 2026-04-17 | Web Phase 6a — `/settings` hub + Staff. |
| 2026-04-17 | Web Phase 5d — LiveFillView (brand-blue flash). |
| 2026-04-17 | Web Phase 5c — observation photos (auth'd blob fetch, camera+library pickers). |
| 2026-04-17 | Web Phase 5b — document extraction on Circuits tab. |
| 2026-04-17 | Web Phase 5a — CCU photo capture + GPT Vision on Circuits tab. |
| 2026-04-17 | Start ground-up web rebuild on `web-rebuild`. Phase 0: Next 16 + React 19 scaffold. |
| 2026-03-04 | Add `/api/analyze-document` (GPT Vision for EICR/EIC fields from photos/PDFs). |
| 2026-02-28 | Raise `COMPACTION_THRESHOLD` 6000→60000 — preserves Sonnet's conversational context. |
| 2026-02-23 | Compaction cost guards (5 checks) + `max_tokens` 2048→4096 + 120 s rate limit. |
| 2026-02-23 | Fix audio loss during VAD warm-up; add `/api/health/ready`; iOS pre-flight check. |
| 2026-02-23 | CCU prompt v2 (4-step methodology, RCD waveforms, device-face amp enforcement). |
| 2026-02-22 | 5-star Phase 8: OpenAPI spec, Swagger UI, pre-commit hooks, ADRs. |

Older entries (before 2026-02-22) are in [docs/reference/changelog.md](docs/reference/changelog.md) and `git log`.

## Future Plans

- Evaluate replacing server-side Python PDF generation with Playwright-only approach
- CCU photo analysis: evaluate newer models as they become available
- Expand E2E test coverage

## iOS Deploy (TestFlight)

See [docs/reference/deploy-testflight.md](docs/reference/deploy-testflight.md) for the deploy script, ASC credentials, TestFlight group IDs, and the onnxruntime `MinimumOSVersion` patch.
