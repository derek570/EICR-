> **REFERENCE COPY — NOT EXECUTABLE.** Execute only the canonical handoff final recorded in `provenance.json`; every tracked copy has an adjacent fail-closed EP policy.

# Plan 00C — durable three-day GPT-5.6 field-evidence gate

Status: **DONE — RP converged / EXECUTION-READY (STAGE A ONLY)**
Dependencies: Plans 00A and 00B merged/deployed; 00B expectation projections and SHA-256 anchor ready for post-Stage-A Derek attestation

Execution guard: exact explicit path only; `--no-chain` is mandatory. The adjacent machine policy must verify 00B's genuine merged/deployed success record and `plan00_expectation_manifest`. EP executes Stage A only; the three-day Stage B remains an operator runbook.

## Normative EP boundary

After RP convergence, `[REFERENCE COPY — Plan 00C execution uses its canonical handoff final only]` executes **Stage A only**: evidence/event code, production manifests, commands, tests, docs, PR, merge and deployment. Stage B and final DONE criteria below are a post-deployment operational runbook, not EP tasks. EP must not materialise them as tasks, mark them skipped/blocked/assumed or include them in its pre-merge ALL PASSED gate.

`.ep-done` means only that this Stage-A execution plan was consumed; it is not deployment evidence. After merge, a verified successful deploy plus durable `stage_a_deployed` event yields `STAGE_A_IMPLEMENTED`. A green deploy whose publisher has not yet run leaves the fold `NOT_STARTED`; the first committed operator action may publish it later. A failed deploy or broken/invalid publisher blocks attestation/cohort initialisation and requires a fresh Stage-A fix-forward handoff/PR; never rerun the consumed final. The committed publisher/runner/intake/attestation/status commands—not a second `/ep`—are the sole post-EP resume/finalisation path. Runtime HOLD/DONE remains outside the tracked index while this cohort gates downstream work.

## Outcome

Create one authoritative version-audited append-only evidence stream and deterministic projections, then use it operationally to collect three distinct genuine field days under live Luna Fast / explicit cache / Terra Standard-low. The gate independently requires the pinned IR semantic case, the complete frozen attested Haiku/Luna corpus, exactly-once manual hearing and zero unwaivable safety regression before later behaviour-changing latency plans may execute.

## Stage A — implementation and deployment tasks for EP

### C1 — one version-audited append-only event authority

Stage A implements the event, fold and projection commands in the repository and exercises them only against temporary test directories; EP creates no Plan 00 evidence/cache file beside the handoff. After EP completion, committed operator commands may atomically regenerate `PLAN-00-EVIDENCE.json` and `PLAN-00-EVIDENCE.md` beside the handoff as non-authoritative local projections of verified S3 object versions. The authoritative layout is:

```text
plan00-evidence/events/_stage-a/machine/<sha256>.json
plan00-evidence/events/<prospective-cohort-id>/machine/<sha256>.json
plan00-evidence/events/<prospective-cohort-id>/derek/<sha256>.json
plan00-evidence/reservations/<cohort-id>/<lane>/<identity>.json
PLAN-00-EVIDENCE.json       # derived projection only
PLAN-00-EVIDENCE.md         # derived projection only
```

- Store canonical JSON events, named by their content hash, beneath the dedicated prefix in the already-versioned private `eicr-files-production` bucket. Reuse the existing field-replay `canonicalBytes`, `evidenceEventHash`, `assertAppendOnly` and `verifyTrustedRun` primitives rather than introducing CloudFormation, Object Lock, IAM roles, PassRole/bootstrap work or a new bucket. Every operator event/reservation publisher and every fold verifies bucket versioning before acting and fails closed if it is absent. The ECS session-manifest publisher instead follows the task-role-bounded receipt proof in C3; it does not require bucket-version-list permissions that the deployed backend intentionally lacks.
- Preserve distinct `machine` and `derek` namespaces. Schema validation permits only `stage_a_deployed`, runner/collector and binding events in machine paths; only the explicitly interactive Derek commands may create `expectations_attested`, `manual_attestation`, `dialogue_hearing_attestation`, `non_safety_decision`, `corpus_gap_decision` and cohort-initialisation decisions in Derek paths. A wrong-kind/wrong-namespace event is invalid and cannot advance the fold. *(Amendment 2026-08-04, Plan 00B-2: `dialogue_hearing_attestation` joins this CLOSED allowlist and C1's validation precedence — an unlisted kind is invalid and cannot advance the fold, so without this entry the family gate below could never be satisfied.)*
- This separation is deliberately procedural, not malice-resistant: Derek is the only maintainer/tester and the repository's current threat model is accidental corruption. The shared operator credentials could forge either namespace, so the plan must not claim cryptographic human identity or adversarial tamper resistance. Any future MFA-backed actor separation/Object Lock is a separate, explicitly approved hardening plan—not a hidden Plan 00 prerequisite.
- The authoritative reader uses `ListObjectVersions`, never `ListObjectsV2`, and folds every visible version and delete marker. Verify version id, checksum and key/content hash. Retain every equal-content VersionId in the audit record, but treat the earliest `LastModified` among byte-identical valid versions of one content-hash key as that semantic event's authoritative `published_at`; later identical versions are idempotent receipt duplicates and can never move the event to a later Europe/London calendar day. Any same-key version with different bytes/hash/checksum, delete marker, unexpected extra version, inaccessible version or mismatch is HOLD/BLOCKED and never omission. JSON, Markdown, consumed ids, days and status are projections only. The committed commands never overwrite or delete evidence and use `assertAppendOnly`; version auditing protects against operator mistakes within the stated single-maintainer threat model.
- `stage_a_deployed` cross-binds the exact successful trusted GitHub deploy run/artifact verified by `verifyTrustedRun` to the runtime-reported ECS commit/image digest/task revision plus config/tool/oracle digests, evidence bucket name/versioning state and event-schema hash. Folding it alone yields `STAGE_A_IMPLEMENTED` and no cohort. Prospective/full cohort fingerprints bind those values. Deployment or fingerprint drift is HOLD/new-cohort, never an empty rebuild.
- Runners publish `attempt_pending` before provider dispatch. Before returning, they publish exactly one terminal PASS/FAIL/INVALID event containing report digest and ordered provider response/message ids. A process crash leaves an orphan PENDING which deterministically holds the cohort; it cannot be omitted or dismissed into a pass. If dispatch may have occurred and no terminal evidence exists, recovery requires a new cohort after an explicit archived reason, not a silent rerun in the same cohort.
- Reserve logical new-work identities/run ordinals with deterministic S3 keys and SigV4 `PutObject If-None-Match: *`; read back and version-check every successful allocation. These allocation objects are never deleted or overwritten. A contender that receives 412 reads and validates the winning allocation, calls no provider, and refolds before choosing the next unused new-work ordinal. A 409 is not proof that another writer won: refold and retry the exact same key/identity, and HOLD/BLOCK on any delete-marker or integrity conflict; never allocate a new ordinal merely because of 409.
- The conditionally created per-attempt-generation reservation object **is** the authoritative `attempt_pending` record—there is no separate attempt lock and hence no lock-only crash state. Its deterministic key is derived from the non-caller-overridable `requirement_key` plus generation, and its canonical body contains one runner-minted cryptographically random opaque `attempt_ref`, the logical-allocation/run-ordinal VersionId, requested model/tier and all prompt/tool/expectation digests. Provider dispatch is allowed only after a 200 response and exact versioned read-back of that object. A crash before conditional creation leaves no attempt state and is safely retryable; a crash after it is an ordinary orphan PENDING and follows the conservative HOLD/new-cohort rule. A run-ordinal allocation without fixture attempts is inert/resumable because no provider can dispatch until each fixture's atomic PENDING reservation succeeds. On 412, read back and validate the current reservation. If its complete canonical body—including attempt ref/key/generation, allocation VersionId, model/tier and every digest—matches the frozen candidate retained by this still-running invocation and its invocation-local dispatch latch proves dispatch has not begun, treat it as recovery of this runner's lost 200 response, retain the returned VersionId/LastModified receipt and permit exactly one provider dispatch. A valid reservation with a different attempt ref is another winner: call no provider. Any same-ref or key/body mismatch is an integrity HOLD/BLOCK. A 409 is not a proven loser: refold and retry the exact same reservation key/ref/body; apply the same 412 recovery rule after that retry, and HOLD/BLOCK on any delete-marker or integrity conflict. Never skip a generation or allocate a new ordinal merely because of 409. Every replacement reuses the exact requirement key/logical allocation but creates the next atomic attempt-generation PENDING with a fresh ref only after the preceding terminal makes that generation eligible.
- Exactly one terminal echoes the atomic PENDING identity and digests. The fold pairs only by `attempt_ref`: a terminal without exactly one pending is `INVALID/HOLD`, a pending without terminal is orphan `HOLD_EVIDENCE`, byte-equal terminal duplicates are idempotent, and conflicting terminals for one ref are BLOCKED. Callers can supply neither `requirement_key` nor `attempt_ref`.
- For pinned-IR evidence, repeat identity is a conditionally reserved cohort-unique ordinal spanning every repetition, never a per-command/per-day 1–5 counter. A genuinely new repetition gets a new lock/ordinal; replacement after INVALID reuses its ordinal/key through the next attempt-generation lock. Calendar-day membership comes only from terminal S3 `published_at`, not the requirement key.
- Every repeatable vendor-corpus execution also conditionally reserves a non-caller-overridable cohort-unique `corpus_run_ordinal` once per model lane and complete corpus run. Include that ordinal in every fixture/turn requirement key for the run; never reset it per command/day. Concurrent complete-run contenders that lose conditional create call no provider and reserve a new ordinal. Haiku and Luna have independent run-ordinal namespaces. Calendar-day membership remains terminal `published_at` only.
- An INVALID terminal leaves its requirement unsatisfied and holds only while no later valid terminal exists for that exact key. A valid replacement must use a fresh `attempt_ref`, fresh provider-call identity and identical requirement key; all INVALID history remains visible. Exactly one valid terminal may count per key. Two distinct valid terminals for one key are an integrity BLOCK and never extra repetitions. INVALID for another key cannot be cleared. Orphan PENDING, semantic safety FAIL, manual FAIL and conflicting-terminal rules remain irreversible and cannot be superseded by replacement.
- Validation precedence is mandatory: schema and namespace, reservation/PENDING pairing, attempt-ref/requirement identity, referenced-report integrity, and production-manifest/manual-reference binding are evaluated before semantic outcome. Only a structurally valid, uniquely paired pinned-IR or safety/certificate-mutation terminal with semantic FAIL, or a structurally valid manifest-bound manual attestation with `manual_result:fail`, irreversibly BLOCKS. An unattached, malformed, wrong-namespace, unknown-ref, duplicate-match or otherwise invalid FAIL is `INVALID/HOLD` under the structural rules and never reaches semantic BLOCKED classification. `cohort_blocked` remains optional explanatory evidence. Duplicate equal terminals are idempotent; conflicting terminals for one attempt are an evidence-integrity violation and always BLOCKED regardless of safety class. That deliberate rule is stricter than the undecided non-safety semantic-mismatch HOLD path. HOLD is reserved for PENDING/infrastructure/unattested expectations and undecided non-safety mismatch.
- After durable publish, read the S3 object-version `LastModified` and version id as authoritative receipt metadata. `published_at` from S3 is authoritative for calendar gating; payload `--day`, `generated_at` and `attested_at` are assertions only. Concurrent creation is commutative. Tests cover duplicate/conflicting terminals, manual fail, optional block-event crash, concurrent pass+failure, crash boundaries, shuffled rebuild, delete-marker-hidden FAIL, missing versions, corrupt hashes and three backdated claimed days published on one S3 day (progress ≤1/3).

### C2 — authoritative runners and artifact identity

- Every IR/A-B runner requires the exact bucket/cohort key prefix and refuses to call a vendor until its PENDING event is durably published and read back with version id.
- Evaluation adapters include PII-free provider response/message ids. A sample identity is a terminal-evidence canonical hash of ordered provider-call ids, deployed fingerprint, requirement key, model/tier and prompt/tool/expectation digests; caller timestamps/ids cannot alter it. It is never the pending key and may be null only on an INVALID terminal that received no provider id. Provider ids/sample identities remain cohort-wide single-use across distinct `attempt_ref`s.
- Runner-owned `generated_at` is immutable but not calendar authority. Rewrapped/copied reports, changed outer digests or reused provider ids fold to the same consumed identity and cannot count twice.
- The status/collector enumerates all event files; callers can choose `--day` but not which attempts exist. Withholding/deleting a separate report cannot hide the terminal event; missing referenced content is HOLD/BLOCKED according to its recorded verdict, never pass.
- Pacing and retry classification follow Plan 00B. Provider rate-limit/network/harness failures are INVALID/HOLD, not semantic pass/fail; a valid replacement attempt needs a new provider-call identity while the invalid event remains visible.

### C3 — server-authored deployment/session manifests

Emit separate allowlisted PII-redacted schemas:

- The start manifest contains only opaque session id, trusted start timestamp, schema version, source/task/config/prompt/tool fingerprint and `ECS_CONTAINER_METADATA_URI_V4/task` TaskARN family/revision plus backend container `ImageID` digest. Completion/actual-execution fields are absent, never null or config-derived.
- The completion manifest contains trusted completion timestamp/status; the same deployment identity; actual provider/model/requested-served tier/cache route; allowlisted Plan 00A per-round transport/model/tier/provenance/effort/cache-token/status evidence; final cost count/revision; and the confirmation/ACK ledger. Every route/cache/manual gate consumes completion evidence only; configured start values can never satisfy an executed-route requirement. *(Amendment 2026-08-04, Plan 00B-2 §C2: the completion-manifest schema additionally gains per-family ask-lifecycle fields and dialogue/address delivery fields — for the `dialogue_script` and `address_mirror` families: produced/emitted/resolved ask evidence where applicable and operation-bound delivery evidence, plus the address-mirror authoritative `address_mirror_delivery_ack` playback proof (the wire carries it today). These fields NEVER include a dialogue attested-hearing field — dialogue hearing is the post-completion `dialogue_hearing_attestation` event in C4. They are DERIVED solely from the 00B `sub_records` bridge (each ask/delivery/playback ledger transition appends a canonical lifecycle sub-record; the builder's five-key input snapshot contract is unchanged). 00C's execution must add publication, collection and fold-consumption tests for these fields — without them the fold could not enforce the family gate. The per-round `round_usage` sub-records carry the counting rule: ordinary-reading (Luna Fast) evidence counts ONLY from accepted `inspector_live` rounds and Terra observation evidence only from an inspector round (keepalive/orphan-review rows get negative tests); a Terra Standard/LOW observation round is provable ONLY on the Responses transport — a chat_completions observation round attributes `'none'` reasoning effort (its function-tools constraint makes the observation override structurally inapplicable) and is a NEGATIVE case for the Terra gate.)*
- The PII-free confirmation evidence has two operation-bound ledgers. `delivery_attempt` preserves every successful fast stream, canonical result-frame send/replay and other server confirmation delivery with refs, aliases, channels and timestamps; delivery count may exceed one and never proves client playback suppression. Each operation also carries its persisted outbox delivery-claim token/process lineage and `delivery_history_ambiguous`. Recovery seeing prior/different lineage without the corresponding successful-send observation sets that flag true and never reconstructs missing pre-crash history. A provisional fast-TTS delivery enters the ledger only after 00B resolves it uniquely to the persisted original `extractionTurnId` + canonical effective slot identity + operation ordinal/digest; unresolved/ambiguous/mismatched provisional rows make the session evidence-invalid and never form independent identities. `playback_start` contains only authenticated ACKs resolving uniquely to that authoritative logical operation, with at least one compatible operation delivery but no invented delivery-instance assignment. Canonical full-body hash de-duplicates only byte-identical ACK retransmissions; distinct accepted bodies for the same operation are distinct starts. The adapter proves `activeSessions.get(sessionId)?.userId === req.user.id`; unknown, stale, wrong-slot/correlation, operation-ambiguous and cross-session ACKs remain non-authoritative telemetry. An early fast ACK is staged by correlation beside its provisional delivery and both promote atomically only after unique authoritative operation binding; mismatched/expired/process-lost provisional evidence makes the session ineligible. Process-restart/address-mirror recovery restores already-persisted source identity but never reconstructs an unbound provisional ACK or missing send. Result turn ids/resolution tokens/client dedupe tokens never mint a new logical identity. Do not add a client wire field merely for evidence.

*(Amendment 2026-08-04b, Plan 00B-3 `EICR_Automation--00b-oracle-evidence-contract-2026-08-04` — the CONSUMER half of the oracle-evidence contract. Extends the 2026-08-04 00B-2 amendment above; the sub_records bridge those fields derive from now carries an explicit, schema-pinned row vocabulary, and 00C's fold MUST consume it under these rules.)*

- **Contract artifact reuse (C0):** 00C's Stage A MUST reuse the committed two-sided derivability artifact at `tests/fixtures/test-contracts/plan00-evidence-contract/` (pre-authored `schema-v1.json`, hand-authored `snapshot-v1.json`/`projection-v1.json` + the ineligible pair) as its session-manifest builder's acceptance test: the builder consuming ONLY the five-key snapshot must reproduce the committed expected projections. The 00B-owned projector (`src/extraction/plan00-evidence-projection.js`, `evidence_projection_v1`) and registry (`src/extraction/plan00-evidence-registry.js`) are enumerated semantic-oracle inputs; `src/extraction/plan00-session-manifest.js` remains EXCLUDED under `deployed_evidence_runtime_digest`.
- **Accepted/rejected row schema (C1):** every attempted ask transition yields exactly ONE mutually exclusive sub-record — an accepted `ask_lifecycle` row (stage produced/emitted/reissued_attempt/resolved/replaced/join_expired) OR a rejected `ask_transition_rejected` audit row `{family, stage_attempted, runtime_id, terminal_attempted?, reason}` with a CLOSED rejection-reason vocabulary (schema-v1 `rejection_reasons`). Validation precedence: structural rejections (resolution_without_emitted, runtime_id_already_bound, emitted_without_produced, ambiguous_produced_match, emitted_without_binding, duplicate_runtime_binding, reissue_without_emitted, replacement_predecessor_unknown, srv_answer_ambiguous) latch the ask ledger invalid AND appear as the rejected row; the ONE transition rejection — `answered_without_full_proof` — produces ONLY the rejected row, latches nothing, and leaves the ask OPEN (it counts in `counts.open_asks_*` at stop). ZERO-CREDIT RULE: a rejected row grants no produced/emitted/resolved family credit ever. Deterministic HOLD: contradictory rows for one runtime id (e.g. an accepted resolved row after a structural rejection for the same transition) are an evidence-integrity contradiction — fold INVALID/HOLD, never silent under-count.
- **Ternary ACK verdicts (C1):** pre-admission unmatched/wrong/stale playback resolutions are NOT transition attempts (no row — telemetry; a later valid ACK succeeds normally); a byte-identical ACK retransmission appears as a `playback_idempotent` audit row keyed to the EXISTING authoritative `playback_evidence` row (same op_key + ack_body_hash) and projects NO additional credit; a ledger-layer integrity rejection appears as `playback_rejected` (+ delivery-ledger latch). Duplicate-POST dedupe and wrong/stale-then-valid ACK sequences are pinned in the shared fixture.
- **Unknown-producer rows (C3):** delivery/playback/ask recording is REGISTRY-ID-only (schema-v1 `producer_registry`); an unknown id appears as an uncreditable `producer_unknown` row `{event_class, producer_id_raw}` with the owning ledger latched invalid. Delivery and playback rows carry `semantic_family` (ordinary_confirmation/dialogue_script/address_mirror/fast_tts) PLUS `transport` as SEPARATE fields, a stable `delivery_ref` (`d:<at_seq>`), `at_seq`, and the operation aliases (claim_lineage, wire_turn_id, dedupe_token); playback rows carry `ack_body_hash` + `source`.
- **Canonical freeze-time rows (C5) and the INELIGIBILITY fold rule:** condition-gated `freeze_invalid` rows — `{condition: ask_invalid|delivery_invalid|producer_invalid|mutation_invalid, reason}` for the latches and `{condition: delivery_prepared_outstanding, count}` for the prepared-but-uncommitted dialogue-delivery COUNT (a count, not a latch — a latch-only fold may not ignore it) — are appended inside the completion freeze after fast-TTS settlement and before the revision-stability read, so they sit INSIDE the latched snapshot with stable revisions. The unconsumed fast-TTS PROVISIONAL condition reaches this class by design: the freeze calls the delivery ledger's existing non-throwing `assertNoUnconsumedProvisionals()` directly, folding it into the `delivery_invalid` latch (reason `fast_provisional_unconsumed`) — never a parallel derivation. **Fold rule:** ANY `freeze_invalid` or `producer_unknown` row in `sub_records` renders that session's evidence INELIGIBLE (fold HOLD/INVALID, zero family credit for every family). `counts.non_quiescent_at_stop`/`counts.revision_instability` keep their existing always-evidence-ineligible rule — including when the non-quiescence is an open Tier-2 ask (`counts.open_asks_dispatcher/dialogue_script/address_mirror`, which an eligible freeze requires ALL zero; a stop-boundary-terminal-resolved ask still counts open).

The start/completion single-flight, reconnect-safe teardown arbiter, zero-count/stable-revision quiescence decision and frozen candidate latch are shipped by Plan 00B's lifecycle chokepoint. 00C implements only `src/extraction/plan00-session-manifest.js` and registers its pure candidate builder/publisher through that hook at server boot; it edits no enumerated semantic-oracle input. The builder receives only 00B's immutable allowlisted snapshot, returns canonical schema bytes/hash/key/checksum synchronously once, and never observes mutable session state. Registration-site wiring such as `src/api.js` belongs only to `deployed_evidence_runtime_digest`. The publisher reuses the latched candidate/promise and never rebuilds summary bytes or regenerates timestamps/keys. A `non_quiescent_at_stop` candidate is durably published for audit but is always evidence-ineligible.

Serialize manifests with `canonicalBytes` and write them under `plan00-session-manifests/<deployment-fingerprint>/<session-id>/start-<content-hash>.json` and `completion-<content-hash>.json` in the same versioned bucket. Use strict content-addressed `PutObject`, read each object back, and record VersionId, LastModified and checksum. A publisher error propagates to the manifest/evidence result and makes the session ineligible for Plan 00 evidence, but must not silence or fail the inspector's live extraction/audible turn.

The ECS publisher uses only the existing task-role permissions and MUST NOT call `GetBucketVersioning`, `ListObjectVersions` or `GetObjectVersion`. Its conditional `PutObject` must return a non-empty VersionId other than the literal `null`, and its immediate current-version `GetObject` read-back must return that same VersionId plus matching content hash/checksum and LastModified. Missing/null/mismatched version evidence or read-back failure makes only that session ineligible and never affects the inspector turn. A conditional-create 412 is a possible idempotent duplicate, not by itself a publisher failure: the publisher performs the same current-version `GetObject` read-back, and matching intended content hash/checksum plus a non-empty, non-`null` VersionId and LastModified makes the existing object the successful receipt; a mismatch makes only that session ineligible. A 409 conditional-write conflict retries the exact same key/body with bounded backoff and never rewrites content. Completion-path re-entry after reconnect therefore folds matching content to one eligible manifest. The operator collector later performs the full version/delete-marker audit with operator credentials.

Map the immutable image digest to the ECR `${github.sha}` tag; never infer an older session's code from current ECS service state or mutable `:latest`. Missing metadata, unmapped digest or start/completion disagreement makes the session invalid. The collector enumerates every object version/delete marker in the exact session prefix and rejects missing pairs, conflicting versions, overwrites, delete markers or hash/checksum mismatches. It accepts only these server-authored objects—never CloudWatch text, client summaries or mutable `cost_summary.json`. Add failed-write, conflict/delete-marker, metadata, pair-mismatch, task-rollover and non-ECS/local invalid tests. Keep transcript, model prose, tool payload and customer data out of manifests.

The content-addressed S3 namespace above is the sole authoritative manifest storage. CloudWatch may retain operational diagnostics but is never collector evidence. Manifests are never emitted over the client WebSocket or added to a client-visible frame. Zero client/wire change is expected; stop if parity changes.

The operator supplies session ids only. The collector fetches allowlisted server artifacts, validates completion/fingerprint/route/timestamps and publishes their digests as `production_session_bound` events. Reject nonexistent ids, caller summaries, edited artifacts, stale/wrong deployment and wrong cohort.

### C4 — expectations, attestation and commands

- After Stage-A deployment, provide the single authoritative command rendering both frozen Plan 00B manifests for Derek. It computes a prospective cohort id/fingerprint; an explicitly interactive confirmation publishes `expectations_attested` beneath the Derek namespace with reviewer, `attested_at` and exact combined hash. Automated runners cannot invoke this interactive path and are schema-restricted to machine events, while the plan openly records that the shared one-maintainer AWS identity is a procedural—not cryptographic—boundary. Runners are read-only with respect to expectations and refuse missing/changed/unattached hashes.
- Provide event publish/fold/status commands and exact examples in the derived artifact. Manual daily attestation requires:
  - `field_session_ids` bound to that day's validated manifests;
  - `field_context=genuine_on_site` explicitly attested by Derek;
  - `manual_heard_by=Derek`, `heard_completed_during_session:true|false`, one exact manifest `confirmation_ref`, `attested_at` and `manual_result=pass|fail`.
- The fold resolves the reference to exactly one persisted logical `(original extractionTurnId, canonical effective slot identity, operation ordinal/digest)` in the bound authoritative completion manifest; it never trusts an attestation-supplied id or replay envelope alone. `manual_result:pass` requires `heard_completed_during_session:true`, `delivery_history_ambiguous:false`, a unique logical operation and exactly one authoritative in-session `playback_start`; delivery-attempt count may exceed one. A second distinct playback start for that operation across channels, reconnect or process-restart recovery cannot PASS, while a byte-identical ACK retransmission remains one. The same certificate slot in two distinct extraction turns remains two legitimate logical confirmations and may independently play once. The daily attestation must choose a non-final, unambiguous operation whose playback-start ACK is already durably captured before completion freeze. A genuine final hearing whose ACK arrives only after freeze or an operation with ambiguous crash lineage is `INVALID/HOLD` for that operation/day, not a telemetry defect; the operator chooses another captured operation rather than delaying normal stop. `false` is permitted only with `manual_result:fail`, which irreversibly BLOCKS. Missing, ambiguous, duplicate or contradictory playback evidence is `INVALID/HOLD` and cannot count.
- Never invent playback evidence. Exactly one authenticated `playback_start` inside the trusted session is mandatory; `heard_completed_during_session:true` then attests that this playback completed and was heard exactly once before session completion. `attested_at` must be after completion and before S3 publication. When authoritative playback-completed telemetry exists, bind its timestamp as well; otherwise the manual statement is completion/hearing evidence, not a fabricated timestamp. S3 `LastModified` remains receipt time, never hearing time. Field provenance and AirPods hearing are separate facts. Reject attestations before completion, caller-invented/unknown refs, false+pass, zero/multiple playback starts or hearing outside the session; add a valid manifest-bound pass case.
- Add commands for Derek's dated approve/reject of each named non-safety mismatch and permitted non-safety corpus-gap deferral. Safety-critical clear/write and certificate-mutation strata cannot be waived.
- *(Amendment 2026-08-04, Plan 00B-2 §C2 — the named family-evidence obligation.)* The gate REQUIRES named `dialogue_script` and `address_mirror` evidence: at least one eligible LIVE lifecycle each, with produced/emitted/resolved ask evidence where applicable and operation-bound delivery, read from the completion manifest's per-family fields (C3 amendment above). The PLAYBACK half is FAMILY-HONEST and splits SERVER evidence from OPERATOR attestation:
  - **address_mirror** requires its channel-correct authoritative `address_mirror_delivery_ack` playback proof IN the completion manifest (the wire carries it today).
  - **dialogue_script** carries ONLY its ask lifecycle and operation-bound delivery evidence IN the completion manifest; the hearing proof is a NEW family-specific POST-COMPLETION attestation EVENT with a FIXED kind and schema — `dialogue_hearing_attestation`, fields `field_session_id`, `dialogue_delivery_ref`, `manual_heard_by`, `heard_completed_during_session`, `manual_result`, `attested_at`. The `dialogue_delivery_ref` must resolve UNIQUELY to one unambiguous operation-bound `dialogue_script` delivery in the bound completion manifest. `heard_completed_during_session:false` is permitted ONLY with `manual_result:fail`; `true` may pair with pass OR fail (a heard-but-semantically-wrong dialogue is a legitimate `manual_result:fail` and BLOCKS, mirroring the sibling manual lane); `false`+pass is rejected at validation precedence. **Fold rule (no new schema field):** the event's `field_session_id` must be among that day's `field_session_ids` bound to validated manifests under a daily attestation carrying `field_context=genuine_on_site`, else INVALID/HOLD at validation precedence — a non-genuine session's dialogue lifecycle must not satisfy the family gate. It requires Derek's heard-completed statement and is the ONLY manual-hearing attestation kind exempt from the exactly-one-`playback_start` rule — explicitly not required for dialogue_script, because a post-completion human fact can never be a server-frozen manifest field, and the existing manual lane demands exactly one authoritative `playback_start`, which dialogue speech STRUCTURALLY cannot produce (today's clients route it through local confirmation paths with no session/turn playback-ACK identity, and a client-wire change is prohibited this wave; this deliberate weakening is named here by design). Ordinary confirmations keep the exactly-one-playback rule and address_mirror keeps the authoritative-ACK rule unchanged. HOLD when either family's required evidence is absent or ambiguous; a targeted attested field probe qualifies. The fold/gate NEGATIVE TESTS for this event are authored by 00C's OWN execution when its fold/gate code exists — this amendment carries the requirement text, the completion-manifest schema fields and the test obligation only. Admission of these families into the recorded corpus requires an explicit capability-boundary extension. Dated follow-up (future client work, NOT this wave): a capability-gated dialogue playback-ACK identity across backend/web/iOS would let dialogue speech carry real playback identity and retire this exemption.

### C5 — projections, docs and Stage-A terminal

- Fold states: `NOT_STARTED`, `STAGE_A_IMPLEMENTED`, `HOLD_EVIDENCE`, `BLOCKED`, `DONE`.
- Handoff-local `PLAN-00-EVIDENCE.json` and Markdown are generated from verified object versions; tracked batch status is never generated. After EP reports a green deployed task, the first explicitly operator-run committed `publish-stage-a` command verifies the exact successful trusted deploy run/artifact and runtime ECS image/task metadata before publishing `stage_a_deployed` under `_stage-a/machine/`; EP itself has no post-deploy command hook, and `.ep-done`/local markers have no evidentiary weight. Before that event, the operator command also proves the deployed task role can `s3:PutObject` and `s3:GetObject` under `arn:aws:s3:::eicr-files-production/plan00-session-manifests/*`, using either read-only operator-credential inspection of the actual inline/managed role policies or one deployed smoke session whose start/completion manifests pass full receipt/read-back validation. Missing/denied proof blocks `stage_a_deployed`; remedy requires a source-committed IAM/infra fix in a fresh fix-forward handoff, never a live-only edit, and starts a new prospective cohort. The operator may run the publisher later after a green deployment; until then status remains `NOT_STARTED`. The attestation command publishes under a prospective cohort id. Initialisation verifies matching deployment and attestation versions before publishing `cohort_initialized`; only then is status `HOLD_EVIDENCE — 0/3`. Missing/changed/unattached attestation prevents initialisation.
- Cohort fingerprint includes deployed commit/image/task/config and trusted deploy-run identity; evidence bucket/versioning/schema; prompt/tool digest; models/tiers/cache/temperature; observation/Loaded-Barrel isolation; oracle/corpus versions; and both attested expectation hashes. It carries two distinct digests: `semantic_oracle_digest`, recomputed from 00B's committed enumerated lifecycle/mutation/capture/ask/audibility/schema/runner input manifest and required to equal the 00B success artifact before cohort initialisation; and `deployed_evidence_runtime_digest`, covering the complete deployed Stage-A source/image including C3 evidence instrumentation and used for deployment/cohort drift. Evidence-publisher work registers through the frozen 00B hook and changes only the runtime digest. Any 00C edit to an enumerated semantic-oracle input—including `sonnet-stream.js` or the hook seam—fails the pre-merge check and requires a fresh reviewed/delivered 00B successor plus updated 00C dependency.
- A relevant source/deployment/prompt/tool/model/tier/cache/oracle/expectation change starts a new cohort id. Archive old events/projections; never delete or reinterpret them.
- Every status, finalise and downstream-latency-plan gate performs a fresh read-only ECS task-definition/image/runtime-config check, resolves the image digest to its ECR source SHA and trusted deployment run, recomputes the deployment fingerprint and compares it with the cohort. Unavailable live state or any image/task/config drift returns `HOLD_EVIDENCE` with `STALE_DEPLOYMENT`; a saved DONE projection alone never unblocks another plan. A new verified deployment event is required before a new cohort can begin.
- Commit the implementation at `scripts/plan00-evidence/` (canonical event, conditional reservation, fold, publish-stage-a, attest and status commands), `src/extraction/plan00-session-manifest.js`, `src/__tests__/plan00-evidence.test.js` and `src/__tests__/plan00-session-manifest.test.js` (adjust names only on verified current-source collision). Update architecture, deployment, field-replay corpus and changelog. Verify Plan 00A A0's tracked derivative/provenance manifest and repo-relative umbrella/child links; do not rematerialise them. The tracked latency-batch index is a static external-status entry and must never receive cohort HOLD/DONE while it gates downstream work; the current workflow would redeploy and stale the cohort. Deliver through normal PR-only workflow and wait for deployed task health.

Stage-A EP `ALL PASSED` covers C1–C5 implementation, tests, docs and diff review before merge. Merge/deploy and waiting for deployed task health are separate post-merge delivery conditions. After a green deploy, the committed operator publisher creates/verifies `stage_a_deployed` and folds to `STAGE_A_IMPLEMENTED`; until it runs, status is `NOT_STARTED`. A deployment failure or defective publisher permits no attestation/cohort and requires a fresh fix-forward handoff. It does not require a field day or vendor evidence.

## Stage B — post-deployment operational runbook, not EP tasks

### Per-day requirements

Collect at least three distinct Europe/London calendar days. Each accepted day contains disjoint evidence:

- at least one collector-resolved production session that Derek binds and attests was genuine on-site work;
- at least five valid paced Luna repetitions of the board-wildcard pinned IR semantic oracle, each with a distinct cohort-unique repetition ordinal;
- one complete paced run of attested `vendor_live_expectations` through Haiku and one through Luna, each independently judged;
- bound production manifests containing at least one successfully completed Luna Fast ordinary-reading round, at least one actually executed Terra Standard observation round with low reasoning and omitted/Standard tier, and nonzero provider-reported cache-read or cache-write evidence for the expected explicit-cache route; configured-but-uninvoked routes and cold-only cache rows are insufficient;
- manual AirPods heard result;
- *(Amendment 2026-08-04, Plan 00B-2 §C2)* the named `dialogue_script` and `address_mirror` family evidence per the C4 amendment: at least one eligible LIVE lifecycle each with produced/emitted/resolved ask evidence where applicable and operation-bound delivery; address-mirror's authoritative `address_mirror_delivery_ack` playback proof in the bound completion manifest; dialogue-script hearing via a valid `dialogue_hearing_attestation` event bound to a genuine-on-site session per the fold rule (HOLD when absent/ambiguous; a targeted attested field probe qualifies);
- every mismatch and any permitted Derek decision.

Operationally, during the bound on-site session Derek must dictate at least one observation and complete at least two extraction turns on the explicit-cache route; otherwise that day cannot satisfy the manifest evidence gate.

An accepted day's PENDING, terminal, bound production manifest and manual-attestation S3 `published_at` values fall within the same Europe/London day, after cohort deployment. `--day`, `generated_at` and `attested_at` must agree with real ordering but cannot move evidence between authoritative receipt days; reject future/backdated/skewed assertions. The manual `attested_at` falls after bound session completion and before its event publication. Provider call ids, sample ids, report digests and production session ids are cohort-wide single-use.

The daily vendor corpus is the attested `vendor_live_expectations` plus named, dated Derek-approved non-safety deferrals. Attested `deterministic_egress_expectations` is gated by the deployed Stage-A CI/oracle fingerprint and is not rerun as vendor evidence. Every fixture belongs to exactly one lane. No safety-critical clear/write or certificate-mutation expectation may be deferred.

### Failure and decision rules

- Any pinned-IR miss, safety/certificate mutation mismatch or manual heard failure makes the cohort irreversibly `BLOCKED`; passing reruns cannot replace it.
- An infrastructure error or orphan PENDING makes `HOLD_EVIDENCE`; it never counts as semantic pass/fail.
- An unapproved non-safety mismatch remains HOLD until Derek publishes an explicit decision keyed by mismatch id. A rejected mismatch blocks; an approved one remains named/quantified.
- A double-TTS recurrence is recorded and routed to the separate exactly-once investigation. It blocks this cohort only if it prevents trustworthy daily hearing evidence.

### Progress and finalisation

- After post-EP attestation/cohort initialisation: `HOLD_EVIDENCE — 0/3`; accepted days advance to `HOLD_EVIDENCE — 1/3`, then `HOLD_EVIDENCE — 2/3`; the third accepted day folds to `DONE` only when every automated/manual gate passes.
- Status regenerates handoff-local JSON/Markdown from version-audited append-only events and the fresh live deployment check; it never writes the tracked index. Never rerun Plan 00C EP after `.ep-done`. Later behaviour-changing latency plans call this live status command and remain blocked until it returns DONE.

## Verification

Stage A tests include canonical hashing, append-only publishing, bucket-versioning refusal, version/delete-marker/corruption/shuffled fold; equal-content versions straddling Europe/London midnight in shuffled orders with earliest receipt controlling day membership; S3 conditional-create 200/412/409 behavior; crash-before-atomic-create, crash-after-atomic-PENDING, proof that lock-only attempt state is impossible, lost-200→same-ref 412 producing exactly one provider call/terminal, different-ref 412 producing zero calls, same-ref mismatched body failing closed, and 409→same-ref 412 recovery for IR, corpus fixtures and INVALID replacements; concurrent IR/new-run allocation with no duplicate dispatch, replacement-generation contention, normal pending/terminal pair, zero-provider-id INVALID, INVALID→valid same-key/ordinal replacement, INVALID-other-key isolation, duplicate valid same-key rejection, terminal-without-pending, conflicting terminals and cross-attempt provider-id reuse; terminal-without-PENDING+FAIL, wrong-key safety FAIL, unknown/duplicate `confirmation_ref`+manual FAIL, and one valid bound failure of each class proving structural validation precedes semantic BLOCKED; IR ordinals 1–5/6–10/11–15 and Haiku/Luna corpus-run ordinals 1/2/3 across three days, reused-ordinal conflict; orphan pending; BLOCKED dominance; wrong-kind/namespace rejection; trusted-run/runtime deployment cross-binding; artifact rewrap/provider-id reuse; append-only session-manifest start/completion pairs; manifest PutObject missing/null VersionId, current-version mismatch and read-back failure proving evidence ineligibility while live extraction/audibility completes; duplicate identical manifest publish returning 412 folded as one idempotent success recording the existing VersionId, 412 with mismatched read-back making only that session ineligible, and bounded 409 same-key/body retry; controlled extraction-before-stop with late usage/confirmation plus concurrent stop/reconnect/time advancement proving one teardown promise/object and either all late facts or explicit `non_quiescent_at_stop`, never eligible partial evidence; two different confirmation refs for one turn/slot, reconnect replay of one logical confirmation, and two distinct turns legitimately confirming the same slot once each; evidence-publisher-only change altering `deployed_evidence_runtime_digest` without changing the semantic digest, and mutation/capture/oracle input change blocking initialisation; manifest ECS metadata/digest mapping/task rollover; reading-only Luna, configured-but-uninvoked Terra, wrong Terra tier/effort and zero cache evidence; expectation partition/hash changes; genuine-field/manual binding; heard/attested/receipt ordering; complete vendor manifests; deterministic egress fingerprint; five IR samples/day; static tracked-status/no-docs-redeploy gate; gap decisions; Stage-A deploy-failure/operator-publish recovery; stale-DONE runtime drift; and deterministic 0/3→DONE projection. Run focused suites, `npm run replay:field-corpus:prepush` and full backend tests.

*(Amendment 2026-08-04, Plan 00B-2 §C2)* Stage-A tests additionally cover the per-family completion-manifest fields: publication, collection and fold consumption of the `dialogue_script`/`address_mirror` ask-lifecycle and delivery fields derived from the `sub_records` bridge; the `dialogue_hearing_attestation` validation matrix (unique-ref resolution, false+pass rejection at validation precedence, the genuine-on-site fold rule, unknown/ambiguous ref INVALID/HOLD) authored against 00C's own fold/gate code; and the `round_usage` counting-rule negatives (keepalive/orphan-review rows never count; chat_completions observation rounds attribute `'none'` and are Terra-gate negatives).

Additional lifecycle/evidence RED cases prove the 00C diff cannot touch 00B's enumerated semantic paths; no-observer parity; every active billable/extraction/frame/outbox/refinement/confirmation count makes completion ineligible with no post-freeze mutation; explicit-stop/disconnect-timeout races against `session_start` and `session_resume` never stale-rebind; a pre-crash successful send followed by different-claim/process recovery records the replay normally but sets `delivery_history_ambiguous:true`, so one replay ACK is HOLD/INVALID and never PASS, while same-process retries retaining every observation use the ordinary one-start/two-start rule; genuinely new utterances to the same slot remain distinct; normal fast plus client-suppressed canonical delivery yields two deliveries/one operation-bound playback start and may PASS, actual fast+canonical playback yields two distinct starts and cannot PASS, suppressed replay adds delivery only, audibly started replay adds a second start, one real-schema ACK after two compatible deliveries remains one start, identical duplicate POST remains one, and an operation-ambiguous ACK is HOLD/INVALID; a pre-operation fast ACK promotes atomically after binding while mismatched/expired/process-lost provisional evidence is ineligible; an attested non-final ACK captured before freeze may PASS, while an ACK arriving after completion freeze yields zero captured starts and HOLD/INVALID, never PASS; configured start-manifest fields cannot satisfy actual-route/cache gates; wrong-user/stale/wrong-slot/wrong-correlation/cross-session playback ACKs remain non-authoritative while one bound ACK succeeds; and `publish-stage-a` refuses absent task-role manifest-prefix read/write proof.

## Final DONE criteria

- Plans 00A/00B delivered and current deployed fingerprint matches the cohort.
- Exact live routes are exercised and proven on every genuine field day: Luna Fast reading, Terra Standard/low observation and nonzero explicit-cache read/write evidence.
- Every valid paced Luna pinned-IR repetition passes across all three accepted days (minimum five per day, at least fifteen total); any miss irreversibly BLOCKS the cohort.
- Both models complete the frozen attested corpus on each day with no unwaived safety regression.
- Every day has a bound genuine-on-site session and manual AirPods PASS.
- Every non-safety mismatch/deferral is named, quantified and explicitly decided.
- No orphan PENDING, missing/corrupt event, reuse, safety failure or BLOCKED event exists.
- *(Amendment 2026-08-04, Plan 00B-2 §C2)* Every accepted day carries the named `dialogue_script` and `address_mirror` family evidence (C4 amendment): eligible live lifecycles with operation-bound delivery, the address-mirror authoritative playback ACK, and a valid genuine-on-site-bound `dialogue_hearing_attestation` for the dialogue family.

---

*Provenance note (2026-08-04):* the family-evidence amendments above (C1 allowlist, C3 completion-manifest fields, C4 named obligation + `dialogue_hearing_attestation`, Stage-B per-day bullet, Verification test obligation, Final-DONE gate) were recorded by Plan 00B-2 (`EICR_Automation--00b-composition-seam-2026-08-04`, /ep session `20260804T125234Z-ep`) per its §C2 two-tier evidence boundary and its refinement rounds 8–9 findings: the frozen corpus cannot traverse the dialogue-script or address-mirror channels, so without this recorded obligation 00C could reach DONE with neither family proved. 00C's pre-claim path and the original-00B success-artifact identity are UNCHANGED.
