I cannot produce recommendations for this session because the debug log is truncated and the manifest is missing. Please re-run analysis after capturing a complete session.

No JSON block was emitted. The parser should flag this as an error rather than silently returning an empty array.
