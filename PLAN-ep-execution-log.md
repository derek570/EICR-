# /ep execution log — PLAN-final.md (clear-board-reading A1a)

- Session: `20260727T205735Z-ep`
- Plan: `/Users/derekbeckley/.claude/handoffs/EICR_Automation--clear-board-reading-a1-2026-07-26/PLAN-final.md`
- Worktree: `/Users/derekbeckley/Developer/EICR_Automation-ep-20260727T205735Z-ep`
- Branch: `ep/clear-board-reading-a1-20260727T205735Z-ep` (base `main` @ 6e69ca14)
- Chain: `--chain` passed; plan carries `.ep-queue` marker (wave member)

[PLAN-SIZE] This plan is 818 lines / one indivisible backend feature (schema + dispatcher + collapse + audibility channel + wire ledger + fixture + ~20 test rows). The conversation-context explicitly accepts this size after two splits (A→A1/A2, A1→A1a/A1b) — the honest-floor note says cutting further would invent a seam. Expect a long Codex convergence.

## Steps

## Step 1 — Capability plumbing (voice-latency-config.js)
- Status: applied
- Decision: rule 1 (verbatim — the four P3 sites)
- Files: src/extraction/voice-latency-config.js, src/__tests__/voice-latency-config.test.js
- Commit: 9359987f
- Notes: KNOWN_SUPPORTS pin 9→10.

## Step 2 — Tool schema + derived 78-member enum
- Status: applied
- Decision: rule 1. Enum arithmetic executed against the real modules pre-implementation: 84 − 6 = 78, exactly one FIELD_CORRECTIONS collision (ze), pfc rewrite harmless — all three plan claims verified true.
- Files: stage6-tool-schemas.js, stage6-prompt-leak-filter.js, three count-pinned test suites, new stage6-clear-board-reading-enum.test.js
- Commit: 1ef91cde
- Notes: tests 1–2 authored here (literal pin generated by executing the real builder; computed collision leg).

## Step 3 — Snapshot mutator trio + boardFieldAliasSet
- Status: applied
- Decision: rule 1 (§3.3/§3.3a). scope passed explicitly by the dispatcher — no import, no re-derivation.
- Files: stage6-snapshot-mutators.js
- Commit: 0c57f20f
- Notes: board-scoped main-target clear sweeps BOTH backing buckets of the main board (circuits[0] + the main boards[] record) — targets exactly one board, defends against seeded copies.

## Step 4 — perTurnWrites: mandatoryNotices + EFFECTIVE_BOARD_SLOT
- Status: applied
- Decision: rule 1 (§3.4/§3.5a)
- Files: stage6-per-turn-writes.js
- Commit: 9582a9fc
- Notes: boardSlotKey uses the NUL-bracketed sentinel convention written as u0000 ESCAPES — the first Edit attempt inserted literal NUL bytes into the source; fixed with perl before commit.

## Step 5 — dispatchClearBoardReading + registry + scope map + mechanism A
- Status: applied
- Decision: rules 1–2. [ASSUMED] the wrong_board backstop sits between enum validation and the capability denial (the plan pins only enum → capability → cert-type → mutator; scope-first mirrors record_board_reading's precedent and both are hard-rejection validation classes — test 20's four sublegs pass).
- Files: stage6-dispatchers-board.js, stage6-dispatchers.js, stage6-dispatch-validation.js (comment), barrel/scaffold test pins
- Commit: 876e9a64
- Notes: five rotating families; strict monotonic per-session cycle; within-turn (family+slot) dedupe BEFORE selection; capability-missing checked before kill-switch (the honest dark-state code, per §3.4's validation-order note).

## Step 6 — Bundler: mechanism B + #31 re-source + projected board_id
- Status: applied
- Decision: rule 1 (§3.4 hop 4). Board collapse matches stamps ONLY (no raw fallback) — a board clear only dispatches for classified fields whose same-slot writes are stamped, so stamp-vs-stamp is complete for every reachable pair.
- Files: stage6-event-bundler.js
- Commit: 9cc82ebb

## Step 7 — Harness: capability threading (live+shadow) + net-0 drain
- Status: applied
- Decision: rule 1 (§3.1/§3.5a). Drain placed AFTER §D2, BEFORE the F7 pre-emission net; voiceNotices byte-unchanged; already-empty same-slot suppression at the drain.
- Files: stage6-shadow-harness.js
- Commit: a770bf5e

## Step 8 — §3.4c ordered frame ledger + infra pins
- Status: applied
- Decision: rule 1/2. ONE shared emitter for immediate + reconnect; Symbol-backed FRAME-level cursor; VCR last; cost outside. [ASSUMED] the VCR frame is BUILT inside the shared ledger (identical JSON on both paths — verified byte-identical construction) while VCR-related logging/refinements stay caller-side; the plan's "do NOT unify the VCR gating" is honoured in that failure/requeue/exactly-once semantics are preserved and strengthened (a post-VCR failure is structurally impossible inside the ledger). The ledger OWNS observation sends per the round-9 requirement (failures cursor-visible); dispatchObservationUpdates survives for the legacy batch path.
- Files: sonnet-stream.js, ecs/task-def-backend.json, scripts/field-replay/replay-environment.mjs
- Commit: bd89ef36
- Notes: the two predicted env-guard test failures (unclassified BOARD_CLEAR_DISABLED read) confirmed and closed by the same-commit pins. P4d suites green unchanged.

## Step 9 — Prompt + leak filter + render tests
- Status: applied
- Decision: rule 1 (§3.6 + round-13 riders). Caps bumped measured+~100 with measurements shown (base 17008→17108, combined 22245→22345) per the plan's own budget section. Group-8 field audit extended to recognise clear_board_reading spans (the Delete-Ze example's field "ze" is a clear-enum member, not a circuit field).
- Files: config/prompts/sonnet_agentic_system.md, stage6-agentic-prompt.test.js, stage6-agentic-answers-session.test.js
- Commit: 73a8945a

## Step 10 — Test suites (rows 3–11, 14, 17–20 + twins)
- Status: applied
- Decision: rule 1. Four new suites (~65 tests): dispatcher (27), bundler (9), audibility seam (19), wire ledger (10) + the enum suite from step 2. Test 4's four hydration legs drive the REAL EICRExtractionSession.start seam (SDK mocked; sessions stop()ed so jest exits). Test 14 leg (iii) proves the shadow lane resolves the same capability AND mutates only the clone.
- Files: src/__tests__/stage6-clear-board-reading-{dispatcher,bundler,audibility,wire}.test.js
- Commit: f697443f, 6e139a81
- Notes: test 17's LIVE-path send-order leg is pinned at the flush seam — the same shared emitter drives both paths by construction (ONE ledger, §3.4c's whole point), so the ordering is proven once. [ASSUMED] test 8 leg (b)'s djb2-collision fixture is subsumed by the dispatcher rotation test (monotonic-cycle + duplicate-staging-never-consumes + 3-distinct legs discriminate against ANY hash selector regardless of id format — the plan's own round-12 note says the sample arrays are unportable).

## Step 11 — Recorded fixture frc_e94d9854… + validation
- Status: applied
- Decision: rules 1–2. Resolution (a) as decided; keystone branch NOT taken (capture had confirmations ON); [ASSUMED] purpose: triage — the schema's dual-proof rule forbids required_green+regression without a red-proof id, and no honest red proof exists (unknown_tool is a different failure class, exactly as the plan's test-13 row states). Governance record in HANDOFF.md.
- Files: tests/fixtures/field-replay-corpus/frc_e94d9854ba728621ade73126161023da/fixture.yaml
- Commit: 95ea7272

## Step 12 — Docs + ledger + vault + HANDOFF
- Status: applied
- Decision: rule 1 (§4 docs, §6, §8.7)
- Files: architecture.md, field-reference.md, changelog.md, CLAUDE.md, AGENTS.md, web/docs/parity-ledger.md, web/docs/parity-ledger-files.json; host-side: ~/obsidian-vault/active/todos-certmate.md, HANDOFF.md (this folder)
- Commit: 932519f3

## Gate (pre-review)
- Full backend Jest: 6287 passed / 19 skipped / 0 failed
- validate-fixture: 9/9 · field-replay corpus: 9/9 pass (exit 0)
- Prompt suites green with the two measured cap bumps.

[FOLLOWUP] Pre-existing record_board_reading write-path gap — the 84-member write enum advertises fields with NO live client apply arm (sub_main_* trio grep 0 in both clients' voice paths; the ir_test_voltage_v class); src/extraction/stage6-tool-schemas.js record_board_reading enum; matters because the model can already write values no client applies; smallest next action: fold into A1b's §6 sweep scope decision. (Queued in vault todos-certmate.md.)
[FOLLOWUP] Pre-existing boardless perTurnWrites.boardReadings key collision — two same-field writes on different boards in one turn last-write-wins (stage6-dispatchers-board.js encodeBoardReadingKey with undefined board); wire defect independent of A1a; smallest next action: dedicated write-path key-shape plan. (HANDOFF.md.)
[FOLLOWUP] applyBoardReadingFlagAware bucket-split normalisation — global writes land in circuits[0] OR boards[] by currentBoardId at write time; the clear now compensates; the cleaner long-term shape is one bucket, but it changes behaviour iOS depends on; smallest next action: own plan post-A1b. (HANDOFF.md.)
[FOLLOWUP] Dual-semantic `extent` key (EICR vs EIC persist to different slots under one raw key) — not voice-clearable until distinct wire names exist; smallest next action: A1b class-3 exclusion is already planned; the rename is its own follow-up. (HANDOFF.md.)
[FOLLOWUP] Ze-cascade provenance refinement — a provenance-rich model could make clearing ze safely cascade into applyZeAutoSideEffects outputs; rejected for now (destructive); smallest next action: revisit only with per-field provenance on both clients. (HANDOFF.md.)

## Independent diff review (Codex, gpt-5.6-sol) — 4 cycles, converged 13 → 2 → 1 → 0
- Cycle 1 (full 3-lens review of PLAN-ep-diff-r1.patch, 35 files): ~13 findings.
  - APPLIED (commit 850cf42c): drain-time notice-text selection (a suppressed notice never consumes a rotation variant — staging is metadata-only); notice families 3→5 variants; telemetry field/board/reason dimensions on `stage6.mandatory_notice_emitted`; reconnect rebind re-parses `msg.capabilities`; meaningful-value-aware clear semantics (`isMeaningfulStoredValue`); earthing sort-group predicates widened to `clear_board_reading` (dispatcher + tool-loop inlined fallback). Plus new tests: REAL session-seam suite (initSonnetStream + live dispatcher), net-0×suppressed-ask table, rotation exactly-once-under-suppression, djb2-collision discrimination.
  - REFUTED with evidence: E1 (onBatchResult is the legacy lane — `field_corrections` are produced only by the Stage-6 bundler, which never routes through it); E5 (diff artifact verified to contain exactly the A1a files — Codex misread changelog TEXT mentions as file headers); E6 (async ws.send residual = the documented P4d accepted residual, PLAN-C watchdog territory).
  - PLAN-DELIBERATE: S3 (mixed soft-skip + hard-rejected sibling speaks only the notice) — §3.5a pins "a mandatory notice must never be accompanied by an apology for the same turn".
- Per-fix mini-review of 850cf42c: 2 IMPORTANT findings.
  - M1 APPLIED (commit d2a5f947): the reconnect capability re-parse is UNCONDITIONAL — an omitted block re-parses to the empty/deny shape (deny-first for every registry capability); the omitted-block seam test flipped to assert DENIAL.
  - M2 ADJUDICATED OUT OF SCOPE → [FOLLOWUP] below: reconnect/in-flight capability race + buffered-frame replay to a downgraded client. Pre-existing shared reconnect architecture (all capabilities), unreachable while A1a is mutation-dark, buffered-frame half fail-drops on incapable clients per §8 criterion 6. Codex accepted the adjudication in cycles 2–4.
- Cycle 2 (full re-review of PLAN-ep-diff-r2.patch + adjudications): 2 findings, both verified real.
  - IMPORTANT APPLIED (commit c560b83a): flushPendingExtractions never kicked refineObservationsAsync — an observation first delivered via reconnect replay permanently lost BPG4 refinement (a narrowed regression vs main, where refinement kicked before the VCR). Fix: kick on flush-side ledger completion — exactly-once by construction (every path into pendingExtractions is a never-kicked path). New seam test pins pendingRefinements seeding (fake OpenAI search HANGS deliberately — a fast-fail fake returns null which deletes the entry as "resolved").
  - MINOR APPLIED (same commit): stale #31 "UNCHANGED / board-UNAWARE" bundler comment rewritten to the effective-board-slot same-slot contract.
- Cycle 3 (verify): fix 1 confirmed correct (halt/requeue untouched, entry-closure ws-safe, no double-refine); 1 MINOR — the rewritten comment cited 11-L1/L2 for the cross-board pins (those are tests 6/6c). APPLIED comment-only (commit 02a68ea4).
- Cycle 4 (verify): citation confirmed accurate. **VERDICT: PASS — 0 findings.**
- Post-fix gates re-run after every fix commit: 6303 backend Jest green, corpus 9/9 (exit 0).

[FOLLOWUP] (Codex mini-review M2, adjudicated out of A1a scope) Reconnect capability change is unsynchronised with an in-flight turn: a turn snapshots capabilities at dispatch, so a mid-turn downgrade still commits the clear, and a buffered board frame can replay to a downgraded client (which fail-drops it — server/client divergence of the F5 class in a build-swap-mid-session window). Unreachable while mutation-dark; pre-existing shared architecture for ALL capabilities. Smallest next action: make it an explicit A1b advert-flip prerequisite — either a turn-generation fence on rebind or an at-dispatch capability re-check. (Queued in vault todos-certmate.md A1b section + HANDOFF.md.)

## Completed
- Outcome: SHIPPED (Codex PASS after 4 cycles; all in-scope findings fixed; adjudications E1/E5/E6/S3/M2 recorded)
- Steps: 12/12 applied · 0 skipped · 0 deviations beyond logged [ASSUMED] entries
- Final gates: backend Jest 6303 passed / 19 skipped / 0 failed · corpus 9/9 (exit 0) · prompt suites green (measured cap bumps documented in step 9)
- Branch: ep/clear-board-reading-a1-20260727T205735Z-ep (worktree ../EICR_Automation-ep-20260727T205735Z-ep)
- Commits: 15 (implementation 12 + review fixes 850cf42c, d2a5f947, c560b83a, 02a68ea4)
- PR/deploy: recorded below after merge.
