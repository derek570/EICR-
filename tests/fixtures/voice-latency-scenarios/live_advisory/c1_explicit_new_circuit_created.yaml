# ============================================================================
# LIVE-LANE ADVISORY PROBE (Codex r2-#5 — C1/C2/C3 prompt-steering pins,
# field-feedback-2026-07-14). Needs a real model; advisory, not a hard gate.
# Run with:
#   node scripts/voice-latency-bench/transcript-replay-direct.mjs --loaded-barrel=off \
#     --scenario=tests/fixtures/voice-latency-scenarios/live_advisory/<this file>
# ============================================================================
name: c1_explicit_new_circuit_created
suite: live_advisory
lane: live-advisory
description: |
  C1 pin (the other side): EXPLICIT new-circuit naming intent still creates
  immediately — no "which existing circuit?" ask for a clearly-new name.

job_state:
  boards:
    - id: main
      designation: "DB-1"
      board_type: main
  circuits:
    - number: 1
      board_id: main
      designation: Upstairs Lights
    - number: 2
      board_id: main
      designation: Water Heater

transcript:
  - at_ms: 0
    text: "Circuit 6 is the garage."
    isFinal: true

expect:
  ask_user_count: { max: 0 }
  tool_call_sequence:
    - tool: create_circuit

config:
  timeout_ms: 60000
  drain_ms: 15000
  fetch_tts: false
