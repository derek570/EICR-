import { TabStub } from '@/components/job/tab-stub';

export default function PdfPage() {
  return (
    <TabStub
      landsIn="Phase 3c"
      title="PDF"
      summary="Preview and download the finished certificate. Generation runs on the Node + Python backend; web streams the result directly."
    />
  );
}
