import { TabStub } from '@/components/job/tab-stub';

export default function InspectorPage() {
  return (
    <TabStub
      landsIn="Phase 3c"
      title="Inspector"
      summary="Inspector name, position, qualifications, signature. Pulls defaults from the Inspectors settings page."
    />
  );
}
