import { TabStub } from '@/components/job/tab-stub';

export default function InstallationPage() {
  return (
    <TabStub
      landsIn="Phase 3a"
      title="Installation"
      summary="Address, occupier, client, date of inspection & next inspection due. Matches iOS InstallationTab."
    />
  );
}
