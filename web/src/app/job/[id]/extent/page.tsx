import { TabStub } from '@/components/job/tab-stub';

export default function ExtentPage() {
  return (
    <TabStub
      landsIn="Phase 3a"
      title="Extent & Type"
      summary="Scope of works, type of installation, limitations agreed with client. EIC only."
    />
  );
}
