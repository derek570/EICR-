import { TabStub } from '@/components/job/tab-stub';

export default function CircuitsPage() {
  return (
    <TabStub
      landsIn="Phase 3b"
      title="Circuits"
      summary="29-column editable table with drag-to-reorder, per-row test readings, and auto-fill from voice. The bulk of CertMate's editing surface."
    />
  );
}
