import { TabStub } from '@/components/job/tab-stub';

export default function BoardPage() {
  return (
    <TabStub
      landsIn="Phase 3a"
      title="Board"
      summary="Consumer unit details — make, model, BS EN rating, main switch, RCD / SPD presence. Matches iOS BoardTab."
    />
  );
}
