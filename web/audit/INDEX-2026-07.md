# iOS ↔ Web Parity — Gap Index (2026-07 sweep)

**Supersedes `web/audit/INDEX.md`** (the Wave A/B 2026-04 backlog) as the live gap index. Produced by WS0 of the iOS↔Web Full-Parity Program (parent plan: `~/.claude/handoffs/EICR_Automation--ios-web-full-parity-program-2026-07-01/PLAN-final.md`; executable slice: `…--parity-ws0-ws1-audit-governance-2026-07-01/PLAN-final.md`), executed 2026-07-02.

Ground rules in force: **iOS is canon** (cite `file:line`); **ZERO backend changes** (Derek 2026-07-01) — `src/`, `config/prompts/`, shared-types/utils, RDS, S3 are read-only for the whole program; every gap closes client-side or becomes a dated deliberate divergence.

**Ledger state after the sweep:** 396 rows (pre-sweep: 367) — `match` 283 / `partial` 84 / `ios-only` 16 / `missing` 13. Every parity-mapping row now carries a stable `id` + `last-verified` column; 25 rows were re-verified 2026-07-02 (all others have blank `last-verified` and count as stale for the CI warning). Row ids below refer to `web/docs/parity-ledger.md`. Wire-shape input: `web/audit/ws3-checklist-2026-07.md`.

## Open parity gaps (WS2–WS9)

One line per product gap: workstream — ledger row id — gap.

- **WS2** — `observations/obs-photo-autolink` — **DEPLOYED 2026-07-02, status `partial` — awaiting iPad Safari device smoke.** Parked branch rebased onto main and merged (4 commits `e880043d`..`577f8107` preserved + adapter/nullable-type fix); capture → pending → bidirectional 60 s auto-link → unassigned pool + From-Job picker live on web. Gap closes (row → `match`, remove this line) ONLY when the device smoke passes (upload-during-resize race + iPadOS camera quirks are runtime-only; todo in `todos-certmate.md`, 2026-07-02). Two dated deliberate divergences recorded on the ledger row: no CCU picker source (no web-persisted `ccu_photo_path`; zero-backend rule), web-extra camera/library chooser. Recording-context precedence constraint satisfied — other recording-context workstreams may proceed.
- **WS3** — `recording/capability-advertising` — web hello frame sends no capability array; universal read-back (`low_conf_readback_v1`) is dormant for web users.
- **WS3** — `observations/obs-card-canonical-wording` — web renders neither canonical `regulation_title`/`regulation_description` nor the "Because …" rationale on observation cards.
- **WS3** — `recording/transcript-gate` — no client-side forward gate on web; every final transcript reaches Sonnet. Full literal `TranscriptGate.shouldForward` port.
- **WS3** — `recording/gate-pass-chime` — the 960 Hz / 80 ms "sent for processing" chime the tour teaches does not exist on web (`playSentForProcessingChime()` to ADD; existing two tones stay).
- **WS3** — `recording/tts-fifo` — web TTS is cancel-before-queue + immediate fetch; iOS is serial FIFO with deferred synth. Ordering/latency parity unimplemented.
- **WS3** — `recording/fast-path-tts` — regex fast-path TTS (`/api/voice-latency/regex-fast-tts`) absent. Required before WS8.
- **WS3** — `recording/playback-telemetry` — playback-start/finish acks absent; `client_playback_telemetry` must not be advertised until live.
- **WS3** — `recording/readback-dedup-rekey` — web confirmation dedup unaudited vs iOS field+circuit+board+value re-key + never-clear bare-"no" UX.
- **WS3** — `recording/ask-context-board-id` — web question handling ignores `ask_user.context_board_id` (board-scoped asks).
- **WS3** — `supply/surge-namespace` — `surge_*` namespace (#58) only partially on web; completeness sweep incl. `spd_type_supply` fallback removal.
- **WS3** — `installation/eic-comments` — #66 `comments` field (EIC divert-to-comments) unverified on web forms/PDF.
- **WS3** — `circuits/lim-sentinel-display` — LIM sentinel parse/apply/store/display in the circuits grid unaudited.
- **WS4** — `recording/flux-migration` — web STT still nova-3 `/v1/listen`; iOS is Flux. One coordinated wave with the iOS keyterm sprint; nova-3 stays as kill-switch fallback.
- **WS5** — `crosscutting/cmdesign-token-deltas` — web tokens diverge from CMDesign values (spacing/radii/heights/elevation/accent scales).
- **WS5** — `crosscutting/ios-signature-styling` — glass card recipe, `cmStatusConduit` bar, hero shimmer, gradient tab underline, spring library, recording pulse border, UPPERCASE labels, monospaced values — none on web.
- **WS5** — `crosscutting/tab-rail-form` — tab rail visual form (rail + paged content vs icon-over-label + underline). Content/gating verified matching 2026-07-02.
- **WS5** — `inspection/section-accents-dup` — local `EICR_SECTION_ACCENTS` duplicates the shared constant.
- **WS6** — `ccu-flow/off-peak-mode` — 6th CCU mode (`addOffPeakBoard`) missing on web — full path, not just the tile.
- **WS6** — `ccu-flow/circuitstab-272` (+ paired rows `ccu-flow/circuitstab-486`, `circuits-misc/circuitstab-278`) — CCU pending-extraction queue (crash-surviving, auto-retry, per-job badge) — CCU photos ONLY.
- **WS6** — `dashboard/tour-v11` — web tour is 10 steps vs iOS 11; missing "conversational + tone" step with the real chime sample.
- **WS6** — `dashboard/job-creation-defaults-flow` — job-creation auto-apply/picker flow (0→standard defaults, 1→auto-apply, 2+→picker with Skip). Frontend-only; preset model already on web.
- **WS6** — `crosscutting/offline-dirty-guard` — iOS `isJobDirty` guard semantics unaudited on web cache refresh.
- **WS6** — `circuits-rail/doc-extraction-parity` — doc-extraction flow parity verification (≤12 files, image+PDF, partial-failure surfacing).
- **WS7** — `crosscutting/pwa-nav-transitions` — installed-mode push/pop navigation feel.
- **WS7** — `crosscutting/pwa-chrome-suppressions` — overscroll / tap-highlight / text-selection / pull-to-refresh suppression audit (also `dashboard/dashboardview-188` policy re-audit).
- **WS7** — `crosscutting/keyboard-accessory-bar` — circuit-cell keyboard toolbar (LIM / N/A / prev / next / Done); LIM wiring acceptance gated on WS3.
- **WS7** — `crosscutting/splash-continuity` — launch/splash continuity with the iOS branded loading view.
- **WS7** — `crosscutting/terms-signature-port` — T&Cs signature capture port (decided 2026-07-01 — un-parked; supersedes the old INDEX "pending legal review" entries).
- **WS7** — `crosscutting/uiimpactfeedbackgenerator` — closest-web-equivalent haptics pass (Vibration API where supported; iOS-Safari absence stays an accepted divergence).
- **WS9** — `pdf/pdf-fidelity` — client-side PDF template port (iOS `EICRHTMLTemplate` is canon) + two-attestation clickwrap parity; server endpoint stays as fallback.

Deliberately NOT gaps (parent §3E — do not "fix"): regex circuit pre-creation (Sonnet-only on both platforms), silent auto-derivations (by design), iOS CCU-photo double-persist (iOS bug to log, not replicate), fuzzy/edit-distance Deepgram garble correction (rejected project-wide — curated equal-weight keyterms only). Existing iOS-only platform-capability rows remain ACCEPTED divergences (parent §6 item 4).

## WS0 execution blockers

Operational blockers from the WS0 session itself — NOT WS2–WS9 parity gaps.

| Blocker | Owner | Missing artifacts | Follow-up |
|---|---|---|---|
| **Visual baseline RE-RUN 2026-07-02 (second session) with parity test creds — WEB COMPLETE, iOS PARTIAL.** All 14 web screens captured at both viewports against production (`parity-test@certmate.uk`, jobs `job_1782978942222` EICR / `job_1782978943693` EIC); iOS simulator built + captured login/dashboard/all-EICR-tabs/CCU-sheet/recording/settings via a temporary XCUITest driver. **iOS EIC job screens NOT capturable: `JobListViewModel.deduplicateApiJobs` collapses jobs BY ADDRESS ("keep the first"), and both seeded jobs share one address — the EIC job is silently dropped from the iOS list.** That is an iOS product bug for legitimate same-address EICR+EIC pairs (log, don't replicate — parent §3E class). Full capture table + deltas: `web/audit/visual-baseline-2026-07/MANIFEST.md`. | Derek / next parity session: EITHER reseed the EIC job at a distinct address and re-run the iOS driver (MANIFEST "iOS captures" notes), OR fix the iOS address-dedupe first | iOS-only: `job-{overview,installation,supply,board,circuits,extent,design,inspection,staff,pdf}-eic` (10 screens) | Parent-§7 WS0 stays PARTIAL until the 10 iOS EIC captures land |

Two frontend bugs FOUND during web capture (recorded in MANIFEST "Visual deltas", fixes owned elsewhere): (1) web login rejects any `company_id: null` user — `LoginResponseSchema` strict-parses `company_id` as `z.string().optional()`, not `.nullable()` (`web/src/lib/adapters/auth.ts`); tiny frontend fix, WS2-adjacent owner. (2) every styled dialog renders double-offset from center — Tailwind v4 standalone `translate: -50% -50%` AND `globals.css` `.cm-dialog-content` `transform: translate(-50%,-50%)` both apply (measured on the live DOM); WS5 owns the fix.

No other verification gaps: the WS1 IAM check ran against live AWS and passed (execution role grants `secretsmanager:GetSecretValue` on the exact `eicr/api-keys` ARN).

## Ledger rows not yet file-mapped

These row ids have no concrete repo-relative web file path in their web-ref cell (prose like "N/A", "MISSING", or a design/flow description), so they are absent from `web/docs/parity-ledger-files.json` and can never trigger the CI staleness warning. Map them when a concrete surface file emerges.

dashboard/dashboardview-593, dashboard/dashboardview-188, dashboard/defaultsmodal-37, dashboard/inspectormodal,
dashboard/createcertificatesheet, overview/jobdetailview-88, overview/jobdetailview-97, overview/jobdetailview-100,
overview/web-src-app-job-id-page-tsx-l64-l121, installation/installationtab-460, supply/supplytab-514, circuits-rail/circuitstab-132,
circuits-rail/circuitstab-142, circuits-rail/circuitstab-173, ccu-flow/circuitstab-272, ccu-flow/circuitstab-486,
ccu-flow/circuitstab-505, circuits-misc/circuitstab-580, circuits-misc/circuitstab-19, circuits-misc/draggedcircuitid,
circuits-misc/circuitstab-278, circuits-misc/circuitstab-130, observations/l215-l216, observations/l217-l220,
observations/l267-l272, observations/l260-l266, observations/l285-l302, observations/tap-observationcardview-open-edit,
pdf/pdf-page-tsx, pdf/pdftab-53, recording/transcript-highlight-flash-keyword-spotlight, recording/livefillview-44,
recording/livefillview-12, recording/debugdashboardview-22, recording/formatelapsed, recording/recording-chrome-tsx-l41-l49,
recording/recording-chrome-tsx, recording/recording-context-tsx, settings-profile/settingshubview-141, settings-profile/settingshubview-179,
settings-profile/settingshubview-196, settings-profile/settingshubview-200, settings-profile/settingshubview-209, settings-profile/settingshubview-217,
settings-profile/debugdashboardview, settings-profile/settingshubview, settings-company/company-name-registration, settings-company/validation-warnings-section,
settings-admin/adminedituserview-25, settings-admin/adminedituserview-32, settings-admin/adminedituserview-33, settings-admin/adminqueueview-1,
settings-admin/inviteemployeeview-1, settings-defaults/defaultsmanagerview-1, settings-defaults/defaultvaluesview-1, settings-defaults/cablesizedefaultsview-1,
settings-defaults/applydefaultssheet-1, settings-password/changepasswordview-1, settings-password/changepasswordview-41, settings-password/changepasswordview,
settings-app/audioimportview, settings-app/termsacceptanceview, crosscutting/cmdatepickerfield, crosscutting/photocaptureview,
crosscutting/photopickerview, crosscutting/typingtext, crosscutting/components-ui-confirm-dialog-tsx, crosscutting/recording-context-tsx,
crosscutting/components-pwa-sw-update-provider-tsx, crosscutting/components-pwa-offline-indicator-tsx, crosscutting/components-ui-numeric-stepper-tsx, crosscutting/components-ui-segmented-control-tsx,
crosscutting/components-ui-select-chips-tsx, crosscutting/components-ui-floating-label-input-tsx, crosscutting/components-ui-multiline-field-tsx, crosscutting/components-ui-icon-button-tsx

## Small facts verified this sweep (WS0 item 5)

- `utterance_end_ms` on web is **1000** (`web/src/lib/recording/deepgram-service.ts:532`, comment + code agree). The Wave B4.1 "2000 → 1500" claim in the old INDEX.md was the stale one.
- Web does **not** render observation `rationale` (decoded into types, never rendered) — folded into `observations/obs-card-canonical-wording`.
- `queueSaveJob` **is** wired into JobProvider's save path (`web/src/lib/job-context.tsx:159`); hub CLAUDE.md corrected.
- iOS live STT is **Deepgram Flux** (`DeepgramService.swift:608`, `/v2/listen`, `flux-general-en`); both CLAUDE.md files corrected (web stays nova-3 until WS4).
- Parked branch `pwa-observation-photo-autolink-2026-05-13` verified (4 commits `e880043d`..`577f8107`) and **pushed to origin** 2026-07-02.
