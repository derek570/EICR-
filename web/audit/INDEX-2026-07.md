# iOS ↔ Web Parity — Gap Index (2026-07 sweep)

**Supersedes `web/audit/INDEX.md`** (the Wave A/B 2026-04 backlog) as the live gap index. Produced by WS0 of the iOS↔Web Full-Parity Program (parent plan: `~/.claude/handoffs/EICR_Automation--ios-web-full-parity-program-2026-07-01/PLAN-final.md`; executable slice: `…--parity-ws0-ws1-audit-governance-2026-07-01/PLAN-final.md`), executed 2026-07-02.

Ground rules in force: **iOS is canon** (cite `file:line`); **ZERO backend changes** (Derek 2026-07-01) — `src/`, `config/prompts/`, shared-types/utils, RDS, S3 are read-only for the whole program; every gap closes client-side or becomes a dated deliberate divergence.

**Ledger state after the sweep:** 396 rows (pre-sweep: 367) — `match` 283 / `partial` 84 / `ios-only` 16 / `missing` 13. Every parity-mapping row now carries a stable `id` + `last-verified` column; 25 rows were re-verified 2026-07-02 (all others have blank `last-verified` and count as stale for the CI warning). Row ids below refer to `web/docs/parity-ledger.md`. Wire-shape input: `web/audit/ws3-checklist-2026-07.md`.

## Open parity gaps (WS2–WS9)

One line per product gap: workstream — ledger row id — gap.

- **WS3 correction (2026-07-02, item 9c)** — `recording/ask-context-board-id` RECLASSIFIED backend-internal / no web action (row → `match`): `ask_user.context_board_id` is threaded through server-side ask flows only; NEITHER client decodes it (zero refs in CertMateUnified Sources AND `web/src/`) — the client answer path is keyed by `tool_call_id`, and web's is pinned by `tests/transcript-gate-wiring.test.ts`. The original checklist/ledger claim that iOS carries the field was wrong.

- **WS2** — `observations/obs-photo-autolink` — **DEPLOYED 2026-07-02, status `partial` — awaiting iPad Safari device smoke.** Parked branch rebased onto main and merged (4 commits `e880043d`..`577f8107` preserved + adapter/nullable-type fix); capture → pending → bidirectional 60 s auto-link → unassigned pool + From-Job picker live on web. Gap closes (row → `match`, remove this line) ONLY when the device smoke passes (upload-during-resize race + iPadOS camera quirks are runtime-only; todo in `todos-certmate.md`, 2026-07-02). Two dated deliberate divergences recorded on the ledger row: no CCU picker source (no web-persisted `ccu_photo_path`; zero-backend rule), web-extra camera/library chooser. Recording-context precedence constraint satisfied — other recording-context workstreams may proceed.
- **WS3-FU `parity-ws3b-voice-latency-2026-07`** — `recording/tts-fifo` — web TTS is cancel-before-queue + immediate fetch; iOS is serial FIFO with deferred synth. NOT shipped in WS3 (2026-07-02); named pre-WS8 follow-up owns it.
- **WS3-FU `parity-ws3b-voice-latency-2026-07`** — `recording/fast-path-tts` — regex fast-path TTS (`/api/voice-latency/regex-fast-tts`) absent. Required before WS8; NOT shipped in WS3 (2026-07-02); named follow-up owns it (advertise `regex_fast_v2` only after live).
- **WS3-FU `parity-ws3b-voice-latency-2026-07`** — `recording/playback-telemetry` — playback-start/finish acks absent; `client_playback_telemetry` must not be advertised until live. NOT shipped in WS3 (2026-07-02); named follow-up owns it.
- **WS4** — `recording/flux-migration` — **PARTIAL/BLOCKED (2026-07-03 `/ep`).** Web Flux path SHIPPED behind an `sttModel` selector + a runtime kill-switch (`DEEPGRAM_STT_MODEL`, `/runtime-config` route, `frontend-taskdef` deploy target), DEFAULT `nova3`. NOT flipped to Flux: the Phase-0 synthetic probe validated the mechanism (LIM) but left insulation/trip-time INCONCLUSIVE, and the iOS `default_config.json` curation + TestFlight gate is unmet. iOS curation HELD — needs a real-audio spot check (surfaced to Derek, `~/.claude/handoffs/EICR_Automation--parity-ws4-flux-wave-2026-07-02/phase0-probe-results.md`). Web nova-3 fallback protected (garble-critical membership + `trip time`→2.5). Flip = one `DEFAULT_STT_MODEL`+task-def commit after the curated list ships via TestFlight. New row `recording/flux-kill-switch` = `match`.
- **WS5** — `crosscutting/cmdesign-token-deltas` — web tokens diverge from CMDesign values (spacing/radii/heights/elevation/accent scales).
- **WS5** — `crosscutting/ios-signature-styling` — glass card recipe, `cmStatusConduit` bar, hero shimmer, gradient tab underline, spring library, recording pulse border, UPPERCASE labels, monospaced values — none on web.
- **WS5** — `crosscutting/tab-rail-form` — tab rail visual form (rail + paged content vs icon-over-label + underline). Content/gating verified matching 2026-07-02.
- **WS5** — `inspection/section-accents-dup` — local `EICR_SECTION_ACCENTS` duplicates the shared constant.
- **WS3 — SEVEN GAPS CLOSED + ONE PENDING-PROD 2026-07-02** (parity-ws3-voice-catchup session; rows flipped with `last-verified` 2026-07-02): `observations/obs-card-canonical-wording` (decode+persist+render all three, MISS-clears), `recording/transcript-gate` (full literal port wired in dispatchFinal), `recording/gate-pass-chime` (sample-accurate `playSentForProcessingChime()`; tour switched to it, tour-local synth deleted), `recording/readback-dedup-rekey` (iOS key algorithm, backend-mirror hash vectors), `supply/surge-namespace` (sweep: complete; voice-apply routing pinned), `installation/eic-comments` (voice divert-to-comments apply added; form cell pre-existed), `circuits/lim-sentinel-display` (audit: complete; 9 regression tests), plus `recording/ask-context-board-id` reclassified (see correction note above). `recording/capability-advertising` is SHIPPED-PENDING-PROD (`partial`): flips to `match` only after post-deploy CloudWatch `voice_latency.startup_log` shows `low_conf_readback_v1` from a web session. Items 4/5/8 NOT shipped — owned by WS3-FU `parity-ws3b-voice-latency-2026-07` (dated ledger rows).
- **WS5 — ALL FOUR GAPS CLOSED 2026-07-02** (parity-ws5-design-system session; rows flipped with `last-verified` 2026-07-02): `crosscutting/cmdesign-token-deltas` → `match` (tokens regenerated from CMDesign; mapping table `web/audit/cmdesign-token-map-2026-07.md`; spacing keys+values kept per user decision, `max-w-*` override computed-verified 768px; dated divergence: `sectionGap=28` page-stack retrofit skipped); `crosscutting/ios-signature-styling` → `match` (glass card, conduit gradient, hero breathe+shimmer, sliding gradient underline, spring easings, field chrome; dated divergence: `formLabel`/`cmFormLabel` UPPERCASE style has zero live iOS call sites — web matches the live floating-label spec instead); `crosscutting/tab-rail-form` → `partial` (rail restyle CLOSED; paged swipe content remains the dated accepted pattern difference, consistent with `overview/ios-tab-model-one-tab`); `inspection/section-accents-dup` → `match` (shared `EICR_INSPECTION_SECTION_CATEGORIES` per `InspectionTab.swift:362-374`). WS5 also fixed the styled-dialog double-offset bug (below) + the recording-chrome photo-chooser undefined CSS vars.
- **WS6 — ALL SIX GAPS CLOSED 2026-07-02** (parity-ws6-workflows session; rows flipped to `match` with `last-verified` 2026-07-02): `ccu-flow/off-peak-mode` (6th mode, full path incl. Board tab + last-mode guard), `ccu-flow/circuitstab-272` + paired `ccu-flow/circuitstab-486` / `circuits-misc/circuitstab-278` (pending-CCU queue: IDB blob store, one idempotency key per capture, terminal-422 retake, auto-retry, banner + Overview pill), `dashboard/tour-v11` (11 steps + chime step; chime synth tour-local until WS3 lands), `dashboard/job-creation-defaults-flow` (+ `dashboard/dashboardview-708`, `dashboard/createcertificatesheet`; 0→standard/1→auto/2+→picker with Skip), `crosscutting/offline-dirty-guard` (audit: guard pre-existed at `job-context.tsx:121-138`; regression tests added), `circuits-rail/doc-extraction-parity` (verification-only: parity confirmed, no code change). Dated divergences recorded on the rows: chime played AFTER narration (Web Speech can't splice mid-utterance); auto-retry listener lives on the circuits page rather than job-level.
- **WS7** — `crosscutting/pwa-nav-transitions` — installed-mode push/pop navigation feel.
- **WS7** — `crosscutting/pwa-chrome-suppressions` — overscroll / tap-highlight / text-selection / pull-to-refresh suppression audit (also `dashboard/dashboardview-188` policy re-audit).
- **WS7** — `crosscutting/keyboard-accessory-bar` — circuit-cell keyboard toolbar (LIM / N/A / prev / next / Done); LIM wiring acceptance gated on WS3.
- **WS7** — `crosscutting/splash-continuity` — launch/splash continuity with the iOS branded loading view.
- **WS7** — `crosscutting/terms-signature-port` — T&Cs signature capture port (decided 2026-07-01 — un-parked; supersedes the old INDEX "pending legal review" entries).
- **WS7** — `crosscutting/uiimpactfeedbackgenerator` — closest-web-equivalent haptics pass (Vibration API where supported; iOS-Safari absence stays an accepted divergence).
- **WS9** — `pdf/pdf-fidelity` — **IMPLEMENTED 2026-07-02 (client render SHIPPED); gap stays OPEN/partial pending field validation.** The client-side template port (`web/src/lib/pdf/` — iOS `EICRHTMLTemplate` port + `PDFGenerator` data-graph mirror + foreignObject-capture renderer producing a real PDF Blob) is live as the PDF tab's primary Generate and PASSED the page-by-page acceptance diff vs iOS reference PDFs (EICR 9/9, EIC 5/5 — `web/audit/ws9-pdf-fidelity-2026-07/`). Two-attestation clickwrap parity confirmed (+ spec-§4.3 no-re-prompt retry, which iOS itself lacks — dated divergence on `pdf/pdftab-270`). REMAINING before this gap closes: field validation accepts the renderer on a real job, THEN the visible "Generate on server (fallback)" action flips behind the debug page (parent §6 item 5 follow-up). Server endpoint untouched.

Deliberately NOT gaps (parent §3E — do not "fix"): regex circuit pre-creation (Sonnet-only on both platforms), silent auto-derivations (by design), iOS CCU-photo double-persist (iOS bug to log, not replicate), fuzzy/edit-distance Deepgram garble correction (rejected project-wide — curated equal-weight keyterms only). Existing iOS-only platform-capability rows remain ACCEPTED divergences (parent §6 item 4).

## WS0 execution blockers

Operational blockers from the WS0 session itself — NOT WS2–WS9 parity gaps.

| Blocker | Owner | Missing artifacts | Follow-up |
|---|---|---|---|
| **Visual baseline RE-RUN 2026-07-02 (second session) with parity test creds — WEB COMPLETE, iOS PARTIAL.** All 14 web screens captured at both viewports against production (`parity-test@certmate.uk`, jobs `job_1782978942222` EICR / `job_1782978943693` EIC); iOS simulator built + captured login/dashboard/all-EICR-tabs/CCU-sheet/recording/settings via a temporary XCUITest driver. **iOS EIC job screens NOT capturable: `JobListViewModel.deduplicateApiJobs` collapses jobs BY ADDRESS ("keep the first"), and both seeded jobs share one address — the EIC job is silently dropped from the iOS list.** That is an iOS product bug for legitimate same-address EICR+EIC pairs (log, don't replicate — parent §3E class). Full capture table + deltas: `web/audit/visual-baseline-2026-07/MANIFEST.md`. | Derek / next parity session: EITHER reseed the EIC job at a distinct address and re-run the iOS driver (MANIFEST "iOS captures" notes), OR fix the iOS address-dedupe first | iOS-only: `job-{overview,installation,supply,board,circuits,extent,design,inspection,staff,pdf}-eic` (10 screens) | Parent-§7 WS0 stays PARTIAL until the 10 iOS EIC captures land |

**WS5 deferred-review note (2026-07-02, user-confirmed):** WS5 acceptance diffed EICR screens against their iOS baselines; EIC screens were reviewed web-before/after with the iOS EICR captures as canon proxy for shared styling. EIC-vs-iOS screen review is DEFERRED until the 10 iOS EIC captures land (mirrored in `visual-baseline-2026-07/MANIFEST.md`).

Two frontend bugs FOUND during web capture (recorded in MANIFEST "Visual deltas"): (1) web login rejects any `company_id: null` user — `LoginResponseSchema` strict-parses `company_id` as `z.string().optional()`, not `.nullable()` (`web/src/lib/adapters/auth.ts`); **FIXED 2026-07-03 (commit `851ba63e`)** — `.nullable()` added to the login schema + `User`/`DiagnosticsUser` types, `adapters.test.ts` gains a `company_id: null` login case (folded into the auto-seed hydration hotfix, same auth-path session). (2) styled-dialog double-offset — **FIXED by WS5 2026-07-02**: `.cm-dialog-content` animation moved off `transform` onto the standalone `scale` property so the Tailwind v4 `translate` utilities are the single translation source; consumer-level duplicate at `terms/page.tsx` cleaned in the same commit; Playwright regression at `web/tests-e2e/dialog-centering.spec.ts` (measured 288px vertical offset pre-fix, centred post-fix on chromium+webkit).

**THIRD frontend bug — FIXED 2026-07-03 (commit `851ba63e`, P1-class data loss):** the installation/supply pages auto-seeded defaults on mount (`ensureDateOfInspection` + `next_inspection_years=5` at `web/src/app/job/[id]/installation/page.tsx`; supply N/A coercions) and the debounced `queueSaveJob` then PUT that state. If the job-detail GET failed or hydrated blank while a cached/summary doc painted, the seeders ran against an EMPTY doc and the save WIPED the job's sections server-side. This actually happened during WS5's local visual re-capture: one transient GET failure blanked the seeded parity EICR job (restored same-session from the twin EIC fixture; capture script now carries a hard read-only guard — `web/tests-e2e/visual-baseline-capture.mjs`). Real-world exposure: any inspector whose detail fetch failed once (offline blip / 5xx / parse error) could lose job data on reconnect. The bug was self-reinforcing — the seed marked the doc dirty, so JobProvider's dirty-guard then REJECTED the fresh network doc, freezing the wipe on screen. **Fix:** `JobProvider` now exposes an `isHydrated` flag, true only once a real `api.job()` doc has been ACCEPTED into provider state (not an IDB cache paint); `/job/[id]/layout.tsx` tracks `networkHydrated` and passes it as `<JobProvider hydrated>`; both seeders gate on `isHydrated`. Mirrors iOS, which seeds only after `load()` succeeds. Fail-safe direction is always "don't seed" — offline (never hydrates) and dirty-rejected-doc paths skip the seed, no data loss; manual edits still save via the outbox. Zero backend. Regression coverage: `web/tests/autoseed-hydration-guard.test.tsx` (11 tests — unhydrated blank → no seed/no save on both tabs; hydration → seed+save; populated job keeps server values; hydrated-at-mount still seeds; JobProvider hydration state machine incl. dirty-rejection). Related-but-distinct from `crosscutting/offline-dirty-guard` (that guard protects local edits from server clobber; this is the reverse direction). Also fixed in the same commit: `company_id` gained `.nullable()` (`adapters/auth.ts` + `User`/`DiagnosticsUser` types) — the WS0 visual-baseline login bug #2 (backend emits literal `company_id: null` for legacy users; strict login parse rejected it).

No other verification gaps: the WS1 IAM check ran against live AWS and passed (execution role grants `secretsmanager:GetSecretValue` on the exact `eicr/api-keys` ARN).

## Ledger rows not yet file-mapped

These row ids have no concrete repo-relative web file path in their web-ref cell (prose like "N/A", "MISSING", or a design/flow description), so they are absent from `web/docs/parity-ledger-files.json` and can never trigger the CI staleness warning. Map them when a concrete surface file emerges.

dashboard/dashboardview-593, dashboard/dashboardview-188, dashboard/defaultsmodal-37, dashboard/inspectormodal,
dashboard/createcertificatesheet, overview/jobdetailview-88, overview/jobdetailview-97, overview/jobdetailview-100,
overview/web-src-app-job-id-page-tsx-l64-l121, installation/installationtab-460, supply/supplytab-514, circuits-rail/circuitstab-132,
circuits-rail/circuitstab-142, circuits-rail/circuitstab-173, ccu-flow/circuitstab-272, ccu-flow/circuitstab-486,
ccu-flow/circuitstab-505, circuits-misc/circuitstab-580, circuits-misc/circuitstab-19, circuits-misc/draggedcircuitid,
circuits-misc/circuitstab-278, circuits-misc/circuitstab-130, observations/l215-l216, observations/l217-l220,
observations/l267-l272, observations/l260-l266, observations/l285-l302, observations/tap-observationcardview-open-edit,
pdf/pdf-page-tsx, pdf/pdftab-53, recording/transcript-highlight-flash-keyword-spotlight, recording/livefillview-44,
recording/livefillview-12, recording/debugdashboardview-22, recording/formatelapsed, recording/recording-chrome-tsx-l41-l49,
recording/recording-chrome-tsx, recording/recording-context-tsx, settings-profile/settingshubview-141, settings-profile/settingshubview-179,
settings-profile/settingshubview-196, settings-profile/settingshubview-200, settings-profile/settingshubview-209, settings-profile/settingshubview-217,
settings-profile/debugdashboardview, settings-profile/settingshubview, settings-company/company-name-registration, settings-company/validation-warnings-section,
settings-admin/adminedituserview-25, settings-admin/adminedituserview-32, settings-admin/adminedituserview-33, settings-admin/adminqueueview-1,
settings-admin/inviteemployeeview-1, settings-defaults/defaultsmanagerview-1, settings-defaults/defaultvaluesview-1, settings-defaults/cablesizedefaultsview-1,
settings-defaults/applydefaultssheet-1, settings-password/changepasswordview-1, settings-password/changepasswordview-41, settings-password/changepasswordview,
settings-app/audioimportview, settings-app/termsacceptanceview, crosscutting/cmdatepickerfield, crosscutting/photocaptureview,
crosscutting/photopickerview, crosscutting/typingtext, crosscutting/components-ui-confirm-dialog-tsx, crosscutting/recording-context-tsx,
crosscutting/components-pwa-sw-update-provider-tsx, crosscutting/components-pwa-offline-indicator-tsx, crosscutting/components-ui-numeric-stepper-tsx, crosscutting/components-ui-segmented-control-tsx,
crosscutting/components-ui-select-chips-tsx, crosscutting/components-ui-floating-label-input-tsx, crosscutting/components-ui-multiline-field-tsx, crosscutting/components-ui-icon-button-tsx

## Small facts verified this sweep (WS0 item 5)

- `utterance_end_ms` on web is **1000** (`web/src/lib/recording/deepgram-service.ts:532`, comment + code agree). The Wave B4.1 "2000 → 1500" claim in the old INDEX.md was the stale one.
- Web does **not** render observation `rationale` (decoded into types, never rendered) — folded into `observations/obs-card-canonical-wording`.
- `queueSaveJob` **is** wired into JobProvider's save path (`web/src/lib/job-context.tsx:159`); hub CLAUDE.md corrected.
- iOS live STT is **Deepgram Flux** (`DeepgramService.swift:608`, `/v2/listen`, `flux-general-en`); both CLAUDE.md files corrected (web stays nova-3 until WS4).
- Parked branch `pwa-observation-photo-autolink-2026-05-13` verified (4 commits `e880043d`..`577f8107`) and **pushed to origin** 2026-07-02.
